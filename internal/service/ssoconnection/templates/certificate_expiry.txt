{{if .Expired}}Single sign-on for {{.Workspace}} has stopped working.

The signing certificate from {{.Provider}} expired on {{.ExpiresAt}}. Until it is
replaced, nobody can sign in to {{.Workspace}} through your identity provider.
{{else}}The signing certificate for {{.Workspace}} expires in {{.DaysLeft}} day{{if ne .DaysLeft 1}}s{{end}}.

{{.Provider}} signs with a certificate that expires on {{.ExpiresAt}}. When it does,
nobody will be able to sign in to {{.Workspace}} through your identity provider.
{{end}}
Copy the current certificate from your provider and save it here:

{{.SettingsURL}}

If anyone still has a Norn password they can sign in with that in the meantime.
