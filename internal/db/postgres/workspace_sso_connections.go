// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceSsoConnection is an object representing the database table.
type WorkspaceSsoConnection struct {
	WorkspaceID  string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	Protocol     string    `boil:"protocol" json:"protocol" toml:"protocol" yaml:"protocol"`
	Provisioning bool      `boil:"provisioning" json:"provisioning" toml:"provisioning" yaml:"provisioning"`
	VerifiedAt   null.Time `boil:"verified_at" json:"verified_at,omitempty" toml:"verified_at" yaml:"verified_at,omitempty"`
	CreatedAt    time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	AdminGroup   string    `boil:"admin_group" json:"admin_group" toml:"admin_group" yaml:"admin_group"`

	R *workspaceSsoConnectionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceSsoConnectionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceSsoConnectionColumns = struct {
	WorkspaceID  string
	Protocol     string
	Provisioning string
	VerifiedAt   string
	CreatedAt    string
	UpdatedAt    string
	AdminGroup   string
}{
	WorkspaceID:  "workspace_id",
	Protocol:     "protocol",
	Provisioning: "provisioning",
	VerifiedAt:   "verified_at",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	AdminGroup:   "admin_group",
}

var WorkspaceSsoConnectionTableColumns = struct {
	WorkspaceID  string
	Protocol     string
	Provisioning string
	VerifiedAt   string
	CreatedAt    string
	UpdatedAt    string
	AdminGroup   string
}{
	WorkspaceID:  "workspace_sso_connections.workspace_id",
	Protocol:     "workspace_sso_connections.protocol",
	Provisioning: "workspace_sso_connections.provisioning",
	VerifiedAt:   "workspace_sso_connections.verified_at",
	CreatedAt:    "workspace_sso_connections.created_at",
	UpdatedAt:    "workspace_sso_connections.updated_at",
	AdminGroup:   "workspace_sso_connections.admin_group",
}

// Generated where

var WorkspaceSsoConnectionWhere = struct {
	WorkspaceID  whereHelperstring
	Protocol     whereHelperstring
	Provisioning whereHelperbool
	VerifiedAt   whereHelpernull_Time
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
	AdminGroup   whereHelperstring
}{
	WorkspaceID:  whereHelperstring{field: "\"workspace_sso_connections\".\"workspace_id\""},
	Protocol:     whereHelperstring{field: "\"workspace_sso_connections\".\"protocol\""},
	Provisioning: whereHelperbool{field: "\"workspace_sso_connections\".\"provisioning\""},
	VerifiedAt:   whereHelpernull_Time{field: "\"workspace_sso_connections\".\"verified_at\""},
	CreatedAt:    whereHelpertime_Time{field: "\"workspace_sso_connections\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"workspace_sso_connections\".\"updated_at\""},
	AdminGroup:   whereHelperstring{field: "\"workspace_sso_connections\".\"admin_group\""},
}

// WorkspaceSsoConnectionRels is where relationship names are stored.
var WorkspaceSsoConnectionRels = struct {
	Workspace                       string
	WorkspaceWorkspaceSsoIdentities string
}{
	Workspace:                       "Workspace",
	WorkspaceWorkspaceSsoIdentities: "WorkspaceWorkspaceSsoIdentities",
}

// workspaceSsoConnectionR is where relationships are stored.
type workspaceSsoConnectionR struct {
	Workspace                       *Workspace                `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
	WorkspaceWorkspaceSsoIdentities WorkspaceSsoIdentitySlice `boil:"WorkspaceWorkspaceSsoIdentities" json:"WorkspaceWorkspaceSsoIdentities" toml:"WorkspaceWorkspaceSsoIdentities" yaml:"WorkspaceWorkspaceSsoIdentities"`
}

// NewStruct creates a new relationship struct
func (*workspaceSsoConnectionR) NewStruct() *workspaceSsoConnectionR {
	return &workspaceSsoConnectionR{}
}

func (o *WorkspaceSsoConnection) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceSsoConnectionR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

func (o *WorkspaceSsoConnection) GetWorkspaceWorkspaceSsoIdentities() WorkspaceSsoIdentitySlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspaceWorkspaceSsoIdentities()
}

func (r *workspaceSsoConnectionR) GetWorkspaceWorkspaceSsoIdentities() WorkspaceSsoIdentitySlice {
	if r == nil {
		return nil
	}

	return r.WorkspaceWorkspaceSsoIdentities
}

// workspaceSsoConnectionL is where Load methods for each relationship are stored.
type workspaceSsoConnectionL struct{}

var (
	workspaceSsoConnectionAllColumns            = []string{"workspace_id", "protocol", "provisioning", "verified_at", "created_at", "updated_at", "admin_group"}
	workspaceSsoConnectionColumnsWithoutDefault = []string{"workspace_id", "protocol"}
	workspaceSsoConnectionColumnsWithDefault    = []string{"provisioning", "verified_at", "created_at", "updated_at", "admin_group"}
	workspaceSsoConnectionPrimaryKeyColumns     = []string{"workspace_id"}
	workspaceSsoConnectionGeneratedColumns      = []string{}
)

type (
	// WorkspaceSsoConnectionSlice is an alias for a slice of pointers to WorkspaceSsoConnection.
	// This should almost always be used instead of []WorkspaceSsoConnection.
	WorkspaceSsoConnectionSlice []*WorkspaceSsoConnection
	// WorkspaceSsoConnectionHook is the signature for custom WorkspaceSsoConnection hook methods
	WorkspaceSsoConnectionHook func(context.Context, boil.ContextExecutor, *WorkspaceSsoConnection) error

	workspaceSsoConnectionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceSsoConnectionType                 = reflect.TypeOf(&WorkspaceSsoConnection{})
	workspaceSsoConnectionMapping              = queries.MakeStructMapping(workspaceSsoConnectionType)
	workspaceSsoConnectionPrimaryKeyMapping, _ = queries.BindMapping(workspaceSsoConnectionType, workspaceSsoConnectionMapping, workspaceSsoConnectionPrimaryKeyColumns)
	workspaceSsoConnectionInsertCacheMut       sync.RWMutex
	workspaceSsoConnectionInsertCache          = make(map[string]insertCache)
	workspaceSsoConnectionUpdateCacheMut       sync.RWMutex
	workspaceSsoConnectionUpdateCache          = make(map[string]updateCache)
	workspaceSsoConnectionUpsertCacheMut       sync.RWMutex
	workspaceSsoConnectionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceSsoConnectionAfterSelectMu sync.Mutex
var workspaceSsoConnectionAfterSelectHooks []WorkspaceSsoConnectionHook

var workspaceSsoConnectionBeforeInsertMu sync.Mutex
var workspaceSsoConnectionBeforeInsertHooks []WorkspaceSsoConnectionHook
var workspaceSsoConnectionAfterInsertMu sync.Mutex
var workspaceSsoConnectionAfterInsertHooks []WorkspaceSsoConnectionHook

var workspaceSsoConnectionBeforeUpdateMu sync.Mutex
var workspaceSsoConnectionBeforeUpdateHooks []WorkspaceSsoConnectionHook
var workspaceSsoConnectionAfterUpdateMu sync.Mutex
var workspaceSsoConnectionAfterUpdateHooks []WorkspaceSsoConnectionHook

var workspaceSsoConnectionBeforeDeleteMu sync.Mutex
var workspaceSsoConnectionBeforeDeleteHooks []WorkspaceSsoConnectionHook
var workspaceSsoConnectionAfterDeleteMu sync.Mutex
var workspaceSsoConnectionAfterDeleteHooks []WorkspaceSsoConnectionHook

var workspaceSsoConnectionBeforeUpsertMu sync.Mutex
var workspaceSsoConnectionBeforeUpsertHooks []WorkspaceSsoConnectionHook
var workspaceSsoConnectionAfterUpsertMu sync.Mutex
var workspaceSsoConnectionAfterUpsertHooks []WorkspaceSsoConnectionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceSsoConnection) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceSsoConnection) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceSsoConnection) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceSsoConnection) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceSsoConnection) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceSsoConnection) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceSsoConnection) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceSsoConnection) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceSsoConnection) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoConnectionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceSsoConnectionHook registers your hook function for all future operations.
func AddWorkspaceSsoConnectionHook(hookPoint boil.HookPoint, workspaceSsoConnectionHook WorkspaceSsoConnectionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceSsoConnectionAfterSelectMu.Lock()
		workspaceSsoConnectionAfterSelectHooks = append(workspaceSsoConnectionAfterSelectHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceSsoConnectionBeforeInsertMu.Lock()
		workspaceSsoConnectionBeforeInsertHooks = append(workspaceSsoConnectionBeforeInsertHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceSsoConnectionAfterInsertMu.Lock()
		workspaceSsoConnectionAfterInsertHooks = append(workspaceSsoConnectionAfterInsertHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceSsoConnectionBeforeUpdateMu.Lock()
		workspaceSsoConnectionBeforeUpdateHooks = append(workspaceSsoConnectionBeforeUpdateHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceSsoConnectionAfterUpdateMu.Lock()
		workspaceSsoConnectionAfterUpdateHooks = append(workspaceSsoConnectionAfterUpdateHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceSsoConnectionBeforeDeleteMu.Lock()
		workspaceSsoConnectionBeforeDeleteHooks = append(workspaceSsoConnectionBeforeDeleteHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceSsoConnectionAfterDeleteMu.Lock()
		workspaceSsoConnectionAfterDeleteHooks = append(workspaceSsoConnectionAfterDeleteHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceSsoConnectionBeforeUpsertMu.Lock()
		workspaceSsoConnectionBeforeUpsertHooks = append(workspaceSsoConnectionBeforeUpsertHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceSsoConnectionAfterUpsertMu.Lock()
		workspaceSsoConnectionAfterUpsertHooks = append(workspaceSsoConnectionAfterUpsertHooks, workspaceSsoConnectionHook)
		workspaceSsoConnectionAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceSsoConnection record from the query.
func (q workspaceSsoConnectionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceSsoConnection, error) {
	o := &WorkspaceSsoConnection{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_sso_connections")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceSsoConnection records from the query.
func (q workspaceSsoConnectionQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceSsoConnectionSlice, error) {
	var o []*WorkspaceSsoConnection

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceSsoConnection slice")
	}

	if len(workspaceSsoConnectionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceSsoConnection records in the query.
func (q workspaceSsoConnectionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_sso_connections rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceSsoConnectionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_sso_connections exists")
	}

	return count > 0, nil
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceSsoConnection) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// WorkspaceWorkspaceSsoIdentities retrieves all the workspace_sso_identity's WorkspaceSsoIdentities with an executor via workspace_id column.
func (o *WorkspaceSsoConnection) WorkspaceWorkspaceSsoIdentities(mods ...qm.QueryMod) workspaceSsoIdentityQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_sso_identities\".\"workspace_id\"=?", o.WorkspaceID),
	)

	return WorkspaceSsoIdentities(queryMods...)
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceSsoConnectionL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSsoConnection any, mods queries.Applicator) error {
	var slice []*WorkspaceSsoConnection
	var object *WorkspaceSsoConnection

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSsoConnection.(*WorkspaceSsoConnection)
		if !ok {
			object = new(WorkspaceSsoConnection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSsoConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSsoConnection))
			}
		}
	} else {
		s, ok := maybeWorkspaceSsoConnection.(*[]*WorkspaceSsoConnection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSsoConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSsoConnection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSsoConnectionR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSsoConnectionR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceSsoConnection = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceSsoConnection = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspaceWorkspaceSsoIdentities allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceSsoConnectionL) LoadWorkspaceWorkspaceSsoIdentities(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSsoConnection any, mods queries.Applicator) error {
	var slice []*WorkspaceSsoConnection
	var object *WorkspaceSsoConnection

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSsoConnection.(*WorkspaceSsoConnection)
		if !ok {
			object = new(WorkspaceSsoConnection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSsoConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSsoConnection))
			}
		}
	} else {
		s, ok := maybeWorkspaceSsoConnection.(*[]*WorkspaceSsoConnection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSsoConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSsoConnection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSsoConnectionR{}
		}
		args[object.WorkspaceID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSsoConnectionR{}
			}
			args[obj.WorkspaceID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_sso_identities`),
		qm.WhereIn(`workspace_sso_identities.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_sso_identities")
	}

	var resultSlice []*WorkspaceSsoIdentity
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_sso_identities")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_sso_identities")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_sso_identities")
	}

	if len(workspaceSsoIdentityAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorkspaceWorkspaceSsoIdentities = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceSsoIdentityR{}
			}
			foreign.R.Workspace = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.WorkspaceID == foreign.WorkspaceID {
				local.R.WorkspaceWorkspaceSsoIdentities = append(local.R.WorkspaceWorkspaceSsoIdentities, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceSsoIdentityR{}
				}
				foreign.R.Workspace = local
				break
			}
		}
	}

	return nil
}

// SetWorkspace of the workspaceSsoConnection to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceSsoConnection.
func (o *WorkspaceSsoConnection) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_sso_connections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceSsoConnectionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.WorkspaceID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceSsoConnectionR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceSsoConnection: o,
		}
	} else {
		related.R.WorkspaceSsoConnection = o
	}

	return nil
}

// AddWorkspaceWorkspaceSsoIdentities adds the given related objects to the existing relationships
// of the workspace_sso_connection, optionally inserting them as new records.
// Appends related to o.R.WorkspaceWorkspaceSsoIdentities.
// Sets related.R.Workspace appropriately.
func (o *WorkspaceSsoConnection) AddWorkspaceWorkspaceSsoIdentities(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceSsoIdentity) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorkspaceID = o.WorkspaceID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_sso_identities\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceSsoIdentityPrimaryKeyColumns),
			)
			values := []any{o.WorkspaceID, rel.WorkspaceID, rel.AccountID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorkspaceID = o.WorkspaceID
		}
	}

	if o.R == nil {
		o.R = &workspaceSsoConnectionR{
			WorkspaceWorkspaceSsoIdentities: related,
		}
	} else {
		o.R.WorkspaceWorkspaceSsoIdentities = append(o.R.WorkspaceWorkspaceSsoIdentities, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceSsoIdentityR{
				Workspace: o,
			}
		} else {
			rel.R.Workspace = o
		}
	}
	return nil
}

// WorkspaceSsoConnections retrieves all the records using an executor.
func WorkspaceSsoConnections(mods ...qm.QueryMod) workspaceSsoConnectionQuery {
	mods = append(mods, qm.From("\"workspace_sso_connections\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_sso_connections\".*"})
	}

	return workspaceSsoConnectionQuery{q}
}

// FindWorkspaceSsoConnection retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceSsoConnection(ctx context.Context, exec boil.ContextExecutor, workspaceID string, selectCols ...string) (*WorkspaceSsoConnection, error) {
	workspaceSsoConnectionObj := &WorkspaceSsoConnection{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_sso_connections\" where \"workspace_id\"=$1", sel,
	)

	q := queries.Raw(query, workspaceID)

	err := q.Bind(ctx, exec, workspaceSsoConnectionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_sso_connections")
	}

	if err = workspaceSsoConnectionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceSsoConnectionObj, err
	}

	return workspaceSsoConnectionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceSsoConnection) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_sso_connections provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceSsoConnectionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceSsoConnectionInsertCacheMut.RLock()
	cache, cached := workspaceSsoConnectionInsertCache[key]
	workspaceSsoConnectionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceSsoConnectionAllColumns,
			workspaceSsoConnectionColumnsWithDefault,
			workspaceSsoConnectionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceSsoConnectionType, workspaceSsoConnectionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceSsoConnectionType, workspaceSsoConnectionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_sso_connections\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_sso_connections\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_sso_connections")
	}

	if !cached {
		workspaceSsoConnectionInsertCacheMut.Lock()
		workspaceSsoConnectionInsertCache[key] = cache
		workspaceSsoConnectionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceSsoConnection.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceSsoConnection) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceSsoConnectionUpdateCacheMut.RLock()
	cache, cached := workspaceSsoConnectionUpdateCache[key]
	workspaceSsoConnectionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceSsoConnectionAllColumns,
			workspaceSsoConnectionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_sso_connections, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_sso_connections\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceSsoConnectionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceSsoConnectionType, workspaceSsoConnectionMapping, append(wl, workspaceSsoConnectionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_sso_connections row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_sso_connections")
	}

	if !cached {
		workspaceSsoConnectionUpdateCacheMut.Lock()
		workspaceSsoConnectionUpdateCache[key] = cache
		workspaceSsoConnectionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceSsoConnectionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_sso_connections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_sso_connections")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceSsoConnectionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSsoConnectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_sso_connections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceSsoConnectionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceSsoConnection slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceSsoConnection")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceSsoConnection) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_sso_connections provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceSsoConnectionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceSsoConnectionUpsertCacheMut.RLock()
	cache, cached := workspaceSsoConnectionUpsertCache[key]
	workspaceSsoConnectionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceSsoConnectionAllColumns,
			workspaceSsoConnectionColumnsWithDefault,
			workspaceSsoConnectionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceSsoConnectionAllColumns,
			workspaceSsoConnectionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_sso_connections, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceSsoConnectionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceSsoConnectionPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_sso_connections, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceSsoConnectionPrimaryKeyColumns))
			copy(conflict, workspaceSsoConnectionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_sso_connections\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceSsoConnectionType, workspaceSsoConnectionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceSsoConnectionType, workspaceSsoConnectionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_sso_connections")
	}

	if !cached {
		workspaceSsoConnectionUpsertCacheMut.Lock()
		workspaceSsoConnectionUpsertCache[key] = cache
		workspaceSsoConnectionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceSsoConnection record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceSsoConnection) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceSsoConnection provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceSsoConnectionPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_sso_connections\" WHERE \"workspace_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_sso_connections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_sso_connections")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceSsoConnectionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceSsoConnectionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_sso_connections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_sso_connections")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceSsoConnectionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceSsoConnectionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSsoConnectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_sso_connections\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceSsoConnectionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceSsoConnection slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_sso_connections")
	}

	if len(workspaceSsoConnectionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceSsoConnection) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceSsoConnection(ctx, exec, o.WorkspaceID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceSsoConnectionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceSsoConnectionSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSsoConnectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_sso_connections\".* FROM \"workspace_sso_connections\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceSsoConnectionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceSsoConnectionSlice")
	}

	*o = slice

	return nil
}

// WorkspaceSsoConnectionExists checks if the WorkspaceSsoConnection row exists.
func WorkspaceSsoConnectionExists(ctx context.Context, exec boil.ContextExecutor, workspaceID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_sso_connections\" where \"workspace_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, workspaceID)
	}
	row := exec.QueryRowContext(ctx, sql, workspaceID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_sso_connections exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceSsoConnection row exists.
func (o *WorkspaceSsoConnection) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceSsoConnectionExists(ctx, exec, o.WorkspaceID)
}
