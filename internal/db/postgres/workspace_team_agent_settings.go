// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceTeamAgentSetting is an object representing the database table.
type WorkspaceTeamAgentSetting struct {
	TeamID           string    `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	WorkspaceID      string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	HoldComments     string    `boil:"hold_comments" json:"hold_comments" toml:"hold_comments" yaml:"hold_comments"`
	HoldStateChanges string    `boil:"hold_state_changes" json:"hold_state_changes" toml:"hold_state_changes" yaml:"hold_state_changes"`
	HoldIssueEdits   string    `boil:"hold_issue_edits" json:"hold_issue_edits" toml:"hold_issue_edits" yaml:"hold_issue_edits"`
	CreatedAt        time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt        time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceTeamAgentSettingR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceTeamAgentSettingL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceTeamAgentSettingColumns = struct {
	TeamID           string
	WorkspaceID      string
	HoldComments     string
	HoldStateChanges string
	HoldIssueEdits   string
	CreatedAt        string
	UpdatedAt        string
}{
	TeamID:           "team_id",
	WorkspaceID:      "workspace_id",
	HoldComments:     "hold_comments",
	HoldStateChanges: "hold_state_changes",
	HoldIssueEdits:   "hold_issue_edits",
	CreatedAt:        "created_at",
	UpdatedAt:        "updated_at",
}

var WorkspaceTeamAgentSettingTableColumns = struct {
	TeamID           string
	WorkspaceID      string
	HoldComments     string
	HoldStateChanges string
	HoldIssueEdits   string
	CreatedAt        string
	UpdatedAt        string
}{
	TeamID:           "workspace_team_agent_settings.team_id",
	WorkspaceID:      "workspace_team_agent_settings.workspace_id",
	HoldComments:     "workspace_team_agent_settings.hold_comments",
	HoldStateChanges: "workspace_team_agent_settings.hold_state_changes",
	HoldIssueEdits:   "workspace_team_agent_settings.hold_issue_edits",
	CreatedAt:        "workspace_team_agent_settings.created_at",
	UpdatedAt:        "workspace_team_agent_settings.updated_at",
}

// Generated where

var WorkspaceTeamAgentSettingWhere = struct {
	TeamID           whereHelperstring
	WorkspaceID      whereHelperstring
	HoldComments     whereHelperstring
	HoldStateChanges whereHelperstring
	HoldIssueEdits   whereHelperstring
	CreatedAt        whereHelpertime_Time
	UpdatedAt        whereHelpertime_Time
}{
	TeamID:           whereHelperstring{field: "\"workspace_team_agent_settings\".\"team_id\""},
	WorkspaceID:      whereHelperstring{field: "\"workspace_team_agent_settings\".\"workspace_id\""},
	HoldComments:     whereHelperstring{field: "\"workspace_team_agent_settings\".\"hold_comments\""},
	HoldStateChanges: whereHelperstring{field: "\"workspace_team_agent_settings\".\"hold_state_changes\""},
	HoldIssueEdits:   whereHelperstring{field: "\"workspace_team_agent_settings\".\"hold_issue_edits\""},
	CreatedAt:        whereHelpertime_Time{field: "\"workspace_team_agent_settings\".\"created_at\""},
	UpdatedAt:        whereHelpertime_Time{field: "\"workspace_team_agent_settings\".\"updated_at\""},
}

// WorkspaceTeamAgentSettingRels is where relationship names are stored.
var WorkspaceTeamAgentSettingRels = struct {
}{}

// workspaceTeamAgentSettingR is where relationships are stored.
type workspaceTeamAgentSettingR struct {
}

// NewStruct creates a new relationship struct
func (*workspaceTeamAgentSettingR) NewStruct() *workspaceTeamAgentSettingR {
	return &workspaceTeamAgentSettingR{}
}

// workspaceTeamAgentSettingL is where Load methods for each relationship are stored.
type workspaceTeamAgentSettingL struct{}

var (
	workspaceTeamAgentSettingAllColumns            = []string{"team_id", "workspace_id", "hold_comments", "hold_state_changes", "hold_issue_edits", "created_at", "updated_at"}
	workspaceTeamAgentSettingColumnsWithoutDefault = []string{"team_id", "workspace_id"}
	workspaceTeamAgentSettingColumnsWithDefault    = []string{"hold_comments", "hold_state_changes", "hold_issue_edits", "created_at", "updated_at"}
	workspaceTeamAgentSettingPrimaryKeyColumns     = []string{"team_id"}
	workspaceTeamAgentSettingGeneratedColumns      = []string{}
)

type (
	// WorkspaceTeamAgentSettingSlice is an alias for a slice of pointers to WorkspaceTeamAgentSetting.
	// This should almost always be used instead of []WorkspaceTeamAgentSetting.
	WorkspaceTeamAgentSettingSlice []*WorkspaceTeamAgentSetting
	// WorkspaceTeamAgentSettingHook is the signature for custom WorkspaceTeamAgentSetting hook methods
	WorkspaceTeamAgentSettingHook func(context.Context, boil.ContextExecutor, *WorkspaceTeamAgentSetting) error

	workspaceTeamAgentSettingQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceTeamAgentSettingType                 = reflect.TypeOf(&WorkspaceTeamAgentSetting{})
	workspaceTeamAgentSettingMapping              = queries.MakeStructMapping(workspaceTeamAgentSettingType)
	workspaceTeamAgentSettingPrimaryKeyMapping, _ = queries.BindMapping(workspaceTeamAgentSettingType, workspaceTeamAgentSettingMapping, workspaceTeamAgentSettingPrimaryKeyColumns)
	workspaceTeamAgentSettingInsertCacheMut       sync.RWMutex
	workspaceTeamAgentSettingInsertCache          = make(map[string]insertCache)
	workspaceTeamAgentSettingUpdateCacheMut       sync.RWMutex
	workspaceTeamAgentSettingUpdateCache          = make(map[string]updateCache)
	workspaceTeamAgentSettingUpsertCacheMut       sync.RWMutex
	workspaceTeamAgentSettingUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceTeamAgentSettingAfterSelectMu sync.Mutex
var workspaceTeamAgentSettingAfterSelectHooks []WorkspaceTeamAgentSettingHook

var workspaceTeamAgentSettingBeforeInsertMu sync.Mutex
var workspaceTeamAgentSettingBeforeInsertHooks []WorkspaceTeamAgentSettingHook
var workspaceTeamAgentSettingAfterInsertMu sync.Mutex
var workspaceTeamAgentSettingAfterInsertHooks []WorkspaceTeamAgentSettingHook

var workspaceTeamAgentSettingBeforeUpdateMu sync.Mutex
var workspaceTeamAgentSettingBeforeUpdateHooks []WorkspaceTeamAgentSettingHook
var workspaceTeamAgentSettingAfterUpdateMu sync.Mutex
var workspaceTeamAgentSettingAfterUpdateHooks []WorkspaceTeamAgentSettingHook

var workspaceTeamAgentSettingBeforeDeleteMu sync.Mutex
var workspaceTeamAgentSettingBeforeDeleteHooks []WorkspaceTeamAgentSettingHook
var workspaceTeamAgentSettingAfterDeleteMu sync.Mutex
var workspaceTeamAgentSettingAfterDeleteHooks []WorkspaceTeamAgentSettingHook

var workspaceTeamAgentSettingBeforeUpsertMu sync.Mutex
var workspaceTeamAgentSettingBeforeUpsertHooks []WorkspaceTeamAgentSettingHook
var workspaceTeamAgentSettingAfterUpsertMu sync.Mutex
var workspaceTeamAgentSettingAfterUpsertHooks []WorkspaceTeamAgentSettingHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceTeamAgentSetting) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceTeamAgentSetting) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceTeamAgentSetting) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceTeamAgentSetting) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceTeamAgentSetting) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceTeamAgentSetting) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceTeamAgentSetting) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceTeamAgentSetting) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceTeamAgentSetting) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamAgentSettingAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceTeamAgentSettingHook registers your hook function for all future operations.
func AddWorkspaceTeamAgentSettingHook(hookPoint boil.HookPoint, workspaceTeamAgentSettingHook WorkspaceTeamAgentSettingHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceTeamAgentSettingAfterSelectMu.Lock()
		workspaceTeamAgentSettingAfterSelectHooks = append(workspaceTeamAgentSettingAfterSelectHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceTeamAgentSettingBeforeInsertMu.Lock()
		workspaceTeamAgentSettingBeforeInsertHooks = append(workspaceTeamAgentSettingBeforeInsertHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceTeamAgentSettingAfterInsertMu.Lock()
		workspaceTeamAgentSettingAfterInsertHooks = append(workspaceTeamAgentSettingAfterInsertHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceTeamAgentSettingBeforeUpdateMu.Lock()
		workspaceTeamAgentSettingBeforeUpdateHooks = append(workspaceTeamAgentSettingBeforeUpdateHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceTeamAgentSettingAfterUpdateMu.Lock()
		workspaceTeamAgentSettingAfterUpdateHooks = append(workspaceTeamAgentSettingAfterUpdateHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceTeamAgentSettingBeforeDeleteMu.Lock()
		workspaceTeamAgentSettingBeforeDeleteHooks = append(workspaceTeamAgentSettingBeforeDeleteHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceTeamAgentSettingAfterDeleteMu.Lock()
		workspaceTeamAgentSettingAfterDeleteHooks = append(workspaceTeamAgentSettingAfterDeleteHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceTeamAgentSettingBeforeUpsertMu.Lock()
		workspaceTeamAgentSettingBeforeUpsertHooks = append(workspaceTeamAgentSettingBeforeUpsertHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceTeamAgentSettingAfterUpsertMu.Lock()
		workspaceTeamAgentSettingAfterUpsertHooks = append(workspaceTeamAgentSettingAfterUpsertHooks, workspaceTeamAgentSettingHook)
		workspaceTeamAgentSettingAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceTeamAgentSetting record from the query.
func (q workspaceTeamAgentSettingQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceTeamAgentSetting, error) {
	o := &WorkspaceTeamAgentSetting{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_team_agent_settings")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceTeamAgentSetting records from the query.
func (q workspaceTeamAgentSettingQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceTeamAgentSettingSlice, error) {
	var o []*WorkspaceTeamAgentSetting

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceTeamAgentSetting slice")
	}

	if len(workspaceTeamAgentSettingAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceTeamAgentSetting records in the query.
func (q workspaceTeamAgentSettingQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_team_agent_settings rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceTeamAgentSettingQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_team_agent_settings exists")
	}

	return count > 0, nil
}

// WorkspaceTeamAgentSettings retrieves all the records using an executor.
func WorkspaceTeamAgentSettings(mods ...qm.QueryMod) workspaceTeamAgentSettingQuery {
	mods = append(mods, qm.From("\"workspace_team_agent_settings\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_team_agent_settings\".*"})
	}

	return workspaceTeamAgentSettingQuery{q}
}

// FindWorkspaceTeamAgentSetting retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceTeamAgentSetting(ctx context.Context, exec boil.ContextExecutor, teamID string, selectCols ...string) (*WorkspaceTeamAgentSetting, error) {
	workspaceTeamAgentSettingObj := &WorkspaceTeamAgentSetting{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_team_agent_settings\" where \"team_id\"=$1", sel,
	)

	q := queries.Raw(query, teamID)

	err := q.Bind(ctx, exec, workspaceTeamAgentSettingObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_team_agent_settings")
	}

	if err = workspaceTeamAgentSettingObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceTeamAgentSettingObj, err
	}

	return workspaceTeamAgentSettingObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceTeamAgentSetting) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_agent_settings provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamAgentSettingColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceTeamAgentSettingInsertCacheMut.RLock()
	cache, cached := workspaceTeamAgentSettingInsertCache[key]
	workspaceTeamAgentSettingInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceTeamAgentSettingAllColumns,
			workspaceTeamAgentSettingColumnsWithDefault,
			workspaceTeamAgentSettingColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamAgentSettingType, workspaceTeamAgentSettingMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceTeamAgentSettingType, workspaceTeamAgentSettingMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_team_agent_settings\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_team_agent_settings\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_team_agent_settings")
	}

	if !cached {
		workspaceTeamAgentSettingInsertCacheMut.Lock()
		workspaceTeamAgentSettingInsertCache[key] = cache
		workspaceTeamAgentSettingInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceTeamAgentSetting.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceTeamAgentSetting) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceTeamAgentSettingUpdateCacheMut.RLock()
	cache, cached := workspaceTeamAgentSettingUpdateCache[key]
	workspaceTeamAgentSettingUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceTeamAgentSettingAllColumns,
			workspaceTeamAgentSettingPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_team_agent_settings, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_team_agent_settings\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceTeamAgentSettingPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceTeamAgentSettingType, workspaceTeamAgentSettingMapping, append(wl, workspaceTeamAgentSettingPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_team_agent_settings row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_team_agent_settings")
	}

	if !cached {
		workspaceTeamAgentSettingUpdateCacheMut.Lock()
		workspaceTeamAgentSettingUpdateCache[key] = cache
		workspaceTeamAgentSettingUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceTeamAgentSettingQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_team_agent_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_team_agent_settings")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceTeamAgentSettingSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamAgentSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_team_agent_settings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceTeamAgentSettingPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceTeamAgentSetting slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceTeamAgentSetting")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceTeamAgentSetting) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_agent_settings provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamAgentSettingColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceTeamAgentSettingUpsertCacheMut.RLock()
	cache, cached := workspaceTeamAgentSettingUpsertCache[key]
	workspaceTeamAgentSettingUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceTeamAgentSettingAllColumns,
			workspaceTeamAgentSettingColumnsWithDefault,
			workspaceTeamAgentSettingColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceTeamAgentSettingAllColumns,
			workspaceTeamAgentSettingPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_team_agent_settings, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceTeamAgentSettingAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceTeamAgentSettingPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_team_agent_settings, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceTeamAgentSettingPrimaryKeyColumns))
			copy(conflict, workspaceTeamAgentSettingPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_team_agent_settings\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamAgentSettingType, workspaceTeamAgentSettingMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceTeamAgentSettingType, workspaceTeamAgentSettingMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_team_agent_settings")
	}

	if !cached {
		workspaceTeamAgentSettingUpsertCacheMut.Lock()
		workspaceTeamAgentSettingUpsertCache[key] = cache
		workspaceTeamAgentSettingUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceTeamAgentSetting record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceTeamAgentSetting) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceTeamAgentSetting provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceTeamAgentSettingPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_team_agent_settings\" WHERE \"team_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_team_agent_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_team_agent_settings")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceTeamAgentSettingQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceTeamAgentSettingQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_team_agent_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_agent_settings")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceTeamAgentSettingSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceTeamAgentSettingBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamAgentSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_team_agent_settings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamAgentSettingPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceTeamAgentSetting slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_agent_settings")
	}

	if len(workspaceTeamAgentSettingAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceTeamAgentSetting) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceTeamAgentSetting(ctx, exec, o.TeamID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceTeamAgentSettingSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceTeamAgentSettingSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamAgentSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_team_agent_settings\".* FROM \"workspace_team_agent_settings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamAgentSettingPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceTeamAgentSettingSlice")
	}

	*o = slice

	return nil
}

// WorkspaceTeamAgentSettingExists checks if the WorkspaceTeamAgentSetting row exists.
func WorkspaceTeamAgentSettingExists(ctx context.Context, exec boil.ContextExecutor, teamID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_team_agent_settings\" where \"team_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, teamID)
	}
	row := exec.QueryRowContext(ctx, sql, teamID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_team_agent_settings exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceTeamAgentSetting row exists.
func (o *WorkspaceTeamAgentSetting) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceTeamAgentSettingExists(ctx, exec, o.TeamID)
}
