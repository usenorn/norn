// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceNotificationDigest is an object representing the database table.
type WorkspaceNotificationDigest struct {
	WorkspaceID     string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	AccountID       string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	WindowStartedAt time.Time `boil:"window_started_at" json:"window_started_at" toml:"window_started_at" yaml:"window_started_at"`
	SentAt          null.Time `boil:"sent_at" json:"sent_at,omitempty" toml:"sent_at" yaml:"sent_at,omitempty"`
	FailedAt        null.Time `boil:"failed_at" json:"failed_at,omitempty" toml:"failed_at" yaml:"failed_at,omitempty"`
	Failure         string    `boil:"failure" json:"failure" toml:"failure" yaml:"failure"`
	CreatedAt       time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceNotificationDigestR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceNotificationDigestL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceNotificationDigestColumns = struct {
	WorkspaceID     string
	AccountID       string
	WindowStartedAt string
	SentAt          string
	FailedAt        string
	Failure         string
	CreatedAt       string
	UpdatedAt       string
}{
	WorkspaceID:     "workspace_id",
	AccountID:       "account_id",
	WindowStartedAt: "window_started_at",
	SentAt:          "sent_at",
	FailedAt:        "failed_at",
	Failure:         "failure",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var WorkspaceNotificationDigestTableColumns = struct {
	WorkspaceID     string
	AccountID       string
	WindowStartedAt string
	SentAt          string
	FailedAt        string
	Failure         string
	CreatedAt       string
	UpdatedAt       string
}{
	WorkspaceID:     "workspace_notification_digests.workspace_id",
	AccountID:       "workspace_notification_digests.account_id",
	WindowStartedAt: "workspace_notification_digests.window_started_at",
	SentAt:          "workspace_notification_digests.sent_at",
	FailedAt:        "workspace_notification_digests.failed_at",
	Failure:         "workspace_notification_digests.failure",
	CreatedAt:       "workspace_notification_digests.created_at",
	UpdatedAt:       "workspace_notification_digests.updated_at",
}

// Generated where

var WorkspaceNotificationDigestWhere = struct {
	WorkspaceID     whereHelperstring
	AccountID       whereHelperstring
	WindowStartedAt whereHelpertime_Time
	SentAt          whereHelpernull_Time
	FailedAt        whereHelpernull_Time
	Failure         whereHelperstring
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
}{
	WorkspaceID:     whereHelperstring{field: "\"workspace_notification_digests\".\"workspace_id\""},
	AccountID:       whereHelperstring{field: "\"workspace_notification_digests\".\"account_id\""},
	WindowStartedAt: whereHelpertime_Time{field: "\"workspace_notification_digests\".\"window_started_at\""},
	SentAt:          whereHelpernull_Time{field: "\"workspace_notification_digests\".\"sent_at\""},
	FailedAt:        whereHelpernull_Time{field: "\"workspace_notification_digests\".\"failed_at\""},
	Failure:         whereHelperstring{field: "\"workspace_notification_digests\".\"failure\""},
	CreatedAt:       whereHelpertime_Time{field: "\"workspace_notification_digests\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"workspace_notification_digests\".\"updated_at\""},
}

// WorkspaceNotificationDigestRels is where relationship names are stored.
var WorkspaceNotificationDigestRels = struct {
}{}

// workspaceNotificationDigestR is where relationships are stored.
type workspaceNotificationDigestR struct {
}

// NewStruct creates a new relationship struct
func (*workspaceNotificationDigestR) NewStruct() *workspaceNotificationDigestR {
	return &workspaceNotificationDigestR{}
}

// workspaceNotificationDigestL is where Load methods for each relationship are stored.
type workspaceNotificationDigestL struct{}

var (
	workspaceNotificationDigestAllColumns            = []string{"workspace_id", "account_id", "window_started_at", "sent_at", "failed_at", "failure", "created_at", "updated_at"}
	workspaceNotificationDigestColumnsWithoutDefault = []string{"workspace_id", "account_id", "window_started_at"}
	workspaceNotificationDigestColumnsWithDefault    = []string{"sent_at", "failed_at", "failure", "created_at", "updated_at"}
	workspaceNotificationDigestPrimaryKeyColumns     = []string{"workspace_id", "account_id", "window_started_at"}
	workspaceNotificationDigestGeneratedColumns      = []string{}
)

type (
	// WorkspaceNotificationDigestSlice is an alias for a slice of pointers to WorkspaceNotificationDigest.
	// This should almost always be used instead of []WorkspaceNotificationDigest.
	WorkspaceNotificationDigestSlice []*WorkspaceNotificationDigest
	// WorkspaceNotificationDigestHook is the signature for custom WorkspaceNotificationDigest hook methods
	WorkspaceNotificationDigestHook func(context.Context, boil.ContextExecutor, *WorkspaceNotificationDigest) error

	workspaceNotificationDigestQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceNotificationDigestType                 = reflect.TypeOf(&WorkspaceNotificationDigest{})
	workspaceNotificationDigestMapping              = queries.MakeStructMapping(workspaceNotificationDigestType)
	workspaceNotificationDigestPrimaryKeyMapping, _ = queries.BindMapping(workspaceNotificationDigestType, workspaceNotificationDigestMapping, workspaceNotificationDigestPrimaryKeyColumns)
	workspaceNotificationDigestInsertCacheMut       sync.RWMutex
	workspaceNotificationDigestInsertCache          = make(map[string]insertCache)
	workspaceNotificationDigestUpdateCacheMut       sync.RWMutex
	workspaceNotificationDigestUpdateCache          = make(map[string]updateCache)
	workspaceNotificationDigestUpsertCacheMut       sync.RWMutex
	workspaceNotificationDigestUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceNotificationDigestAfterSelectMu sync.Mutex
var workspaceNotificationDigestAfterSelectHooks []WorkspaceNotificationDigestHook

var workspaceNotificationDigestBeforeInsertMu sync.Mutex
var workspaceNotificationDigestBeforeInsertHooks []WorkspaceNotificationDigestHook
var workspaceNotificationDigestAfterInsertMu sync.Mutex
var workspaceNotificationDigestAfterInsertHooks []WorkspaceNotificationDigestHook

var workspaceNotificationDigestBeforeUpdateMu sync.Mutex
var workspaceNotificationDigestBeforeUpdateHooks []WorkspaceNotificationDigestHook
var workspaceNotificationDigestAfterUpdateMu sync.Mutex
var workspaceNotificationDigestAfterUpdateHooks []WorkspaceNotificationDigestHook

var workspaceNotificationDigestBeforeDeleteMu sync.Mutex
var workspaceNotificationDigestBeforeDeleteHooks []WorkspaceNotificationDigestHook
var workspaceNotificationDigestAfterDeleteMu sync.Mutex
var workspaceNotificationDigestAfterDeleteHooks []WorkspaceNotificationDigestHook

var workspaceNotificationDigestBeforeUpsertMu sync.Mutex
var workspaceNotificationDigestBeforeUpsertHooks []WorkspaceNotificationDigestHook
var workspaceNotificationDigestAfterUpsertMu sync.Mutex
var workspaceNotificationDigestAfterUpsertHooks []WorkspaceNotificationDigestHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceNotificationDigest) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceNotificationDigest) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceNotificationDigest) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceNotificationDigest) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceNotificationDigest) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceNotificationDigest) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceNotificationDigest) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceNotificationDigest) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceNotificationDigest) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDigestAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceNotificationDigestHook registers your hook function for all future operations.
func AddWorkspaceNotificationDigestHook(hookPoint boil.HookPoint, workspaceNotificationDigestHook WorkspaceNotificationDigestHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceNotificationDigestAfterSelectMu.Lock()
		workspaceNotificationDigestAfterSelectHooks = append(workspaceNotificationDigestAfterSelectHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceNotificationDigestBeforeInsertMu.Lock()
		workspaceNotificationDigestBeforeInsertHooks = append(workspaceNotificationDigestBeforeInsertHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceNotificationDigestAfterInsertMu.Lock()
		workspaceNotificationDigestAfterInsertHooks = append(workspaceNotificationDigestAfterInsertHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceNotificationDigestBeforeUpdateMu.Lock()
		workspaceNotificationDigestBeforeUpdateHooks = append(workspaceNotificationDigestBeforeUpdateHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceNotificationDigestAfterUpdateMu.Lock()
		workspaceNotificationDigestAfterUpdateHooks = append(workspaceNotificationDigestAfterUpdateHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceNotificationDigestBeforeDeleteMu.Lock()
		workspaceNotificationDigestBeforeDeleteHooks = append(workspaceNotificationDigestBeforeDeleteHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceNotificationDigestAfterDeleteMu.Lock()
		workspaceNotificationDigestAfterDeleteHooks = append(workspaceNotificationDigestAfterDeleteHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceNotificationDigestBeforeUpsertMu.Lock()
		workspaceNotificationDigestBeforeUpsertHooks = append(workspaceNotificationDigestBeforeUpsertHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceNotificationDigestAfterUpsertMu.Lock()
		workspaceNotificationDigestAfterUpsertHooks = append(workspaceNotificationDigestAfterUpsertHooks, workspaceNotificationDigestHook)
		workspaceNotificationDigestAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceNotificationDigest record from the query.
func (q workspaceNotificationDigestQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceNotificationDigest, error) {
	o := &WorkspaceNotificationDigest{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_notification_digests")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceNotificationDigest records from the query.
func (q workspaceNotificationDigestQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceNotificationDigestSlice, error) {
	var o []*WorkspaceNotificationDigest

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceNotificationDigest slice")
	}

	if len(workspaceNotificationDigestAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceNotificationDigest records in the query.
func (q workspaceNotificationDigestQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_notification_digests rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceNotificationDigestQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_notification_digests exists")
	}

	return count > 0, nil
}

// WorkspaceNotificationDigests retrieves all the records using an executor.
func WorkspaceNotificationDigests(mods ...qm.QueryMod) workspaceNotificationDigestQuery {
	mods = append(mods, qm.From("\"workspace_notification_digests\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_notification_digests\".*"})
	}

	return workspaceNotificationDigestQuery{q}
}

// FindWorkspaceNotificationDigest retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceNotificationDigest(ctx context.Context, exec boil.ContextExecutor, workspaceID string, accountID string, windowStartedAt time.Time, selectCols ...string) (*WorkspaceNotificationDigest, error) {
	workspaceNotificationDigestObj := &WorkspaceNotificationDigest{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_notification_digests\" where \"workspace_id\"=$1 AND \"account_id\"=$2 AND \"window_started_at\"=$3", sel,
	)

	q := queries.Raw(query, workspaceID, accountID, windowStartedAt)

	err := q.Bind(ctx, exec, workspaceNotificationDigestObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_notification_digests")
	}

	if err = workspaceNotificationDigestObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceNotificationDigestObj, err
	}

	return workspaceNotificationDigestObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceNotificationDigest) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_notification_digests provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceNotificationDigestColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceNotificationDigestInsertCacheMut.RLock()
	cache, cached := workspaceNotificationDigestInsertCache[key]
	workspaceNotificationDigestInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceNotificationDigestAllColumns,
			workspaceNotificationDigestColumnsWithDefault,
			workspaceNotificationDigestColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceNotificationDigestType, workspaceNotificationDigestMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceNotificationDigestType, workspaceNotificationDigestMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_notification_digests\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_notification_digests\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_notification_digests")
	}

	if !cached {
		workspaceNotificationDigestInsertCacheMut.Lock()
		workspaceNotificationDigestInsertCache[key] = cache
		workspaceNotificationDigestInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceNotificationDigest.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceNotificationDigest) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceNotificationDigestUpdateCacheMut.RLock()
	cache, cached := workspaceNotificationDigestUpdateCache[key]
	workspaceNotificationDigestUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceNotificationDigestAllColumns,
			workspaceNotificationDigestPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_notification_digests, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_notification_digests\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceNotificationDigestPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceNotificationDigestType, workspaceNotificationDigestMapping, append(wl, workspaceNotificationDigestPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_notification_digests row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_notification_digests")
	}

	if !cached {
		workspaceNotificationDigestUpdateCacheMut.Lock()
		workspaceNotificationDigestUpdateCache[key] = cache
		workspaceNotificationDigestUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceNotificationDigestQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_notification_digests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_notification_digests")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceNotificationDigestSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceNotificationDigestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_notification_digests\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceNotificationDigestPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceNotificationDigest slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceNotificationDigest")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceNotificationDigest) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_notification_digests provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceNotificationDigestColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceNotificationDigestUpsertCacheMut.RLock()
	cache, cached := workspaceNotificationDigestUpsertCache[key]
	workspaceNotificationDigestUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceNotificationDigestAllColumns,
			workspaceNotificationDigestColumnsWithDefault,
			workspaceNotificationDigestColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceNotificationDigestAllColumns,
			workspaceNotificationDigestPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_notification_digests, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceNotificationDigestAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceNotificationDigestPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_notification_digests, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceNotificationDigestPrimaryKeyColumns))
			copy(conflict, workspaceNotificationDigestPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_notification_digests\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceNotificationDigestType, workspaceNotificationDigestMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceNotificationDigestType, workspaceNotificationDigestMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_notification_digests")
	}

	if !cached {
		workspaceNotificationDigestUpsertCacheMut.Lock()
		workspaceNotificationDigestUpsertCache[key] = cache
		workspaceNotificationDigestUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceNotificationDigest record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceNotificationDigest) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceNotificationDigest provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceNotificationDigestPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_notification_digests\" WHERE \"workspace_id\"=$1 AND \"account_id\"=$2 AND \"window_started_at\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_notification_digests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_notification_digests")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceNotificationDigestQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceNotificationDigestQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_notification_digests")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_notification_digests")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceNotificationDigestSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceNotificationDigestBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceNotificationDigestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_notification_digests\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceNotificationDigestPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceNotificationDigest slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_notification_digests")
	}

	if len(workspaceNotificationDigestAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceNotificationDigest) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceNotificationDigest(ctx, exec, o.WorkspaceID, o.AccountID, o.WindowStartedAt)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceNotificationDigestSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceNotificationDigestSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceNotificationDigestPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_notification_digests\".* FROM \"workspace_notification_digests\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceNotificationDigestPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceNotificationDigestSlice")
	}

	*o = slice

	return nil
}

// WorkspaceNotificationDigestExists checks if the WorkspaceNotificationDigest row exists.
func WorkspaceNotificationDigestExists(ctx context.Context, exec boil.ContextExecutor, workspaceID string, accountID string, windowStartedAt time.Time) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_notification_digests\" where \"workspace_id\"=$1 AND \"account_id\"=$2 AND \"window_started_at\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, workspaceID, accountID, windowStartedAt)
	}
	row := exec.QueryRowContext(ctx, sql, workspaceID, accountID, windowStartedAt)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_notification_digests exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceNotificationDigest row exists.
func (o *WorkspaceNotificationDigest) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceNotificationDigestExists(ctx, exec, o.WorkspaceID, o.AccountID, o.WindowStartedAt)
}
