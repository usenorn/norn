// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// Account is an object representing the database table.
type Account struct {
	ID              string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Status          string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	Email           null.String `boil:"email" json:"email,omitempty" toml:"email" yaml:"email,omitempty"`
	DisplayName     null.String `boil:"display_name" json:"display_name,omitempty" toml:"display_name" yaml:"display_name,omitempty"`
	AvatarObjectKey null.String `boil:"avatar_object_key" json:"avatar_object_key,omitempty" toml:"avatar_object_key" yaml:"avatar_object_key,omitempty"`
	Timezone        null.String `boil:"timezone" json:"timezone,omitempty" toml:"timezone" yaml:"timezone,omitempty"`
	PasswordHash    null.String `boil:"password_hash" json:"password_hash,omitempty" toml:"password_hash" yaml:"password_hash,omitempty"`
	DeactivatedAt   null.Time   `boil:"deactivated_at" json:"deactivated_at,omitempty" toml:"deactivated_at" yaml:"deactivated_at,omitempty"`
	DeletedAt       null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	CreatedAt       time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	InstanceAdmin   bool        `boil:"instance_admin" json:"instance_admin" toml:"instance_admin" yaml:"instance_admin"`
	Kind            string      `boil:"kind" json:"kind" toml:"kind" yaml:"kind"`

	R *accountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L accountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AccountColumns = struct {
	ID              string
	Status          string
	Email           string
	DisplayName     string
	AvatarObjectKey string
	Timezone        string
	PasswordHash    string
	DeactivatedAt   string
	DeletedAt       string
	CreatedAt       string
	UpdatedAt       string
	InstanceAdmin   string
	Kind            string
}{
	ID:              "id",
	Status:          "status",
	Email:           "email",
	DisplayName:     "display_name",
	AvatarObjectKey: "avatar_object_key",
	Timezone:        "timezone",
	PasswordHash:    "password_hash",
	DeactivatedAt:   "deactivated_at",
	DeletedAt:       "deleted_at",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
	InstanceAdmin:   "instance_admin",
	Kind:            "kind",
}

var AccountTableColumns = struct {
	ID              string
	Status          string
	Email           string
	DisplayName     string
	AvatarObjectKey string
	Timezone        string
	PasswordHash    string
	DeactivatedAt   string
	DeletedAt       string
	CreatedAt       string
	UpdatedAt       string
	InstanceAdmin   string
	Kind            string
}{
	ID:              "accounts.id",
	Status:          "accounts.status",
	Email:           "accounts.email",
	DisplayName:     "accounts.display_name",
	AvatarObjectKey: "accounts.avatar_object_key",
	Timezone:        "accounts.timezone",
	PasswordHash:    "accounts.password_hash",
	DeactivatedAt:   "accounts.deactivated_at",
	DeletedAt:       "accounts.deleted_at",
	CreatedAt:       "accounts.created_at",
	UpdatedAt:       "accounts.updated_at",
	InstanceAdmin:   "accounts.instance_admin",
	Kind:            "accounts.kind",
}

// Generated where

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_String) LIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" LIKE ?", x)
}
func (w whereHelpernull_String) NLIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT LIKE ?", x)
}
func (w whereHelpernull_String) ILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" ILIKE ?", x)
}
func (w whereHelpernull_String) NILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT ILIKE ?", x)
}
func (w whereHelpernull_String) SIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" SIMILAR TO ?", x)
}
func (w whereHelpernull_String) NSIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT SIMILAR TO ?", x)
}
func (w whereHelpernull_String) IN(slice []string) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_String) NIN(slice []string) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelperbool struct{ field string }

func (w whereHelperbool) EQ(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperbool) NEQ(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperbool) LT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperbool) LTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperbool) GT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperbool) GTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

var AccountWhere = struct {
	ID              whereHelperstring
	Status          whereHelperstring
	Email           whereHelpernull_String
	DisplayName     whereHelpernull_String
	AvatarObjectKey whereHelpernull_String
	Timezone        whereHelpernull_String
	PasswordHash    whereHelpernull_String
	DeactivatedAt   whereHelpernull_Time
	DeletedAt       whereHelpernull_Time
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
	InstanceAdmin   whereHelperbool
	Kind            whereHelperstring
}{
	ID:              whereHelperstring{field: "\"accounts\".\"id\""},
	Status:          whereHelperstring{field: "\"accounts\".\"status\""},
	Email:           whereHelpernull_String{field: "\"accounts\".\"email\""},
	DisplayName:     whereHelpernull_String{field: "\"accounts\".\"display_name\""},
	AvatarObjectKey: whereHelpernull_String{field: "\"accounts\".\"avatar_object_key\""},
	Timezone:        whereHelpernull_String{field: "\"accounts\".\"timezone\""},
	PasswordHash:    whereHelpernull_String{field: "\"accounts\".\"password_hash\""},
	DeactivatedAt:   whereHelpernull_Time{field: "\"accounts\".\"deactivated_at\""},
	DeletedAt:       whereHelpernull_Time{field: "\"accounts\".\"deleted_at\""},
	CreatedAt:       whereHelpertime_Time{field: "\"accounts\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"accounts\".\"updated_at\""},
	InstanceAdmin:   whereHelperbool{field: "\"accounts\".\"instance_admin\""},
	Kind:            whereHelperstring{field: "\"accounts\".\"kind\""},
}

// AccountRels is where relationship names are stored.
var AccountRels = struct {
	AccountEmailChanges                        string
	AccountPasswordHistories                   string
	AccountPasswordResets                      string
	ActorAccountWorkspaceActivities            string
	IssuedByWorkspaceBreakGlassCodes           string
	RequestedByAccountWorkspaceBulkActions     string
	ActorAccountWorkspaceCycleScopeChanges     string
	ClosedByWorkspaceCycles                    string
	AcceptedByAccountWorkspaceInvitations      string
	InvitedByAccountWorkspaceInvitations       string
	WorkspaceIssueCommentMentions              string
	WorkspaceIssueCommentReactions             string
	CreatedByAccountWorkspaceIssueRelations    string
	AssigneeAccountWorkspaceIssues             string
	CreatedByAccountWorkspaceIssues            string
	TriageDecidedByAccountWorkspaceIssues      string
	WorkspaceMemberships                       string
	AuthorAccountWorkspaceProjectStatusUpdates string
	LeadAccountWorkspaceProjects               string
	WorkspaceSsoIdentities                     string
}{
	AccountEmailChanges:                        "AccountEmailChanges",
	AccountPasswordHistories:                   "AccountPasswordHistories",
	AccountPasswordResets:                      "AccountPasswordResets",
	ActorAccountWorkspaceActivities:            "ActorAccountWorkspaceActivities",
	IssuedByWorkspaceBreakGlassCodes:           "IssuedByWorkspaceBreakGlassCodes",
	RequestedByAccountWorkspaceBulkActions:     "RequestedByAccountWorkspaceBulkActions",
	ActorAccountWorkspaceCycleScopeChanges:     "ActorAccountWorkspaceCycleScopeChanges",
	ClosedByWorkspaceCycles:                    "ClosedByWorkspaceCycles",
	AcceptedByAccountWorkspaceInvitations:      "AcceptedByAccountWorkspaceInvitations",
	InvitedByAccountWorkspaceInvitations:       "InvitedByAccountWorkspaceInvitations",
	WorkspaceIssueCommentMentions:              "WorkspaceIssueCommentMentions",
	WorkspaceIssueCommentReactions:             "WorkspaceIssueCommentReactions",
	CreatedByAccountWorkspaceIssueRelations:    "CreatedByAccountWorkspaceIssueRelations",
	AssigneeAccountWorkspaceIssues:             "AssigneeAccountWorkspaceIssues",
	CreatedByAccountWorkspaceIssues:            "CreatedByAccountWorkspaceIssues",
	TriageDecidedByAccountWorkspaceIssues:      "TriageDecidedByAccountWorkspaceIssues",
	WorkspaceMemberships:                       "WorkspaceMemberships",
	AuthorAccountWorkspaceProjectStatusUpdates: "AuthorAccountWorkspaceProjectStatusUpdates",
	LeadAccountWorkspaceProjects:               "LeadAccountWorkspaceProjects",
	WorkspaceSsoIdentities:                     "WorkspaceSsoIdentities",
}

// accountR is where relationships are stored.
type accountR struct {
	AccountEmailChanges                        AccountEmailChangeSlice            `boil:"AccountEmailChanges" json:"AccountEmailChanges" toml:"AccountEmailChanges" yaml:"AccountEmailChanges"`
	AccountPasswordHistories                   AccountPasswordHistorySlice        `boil:"AccountPasswordHistories" json:"AccountPasswordHistories" toml:"AccountPasswordHistories" yaml:"AccountPasswordHistories"`
	AccountPasswordResets                      AccountPasswordResetSlice          `boil:"AccountPasswordResets" json:"AccountPasswordResets" toml:"AccountPasswordResets" yaml:"AccountPasswordResets"`
	ActorAccountWorkspaceActivities            WorkspaceActivitySlice             `boil:"ActorAccountWorkspaceActivities" json:"ActorAccountWorkspaceActivities" toml:"ActorAccountWorkspaceActivities" yaml:"ActorAccountWorkspaceActivities"`
	IssuedByWorkspaceBreakGlassCodes           WorkspaceBreakGlassCodeSlice       `boil:"IssuedByWorkspaceBreakGlassCodes" json:"IssuedByWorkspaceBreakGlassCodes" toml:"IssuedByWorkspaceBreakGlassCodes" yaml:"IssuedByWorkspaceBreakGlassCodes"`
	RequestedByAccountWorkspaceBulkActions     WorkspaceBulkActionSlice           `boil:"RequestedByAccountWorkspaceBulkActions" json:"RequestedByAccountWorkspaceBulkActions" toml:"RequestedByAccountWorkspaceBulkActions" yaml:"RequestedByAccountWorkspaceBulkActions"`
	ActorAccountWorkspaceCycleScopeChanges     WorkspaceCycleScopeChangeSlice     `boil:"ActorAccountWorkspaceCycleScopeChanges" json:"ActorAccountWorkspaceCycleScopeChanges" toml:"ActorAccountWorkspaceCycleScopeChanges" yaml:"ActorAccountWorkspaceCycleScopeChanges"`
	ClosedByWorkspaceCycles                    WorkspaceCycleSlice                `boil:"ClosedByWorkspaceCycles" json:"ClosedByWorkspaceCycles" toml:"ClosedByWorkspaceCycles" yaml:"ClosedByWorkspaceCycles"`
	AcceptedByAccountWorkspaceInvitations      WorkspaceInvitationSlice           `boil:"AcceptedByAccountWorkspaceInvitations" json:"AcceptedByAccountWorkspaceInvitations" toml:"AcceptedByAccountWorkspaceInvitations" yaml:"AcceptedByAccountWorkspaceInvitations"`
	InvitedByAccountWorkspaceInvitations       WorkspaceInvitationSlice           `boil:"InvitedByAccountWorkspaceInvitations" json:"InvitedByAccountWorkspaceInvitations" toml:"InvitedByAccountWorkspaceInvitations" yaml:"InvitedByAccountWorkspaceInvitations"`
	WorkspaceIssueCommentMentions              WorkspaceIssueCommentMentionSlice  `boil:"WorkspaceIssueCommentMentions" json:"WorkspaceIssueCommentMentions" toml:"WorkspaceIssueCommentMentions" yaml:"WorkspaceIssueCommentMentions"`
	WorkspaceIssueCommentReactions             WorkspaceIssueCommentReactionSlice `boil:"WorkspaceIssueCommentReactions" json:"WorkspaceIssueCommentReactions" toml:"WorkspaceIssueCommentReactions" yaml:"WorkspaceIssueCommentReactions"`
	CreatedByAccountWorkspaceIssueRelations    WorkspaceIssueRelationSlice        `boil:"CreatedByAccountWorkspaceIssueRelations" json:"CreatedByAccountWorkspaceIssueRelations" toml:"CreatedByAccountWorkspaceIssueRelations" yaml:"CreatedByAccountWorkspaceIssueRelations"`
	AssigneeAccountWorkspaceIssues             WorkspaceIssueSlice                `boil:"AssigneeAccountWorkspaceIssues" json:"AssigneeAccountWorkspaceIssues" toml:"AssigneeAccountWorkspaceIssues" yaml:"AssigneeAccountWorkspaceIssues"`
	CreatedByAccountWorkspaceIssues            WorkspaceIssueSlice                `boil:"CreatedByAccountWorkspaceIssues" json:"CreatedByAccountWorkspaceIssues" toml:"CreatedByAccountWorkspaceIssues" yaml:"CreatedByAccountWorkspaceIssues"`
	TriageDecidedByAccountWorkspaceIssues      WorkspaceIssueSlice                `boil:"TriageDecidedByAccountWorkspaceIssues" json:"TriageDecidedByAccountWorkspaceIssues" toml:"TriageDecidedByAccountWorkspaceIssues" yaml:"TriageDecidedByAccountWorkspaceIssues"`
	WorkspaceMemberships                       WorkspaceMembershipSlice           `boil:"WorkspaceMemberships" json:"WorkspaceMemberships" toml:"WorkspaceMemberships" yaml:"WorkspaceMemberships"`
	AuthorAccountWorkspaceProjectStatusUpdates WorkspaceProjectStatusUpdateSlice  `boil:"AuthorAccountWorkspaceProjectStatusUpdates" json:"AuthorAccountWorkspaceProjectStatusUpdates" toml:"AuthorAccountWorkspaceProjectStatusUpdates" yaml:"AuthorAccountWorkspaceProjectStatusUpdates"`
	LeadAccountWorkspaceProjects               WorkspaceProjectSlice              `boil:"LeadAccountWorkspaceProjects" json:"LeadAccountWorkspaceProjects" toml:"LeadAccountWorkspaceProjects" yaml:"LeadAccountWorkspaceProjects"`
	WorkspaceSsoIdentities                     WorkspaceSsoIdentitySlice          `boil:"WorkspaceSsoIdentities" json:"WorkspaceSsoIdentities" toml:"WorkspaceSsoIdentities" yaml:"WorkspaceSsoIdentities"`
}

// NewStruct creates a new relationship struct
func (*accountR) NewStruct() *accountR {
	return &accountR{}
}

func (o *Account) GetAccountEmailChanges() AccountEmailChangeSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAccountEmailChanges()
}

func (r *accountR) GetAccountEmailChanges() AccountEmailChangeSlice {
	if r == nil {
		return nil
	}

	return r.AccountEmailChanges
}

func (o *Account) GetAccountPasswordHistories() AccountPasswordHistorySlice {
	if o == nil {
		return nil
	}

	return o.R.GetAccountPasswordHistories()
}

func (r *accountR) GetAccountPasswordHistories() AccountPasswordHistorySlice {
	if r == nil {
		return nil
	}

	return r.AccountPasswordHistories
}

func (o *Account) GetAccountPasswordResets() AccountPasswordResetSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAccountPasswordResets()
}

func (r *accountR) GetAccountPasswordResets() AccountPasswordResetSlice {
	if r == nil {
		return nil
	}

	return r.AccountPasswordResets
}

func (o *Account) GetActorAccountWorkspaceActivities() WorkspaceActivitySlice {
	if o == nil {
		return nil
	}

	return o.R.GetActorAccountWorkspaceActivities()
}

func (r *accountR) GetActorAccountWorkspaceActivities() WorkspaceActivitySlice {
	if r == nil {
		return nil
	}

	return r.ActorAccountWorkspaceActivities
}

func (o *Account) GetIssuedByWorkspaceBreakGlassCodes() WorkspaceBreakGlassCodeSlice {
	if o == nil {
		return nil
	}

	return o.R.GetIssuedByWorkspaceBreakGlassCodes()
}

func (r *accountR) GetIssuedByWorkspaceBreakGlassCodes() WorkspaceBreakGlassCodeSlice {
	if r == nil {
		return nil
	}

	return r.IssuedByWorkspaceBreakGlassCodes
}

func (o *Account) GetRequestedByAccountWorkspaceBulkActions() WorkspaceBulkActionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetRequestedByAccountWorkspaceBulkActions()
}

func (r *accountR) GetRequestedByAccountWorkspaceBulkActions() WorkspaceBulkActionSlice {
	if r == nil {
		return nil
	}

	return r.RequestedByAccountWorkspaceBulkActions
}

func (o *Account) GetActorAccountWorkspaceCycleScopeChanges() WorkspaceCycleScopeChangeSlice {
	if o == nil {
		return nil
	}

	return o.R.GetActorAccountWorkspaceCycleScopeChanges()
}

func (r *accountR) GetActorAccountWorkspaceCycleScopeChanges() WorkspaceCycleScopeChangeSlice {
	if r == nil {
		return nil
	}

	return r.ActorAccountWorkspaceCycleScopeChanges
}

func (o *Account) GetClosedByWorkspaceCycles() WorkspaceCycleSlice {
	if o == nil {
		return nil
	}

	return o.R.GetClosedByWorkspaceCycles()
}

func (r *accountR) GetClosedByWorkspaceCycles() WorkspaceCycleSlice {
	if r == nil {
		return nil
	}

	return r.ClosedByWorkspaceCycles
}

func (o *Account) GetAcceptedByAccountWorkspaceInvitations() WorkspaceInvitationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAcceptedByAccountWorkspaceInvitations()
}

func (r *accountR) GetAcceptedByAccountWorkspaceInvitations() WorkspaceInvitationSlice {
	if r == nil {
		return nil
	}

	return r.AcceptedByAccountWorkspaceInvitations
}

func (o *Account) GetInvitedByAccountWorkspaceInvitations() WorkspaceInvitationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetInvitedByAccountWorkspaceInvitations()
}

func (r *accountR) GetInvitedByAccountWorkspaceInvitations() WorkspaceInvitationSlice {
	if r == nil {
		return nil
	}

	return r.InvitedByAccountWorkspaceInvitations
}

func (o *Account) GetWorkspaceIssueCommentMentions() WorkspaceIssueCommentMentionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspaceIssueCommentMentions()
}

func (r *accountR) GetWorkspaceIssueCommentMentions() WorkspaceIssueCommentMentionSlice {
	if r == nil {
		return nil
	}

	return r.WorkspaceIssueCommentMentions
}

func (o *Account) GetWorkspaceIssueCommentReactions() WorkspaceIssueCommentReactionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspaceIssueCommentReactions()
}

func (r *accountR) GetWorkspaceIssueCommentReactions() WorkspaceIssueCommentReactionSlice {
	if r == nil {
		return nil
	}

	return r.WorkspaceIssueCommentReactions
}

func (o *Account) GetCreatedByAccountWorkspaceIssueRelations() WorkspaceIssueRelationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByAccountWorkspaceIssueRelations()
}

func (r *accountR) GetCreatedByAccountWorkspaceIssueRelations() WorkspaceIssueRelationSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByAccountWorkspaceIssueRelations
}

func (o *Account) GetAssigneeAccountWorkspaceIssues() WorkspaceIssueSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAssigneeAccountWorkspaceIssues()
}

func (r *accountR) GetAssigneeAccountWorkspaceIssues() WorkspaceIssueSlice {
	if r == nil {
		return nil
	}

	return r.AssigneeAccountWorkspaceIssues
}

func (o *Account) GetCreatedByAccountWorkspaceIssues() WorkspaceIssueSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByAccountWorkspaceIssues()
}

func (r *accountR) GetCreatedByAccountWorkspaceIssues() WorkspaceIssueSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByAccountWorkspaceIssues
}

func (o *Account) GetTriageDecidedByAccountWorkspaceIssues() WorkspaceIssueSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTriageDecidedByAccountWorkspaceIssues()
}

func (r *accountR) GetTriageDecidedByAccountWorkspaceIssues() WorkspaceIssueSlice {
	if r == nil {
		return nil
	}

	return r.TriageDecidedByAccountWorkspaceIssues
}

func (o *Account) GetWorkspaceMemberships() WorkspaceMembershipSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspaceMemberships()
}

func (r *accountR) GetWorkspaceMemberships() WorkspaceMembershipSlice {
	if r == nil {
		return nil
	}

	return r.WorkspaceMemberships
}

func (o *Account) GetAuthorAccountWorkspaceProjectStatusUpdates() WorkspaceProjectStatusUpdateSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAuthorAccountWorkspaceProjectStatusUpdates()
}

func (r *accountR) GetAuthorAccountWorkspaceProjectStatusUpdates() WorkspaceProjectStatusUpdateSlice {
	if r == nil {
		return nil
	}

	return r.AuthorAccountWorkspaceProjectStatusUpdates
}

func (o *Account) GetLeadAccountWorkspaceProjects() WorkspaceProjectSlice {
	if o == nil {
		return nil
	}

	return o.R.GetLeadAccountWorkspaceProjects()
}

func (r *accountR) GetLeadAccountWorkspaceProjects() WorkspaceProjectSlice {
	if r == nil {
		return nil
	}

	return r.LeadAccountWorkspaceProjects
}

func (o *Account) GetWorkspaceSsoIdentities() WorkspaceSsoIdentitySlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspaceSsoIdentities()
}

func (r *accountR) GetWorkspaceSsoIdentities() WorkspaceSsoIdentitySlice {
	if r == nil {
		return nil
	}

	return r.WorkspaceSsoIdentities
}

// accountL is where Load methods for each relationship are stored.
type accountL struct{}

var (
	accountAllColumns            = []string{"id", "status", "email", "display_name", "avatar_object_key", "timezone", "password_hash", "deactivated_at", "deleted_at", "created_at", "updated_at", "instance_admin", "kind"}
	accountColumnsWithoutDefault = []string{"status"}
	accountColumnsWithDefault    = []string{"id", "email", "display_name", "avatar_object_key", "timezone", "password_hash", "deactivated_at", "deleted_at", "created_at", "updated_at", "instance_admin", "kind"}
	accountPrimaryKeyColumns     = []string{"id"}
	accountGeneratedColumns      = []string{}
)

type (
	// AccountSlice is an alias for a slice of pointers to Account.
	// This should almost always be used instead of []Account.
	AccountSlice []*Account
	// AccountHook is the signature for custom Account hook methods
	AccountHook func(context.Context, boil.ContextExecutor, *Account) error

	accountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	accountType                 = reflect.TypeOf(&Account{})
	accountMapping              = queries.MakeStructMapping(accountType)
	accountPrimaryKeyMapping, _ = queries.BindMapping(accountType, accountMapping, accountPrimaryKeyColumns)
	accountInsertCacheMut       sync.RWMutex
	accountInsertCache          = make(map[string]insertCache)
	accountUpdateCacheMut       sync.RWMutex
	accountUpdateCache          = make(map[string]updateCache)
	accountUpsertCacheMut       sync.RWMutex
	accountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var accountAfterSelectMu sync.Mutex
var accountAfterSelectHooks []AccountHook

var accountBeforeInsertMu sync.Mutex
var accountBeforeInsertHooks []AccountHook
var accountAfterInsertMu sync.Mutex
var accountAfterInsertHooks []AccountHook

var accountBeforeUpdateMu sync.Mutex
var accountBeforeUpdateHooks []AccountHook
var accountAfterUpdateMu sync.Mutex
var accountAfterUpdateHooks []AccountHook

var accountBeforeDeleteMu sync.Mutex
var accountBeforeDeleteHooks []AccountHook
var accountAfterDeleteMu sync.Mutex
var accountAfterDeleteHooks []AccountHook

var accountBeforeUpsertMu sync.Mutex
var accountBeforeUpsertHooks []AccountHook
var accountAfterUpsertMu sync.Mutex
var accountAfterUpsertHooks []AccountHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Account) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Account) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Account) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Account) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Account) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Account) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Account) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Account) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Account) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAccountHook registers your hook function for all future operations.
func AddAccountHook(hookPoint boil.HookPoint, accountHook AccountHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		accountAfterSelectMu.Lock()
		accountAfterSelectHooks = append(accountAfterSelectHooks, accountHook)
		accountAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		accountBeforeInsertMu.Lock()
		accountBeforeInsertHooks = append(accountBeforeInsertHooks, accountHook)
		accountBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		accountAfterInsertMu.Lock()
		accountAfterInsertHooks = append(accountAfterInsertHooks, accountHook)
		accountAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		accountBeforeUpdateMu.Lock()
		accountBeforeUpdateHooks = append(accountBeforeUpdateHooks, accountHook)
		accountBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		accountAfterUpdateMu.Lock()
		accountAfterUpdateHooks = append(accountAfterUpdateHooks, accountHook)
		accountAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		accountBeforeDeleteMu.Lock()
		accountBeforeDeleteHooks = append(accountBeforeDeleteHooks, accountHook)
		accountBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		accountAfterDeleteMu.Lock()
		accountAfterDeleteHooks = append(accountAfterDeleteHooks, accountHook)
		accountAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		accountBeforeUpsertMu.Lock()
		accountBeforeUpsertHooks = append(accountBeforeUpsertHooks, accountHook)
		accountBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		accountAfterUpsertMu.Lock()
		accountAfterUpsertHooks = append(accountAfterUpsertHooks, accountHook)
		accountAfterUpsertMu.Unlock()
	}
}

// One returns a single account record from the query.
func (q accountQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Account, error) {
	o := &Account{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for accounts")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Account records from the query.
func (q accountQuery) All(ctx context.Context, exec boil.ContextExecutor) (AccountSlice, error) {
	var o []*Account

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to Account slice")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Account records in the query.
func (q accountQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count accounts rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q accountQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if accounts exists")
	}

	return count > 0, nil
}

// AccountEmailChanges retrieves all the account_email_change's AccountEmailChanges with an executor.
func (o *Account) AccountEmailChanges(mods ...qm.QueryMod) accountEmailChangeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"account_email_changes\".\"account_id\"=?", o.ID),
	)

	return AccountEmailChanges(queryMods...)
}

// AccountPasswordHistories retrieves all the account_password_history's AccountPasswordHistories with an executor.
func (o *Account) AccountPasswordHistories(mods ...qm.QueryMod) accountPasswordHistoryQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"account_password_history\".\"account_id\"=?", o.ID),
	)

	return AccountPasswordHistories(queryMods...)
}

// AccountPasswordResets retrieves all the account_password_reset's AccountPasswordResets with an executor.
func (o *Account) AccountPasswordResets(mods ...qm.QueryMod) accountPasswordResetQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"account_password_resets\".\"account_id\"=?", o.ID),
	)

	return AccountPasswordResets(queryMods...)
}

// ActorAccountWorkspaceActivities retrieves all the workspace_activity's WorkspaceActivities with an executor via actor_account_id column.
func (o *Account) ActorAccountWorkspaceActivities(mods ...qm.QueryMod) workspaceActivityQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_activity\".\"actor_account_id\"=?", o.ID),
	)

	return WorkspaceActivities(queryMods...)
}

// IssuedByWorkspaceBreakGlassCodes retrieves all the workspace_break_glass_code's WorkspaceBreakGlassCodes with an executor via issued_by column.
func (o *Account) IssuedByWorkspaceBreakGlassCodes(mods ...qm.QueryMod) workspaceBreakGlassCodeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_break_glass_codes\".\"issued_by\"=?", o.ID),
	)

	return WorkspaceBreakGlassCodes(queryMods...)
}

// RequestedByAccountWorkspaceBulkActions retrieves all the workspace_bulk_action's WorkspaceBulkActions with an executor via requested_by_account_id column.
func (o *Account) RequestedByAccountWorkspaceBulkActions(mods ...qm.QueryMod) workspaceBulkActionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_bulk_actions\".\"requested_by_account_id\"=?", o.ID),
	)

	return WorkspaceBulkActions(queryMods...)
}

// ActorAccountWorkspaceCycleScopeChanges retrieves all the workspace_cycle_scope_change's WorkspaceCycleScopeChanges with an executor via actor_account_id column.
func (o *Account) ActorAccountWorkspaceCycleScopeChanges(mods ...qm.QueryMod) workspaceCycleScopeChangeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_cycle_scope_changes\".\"actor_account_id\"=?", o.ID),
	)

	return WorkspaceCycleScopeChanges(queryMods...)
}

// ClosedByWorkspaceCycles retrieves all the workspace_cycle's WorkspaceCycles with an executor via closed_by column.
func (o *Account) ClosedByWorkspaceCycles(mods ...qm.QueryMod) workspaceCycleQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_cycles\".\"closed_by\"=?", o.ID),
	)

	return WorkspaceCycles(queryMods...)
}

// AcceptedByAccountWorkspaceInvitations retrieves all the workspace_invitation's WorkspaceInvitations with an executor via accepted_by_account_id column.
func (o *Account) AcceptedByAccountWorkspaceInvitations(mods ...qm.QueryMod) workspaceInvitationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_invitations\".\"accepted_by_account_id\"=?", o.ID),
	)

	return WorkspaceInvitations(queryMods...)
}

// InvitedByAccountWorkspaceInvitations retrieves all the workspace_invitation's WorkspaceInvitations with an executor via invited_by_account_id column.
func (o *Account) InvitedByAccountWorkspaceInvitations(mods ...qm.QueryMod) workspaceInvitationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_invitations\".\"invited_by_account_id\"=?", o.ID),
	)

	return WorkspaceInvitations(queryMods...)
}

// WorkspaceIssueCommentMentions retrieves all the workspace_issue_comment_mention's WorkspaceIssueCommentMentions with an executor.
func (o *Account) WorkspaceIssueCommentMentions(mods ...qm.QueryMod) workspaceIssueCommentMentionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issue_comment_mentions\".\"account_id\"=?", o.ID),
	)

	return WorkspaceIssueCommentMentions(queryMods...)
}

// WorkspaceIssueCommentReactions retrieves all the workspace_issue_comment_reaction's WorkspaceIssueCommentReactions with an executor.
func (o *Account) WorkspaceIssueCommentReactions(mods ...qm.QueryMod) workspaceIssueCommentReactionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issue_comment_reactions\".\"account_id\"=?", o.ID),
	)

	return WorkspaceIssueCommentReactions(queryMods...)
}

// CreatedByAccountWorkspaceIssueRelations retrieves all the workspace_issue_relation's WorkspaceIssueRelations with an executor via created_by_account_id column.
func (o *Account) CreatedByAccountWorkspaceIssueRelations(mods ...qm.QueryMod) workspaceIssueRelationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issue_relations\".\"created_by_account_id\"=?", o.ID),
	)

	return WorkspaceIssueRelations(queryMods...)
}

// AssigneeAccountWorkspaceIssues retrieves all the workspace_issue's WorkspaceIssues with an executor via assignee_account_id column.
func (o *Account) AssigneeAccountWorkspaceIssues(mods ...qm.QueryMod) workspaceIssueQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issues\".\"assignee_account_id\"=?", o.ID),
	)

	return WorkspaceIssues(queryMods...)
}

// CreatedByAccountWorkspaceIssues retrieves all the workspace_issue's WorkspaceIssues with an executor via created_by_account_id column.
func (o *Account) CreatedByAccountWorkspaceIssues(mods ...qm.QueryMod) workspaceIssueQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issues\".\"created_by_account_id\"=?", o.ID),
	)

	return WorkspaceIssues(queryMods...)
}

// TriageDecidedByAccountWorkspaceIssues retrieves all the workspace_issue's WorkspaceIssues with an executor via triage_decided_by_account_id column.
func (o *Account) TriageDecidedByAccountWorkspaceIssues(mods ...qm.QueryMod) workspaceIssueQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issues\".\"triage_decided_by_account_id\"=?", o.ID),
	)

	return WorkspaceIssues(queryMods...)
}

// WorkspaceMemberships retrieves all the workspace_membership's WorkspaceMemberships with an executor.
func (o *Account) WorkspaceMemberships(mods ...qm.QueryMod) workspaceMembershipQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_memberships\".\"account_id\"=?", o.ID),
	)

	return WorkspaceMemberships(queryMods...)
}

// AuthorAccountWorkspaceProjectStatusUpdates retrieves all the workspace_project_status_update's WorkspaceProjectStatusUpdates with an executor via author_account_id column.
func (o *Account) AuthorAccountWorkspaceProjectStatusUpdates(mods ...qm.QueryMod) workspaceProjectStatusUpdateQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_project_status_updates\".\"author_account_id\"=?", o.ID),
	)

	return WorkspaceProjectStatusUpdates(queryMods...)
}

// LeadAccountWorkspaceProjects retrieves all the workspace_project's WorkspaceProjects with an executor via lead_account_id column.
func (o *Account) LeadAccountWorkspaceProjects(mods ...qm.QueryMod) workspaceProjectQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_projects\".\"lead_account_id\"=?", o.ID),
	)

	return WorkspaceProjects(queryMods...)
}

// WorkspaceSsoIdentities retrieves all the workspace_sso_identity's WorkspaceSsoIdentities with an executor.
func (o *Account) WorkspaceSsoIdentities(mods ...qm.QueryMod) workspaceSsoIdentityQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_sso_identities\".\"account_id\"=?", o.ID),
	)

	return WorkspaceSsoIdentities(queryMods...)
}

// LoadAccountEmailChanges allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadAccountEmailChanges(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`account_email_changes`),
		qm.WhereIn(`account_email_changes.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load account_email_changes")
	}

	var resultSlice []*AccountEmailChange
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice account_email_changes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on account_email_changes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for account_email_changes")
	}

	if len(accountEmailChangeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AccountEmailChanges = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &accountEmailChangeR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AccountID {
				local.R.AccountEmailChanges = append(local.R.AccountEmailChanges, foreign)
				if foreign.R == nil {
					foreign.R = &accountEmailChangeR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// LoadAccountPasswordHistories allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadAccountPasswordHistories(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`account_password_history`),
		qm.WhereIn(`account_password_history.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load account_password_history")
	}

	var resultSlice []*AccountPasswordHistory
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice account_password_history")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on account_password_history")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for account_password_history")
	}

	if len(accountPasswordHistoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AccountPasswordHistories = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &accountPasswordHistoryR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AccountID {
				local.R.AccountPasswordHistories = append(local.R.AccountPasswordHistories, foreign)
				if foreign.R == nil {
					foreign.R = &accountPasswordHistoryR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// LoadAccountPasswordResets allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadAccountPasswordResets(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`account_password_resets`),
		qm.WhereIn(`account_password_resets.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load account_password_resets")
	}

	var resultSlice []*AccountPasswordReset
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice account_password_resets")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on account_password_resets")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for account_password_resets")
	}

	if len(accountPasswordResetAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AccountPasswordResets = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &accountPasswordResetR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AccountID {
				local.R.AccountPasswordResets = append(local.R.AccountPasswordResets, foreign)
				if foreign.R == nil {
					foreign.R = &accountPasswordResetR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// LoadActorAccountWorkspaceActivities allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadActorAccountWorkspaceActivities(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_activity`),
		qm.WhereIn(`workspace_activity.actor_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_activity")
	}

	var resultSlice []*WorkspaceActivity
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_activity")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_activity")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_activity")
	}

	if len(workspaceActivityAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ActorAccountWorkspaceActivities = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceActivityR{}
			}
			foreign.R.ActorAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.ActorAccountID) {
				local.R.ActorAccountWorkspaceActivities = append(local.R.ActorAccountWorkspaceActivities, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceActivityR{}
				}
				foreign.R.ActorAccount = local
				break
			}
		}
	}

	return nil
}

// LoadIssuedByWorkspaceBreakGlassCodes allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadIssuedByWorkspaceBreakGlassCodes(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_break_glass_codes`),
		qm.WhereIn(`workspace_break_glass_codes.issued_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_break_glass_codes")
	}

	var resultSlice []*WorkspaceBreakGlassCode
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_break_glass_codes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_break_glass_codes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_break_glass_codes")
	}

	if len(workspaceBreakGlassCodeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.IssuedByWorkspaceBreakGlassCodes = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceBreakGlassCodeR{}
			}
			foreign.R.IssuedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.IssuedBy) {
				local.R.IssuedByWorkspaceBreakGlassCodes = append(local.R.IssuedByWorkspaceBreakGlassCodes, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceBreakGlassCodeR{}
				}
				foreign.R.IssuedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadRequestedByAccountWorkspaceBulkActions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadRequestedByAccountWorkspaceBulkActions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_bulk_actions`),
		qm.WhereIn(`workspace_bulk_actions.requested_by_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_bulk_actions")
	}

	var resultSlice []*WorkspaceBulkAction
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_bulk_actions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_bulk_actions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_bulk_actions")
	}

	if len(workspaceBulkActionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.RequestedByAccountWorkspaceBulkActions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceBulkActionR{}
			}
			foreign.R.RequestedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.RequestedByAccountID) {
				local.R.RequestedByAccountWorkspaceBulkActions = append(local.R.RequestedByAccountWorkspaceBulkActions, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceBulkActionR{}
				}
				foreign.R.RequestedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadActorAccountWorkspaceCycleScopeChanges allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadActorAccountWorkspaceCycleScopeChanges(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_cycle_scope_changes`),
		qm.WhereIn(`workspace_cycle_scope_changes.actor_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_cycle_scope_changes")
	}

	var resultSlice []*WorkspaceCycleScopeChange
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_cycle_scope_changes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_cycle_scope_changes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_cycle_scope_changes")
	}

	if len(workspaceCycleScopeChangeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ActorAccountWorkspaceCycleScopeChanges = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceCycleScopeChangeR{}
			}
			foreign.R.ActorAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.ActorAccountID) {
				local.R.ActorAccountWorkspaceCycleScopeChanges = append(local.R.ActorAccountWorkspaceCycleScopeChanges, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceCycleScopeChangeR{}
				}
				foreign.R.ActorAccount = local
				break
			}
		}
	}

	return nil
}

// LoadClosedByWorkspaceCycles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadClosedByWorkspaceCycles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_cycles`),
		qm.WhereIn(`workspace_cycles.closed_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_cycles")
	}

	var resultSlice []*WorkspaceCycle
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_cycles")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_cycles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_cycles")
	}

	if len(workspaceCycleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ClosedByWorkspaceCycles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceCycleR{}
			}
			foreign.R.ClosedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.ClosedBy) {
				local.R.ClosedByWorkspaceCycles = append(local.R.ClosedByWorkspaceCycles, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceCycleR{}
				}
				foreign.R.ClosedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadAcceptedByAccountWorkspaceInvitations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadAcceptedByAccountWorkspaceInvitations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_invitations`),
		qm.WhereIn(`workspace_invitations.accepted_by_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_invitations")
	}

	var resultSlice []*WorkspaceInvitation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_invitations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_invitations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_invitations")
	}

	if len(workspaceInvitationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AcceptedByAccountWorkspaceInvitations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceInvitationR{}
			}
			foreign.R.AcceptedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.AcceptedByAccountID) {
				local.R.AcceptedByAccountWorkspaceInvitations = append(local.R.AcceptedByAccountWorkspaceInvitations, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceInvitationR{}
				}
				foreign.R.AcceptedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadInvitedByAccountWorkspaceInvitations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadInvitedByAccountWorkspaceInvitations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_invitations`),
		qm.WhereIn(`workspace_invitations.invited_by_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_invitations")
	}

	var resultSlice []*WorkspaceInvitation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_invitations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_invitations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_invitations")
	}

	if len(workspaceInvitationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.InvitedByAccountWorkspaceInvitations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceInvitationR{}
			}
			foreign.R.InvitedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.InvitedByAccountID) {
				local.R.InvitedByAccountWorkspaceInvitations = append(local.R.InvitedByAccountWorkspaceInvitations, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceInvitationR{}
				}
				foreign.R.InvitedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspaceIssueCommentMentions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadWorkspaceIssueCommentMentions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issue_comment_mentions`),
		qm.WhereIn(`workspace_issue_comment_mentions.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issue_comment_mentions")
	}

	var resultSlice []*WorkspaceIssueCommentMention
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issue_comment_mentions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issue_comment_mentions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issue_comment_mentions")
	}

	if len(workspaceIssueCommentMentionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorkspaceIssueCommentMentions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueCommentMentionR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.AccountID) {
				local.R.WorkspaceIssueCommentMentions = append(local.R.WorkspaceIssueCommentMentions, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueCommentMentionR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspaceIssueCommentReactions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadWorkspaceIssueCommentReactions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issue_comment_reactions`),
		qm.WhereIn(`workspace_issue_comment_reactions.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issue_comment_reactions")
	}

	var resultSlice []*WorkspaceIssueCommentReaction
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issue_comment_reactions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issue_comment_reactions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issue_comment_reactions")
	}

	if len(workspaceIssueCommentReactionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorkspaceIssueCommentReactions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueCommentReactionR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AccountID {
				local.R.WorkspaceIssueCommentReactions = append(local.R.WorkspaceIssueCommentReactions, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueCommentReactionR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByAccountWorkspaceIssueRelations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadCreatedByAccountWorkspaceIssueRelations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issue_relations`),
		qm.WhereIn(`workspace_issue_relations.created_by_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issue_relations")
	}

	var resultSlice []*WorkspaceIssueRelation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issue_relations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issue_relations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issue_relations")
	}

	if len(workspaceIssueRelationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CreatedByAccountWorkspaceIssueRelations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueRelationR{}
			}
			foreign.R.CreatedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.CreatedByAccountID) {
				local.R.CreatedByAccountWorkspaceIssueRelations = append(local.R.CreatedByAccountWorkspaceIssueRelations, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueRelationR{}
				}
				foreign.R.CreatedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadAssigneeAccountWorkspaceIssues allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadAssigneeAccountWorkspaceIssues(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.assignee_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issues")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issues")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AssigneeAccountWorkspaceIssues = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueR{}
			}
			foreign.R.AssigneeAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.AssigneeAccountID) {
				local.R.AssigneeAccountWorkspaceIssues = append(local.R.AssigneeAccountWorkspaceIssues, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.AssigneeAccount = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByAccountWorkspaceIssues allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadCreatedByAccountWorkspaceIssues(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.created_by_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issues")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issues")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CreatedByAccountWorkspaceIssues = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueR{}
			}
			foreign.R.CreatedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.CreatedByAccountID) {
				local.R.CreatedByAccountWorkspaceIssues = append(local.R.CreatedByAccountWorkspaceIssues, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.CreatedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadTriageDecidedByAccountWorkspaceIssues allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadTriageDecidedByAccountWorkspaceIssues(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.triage_decided_by_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issues")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issues")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TriageDecidedByAccountWorkspaceIssues = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueR{}
			}
			foreign.R.TriageDecidedByAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.TriageDecidedByAccountID) {
				local.R.TriageDecidedByAccountWorkspaceIssues = append(local.R.TriageDecidedByAccountWorkspaceIssues, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.TriageDecidedByAccount = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspaceMemberships allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadWorkspaceMemberships(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_memberships`),
		qm.WhereIn(`workspace_memberships.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_memberships")
	}

	var resultSlice []*WorkspaceMembership
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_memberships")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_memberships")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_memberships")
	}

	if len(workspaceMembershipAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorkspaceMemberships = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceMembershipR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AccountID {
				local.R.WorkspaceMemberships = append(local.R.WorkspaceMemberships, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceMembershipR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// LoadAuthorAccountWorkspaceProjectStatusUpdates allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadAuthorAccountWorkspaceProjectStatusUpdates(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_project_status_updates`),
		qm.WhereIn(`workspace_project_status_updates.author_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_project_status_updates")
	}

	var resultSlice []*WorkspaceProjectStatusUpdate
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_project_status_updates")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_project_status_updates")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_project_status_updates")
	}

	if len(workspaceProjectStatusUpdateAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AuthorAccountWorkspaceProjectStatusUpdates = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceProjectStatusUpdateR{}
			}
			foreign.R.AuthorAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.AuthorAccountID) {
				local.R.AuthorAccountWorkspaceProjectStatusUpdates = append(local.R.AuthorAccountWorkspaceProjectStatusUpdates, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceProjectStatusUpdateR{}
				}
				foreign.R.AuthorAccount = local
				break
			}
		}
	}

	return nil
}

// LoadLeadAccountWorkspaceProjects allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadLeadAccountWorkspaceProjects(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_projects`),
		qm.WhereIn(`workspace_projects.lead_account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_projects")
	}

	var resultSlice []*WorkspaceProject
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_projects")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_projects")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_projects")
	}

	if len(workspaceProjectAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.LeadAccountWorkspaceProjects = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceProjectR{}
			}
			foreign.R.LeadAccount = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.LeadAccountID) {
				local.R.LeadAccountWorkspaceProjects = append(local.R.LeadAccountWorkspaceProjects, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceProjectR{}
				}
				foreign.R.LeadAccount = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspaceSsoIdentities allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (accountL) LoadWorkspaceSsoIdentities(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccount any, mods queries.Applicator) error {
	var slice []*Account
	var object *Account

	if singular {
		var ok bool
		object, ok = maybeAccount.(*Account)
		if !ok {
			object = new(Account)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccount))
			}
		}
	} else {
		s, ok := maybeAccount.(*[]*Account)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_sso_identities`),
		qm.WhereIn(`workspace_sso_identities.account_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_sso_identities")
	}

	var resultSlice []*WorkspaceSsoIdentity
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_sso_identities")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_sso_identities")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_sso_identities")
	}

	if len(workspaceSsoIdentityAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorkspaceSsoIdentities = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceSsoIdentityR{}
			}
			foreign.R.Account = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AccountID {
				local.R.WorkspaceSsoIdentities = append(local.R.WorkspaceSsoIdentities, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceSsoIdentityR{}
				}
				foreign.R.Account = local
				break
			}
		}
	}

	return nil
}

// AddAccountEmailChanges adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.AccountEmailChanges.
// Sets related.R.Account appropriately.
func (o *Account) AddAccountEmailChanges(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AccountEmailChange) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"account_email_changes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, accountEmailChangePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountID = o.ID
		}
	}

	if o.R == nil {
		o.R = &accountR{
			AccountEmailChanges: related,
		}
	} else {
		o.R.AccountEmailChanges = append(o.R.AccountEmailChanges, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &accountEmailChangeR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// AddAccountPasswordHistories adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.AccountPasswordHistories.
// Sets related.R.Account appropriately.
func (o *Account) AddAccountPasswordHistories(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AccountPasswordHistory) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"account_password_history\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, accountPasswordHistoryPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountID = o.ID
		}
	}

	if o.R == nil {
		o.R = &accountR{
			AccountPasswordHistories: related,
		}
	} else {
		o.R.AccountPasswordHistories = append(o.R.AccountPasswordHistories, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &accountPasswordHistoryR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// AddAccountPasswordResets adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.AccountPasswordResets.
// Sets related.R.Account appropriately.
func (o *Account) AddAccountPasswordResets(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*AccountPasswordReset) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"account_password_resets\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, accountPasswordResetPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountID = o.ID
		}
	}

	if o.R == nil {
		o.R = &accountR{
			AccountPasswordResets: related,
		}
	} else {
		o.R.AccountPasswordResets = append(o.R.AccountPasswordResets, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &accountPasswordResetR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// AddActorAccountWorkspaceActivities adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.ActorAccountWorkspaceActivities.
// Sets related.R.ActorAccount appropriately.
func (o *Account) AddActorAccountWorkspaceActivities(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceActivity) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ActorAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_activity\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"actor_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceActivityPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ActorAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			ActorAccountWorkspaceActivities: related,
		}
	} else {
		o.R.ActorAccountWorkspaceActivities = append(o.R.ActorAccountWorkspaceActivities, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceActivityR{
				ActorAccount: o,
			}
		} else {
			rel.R.ActorAccount = o
		}
	}
	return nil
}

// SetActorAccountWorkspaceActivities removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ActorAccount's ActorAccountWorkspaceActivities accordingly.
// Replaces o.R.ActorAccountWorkspaceActivities with related.
// Sets related.R.ActorAccount's ActorAccountWorkspaceActivities accordingly.
func (o *Account) SetActorAccountWorkspaceActivities(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceActivity) error {
	query := "update \"workspace_activity\" set \"actor_account_id\" = null where \"actor_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.ActorAccountWorkspaceActivities {
			queries.SetScanner(&rel.ActorAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.ActorAccount = nil
		}
		o.R.ActorAccountWorkspaceActivities = nil
	}

	return o.AddActorAccountWorkspaceActivities(ctx, exec, insert, related...)
}

// RemoveActorAccountWorkspaceActivities relationships from objects passed in.
// Removes related items from R.ActorAccountWorkspaceActivities (uses pointer comparison, removal does not keep order)
// Sets related.R.ActorAccount.
func (o *Account) RemoveActorAccountWorkspaceActivities(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceActivity) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ActorAccountID, nil)
		if rel.R != nil {
			rel.R.ActorAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("actor_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ActorAccountWorkspaceActivities {
			if rel != ri {
				continue
			}

			ln := len(o.R.ActorAccountWorkspaceActivities)
			if ln > 1 && i < ln-1 {
				o.R.ActorAccountWorkspaceActivities[i] = o.R.ActorAccountWorkspaceActivities[ln-1]
			}
			o.R.ActorAccountWorkspaceActivities = o.R.ActorAccountWorkspaceActivities[:ln-1]
			break
		}
	}

	return nil
}

// AddIssuedByWorkspaceBreakGlassCodes adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.IssuedByWorkspaceBreakGlassCodes.
// Sets related.R.IssuedByAccount appropriately.
func (o *Account) AddIssuedByWorkspaceBreakGlassCodes(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceBreakGlassCode) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.IssuedBy, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_break_glass_codes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"issued_by"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceBreakGlassCodePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.IssuedBy, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			IssuedByWorkspaceBreakGlassCodes: related,
		}
	} else {
		o.R.IssuedByWorkspaceBreakGlassCodes = append(o.R.IssuedByWorkspaceBreakGlassCodes, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceBreakGlassCodeR{
				IssuedByAccount: o,
			}
		} else {
			rel.R.IssuedByAccount = o
		}
	}
	return nil
}

// SetIssuedByWorkspaceBreakGlassCodes removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.IssuedByAccount's IssuedByWorkspaceBreakGlassCodes accordingly.
// Replaces o.R.IssuedByWorkspaceBreakGlassCodes with related.
// Sets related.R.IssuedByAccount's IssuedByWorkspaceBreakGlassCodes accordingly.
func (o *Account) SetIssuedByWorkspaceBreakGlassCodes(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceBreakGlassCode) error {
	query := "update \"workspace_break_glass_codes\" set \"issued_by\" = null where \"issued_by\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.IssuedByWorkspaceBreakGlassCodes {
			queries.SetScanner(&rel.IssuedBy, nil)
			if rel.R == nil {
				continue
			}

			rel.R.IssuedByAccount = nil
		}
		o.R.IssuedByWorkspaceBreakGlassCodes = nil
	}

	return o.AddIssuedByWorkspaceBreakGlassCodes(ctx, exec, insert, related...)
}

// RemoveIssuedByWorkspaceBreakGlassCodes relationships from objects passed in.
// Removes related items from R.IssuedByWorkspaceBreakGlassCodes (uses pointer comparison, removal does not keep order)
// Sets related.R.IssuedByAccount.
func (o *Account) RemoveIssuedByWorkspaceBreakGlassCodes(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceBreakGlassCode) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.IssuedBy, nil)
		if rel.R != nil {
			rel.R.IssuedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("issued_by")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.IssuedByWorkspaceBreakGlassCodes {
			if rel != ri {
				continue
			}

			ln := len(o.R.IssuedByWorkspaceBreakGlassCodes)
			if ln > 1 && i < ln-1 {
				o.R.IssuedByWorkspaceBreakGlassCodes[i] = o.R.IssuedByWorkspaceBreakGlassCodes[ln-1]
			}
			o.R.IssuedByWorkspaceBreakGlassCodes = o.R.IssuedByWorkspaceBreakGlassCodes[:ln-1]
			break
		}
	}

	return nil
}

// AddRequestedByAccountWorkspaceBulkActions adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.RequestedByAccountWorkspaceBulkActions.
// Sets related.R.RequestedByAccount appropriately.
func (o *Account) AddRequestedByAccountWorkspaceBulkActions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceBulkAction) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.RequestedByAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_bulk_actions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"requested_by_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceBulkActionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.RequestedByAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			RequestedByAccountWorkspaceBulkActions: related,
		}
	} else {
		o.R.RequestedByAccountWorkspaceBulkActions = append(o.R.RequestedByAccountWorkspaceBulkActions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceBulkActionR{
				RequestedByAccount: o,
			}
		} else {
			rel.R.RequestedByAccount = o
		}
	}
	return nil
}

// SetRequestedByAccountWorkspaceBulkActions removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.RequestedByAccount's RequestedByAccountWorkspaceBulkActions accordingly.
// Replaces o.R.RequestedByAccountWorkspaceBulkActions with related.
// Sets related.R.RequestedByAccount's RequestedByAccountWorkspaceBulkActions accordingly.
func (o *Account) SetRequestedByAccountWorkspaceBulkActions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceBulkAction) error {
	query := "update \"workspace_bulk_actions\" set \"requested_by_account_id\" = null where \"requested_by_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.RequestedByAccountWorkspaceBulkActions {
			queries.SetScanner(&rel.RequestedByAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.RequestedByAccount = nil
		}
		o.R.RequestedByAccountWorkspaceBulkActions = nil
	}

	return o.AddRequestedByAccountWorkspaceBulkActions(ctx, exec, insert, related...)
}

// RemoveRequestedByAccountWorkspaceBulkActions relationships from objects passed in.
// Removes related items from R.RequestedByAccountWorkspaceBulkActions (uses pointer comparison, removal does not keep order)
// Sets related.R.RequestedByAccount.
func (o *Account) RemoveRequestedByAccountWorkspaceBulkActions(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceBulkAction) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.RequestedByAccountID, nil)
		if rel.R != nil {
			rel.R.RequestedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("requested_by_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.RequestedByAccountWorkspaceBulkActions {
			if rel != ri {
				continue
			}

			ln := len(o.R.RequestedByAccountWorkspaceBulkActions)
			if ln > 1 && i < ln-1 {
				o.R.RequestedByAccountWorkspaceBulkActions[i] = o.R.RequestedByAccountWorkspaceBulkActions[ln-1]
			}
			o.R.RequestedByAccountWorkspaceBulkActions = o.R.RequestedByAccountWorkspaceBulkActions[:ln-1]
			break
		}
	}

	return nil
}

// AddActorAccountWorkspaceCycleScopeChanges adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.ActorAccountWorkspaceCycleScopeChanges.
// Sets related.R.ActorAccount appropriately.
func (o *Account) AddActorAccountWorkspaceCycleScopeChanges(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceCycleScopeChange) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ActorAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_cycle_scope_changes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"actor_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceCycleScopeChangePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ActorAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			ActorAccountWorkspaceCycleScopeChanges: related,
		}
	} else {
		o.R.ActorAccountWorkspaceCycleScopeChanges = append(o.R.ActorAccountWorkspaceCycleScopeChanges, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceCycleScopeChangeR{
				ActorAccount: o,
			}
		} else {
			rel.R.ActorAccount = o
		}
	}
	return nil
}

// SetActorAccountWorkspaceCycleScopeChanges removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ActorAccount's ActorAccountWorkspaceCycleScopeChanges accordingly.
// Replaces o.R.ActorAccountWorkspaceCycleScopeChanges with related.
// Sets related.R.ActorAccount's ActorAccountWorkspaceCycleScopeChanges accordingly.
func (o *Account) SetActorAccountWorkspaceCycleScopeChanges(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceCycleScopeChange) error {
	query := "update \"workspace_cycle_scope_changes\" set \"actor_account_id\" = null where \"actor_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.ActorAccountWorkspaceCycleScopeChanges {
			queries.SetScanner(&rel.ActorAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.ActorAccount = nil
		}
		o.R.ActorAccountWorkspaceCycleScopeChanges = nil
	}

	return o.AddActorAccountWorkspaceCycleScopeChanges(ctx, exec, insert, related...)
}

// RemoveActorAccountWorkspaceCycleScopeChanges relationships from objects passed in.
// Removes related items from R.ActorAccountWorkspaceCycleScopeChanges (uses pointer comparison, removal does not keep order)
// Sets related.R.ActorAccount.
func (o *Account) RemoveActorAccountWorkspaceCycleScopeChanges(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceCycleScopeChange) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ActorAccountID, nil)
		if rel.R != nil {
			rel.R.ActorAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("actor_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ActorAccountWorkspaceCycleScopeChanges {
			if rel != ri {
				continue
			}

			ln := len(o.R.ActorAccountWorkspaceCycleScopeChanges)
			if ln > 1 && i < ln-1 {
				o.R.ActorAccountWorkspaceCycleScopeChanges[i] = o.R.ActorAccountWorkspaceCycleScopeChanges[ln-1]
			}
			o.R.ActorAccountWorkspaceCycleScopeChanges = o.R.ActorAccountWorkspaceCycleScopeChanges[:ln-1]
			break
		}
	}

	return nil
}

// AddClosedByWorkspaceCycles adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.ClosedByWorkspaceCycles.
// Sets related.R.ClosedByAccount appropriately.
func (o *Account) AddClosedByWorkspaceCycles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceCycle) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ClosedBy, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_cycles\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"closed_by"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceCyclePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ClosedBy, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			ClosedByWorkspaceCycles: related,
		}
	} else {
		o.R.ClosedByWorkspaceCycles = append(o.R.ClosedByWorkspaceCycles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceCycleR{
				ClosedByAccount: o,
			}
		} else {
			rel.R.ClosedByAccount = o
		}
	}
	return nil
}

// SetClosedByWorkspaceCycles removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ClosedByAccount's ClosedByWorkspaceCycles accordingly.
// Replaces o.R.ClosedByWorkspaceCycles with related.
// Sets related.R.ClosedByAccount's ClosedByWorkspaceCycles accordingly.
func (o *Account) SetClosedByWorkspaceCycles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceCycle) error {
	query := "update \"workspace_cycles\" set \"closed_by\" = null where \"closed_by\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.ClosedByWorkspaceCycles {
			queries.SetScanner(&rel.ClosedBy, nil)
			if rel.R == nil {
				continue
			}

			rel.R.ClosedByAccount = nil
		}
		o.R.ClosedByWorkspaceCycles = nil
	}

	return o.AddClosedByWorkspaceCycles(ctx, exec, insert, related...)
}

// RemoveClosedByWorkspaceCycles relationships from objects passed in.
// Removes related items from R.ClosedByWorkspaceCycles (uses pointer comparison, removal does not keep order)
// Sets related.R.ClosedByAccount.
func (o *Account) RemoveClosedByWorkspaceCycles(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceCycle) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ClosedBy, nil)
		if rel.R != nil {
			rel.R.ClosedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("closed_by")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ClosedByWorkspaceCycles {
			if rel != ri {
				continue
			}

			ln := len(o.R.ClosedByWorkspaceCycles)
			if ln > 1 && i < ln-1 {
				o.R.ClosedByWorkspaceCycles[i] = o.R.ClosedByWorkspaceCycles[ln-1]
			}
			o.R.ClosedByWorkspaceCycles = o.R.ClosedByWorkspaceCycles[:ln-1]
			break
		}
	}

	return nil
}

// AddAcceptedByAccountWorkspaceInvitations adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.AcceptedByAccountWorkspaceInvitations.
// Sets related.R.AcceptedByAccount appropriately.
func (o *Account) AddAcceptedByAccountWorkspaceInvitations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceInvitation) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.AcceptedByAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_invitations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"accepted_by_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceInvitationPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.AcceptedByAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			AcceptedByAccountWorkspaceInvitations: related,
		}
	} else {
		o.R.AcceptedByAccountWorkspaceInvitations = append(o.R.AcceptedByAccountWorkspaceInvitations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceInvitationR{
				AcceptedByAccount: o,
			}
		} else {
			rel.R.AcceptedByAccount = o
		}
	}
	return nil
}

// SetAcceptedByAccountWorkspaceInvitations removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.AcceptedByAccount's AcceptedByAccountWorkspaceInvitations accordingly.
// Replaces o.R.AcceptedByAccountWorkspaceInvitations with related.
// Sets related.R.AcceptedByAccount's AcceptedByAccountWorkspaceInvitations accordingly.
func (o *Account) SetAcceptedByAccountWorkspaceInvitations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceInvitation) error {
	query := "update \"workspace_invitations\" set \"accepted_by_account_id\" = null where \"accepted_by_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AcceptedByAccountWorkspaceInvitations {
			queries.SetScanner(&rel.AcceptedByAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.AcceptedByAccount = nil
		}
		o.R.AcceptedByAccountWorkspaceInvitations = nil
	}

	return o.AddAcceptedByAccountWorkspaceInvitations(ctx, exec, insert, related...)
}

// RemoveAcceptedByAccountWorkspaceInvitations relationships from objects passed in.
// Removes related items from R.AcceptedByAccountWorkspaceInvitations (uses pointer comparison, removal does not keep order)
// Sets related.R.AcceptedByAccount.
func (o *Account) RemoveAcceptedByAccountWorkspaceInvitations(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceInvitation) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.AcceptedByAccountID, nil)
		if rel.R != nil {
			rel.R.AcceptedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("accepted_by_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AcceptedByAccountWorkspaceInvitations {
			if rel != ri {
				continue
			}

			ln := len(o.R.AcceptedByAccountWorkspaceInvitations)
			if ln > 1 && i < ln-1 {
				o.R.AcceptedByAccountWorkspaceInvitations[i] = o.R.AcceptedByAccountWorkspaceInvitations[ln-1]
			}
			o.R.AcceptedByAccountWorkspaceInvitations = o.R.AcceptedByAccountWorkspaceInvitations[:ln-1]
			break
		}
	}

	return nil
}

// AddInvitedByAccountWorkspaceInvitations adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.InvitedByAccountWorkspaceInvitations.
// Sets related.R.InvitedByAccount appropriately.
func (o *Account) AddInvitedByAccountWorkspaceInvitations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceInvitation) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.InvitedByAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_invitations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"invited_by_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceInvitationPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.InvitedByAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			InvitedByAccountWorkspaceInvitations: related,
		}
	} else {
		o.R.InvitedByAccountWorkspaceInvitations = append(o.R.InvitedByAccountWorkspaceInvitations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceInvitationR{
				InvitedByAccount: o,
			}
		} else {
			rel.R.InvitedByAccount = o
		}
	}
	return nil
}

// SetInvitedByAccountWorkspaceInvitations removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.InvitedByAccount's InvitedByAccountWorkspaceInvitations accordingly.
// Replaces o.R.InvitedByAccountWorkspaceInvitations with related.
// Sets related.R.InvitedByAccount's InvitedByAccountWorkspaceInvitations accordingly.
func (o *Account) SetInvitedByAccountWorkspaceInvitations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceInvitation) error {
	query := "update \"workspace_invitations\" set \"invited_by_account_id\" = null where \"invited_by_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.InvitedByAccountWorkspaceInvitations {
			queries.SetScanner(&rel.InvitedByAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.InvitedByAccount = nil
		}
		o.R.InvitedByAccountWorkspaceInvitations = nil
	}

	return o.AddInvitedByAccountWorkspaceInvitations(ctx, exec, insert, related...)
}

// RemoveInvitedByAccountWorkspaceInvitations relationships from objects passed in.
// Removes related items from R.InvitedByAccountWorkspaceInvitations (uses pointer comparison, removal does not keep order)
// Sets related.R.InvitedByAccount.
func (o *Account) RemoveInvitedByAccountWorkspaceInvitations(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceInvitation) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.InvitedByAccountID, nil)
		if rel.R != nil {
			rel.R.InvitedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("invited_by_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.InvitedByAccountWorkspaceInvitations {
			if rel != ri {
				continue
			}

			ln := len(o.R.InvitedByAccountWorkspaceInvitations)
			if ln > 1 && i < ln-1 {
				o.R.InvitedByAccountWorkspaceInvitations[i] = o.R.InvitedByAccountWorkspaceInvitations[ln-1]
			}
			o.R.InvitedByAccountWorkspaceInvitations = o.R.InvitedByAccountWorkspaceInvitations[:ln-1]
			break
		}
	}

	return nil
}

// AddWorkspaceIssueCommentMentions adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.WorkspaceIssueCommentMentions.
// Sets related.R.Account appropriately.
func (o *Account) AddWorkspaceIssueCommentMentions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssueCommentMention) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.AccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issue_comment_mentions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssueCommentMentionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.AccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			WorkspaceIssueCommentMentions: related,
		}
	} else {
		o.R.WorkspaceIssueCommentMentions = append(o.R.WorkspaceIssueCommentMentions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueCommentMentionR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// SetWorkspaceIssueCommentMentions removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Account's WorkspaceIssueCommentMentions accordingly.
// Replaces o.R.WorkspaceIssueCommentMentions with related.
// Sets related.R.Account's WorkspaceIssueCommentMentions accordingly.
func (o *Account) SetWorkspaceIssueCommentMentions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssueCommentMention) error {
	query := "update \"workspace_issue_comment_mentions\" set \"account_id\" = null where \"account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.WorkspaceIssueCommentMentions {
			queries.SetScanner(&rel.AccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Account = nil
		}
		o.R.WorkspaceIssueCommentMentions = nil
	}

	return o.AddWorkspaceIssueCommentMentions(ctx, exec, insert, related...)
}

// RemoveWorkspaceIssueCommentMentions relationships from objects passed in.
// Removes related items from R.WorkspaceIssueCommentMentions (uses pointer comparison, removal does not keep order)
// Sets related.R.Account.
func (o *Account) RemoveWorkspaceIssueCommentMentions(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceIssueCommentMention) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.AccountID, nil)
		if rel.R != nil {
			rel.R.Account = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.WorkspaceIssueCommentMentions {
			if rel != ri {
				continue
			}

			ln := len(o.R.WorkspaceIssueCommentMentions)
			if ln > 1 && i < ln-1 {
				o.R.WorkspaceIssueCommentMentions[i] = o.R.WorkspaceIssueCommentMentions[ln-1]
			}
			o.R.WorkspaceIssueCommentMentions = o.R.WorkspaceIssueCommentMentions[:ln-1]
			break
		}
	}

	return nil
}

// AddWorkspaceIssueCommentReactions adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.WorkspaceIssueCommentReactions.
// Sets related.R.Account appropriately.
func (o *Account) AddWorkspaceIssueCommentReactions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssueCommentReaction) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issue_comment_reactions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssueCommentReactionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.CommentID, rel.AccountID, rel.Reaction}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountID = o.ID
		}
	}

	if o.R == nil {
		o.R = &accountR{
			WorkspaceIssueCommentReactions: related,
		}
	} else {
		o.R.WorkspaceIssueCommentReactions = append(o.R.WorkspaceIssueCommentReactions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueCommentReactionR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// AddCreatedByAccountWorkspaceIssueRelations adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.CreatedByAccountWorkspaceIssueRelations.
// Sets related.R.CreatedByAccount appropriately.
func (o *Account) AddCreatedByAccountWorkspaceIssueRelations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssueRelation) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.CreatedByAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issue_relations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssueRelationPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.CreatedByAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			CreatedByAccountWorkspaceIssueRelations: related,
		}
	} else {
		o.R.CreatedByAccountWorkspaceIssueRelations = append(o.R.CreatedByAccountWorkspaceIssueRelations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueRelationR{
				CreatedByAccount: o,
			}
		} else {
			rel.R.CreatedByAccount = o
		}
	}
	return nil
}

// SetCreatedByAccountWorkspaceIssueRelations removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.CreatedByAccount's CreatedByAccountWorkspaceIssueRelations accordingly.
// Replaces o.R.CreatedByAccountWorkspaceIssueRelations with related.
// Sets related.R.CreatedByAccount's CreatedByAccountWorkspaceIssueRelations accordingly.
func (o *Account) SetCreatedByAccountWorkspaceIssueRelations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssueRelation) error {
	query := "update \"workspace_issue_relations\" set \"created_by_account_id\" = null where \"created_by_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.CreatedByAccountWorkspaceIssueRelations {
			queries.SetScanner(&rel.CreatedByAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.CreatedByAccount = nil
		}
		o.R.CreatedByAccountWorkspaceIssueRelations = nil
	}

	return o.AddCreatedByAccountWorkspaceIssueRelations(ctx, exec, insert, related...)
}

// RemoveCreatedByAccountWorkspaceIssueRelations relationships from objects passed in.
// Removes related items from R.CreatedByAccountWorkspaceIssueRelations (uses pointer comparison, removal does not keep order)
// Sets related.R.CreatedByAccount.
func (o *Account) RemoveCreatedByAccountWorkspaceIssueRelations(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceIssueRelation) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.CreatedByAccountID, nil)
		if rel.R != nil {
			rel.R.CreatedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("created_by_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.CreatedByAccountWorkspaceIssueRelations {
			if rel != ri {
				continue
			}

			ln := len(o.R.CreatedByAccountWorkspaceIssueRelations)
			if ln > 1 && i < ln-1 {
				o.R.CreatedByAccountWorkspaceIssueRelations[i] = o.R.CreatedByAccountWorkspaceIssueRelations[ln-1]
			}
			o.R.CreatedByAccountWorkspaceIssueRelations = o.R.CreatedByAccountWorkspaceIssueRelations[:ln-1]
			break
		}
	}

	return nil
}

// AddAssigneeAccountWorkspaceIssues adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.AssigneeAccountWorkspaceIssues.
// Sets related.R.AssigneeAccount appropriately.
func (o *Account) AddAssigneeAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssue) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.AssigneeAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issues\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"assignee_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssuePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.AssigneeAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			AssigneeAccountWorkspaceIssues: related,
		}
	} else {
		o.R.AssigneeAccountWorkspaceIssues = append(o.R.AssigneeAccountWorkspaceIssues, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueR{
				AssigneeAccount: o,
			}
		} else {
			rel.R.AssigneeAccount = o
		}
	}
	return nil
}

// SetAssigneeAccountWorkspaceIssues removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.AssigneeAccount's AssigneeAccountWorkspaceIssues accordingly.
// Replaces o.R.AssigneeAccountWorkspaceIssues with related.
// Sets related.R.AssigneeAccount's AssigneeAccountWorkspaceIssues accordingly.
func (o *Account) SetAssigneeAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssue) error {
	query := "update \"workspace_issues\" set \"assignee_account_id\" = null where \"assignee_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AssigneeAccountWorkspaceIssues {
			queries.SetScanner(&rel.AssigneeAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.AssigneeAccount = nil
		}
		o.R.AssigneeAccountWorkspaceIssues = nil
	}

	return o.AddAssigneeAccountWorkspaceIssues(ctx, exec, insert, related...)
}

// RemoveAssigneeAccountWorkspaceIssues relationships from objects passed in.
// Removes related items from R.AssigneeAccountWorkspaceIssues (uses pointer comparison, removal does not keep order)
// Sets related.R.AssigneeAccount.
func (o *Account) RemoveAssigneeAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceIssue) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.AssigneeAccountID, nil)
		if rel.R != nil {
			rel.R.AssigneeAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("assignee_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AssigneeAccountWorkspaceIssues {
			if rel != ri {
				continue
			}

			ln := len(o.R.AssigneeAccountWorkspaceIssues)
			if ln > 1 && i < ln-1 {
				o.R.AssigneeAccountWorkspaceIssues[i] = o.R.AssigneeAccountWorkspaceIssues[ln-1]
			}
			o.R.AssigneeAccountWorkspaceIssues = o.R.AssigneeAccountWorkspaceIssues[:ln-1]
			break
		}
	}

	return nil
}

// AddCreatedByAccountWorkspaceIssues adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.CreatedByAccountWorkspaceIssues.
// Sets related.R.CreatedByAccount appropriately.
func (o *Account) AddCreatedByAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssue) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.CreatedByAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issues\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssuePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.CreatedByAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			CreatedByAccountWorkspaceIssues: related,
		}
	} else {
		o.R.CreatedByAccountWorkspaceIssues = append(o.R.CreatedByAccountWorkspaceIssues, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueR{
				CreatedByAccount: o,
			}
		} else {
			rel.R.CreatedByAccount = o
		}
	}
	return nil
}

// SetCreatedByAccountWorkspaceIssues removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.CreatedByAccount's CreatedByAccountWorkspaceIssues accordingly.
// Replaces o.R.CreatedByAccountWorkspaceIssues with related.
// Sets related.R.CreatedByAccount's CreatedByAccountWorkspaceIssues accordingly.
func (o *Account) SetCreatedByAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssue) error {
	query := "update \"workspace_issues\" set \"created_by_account_id\" = null where \"created_by_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.CreatedByAccountWorkspaceIssues {
			queries.SetScanner(&rel.CreatedByAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.CreatedByAccount = nil
		}
		o.R.CreatedByAccountWorkspaceIssues = nil
	}

	return o.AddCreatedByAccountWorkspaceIssues(ctx, exec, insert, related...)
}

// RemoveCreatedByAccountWorkspaceIssues relationships from objects passed in.
// Removes related items from R.CreatedByAccountWorkspaceIssues (uses pointer comparison, removal does not keep order)
// Sets related.R.CreatedByAccount.
func (o *Account) RemoveCreatedByAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceIssue) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.CreatedByAccountID, nil)
		if rel.R != nil {
			rel.R.CreatedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("created_by_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.CreatedByAccountWorkspaceIssues {
			if rel != ri {
				continue
			}

			ln := len(o.R.CreatedByAccountWorkspaceIssues)
			if ln > 1 && i < ln-1 {
				o.R.CreatedByAccountWorkspaceIssues[i] = o.R.CreatedByAccountWorkspaceIssues[ln-1]
			}
			o.R.CreatedByAccountWorkspaceIssues = o.R.CreatedByAccountWorkspaceIssues[:ln-1]
			break
		}
	}

	return nil
}

// AddTriageDecidedByAccountWorkspaceIssues adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.TriageDecidedByAccountWorkspaceIssues.
// Sets related.R.TriageDecidedByAccount appropriately.
func (o *Account) AddTriageDecidedByAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssue) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.TriageDecidedByAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issues\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"triage_decided_by_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssuePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.TriageDecidedByAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			TriageDecidedByAccountWorkspaceIssues: related,
		}
	} else {
		o.R.TriageDecidedByAccountWorkspaceIssues = append(o.R.TriageDecidedByAccountWorkspaceIssues, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueR{
				TriageDecidedByAccount: o,
			}
		} else {
			rel.R.TriageDecidedByAccount = o
		}
	}
	return nil
}

// SetTriageDecidedByAccountWorkspaceIssues removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.TriageDecidedByAccount's TriageDecidedByAccountWorkspaceIssues accordingly.
// Replaces o.R.TriageDecidedByAccountWorkspaceIssues with related.
// Sets related.R.TriageDecidedByAccount's TriageDecidedByAccountWorkspaceIssues accordingly.
func (o *Account) SetTriageDecidedByAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssue) error {
	query := "update \"workspace_issues\" set \"triage_decided_by_account_id\" = null where \"triage_decided_by_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.TriageDecidedByAccountWorkspaceIssues {
			queries.SetScanner(&rel.TriageDecidedByAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.TriageDecidedByAccount = nil
		}
		o.R.TriageDecidedByAccountWorkspaceIssues = nil
	}

	return o.AddTriageDecidedByAccountWorkspaceIssues(ctx, exec, insert, related...)
}

// RemoveTriageDecidedByAccountWorkspaceIssues relationships from objects passed in.
// Removes related items from R.TriageDecidedByAccountWorkspaceIssues (uses pointer comparison, removal does not keep order)
// Sets related.R.TriageDecidedByAccount.
func (o *Account) RemoveTriageDecidedByAccountWorkspaceIssues(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceIssue) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.TriageDecidedByAccountID, nil)
		if rel.R != nil {
			rel.R.TriageDecidedByAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("triage_decided_by_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.TriageDecidedByAccountWorkspaceIssues {
			if rel != ri {
				continue
			}

			ln := len(o.R.TriageDecidedByAccountWorkspaceIssues)
			if ln > 1 && i < ln-1 {
				o.R.TriageDecidedByAccountWorkspaceIssues[i] = o.R.TriageDecidedByAccountWorkspaceIssues[ln-1]
			}
			o.R.TriageDecidedByAccountWorkspaceIssues = o.R.TriageDecidedByAccountWorkspaceIssues[:ln-1]
			break
		}
	}

	return nil
}

// AddWorkspaceMemberships adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.WorkspaceMemberships.
// Sets related.R.Account appropriately.
func (o *Account) AddWorkspaceMemberships(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceMembership) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_memberships\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceMembershipPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountID = o.ID
		}
	}

	if o.R == nil {
		o.R = &accountR{
			WorkspaceMemberships: related,
		}
	} else {
		o.R.WorkspaceMemberships = append(o.R.WorkspaceMemberships, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceMembershipR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// AddAuthorAccountWorkspaceProjectStatusUpdates adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.AuthorAccountWorkspaceProjectStatusUpdates.
// Sets related.R.AuthorAccount appropriately.
func (o *Account) AddAuthorAccountWorkspaceProjectStatusUpdates(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceProjectStatusUpdate) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.AuthorAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_project_status_updates\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"author_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceProjectStatusUpdatePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.AuthorAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			AuthorAccountWorkspaceProjectStatusUpdates: related,
		}
	} else {
		o.R.AuthorAccountWorkspaceProjectStatusUpdates = append(o.R.AuthorAccountWorkspaceProjectStatusUpdates, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceProjectStatusUpdateR{
				AuthorAccount: o,
			}
		} else {
			rel.R.AuthorAccount = o
		}
	}
	return nil
}

// SetAuthorAccountWorkspaceProjectStatusUpdates removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.AuthorAccount's AuthorAccountWorkspaceProjectStatusUpdates accordingly.
// Replaces o.R.AuthorAccountWorkspaceProjectStatusUpdates with related.
// Sets related.R.AuthorAccount's AuthorAccountWorkspaceProjectStatusUpdates accordingly.
func (o *Account) SetAuthorAccountWorkspaceProjectStatusUpdates(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceProjectStatusUpdate) error {
	query := "update \"workspace_project_status_updates\" set \"author_account_id\" = null where \"author_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.AuthorAccountWorkspaceProjectStatusUpdates {
			queries.SetScanner(&rel.AuthorAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.AuthorAccount = nil
		}
		o.R.AuthorAccountWorkspaceProjectStatusUpdates = nil
	}

	return o.AddAuthorAccountWorkspaceProjectStatusUpdates(ctx, exec, insert, related...)
}

// RemoveAuthorAccountWorkspaceProjectStatusUpdates relationships from objects passed in.
// Removes related items from R.AuthorAccountWorkspaceProjectStatusUpdates (uses pointer comparison, removal does not keep order)
// Sets related.R.AuthorAccount.
func (o *Account) RemoveAuthorAccountWorkspaceProjectStatusUpdates(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceProjectStatusUpdate) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.AuthorAccountID, nil)
		if rel.R != nil {
			rel.R.AuthorAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("author_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.AuthorAccountWorkspaceProjectStatusUpdates {
			if rel != ri {
				continue
			}

			ln := len(o.R.AuthorAccountWorkspaceProjectStatusUpdates)
			if ln > 1 && i < ln-1 {
				o.R.AuthorAccountWorkspaceProjectStatusUpdates[i] = o.R.AuthorAccountWorkspaceProjectStatusUpdates[ln-1]
			}
			o.R.AuthorAccountWorkspaceProjectStatusUpdates = o.R.AuthorAccountWorkspaceProjectStatusUpdates[:ln-1]
			break
		}
	}

	return nil
}

// AddLeadAccountWorkspaceProjects adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.LeadAccountWorkspaceProjects.
// Sets related.R.LeadAccount appropriately.
func (o *Account) AddLeadAccountWorkspaceProjects(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceProject) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.LeadAccountID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_projects\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"lead_account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceProjectPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.LeadAccountID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &accountR{
			LeadAccountWorkspaceProjects: related,
		}
	} else {
		o.R.LeadAccountWorkspaceProjects = append(o.R.LeadAccountWorkspaceProjects, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceProjectR{
				LeadAccount: o,
			}
		} else {
			rel.R.LeadAccount = o
		}
	}
	return nil
}

// SetLeadAccountWorkspaceProjects removes all previously related items of the
// account replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.LeadAccount's LeadAccountWorkspaceProjects accordingly.
// Replaces o.R.LeadAccountWorkspaceProjects with related.
// Sets related.R.LeadAccount's LeadAccountWorkspaceProjects accordingly.
func (o *Account) SetLeadAccountWorkspaceProjects(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceProject) error {
	query := "update \"workspace_projects\" set \"lead_account_id\" = null where \"lead_account_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.LeadAccountWorkspaceProjects {
			queries.SetScanner(&rel.LeadAccountID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.LeadAccount = nil
		}
		o.R.LeadAccountWorkspaceProjects = nil
	}

	return o.AddLeadAccountWorkspaceProjects(ctx, exec, insert, related...)
}

// RemoveLeadAccountWorkspaceProjects relationships from objects passed in.
// Removes related items from R.LeadAccountWorkspaceProjects (uses pointer comparison, removal does not keep order)
// Sets related.R.LeadAccount.
func (o *Account) RemoveLeadAccountWorkspaceProjects(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceProject) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.LeadAccountID, nil)
		if rel.R != nil {
			rel.R.LeadAccount = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("lead_account_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.LeadAccountWorkspaceProjects {
			if rel != ri {
				continue
			}

			ln := len(o.R.LeadAccountWorkspaceProjects)
			if ln > 1 && i < ln-1 {
				o.R.LeadAccountWorkspaceProjects[i] = o.R.LeadAccountWorkspaceProjects[ln-1]
			}
			o.R.LeadAccountWorkspaceProjects = o.R.LeadAccountWorkspaceProjects[:ln-1]
			break
		}
	}

	return nil
}

// AddWorkspaceSsoIdentities adds the given related objects to the existing relationships
// of the account, optionally inserting them as new records.
// Appends related to o.R.WorkspaceSsoIdentities.
// Sets related.R.Account appropriately.
func (o *Account) AddWorkspaceSsoIdentities(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceSsoIdentity) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AccountID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_sso_identities\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceSsoIdentityPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.WorkspaceID, rel.AccountID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AccountID = o.ID
		}
	}

	if o.R == nil {
		o.R = &accountR{
			WorkspaceSsoIdentities: related,
		}
	} else {
		o.R.WorkspaceSsoIdentities = append(o.R.WorkspaceSsoIdentities, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceSsoIdentityR{
				Account: o,
			}
		} else {
			rel.R.Account = o
		}
	}
	return nil
}

// Accounts retrieves all the records using an executor.
func Accounts(mods ...qm.QueryMod) accountQuery {
	mods = append(mods, qm.From("\"accounts\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"accounts\".*"})
	}

	return accountQuery{q}
}

// FindAccount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAccount(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*Account, error) {
	accountObj := &Account{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"accounts\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, accountObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from accounts")
	}

	if err = accountObj.doAfterSelectHooks(ctx, exec); err != nil {
		return accountObj, err
	}

	return accountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Account) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no accounts provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	accountInsertCacheMut.RLock()
	cache, cached := accountInsertCache[key]
	accountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			accountAllColumns,
			accountColumnsWithDefault,
			accountColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(accountType, accountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(accountType, accountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"accounts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"accounts\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into accounts")
	}

	if !cached {
		accountInsertCacheMut.Lock()
		accountInsertCache[key] = cache
		accountInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the Account.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Account) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	accountUpdateCacheMut.RLock()
	cache, cached := accountUpdateCache[key]
	accountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			accountAllColumns,
			accountPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update accounts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"accounts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, accountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(accountType, accountMapping, append(wl, accountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update accounts row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for accounts")
	}

	if !cached {
		accountUpdateCacheMut.Lock()
		accountUpdateCache[key] = cache
		accountUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q accountQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for accounts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for accounts")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AccountSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"accounts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, accountPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in account slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all account")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Account) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no accounts provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	accountUpsertCacheMut.RLock()
	cache, cached := accountUpsertCache[key]
	accountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			accountAllColumns,
			accountColumnsWithDefault,
			accountColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			accountAllColumns,
			accountPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert accounts, could not build update column list")
		}

		ret := strmangle.SetComplement(accountAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(accountPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert accounts, could not build conflict column list")
			}

			conflict = make([]string, len(accountPrimaryKeyColumns))
			copy(conflict, accountPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"accounts\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(accountType, accountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(accountType, accountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert accounts")
	}

	if !cached {
		accountUpsertCacheMut.Lock()
		accountUpsertCache[key] = cache
		accountUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single Account record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Account) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no Account provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), accountPrimaryKeyMapping)
	sql := "DELETE FROM \"accounts\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from accounts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for accounts")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q accountQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no accountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from accounts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for accounts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AccountSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(accountBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"accounts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, accountPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from account slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for accounts")
	}

	if len(accountAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Account) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAccount(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AccountSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AccountSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"accounts\".* FROM \"accounts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, accountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in AccountSlice")
	}

	*o = slice

	return nil
}

// AccountExists checks if the Account row exists.
func AccountExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"accounts\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if accounts exists")
	}

	return exists, nil
}

// Exists checks if the Account row exists.
func (o *Account) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AccountExists(ctx, exec, o.ID)
}
