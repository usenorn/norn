// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceAgentProposal is an object representing the database table.
type WorkspaceAgentProposal struct {
	ID                 string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID        string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	AgentID            string      `boil:"agent_id" json:"agent_id" toml:"agent_id" yaml:"agent_id"`
	IssueID            null.String `boil:"issue_id" json:"issue_id,omitempty" toml:"issue_id" yaml:"issue_id,omitempty"`
	TeamID             string      `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	Action             string      `boil:"action" json:"action" toml:"action" yaml:"action"`
	Change             types.JSON  `boil:"change" json:"change" toml:"change" yaml:"change"`
	Status             string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	DecidedByAccountID null.String `boil:"decided_by_account_id" json:"decided_by_account_id,omitempty" toml:"decided_by_account_id" yaml:"decided_by_account_id,omitempty"`
	DecidedAt          null.Time   `boil:"decided_at" json:"decided_at,omitempty" toml:"decided_at" yaml:"decided_at,omitempty"`
	Failure            null.String `boil:"failure" json:"failure,omitempty" toml:"failure" yaml:"failure,omitempty"`
	CreatedAt          time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt          time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	Reasoning          types.JSON  `boil:"reasoning" json:"reasoning" toml:"reasoning" yaml:"reasoning"`

	R *workspaceAgentProposalR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceAgentProposalL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceAgentProposalColumns = struct {
	ID                 string
	WorkspaceID        string
	AgentID            string
	IssueID            string
	TeamID             string
	Action             string
	Change             string
	Status             string
	DecidedByAccountID string
	DecidedAt          string
	Failure            string
	CreatedAt          string
	UpdatedAt          string
	Reasoning          string
}{
	ID:                 "id",
	WorkspaceID:        "workspace_id",
	AgentID:            "agent_id",
	IssueID:            "issue_id",
	TeamID:             "team_id",
	Action:             "action",
	Change:             "change",
	Status:             "status",
	DecidedByAccountID: "decided_by_account_id",
	DecidedAt:          "decided_at",
	Failure:            "failure",
	CreatedAt:          "created_at",
	UpdatedAt:          "updated_at",
	Reasoning:          "reasoning",
}

var WorkspaceAgentProposalTableColumns = struct {
	ID                 string
	WorkspaceID        string
	AgentID            string
	IssueID            string
	TeamID             string
	Action             string
	Change             string
	Status             string
	DecidedByAccountID string
	DecidedAt          string
	Failure            string
	CreatedAt          string
	UpdatedAt          string
	Reasoning          string
}{
	ID:                 "workspace_agent_proposals.id",
	WorkspaceID:        "workspace_agent_proposals.workspace_id",
	AgentID:            "workspace_agent_proposals.agent_id",
	IssueID:            "workspace_agent_proposals.issue_id",
	TeamID:             "workspace_agent_proposals.team_id",
	Action:             "workspace_agent_proposals.action",
	Change:             "workspace_agent_proposals.change",
	Status:             "workspace_agent_proposals.status",
	DecidedByAccountID: "workspace_agent_proposals.decided_by_account_id",
	DecidedAt:          "workspace_agent_proposals.decided_at",
	Failure:            "workspace_agent_proposals.failure",
	CreatedAt:          "workspace_agent_proposals.created_at",
	UpdatedAt:          "workspace_agent_proposals.updated_at",
	Reasoning:          "workspace_agent_proposals.reasoning",
}

// Generated where

type whereHelpertypes_JSON struct{ field string }

func (w whereHelpertypes_JSON) EQ(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_JSON) NEQ(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_JSON) LT(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_JSON) LTE(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_JSON) GT(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_JSON) GTE(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var WorkspaceAgentProposalWhere = struct {
	ID                 whereHelperstring
	WorkspaceID        whereHelperstring
	AgentID            whereHelperstring
	IssueID            whereHelpernull_String
	TeamID             whereHelperstring
	Action             whereHelperstring
	Change             whereHelpertypes_JSON
	Status             whereHelperstring
	DecidedByAccountID whereHelpernull_String
	DecidedAt          whereHelpernull_Time
	Failure            whereHelpernull_String
	CreatedAt          whereHelpertime_Time
	UpdatedAt          whereHelpertime_Time
	Reasoning          whereHelpertypes_JSON
}{
	ID:                 whereHelperstring{field: "\"workspace_agent_proposals\".\"id\""},
	WorkspaceID:        whereHelperstring{field: "\"workspace_agent_proposals\".\"workspace_id\""},
	AgentID:            whereHelperstring{field: "\"workspace_agent_proposals\".\"agent_id\""},
	IssueID:            whereHelpernull_String{field: "\"workspace_agent_proposals\".\"issue_id\""},
	TeamID:             whereHelperstring{field: "\"workspace_agent_proposals\".\"team_id\""},
	Action:             whereHelperstring{field: "\"workspace_agent_proposals\".\"action\""},
	Change:             whereHelpertypes_JSON{field: "\"workspace_agent_proposals\".\"change\""},
	Status:             whereHelperstring{field: "\"workspace_agent_proposals\".\"status\""},
	DecidedByAccountID: whereHelpernull_String{field: "\"workspace_agent_proposals\".\"decided_by_account_id\""},
	DecidedAt:          whereHelpernull_Time{field: "\"workspace_agent_proposals\".\"decided_at\""},
	Failure:            whereHelpernull_String{field: "\"workspace_agent_proposals\".\"failure\""},
	CreatedAt:          whereHelpertime_Time{field: "\"workspace_agent_proposals\".\"created_at\""},
	UpdatedAt:          whereHelpertime_Time{field: "\"workspace_agent_proposals\".\"updated_at\""},
	Reasoning:          whereHelpertypes_JSON{field: "\"workspace_agent_proposals\".\"reasoning\""},
}

// WorkspaceAgentProposalRels is where relationship names are stored.
var WorkspaceAgentProposalRels = struct {
	Agent            string
	DecidedByAccount string
	Issue            string
	Workspace        string
}{
	Agent:            "Agent",
	DecidedByAccount: "DecidedByAccount",
	Issue:            "Issue",
	Workspace:        "Workspace",
}

// workspaceAgentProposalR is where relationships are stored.
type workspaceAgentProposalR struct {
	Agent            *WorkspaceAgent `boil:"Agent" json:"Agent" toml:"Agent" yaml:"Agent"`
	DecidedByAccount *Account        `boil:"DecidedByAccount" json:"DecidedByAccount" toml:"DecidedByAccount" yaml:"DecidedByAccount"`
	Issue            *WorkspaceIssue `boil:"Issue" json:"Issue" toml:"Issue" yaml:"Issue"`
	Workspace        *Workspace      `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*workspaceAgentProposalR) NewStruct() *workspaceAgentProposalR {
	return &workspaceAgentProposalR{}
}

func (o *WorkspaceAgentProposal) GetAgent() *WorkspaceAgent {
	if o == nil {
		return nil
	}

	return o.R.GetAgent()
}

func (r *workspaceAgentProposalR) GetAgent() *WorkspaceAgent {
	if r == nil {
		return nil
	}

	return r.Agent
}

func (o *WorkspaceAgentProposal) GetDecidedByAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetDecidedByAccount()
}

func (r *workspaceAgentProposalR) GetDecidedByAccount() *Account {
	if r == nil {
		return nil
	}

	return r.DecidedByAccount
}

func (o *WorkspaceAgentProposal) GetIssue() *WorkspaceIssue {
	if o == nil {
		return nil
	}

	return o.R.GetIssue()
}

func (r *workspaceAgentProposalR) GetIssue() *WorkspaceIssue {
	if r == nil {
		return nil
	}

	return r.Issue
}

func (o *WorkspaceAgentProposal) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceAgentProposalR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// workspaceAgentProposalL is where Load methods for each relationship are stored.
type workspaceAgentProposalL struct{}

var (
	workspaceAgentProposalAllColumns            = []string{"id", "workspace_id", "agent_id", "issue_id", "team_id", "action", "change", "status", "decided_by_account_id", "decided_at", "failure", "created_at", "updated_at", "reasoning"}
	workspaceAgentProposalColumnsWithoutDefault = []string{"workspace_id", "agent_id", "team_id", "action", "change"}
	workspaceAgentProposalColumnsWithDefault    = []string{"id", "issue_id", "status", "decided_by_account_id", "decided_at", "failure", "created_at", "updated_at", "reasoning"}
	workspaceAgentProposalPrimaryKeyColumns     = []string{"id"}
	workspaceAgentProposalGeneratedColumns      = []string{}
)

type (
	// WorkspaceAgentProposalSlice is an alias for a slice of pointers to WorkspaceAgentProposal.
	// This should almost always be used instead of []WorkspaceAgentProposal.
	WorkspaceAgentProposalSlice []*WorkspaceAgentProposal
	// WorkspaceAgentProposalHook is the signature for custom WorkspaceAgentProposal hook methods
	WorkspaceAgentProposalHook func(context.Context, boil.ContextExecutor, *WorkspaceAgentProposal) error

	workspaceAgentProposalQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceAgentProposalType                 = reflect.TypeOf(&WorkspaceAgentProposal{})
	workspaceAgentProposalMapping              = queries.MakeStructMapping(workspaceAgentProposalType)
	workspaceAgentProposalPrimaryKeyMapping, _ = queries.BindMapping(workspaceAgentProposalType, workspaceAgentProposalMapping, workspaceAgentProposalPrimaryKeyColumns)
	workspaceAgentProposalInsertCacheMut       sync.RWMutex
	workspaceAgentProposalInsertCache          = make(map[string]insertCache)
	workspaceAgentProposalUpdateCacheMut       sync.RWMutex
	workspaceAgentProposalUpdateCache          = make(map[string]updateCache)
	workspaceAgentProposalUpsertCacheMut       sync.RWMutex
	workspaceAgentProposalUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceAgentProposalAfterSelectMu sync.Mutex
var workspaceAgentProposalAfterSelectHooks []WorkspaceAgentProposalHook

var workspaceAgentProposalBeforeInsertMu sync.Mutex
var workspaceAgentProposalBeforeInsertHooks []WorkspaceAgentProposalHook
var workspaceAgentProposalAfterInsertMu sync.Mutex
var workspaceAgentProposalAfterInsertHooks []WorkspaceAgentProposalHook

var workspaceAgentProposalBeforeUpdateMu sync.Mutex
var workspaceAgentProposalBeforeUpdateHooks []WorkspaceAgentProposalHook
var workspaceAgentProposalAfterUpdateMu sync.Mutex
var workspaceAgentProposalAfterUpdateHooks []WorkspaceAgentProposalHook

var workspaceAgentProposalBeforeDeleteMu sync.Mutex
var workspaceAgentProposalBeforeDeleteHooks []WorkspaceAgentProposalHook
var workspaceAgentProposalAfterDeleteMu sync.Mutex
var workspaceAgentProposalAfterDeleteHooks []WorkspaceAgentProposalHook

var workspaceAgentProposalBeforeUpsertMu sync.Mutex
var workspaceAgentProposalBeforeUpsertHooks []WorkspaceAgentProposalHook
var workspaceAgentProposalAfterUpsertMu sync.Mutex
var workspaceAgentProposalAfterUpsertHooks []WorkspaceAgentProposalHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceAgentProposal) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceAgentProposal) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceAgentProposal) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceAgentProposal) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceAgentProposal) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceAgentProposal) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceAgentProposal) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceAgentProposal) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceAgentProposal) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentProposalAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceAgentProposalHook registers your hook function for all future operations.
func AddWorkspaceAgentProposalHook(hookPoint boil.HookPoint, workspaceAgentProposalHook WorkspaceAgentProposalHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceAgentProposalAfterSelectMu.Lock()
		workspaceAgentProposalAfterSelectHooks = append(workspaceAgentProposalAfterSelectHooks, workspaceAgentProposalHook)
		workspaceAgentProposalAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceAgentProposalBeforeInsertMu.Lock()
		workspaceAgentProposalBeforeInsertHooks = append(workspaceAgentProposalBeforeInsertHooks, workspaceAgentProposalHook)
		workspaceAgentProposalBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceAgentProposalAfterInsertMu.Lock()
		workspaceAgentProposalAfterInsertHooks = append(workspaceAgentProposalAfterInsertHooks, workspaceAgentProposalHook)
		workspaceAgentProposalAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceAgentProposalBeforeUpdateMu.Lock()
		workspaceAgentProposalBeforeUpdateHooks = append(workspaceAgentProposalBeforeUpdateHooks, workspaceAgentProposalHook)
		workspaceAgentProposalBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceAgentProposalAfterUpdateMu.Lock()
		workspaceAgentProposalAfterUpdateHooks = append(workspaceAgentProposalAfterUpdateHooks, workspaceAgentProposalHook)
		workspaceAgentProposalAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceAgentProposalBeforeDeleteMu.Lock()
		workspaceAgentProposalBeforeDeleteHooks = append(workspaceAgentProposalBeforeDeleteHooks, workspaceAgentProposalHook)
		workspaceAgentProposalBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceAgentProposalAfterDeleteMu.Lock()
		workspaceAgentProposalAfterDeleteHooks = append(workspaceAgentProposalAfterDeleteHooks, workspaceAgentProposalHook)
		workspaceAgentProposalAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceAgentProposalBeforeUpsertMu.Lock()
		workspaceAgentProposalBeforeUpsertHooks = append(workspaceAgentProposalBeforeUpsertHooks, workspaceAgentProposalHook)
		workspaceAgentProposalBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceAgentProposalAfterUpsertMu.Lock()
		workspaceAgentProposalAfterUpsertHooks = append(workspaceAgentProposalAfterUpsertHooks, workspaceAgentProposalHook)
		workspaceAgentProposalAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceAgentProposal record from the query.
func (q workspaceAgentProposalQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceAgentProposal, error) {
	o := &WorkspaceAgentProposal{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_agent_proposals")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceAgentProposal records from the query.
func (q workspaceAgentProposalQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceAgentProposalSlice, error) {
	var o []*WorkspaceAgentProposal

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceAgentProposal slice")
	}

	if len(workspaceAgentProposalAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceAgentProposal records in the query.
func (q workspaceAgentProposalQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_agent_proposals rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceAgentProposalQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_agent_proposals exists")
	}

	return count > 0, nil
}

// Agent pointed to by the foreign key.
func (o *WorkspaceAgentProposal) Agent(mods ...qm.QueryMod) workspaceAgentQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AgentID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceAgents(queryMods...)
}

// DecidedByAccount pointed to by the foreign key.
func (o *WorkspaceAgentProposal) DecidedByAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.DecidedByAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Issue pointed to by the foreign key.
func (o *WorkspaceAgentProposal) Issue(mods ...qm.QueryMod) workspaceIssueQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IssueID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceIssues(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceAgentProposal) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadAgent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentProposalL) LoadAgent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentProposal any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentProposal
	var object *WorkspaceAgentProposal

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentProposal.(*WorkspaceAgentProposal)
		if !ok {
			object = new(WorkspaceAgentProposal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentProposal))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentProposal.(*[]*WorkspaceAgentProposal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentProposal))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentProposalR{}
		}
		args[object.AgentID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentProposalR{}
			}

			args[obj.AgentID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agents`),
		qm.WhereIn(`workspace_agents.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceAgent")
	}

	var resultSlice []*WorkspaceAgent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceAgent")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_agents")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agents")
	}

	if len(workspaceAgentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Agent = foreign
		if foreign.R == nil {
			foreign.R = &workspaceAgentR{}
		}
		foreign.R.AgentWorkspaceAgentProposals = append(foreign.R.AgentWorkspaceAgentProposals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AgentID == foreign.ID {
				local.R.Agent = foreign
				if foreign.R == nil {
					foreign.R = &workspaceAgentR{}
				}
				foreign.R.AgentWorkspaceAgentProposals = append(foreign.R.AgentWorkspaceAgentProposals, local)
				break
			}
		}
	}

	return nil
}

// LoadDecidedByAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentProposalL) LoadDecidedByAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentProposal any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentProposal
	var object *WorkspaceAgentProposal

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentProposal.(*WorkspaceAgentProposal)
		if !ok {
			object = new(WorkspaceAgentProposal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentProposal))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentProposal.(*[]*WorkspaceAgentProposal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentProposal))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentProposalR{}
		}
		if !queries.IsNil(object.DecidedByAccountID) {
			args[object.DecidedByAccountID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentProposalR{}
			}

			if !queries.IsNil(obj.DecidedByAccountID) {
				args[obj.DecidedByAccountID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DecidedByAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.DecidedByAccountWorkspaceAgentProposals = append(foreign.R.DecidedByAccountWorkspaceAgentProposals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.DecidedByAccountID, foreign.ID) {
				local.R.DecidedByAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.DecidedByAccountWorkspaceAgentProposals = append(foreign.R.DecidedByAccountWorkspaceAgentProposals, local)
				break
			}
		}
	}

	return nil
}

// LoadIssue allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentProposalL) LoadIssue(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentProposal any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentProposal
	var object *WorkspaceAgentProposal

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentProposal.(*WorkspaceAgentProposal)
		if !ok {
			object = new(WorkspaceAgentProposal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentProposal))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentProposal.(*[]*WorkspaceAgentProposal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentProposal))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentProposalR{}
		}
		if !queries.IsNil(object.IssueID) {
			args[object.IssueID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentProposalR{}
			}

			if !queries.IsNil(obj.IssueID) {
				args[obj.IssueID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceIssue")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceIssue")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Issue = foreign
		if foreign.R == nil {
			foreign.R = &workspaceIssueR{}
		}
		foreign.R.IssueWorkspaceAgentProposals = append(foreign.R.IssueWorkspaceAgentProposals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.IssueID, foreign.ID) {
				local.R.Issue = foreign
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.IssueWorkspaceAgentProposals = append(foreign.R.IssueWorkspaceAgentProposals, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentProposalL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentProposal any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentProposal
	var object *WorkspaceAgentProposal

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentProposal.(*WorkspaceAgentProposal)
		if !ok {
			object = new(WorkspaceAgentProposal)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentProposal))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentProposal.(*[]*WorkspaceAgentProposal)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentProposal)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentProposal))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentProposalR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentProposalR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceAgentProposals = append(foreign.R.WorkspaceAgentProposals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceAgentProposals = append(foreign.R.WorkspaceAgentProposals, local)
				break
			}
		}
	}

	return nil
}

// SetAgent of the workspaceAgentProposal to the related item.
// Sets o.R.Agent to related.
// Adds o to related.R.AgentWorkspaceAgentProposals.
func (o *WorkspaceAgentProposal) SetAgent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceAgent) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentProposalPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AgentID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentProposalR{
			Agent: related,
		}
	} else {
		o.R.Agent = related
	}

	if related.R == nil {
		related.R = &workspaceAgentR{
			AgentWorkspaceAgentProposals: WorkspaceAgentProposalSlice{o},
		}
	} else {
		related.R.AgentWorkspaceAgentProposals = append(related.R.AgentWorkspaceAgentProposals, o)
	}

	return nil
}

// SetDecidedByAccount of the workspaceAgentProposal to the related item.
// Sets o.R.DecidedByAccount to related.
// Adds o to related.R.DecidedByAccountWorkspaceAgentProposals.
func (o *WorkspaceAgentProposal) SetDecidedByAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"decided_by_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentProposalPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.DecidedByAccountID, related.ID)
	if o.R == nil {
		o.R = &workspaceAgentProposalR{
			DecidedByAccount: related,
		}
	} else {
		o.R.DecidedByAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			DecidedByAccountWorkspaceAgentProposals: WorkspaceAgentProposalSlice{o},
		}
	} else {
		related.R.DecidedByAccountWorkspaceAgentProposals = append(related.R.DecidedByAccountWorkspaceAgentProposals, o)
	}

	return nil
}

// RemoveDecidedByAccount relationship.
// Sets o.R.DecidedByAccount to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceAgentProposal) RemoveDecidedByAccount(ctx context.Context, exec boil.ContextExecutor, related *Account) error {
	var err error

	queries.SetScanner(&o.DecidedByAccountID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("decided_by_account_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.DecidedByAccount = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.DecidedByAccountWorkspaceAgentProposals {
		if queries.Equal(o.DecidedByAccountID, ri.DecidedByAccountID) {
			continue
		}

		ln := len(related.R.DecidedByAccountWorkspaceAgentProposals)
		if ln > 1 && i < ln-1 {
			related.R.DecidedByAccountWorkspaceAgentProposals[i] = related.R.DecidedByAccountWorkspaceAgentProposals[ln-1]
		}
		related.R.DecidedByAccountWorkspaceAgentProposals = related.R.DecidedByAccountWorkspaceAgentProposals[:ln-1]
		break
	}
	return nil
}

// SetIssue of the workspaceAgentProposal to the related item.
// Sets o.R.Issue to related.
// Adds o to related.R.IssueWorkspaceAgentProposals.
func (o *WorkspaceAgentProposal) SetIssue(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceIssue) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"issue_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentProposalPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.IssueID, related.ID)
	if o.R == nil {
		o.R = &workspaceAgentProposalR{
			Issue: related,
		}
	} else {
		o.R.Issue = related
	}

	if related.R == nil {
		related.R = &workspaceIssueR{
			IssueWorkspaceAgentProposals: WorkspaceAgentProposalSlice{o},
		}
	} else {
		related.R.IssueWorkspaceAgentProposals = append(related.R.IssueWorkspaceAgentProposals, o)
	}

	return nil
}

// RemoveIssue relationship.
// Sets o.R.Issue to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceAgentProposal) RemoveIssue(ctx context.Context, exec boil.ContextExecutor, related *WorkspaceIssue) error {
	var err error

	queries.SetScanner(&o.IssueID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("issue_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Issue = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.IssueWorkspaceAgentProposals {
		if queries.Equal(o.IssueID, ri.IssueID) {
			continue
		}

		ln := len(related.R.IssueWorkspaceAgentProposals)
		if ln > 1 && i < ln-1 {
			related.R.IssueWorkspaceAgentProposals[i] = related.R.IssueWorkspaceAgentProposals[ln-1]
		}
		related.R.IssueWorkspaceAgentProposals = related.R.IssueWorkspaceAgentProposals[:ln-1]
		break
	}
	return nil
}

// SetWorkspace of the workspaceAgentProposal to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceAgentProposals.
func (o *WorkspaceAgentProposal) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentProposalPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentProposalR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceAgentProposals: WorkspaceAgentProposalSlice{o},
		}
	} else {
		related.R.WorkspaceAgentProposals = append(related.R.WorkspaceAgentProposals, o)
	}

	return nil
}

// WorkspaceAgentProposals retrieves all the records using an executor.
func WorkspaceAgentProposals(mods ...qm.QueryMod) workspaceAgentProposalQuery {
	mods = append(mods, qm.From("\"workspace_agent_proposals\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_agent_proposals\".*"})
	}

	return workspaceAgentProposalQuery{q}
}

// FindWorkspaceAgentProposal retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceAgentProposal(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceAgentProposal, error) {
	workspaceAgentProposalObj := &WorkspaceAgentProposal{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_agent_proposals\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceAgentProposalObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_agent_proposals")
	}

	if err = workspaceAgentProposalObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceAgentProposalObj, err
	}

	return workspaceAgentProposalObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceAgentProposal) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_proposals provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentProposalColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceAgentProposalInsertCacheMut.RLock()
	cache, cached := workspaceAgentProposalInsertCache[key]
	workspaceAgentProposalInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceAgentProposalAllColumns,
			workspaceAgentProposalColumnsWithDefault,
			workspaceAgentProposalColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentProposalType, workspaceAgentProposalMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceAgentProposalType, workspaceAgentProposalMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_agent_proposals\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_agent_proposals\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_agent_proposals")
	}

	if !cached {
		workspaceAgentProposalInsertCacheMut.Lock()
		workspaceAgentProposalInsertCache[key] = cache
		workspaceAgentProposalInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceAgentProposal.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceAgentProposal) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceAgentProposalUpdateCacheMut.RLock()
	cache, cached := workspaceAgentProposalUpdateCache[key]
	workspaceAgentProposalUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceAgentProposalAllColumns,
			workspaceAgentProposalPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_agent_proposals, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceAgentProposalPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceAgentProposalType, workspaceAgentProposalMapping, append(wl, workspaceAgentProposalPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_agent_proposals row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_agent_proposals")
	}

	if !cached {
		workspaceAgentProposalUpdateCacheMut.Lock()
		workspaceAgentProposalUpdateCache[key] = cache
		workspaceAgentProposalUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceAgentProposalQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_agent_proposals")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_agent_proposals")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceAgentProposalSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentProposalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceAgentProposalPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceAgentProposal slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceAgentProposal")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceAgentProposal) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_proposals provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentProposalColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceAgentProposalUpsertCacheMut.RLock()
	cache, cached := workspaceAgentProposalUpsertCache[key]
	workspaceAgentProposalUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceAgentProposalAllColumns,
			workspaceAgentProposalColumnsWithDefault,
			workspaceAgentProposalColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceAgentProposalAllColumns,
			workspaceAgentProposalPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_agent_proposals, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceAgentProposalAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceAgentProposalPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_agent_proposals, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceAgentProposalPrimaryKeyColumns))
			copy(conflict, workspaceAgentProposalPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_agent_proposals\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentProposalType, workspaceAgentProposalMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceAgentProposalType, workspaceAgentProposalMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_agent_proposals")
	}

	if !cached {
		workspaceAgentProposalUpsertCacheMut.Lock()
		workspaceAgentProposalUpsertCache[key] = cache
		workspaceAgentProposalUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceAgentProposal record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceAgentProposal) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceAgentProposal provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceAgentProposalPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_agent_proposals\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_agent_proposals")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_agent_proposals")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceAgentProposalQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceAgentProposalQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_agent_proposals")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_proposals")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceAgentProposalSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceAgentProposalBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentProposalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_agent_proposals\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentProposalPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceAgentProposal slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_proposals")
	}

	if len(workspaceAgentProposalAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceAgentProposal) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceAgentProposal(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceAgentProposalSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceAgentProposalSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentProposalPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_agent_proposals\".* FROM \"workspace_agent_proposals\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentProposalPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceAgentProposalSlice")
	}

	*o = slice

	return nil
}

// WorkspaceAgentProposalExists checks if the WorkspaceAgentProposal row exists.
func WorkspaceAgentProposalExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_agent_proposals\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_agent_proposals exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceAgentProposal row exists.
func (o *WorkspaceAgentProposal) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceAgentProposalExists(ctx, exec, o.ID)
}
