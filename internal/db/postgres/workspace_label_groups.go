// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceLabelGroup is an object representing the database table.
type WorkspaceLabelGroup struct {
	ID          string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	Name        string    `boil:"name" json:"name" toml:"name" yaml:"name"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceLabelGroupR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceLabelGroupL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceLabelGroupColumns = struct {
	ID          string
	WorkspaceID string
	Name        string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	WorkspaceID: "workspace_id",
	Name:        "name",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var WorkspaceLabelGroupTableColumns = struct {
	ID          string
	WorkspaceID string
	Name        string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "workspace_label_groups.id",
	WorkspaceID: "workspace_label_groups.workspace_id",
	Name:        "workspace_label_groups.name",
	CreatedAt:   "workspace_label_groups.created_at",
	UpdatedAt:   "workspace_label_groups.updated_at",
}

// Generated where

var WorkspaceLabelGroupWhere = struct {
	ID          whereHelperstring
	WorkspaceID whereHelperstring
	Name        whereHelperstring
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"workspace_label_groups\".\"id\""},
	WorkspaceID: whereHelperstring{field: "\"workspace_label_groups\".\"workspace_id\""},
	Name:        whereHelperstring{field: "\"workspace_label_groups\".\"name\""},
	CreatedAt:   whereHelpertime_Time{field: "\"workspace_label_groups\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"workspace_label_groups\".\"updated_at\""},
}

// WorkspaceLabelGroupRels is where relationship names are stored.
var WorkspaceLabelGroupRels = struct {
	Workspace string
}{
	Workspace: "Workspace",
}

// workspaceLabelGroupR is where relationships are stored.
type workspaceLabelGroupR struct {
	Workspace *Workspace `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*workspaceLabelGroupR) NewStruct() *workspaceLabelGroupR {
	return &workspaceLabelGroupR{}
}

func (o *WorkspaceLabelGroup) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceLabelGroupR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// workspaceLabelGroupL is where Load methods for each relationship are stored.
type workspaceLabelGroupL struct{}

var (
	workspaceLabelGroupAllColumns            = []string{"id", "workspace_id", "name", "created_at", "updated_at"}
	workspaceLabelGroupColumnsWithoutDefault = []string{"workspace_id", "name"}
	workspaceLabelGroupColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	workspaceLabelGroupPrimaryKeyColumns     = []string{"id"}
	workspaceLabelGroupGeneratedColumns      = []string{}
)

type (
	// WorkspaceLabelGroupSlice is an alias for a slice of pointers to WorkspaceLabelGroup.
	// This should almost always be used instead of []WorkspaceLabelGroup.
	WorkspaceLabelGroupSlice []*WorkspaceLabelGroup
	// WorkspaceLabelGroupHook is the signature for custom WorkspaceLabelGroup hook methods
	WorkspaceLabelGroupHook func(context.Context, boil.ContextExecutor, *WorkspaceLabelGroup) error

	workspaceLabelGroupQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceLabelGroupType                 = reflect.TypeOf(&WorkspaceLabelGroup{})
	workspaceLabelGroupMapping              = queries.MakeStructMapping(workspaceLabelGroupType)
	workspaceLabelGroupPrimaryKeyMapping, _ = queries.BindMapping(workspaceLabelGroupType, workspaceLabelGroupMapping, workspaceLabelGroupPrimaryKeyColumns)
	workspaceLabelGroupInsertCacheMut       sync.RWMutex
	workspaceLabelGroupInsertCache          = make(map[string]insertCache)
	workspaceLabelGroupUpdateCacheMut       sync.RWMutex
	workspaceLabelGroupUpdateCache          = make(map[string]updateCache)
	workspaceLabelGroupUpsertCacheMut       sync.RWMutex
	workspaceLabelGroupUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceLabelGroupAfterSelectMu sync.Mutex
var workspaceLabelGroupAfterSelectHooks []WorkspaceLabelGroupHook

var workspaceLabelGroupBeforeInsertMu sync.Mutex
var workspaceLabelGroupBeforeInsertHooks []WorkspaceLabelGroupHook
var workspaceLabelGroupAfterInsertMu sync.Mutex
var workspaceLabelGroupAfterInsertHooks []WorkspaceLabelGroupHook

var workspaceLabelGroupBeforeUpdateMu sync.Mutex
var workspaceLabelGroupBeforeUpdateHooks []WorkspaceLabelGroupHook
var workspaceLabelGroupAfterUpdateMu sync.Mutex
var workspaceLabelGroupAfterUpdateHooks []WorkspaceLabelGroupHook

var workspaceLabelGroupBeforeDeleteMu sync.Mutex
var workspaceLabelGroupBeforeDeleteHooks []WorkspaceLabelGroupHook
var workspaceLabelGroupAfterDeleteMu sync.Mutex
var workspaceLabelGroupAfterDeleteHooks []WorkspaceLabelGroupHook

var workspaceLabelGroupBeforeUpsertMu sync.Mutex
var workspaceLabelGroupBeforeUpsertHooks []WorkspaceLabelGroupHook
var workspaceLabelGroupAfterUpsertMu sync.Mutex
var workspaceLabelGroupAfterUpsertHooks []WorkspaceLabelGroupHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceLabelGroup) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceLabelGroup) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceLabelGroup) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceLabelGroup) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceLabelGroup) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceLabelGroup) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceLabelGroup) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceLabelGroup) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceLabelGroup) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceLabelGroupAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceLabelGroupHook registers your hook function for all future operations.
func AddWorkspaceLabelGroupHook(hookPoint boil.HookPoint, workspaceLabelGroupHook WorkspaceLabelGroupHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceLabelGroupAfterSelectMu.Lock()
		workspaceLabelGroupAfterSelectHooks = append(workspaceLabelGroupAfterSelectHooks, workspaceLabelGroupHook)
		workspaceLabelGroupAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceLabelGroupBeforeInsertMu.Lock()
		workspaceLabelGroupBeforeInsertHooks = append(workspaceLabelGroupBeforeInsertHooks, workspaceLabelGroupHook)
		workspaceLabelGroupBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceLabelGroupAfterInsertMu.Lock()
		workspaceLabelGroupAfterInsertHooks = append(workspaceLabelGroupAfterInsertHooks, workspaceLabelGroupHook)
		workspaceLabelGroupAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceLabelGroupBeforeUpdateMu.Lock()
		workspaceLabelGroupBeforeUpdateHooks = append(workspaceLabelGroupBeforeUpdateHooks, workspaceLabelGroupHook)
		workspaceLabelGroupBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceLabelGroupAfterUpdateMu.Lock()
		workspaceLabelGroupAfterUpdateHooks = append(workspaceLabelGroupAfterUpdateHooks, workspaceLabelGroupHook)
		workspaceLabelGroupAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceLabelGroupBeforeDeleteMu.Lock()
		workspaceLabelGroupBeforeDeleteHooks = append(workspaceLabelGroupBeforeDeleteHooks, workspaceLabelGroupHook)
		workspaceLabelGroupBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceLabelGroupAfterDeleteMu.Lock()
		workspaceLabelGroupAfterDeleteHooks = append(workspaceLabelGroupAfterDeleteHooks, workspaceLabelGroupHook)
		workspaceLabelGroupAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceLabelGroupBeforeUpsertMu.Lock()
		workspaceLabelGroupBeforeUpsertHooks = append(workspaceLabelGroupBeforeUpsertHooks, workspaceLabelGroupHook)
		workspaceLabelGroupBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceLabelGroupAfterUpsertMu.Lock()
		workspaceLabelGroupAfterUpsertHooks = append(workspaceLabelGroupAfterUpsertHooks, workspaceLabelGroupHook)
		workspaceLabelGroupAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceLabelGroup record from the query.
func (q workspaceLabelGroupQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceLabelGroup, error) {
	o := &WorkspaceLabelGroup{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_label_groups")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceLabelGroup records from the query.
func (q workspaceLabelGroupQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceLabelGroupSlice, error) {
	var o []*WorkspaceLabelGroup

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceLabelGroup slice")
	}

	if len(workspaceLabelGroupAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceLabelGroup records in the query.
func (q workspaceLabelGroupQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_label_groups rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceLabelGroupQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_label_groups exists")
	}

	return count > 0, nil
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceLabelGroup) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceLabelGroupL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceLabelGroup any, mods queries.Applicator) error {
	var slice []*WorkspaceLabelGroup
	var object *WorkspaceLabelGroup

	if singular {
		var ok bool
		object, ok = maybeWorkspaceLabelGroup.(*WorkspaceLabelGroup)
		if !ok {
			object = new(WorkspaceLabelGroup)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceLabelGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceLabelGroup))
			}
		}
	} else {
		s, ok := maybeWorkspaceLabelGroup.(*[]*WorkspaceLabelGroup)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceLabelGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceLabelGroup))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceLabelGroupR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceLabelGroupR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceLabelGroups = append(foreign.R.WorkspaceLabelGroups, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceLabelGroups = append(foreign.R.WorkspaceLabelGroups, local)
				break
			}
		}
	}

	return nil
}

// SetWorkspace of the workspaceLabelGroup to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceLabelGroups.
func (o *WorkspaceLabelGroup) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_label_groups\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceLabelGroupPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceLabelGroupR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceLabelGroups: WorkspaceLabelGroupSlice{o},
		}
	} else {
		related.R.WorkspaceLabelGroups = append(related.R.WorkspaceLabelGroups, o)
	}

	return nil
}

// WorkspaceLabelGroups retrieves all the records using an executor.
func WorkspaceLabelGroups(mods ...qm.QueryMod) workspaceLabelGroupQuery {
	mods = append(mods, qm.From("\"workspace_label_groups\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_label_groups\".*"})
	}

	return workspaceLabelGroupQuery{q}
}

// FindWorkspaceLabelGroup retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceLabelGroup(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceLabelGroup, error) {
	workspaceLabelGroupObj := &WorkspaceLabelGroup{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_label_groups\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceLabelGroupObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_label_groups")
	}

	if err = workspaceLabelGroupObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceLabelGroupObj, err
	}

	return workspaceLabelGroupObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceLabelGroup) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_label_groups provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceLabelGroupColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceLabelGroupInsertCacheMut.RLock()
	cache, cached := workspaceLabelGroupInsertCache[key]
	workspaceLabelGroupInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceLabelGroupAllColumns,
			workspaceLabelGroupColumnsWithDefault,
			workspaceLabelGroupColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceLabelGroupType, workspaceLabelGroupMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceLabelGroupType, workspaceLabelGroupMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_label_groups\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_label_groups\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_label_groups")
	}

	if !cached {
		workspaceLabelGroupInsertCacheMut.Lock()
		workspaceLabelGroupInsertCache[key] = cache
		workspaceLabelGroupInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceLabelGroup.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceLabelGroup) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceLabelGroupUpdateCacheMut.RLock()
	cache, cached := workspaceLabelGroupUpdateCache[key]
	workspaceLabelGroupUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceLabelGroupAllColumns,
			workspaceLabelGroupPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_label_groups, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_label_groups\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceLabelGroupPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceLabelGroupType, workspaceLabelGroupMapping, append(wl, workspaceLabelGroupPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_label_groups row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_label_groups")
	}

	if !cached {
		workspaceLabelGroupUpdateCacheMut.Lock()
		workspaceLabelGroupUpdateCache[key] = cache
		workspaceLabelGroupUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceLabelGroupQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_label_groups")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_label_groups")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceLabelGroupSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceLabelGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_label_groups\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceLabelGroupPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceLabelGroup slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceLabelGroup")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceLabelGroup) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_label_groups provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceLabelGroupColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceLabelGroupUpsertCacheMut.RLock()
	cache, cached := workspaceLabelGroupUpsertCache[key]
	workspaceLabelGroupUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceLabelGroupAllColumns,
			workspaceLabelGroupColumnsWithDefault,
			workspaceLabelGroupColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceLabelGroupAllColumns,
			workspaceLabelGroupPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_label_groups, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceLabelGroupAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceLabelGroupPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_label_groups, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceLabelGroupPrimaryKeyColumns))
			copy(conflict, workspaceLabelGroupPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_label_groups\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceLabelGroupType, workspaceLabelGroupMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceLabelGroupType, workspaceLabelGroupMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_label_groups")
	}

	if !cached {
		workspaceLabelGroupUpsertCacheMut.Lock()
		workspaceLabelGroupUpsertCache[key] = cache
		workspaceLabelGroupUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceLabelGroup record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceLabelGroup) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceLabelGroup provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceLabelGroupPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_label_groups\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_label_groups")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_label_groups")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceLabelGroupQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceLabelGroupQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_label_groups")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_label_groups")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceLabelGroupSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceLabelGroupBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceLabelGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_label_groups\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceLabelGroupPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceLabelGroup slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_label_groups")
	}

	if len(workspaceLabelGroupAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceLabelGroup) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceLabelGroup(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceLabelGroupSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceLabelGroupSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceLabelGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_label_groups\".* FROM \"workspace_label_groups\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceLabelGroupPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceLabelGroupSlice")
	}

	*o = slice

	return nil
}

// WorkspaceLabelGroupExists checks if the WorkspaceLabelGroup row exists.
func WorkspaceLabelGroupExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_label_groups\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_label_groups exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceLabelGroup row exists.
func (o *WorkspaceLabelGroup) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceLabelGroupExists(ctx, exec, o.ID)
}
