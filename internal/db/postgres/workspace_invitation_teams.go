// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceInvitationTeam is an object representing the database table.
type WorkspaceInvitationTeam struct {
	InvitationID string    `boil:"invitation_id" json:"invitation_id" toml:"invitation_id" yaml:"invitation_id"`
	TeamID       string    `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	CreatedAt    time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *workspaceInvitationTeamR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceInvitationTeamL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceInvitationTeamColumns = struct {
	InvitationID string
	TeamID       string
	CreatedAt    string
}{
	InvitationID: "invitation_id",
	TeamID:       "team_id",
	CreatedAt:    "created_at",
}

var WorkspaceInvitationTeamTableColumns = struct {
	InvitationID string
	TeamID       string
	CreatedAt    string
}{
	InvitationID: "workspace_invitation_teams.invitation_id",
	TeamID:       "workspace_invitation_teams.team_id",
	CreatedAt:    "workspace_invitation_teams.created_at",
}

// Generated where

var WorkspaceInvitationTeamWhere = struct {
	InvitationID whereHelperstring
	TeamID       whereHelperstring
	CreatedAt    whereHelpertime_Time
}{
	InvitationID: whereHelperstring{field: "\"workspace_invitation_teams\".\"invitation_id\""},
	TeamID:       whereHelperstring{field: "\"workspace_invitation_teams\".\"team_id\""},
	CreatedAt:    whereHelpertime_Time{field: "\"workspace_invitation_teams\".\"created_at\""},
}

// WorkspaceInvitationTeamRels is where relationship names are stored.
var WorkspaceInvitationTeamRels = struct {
	Invitation string
	Team       string
}{
	Invitation: "Invitation",
	Team:       "Team",
}

// workspaceInvitationTeamR is where relationships are stored.
type workspaceInvitationTeamR struct {
	Invitation *WorkspaceInvitation `boil:"Invitation" json:"Invitation" toml:"Invitation" yaml:"Invitation"`
	Team       *WorkspaceTeam       `boil:"Team" json:"Team" toml:"Team" yaml:"Team"`
}

// NewStruct creates a new relationship struct
func (*workspaceInvitationTeamR) NewStruct() *workspaceInvitationTeamR {
	return &workspaceInvitationTeamR{}
}

func (o *WorkspaceInvitationTeam) GetInvitation() *WorkspaceInvitation {
	if o == nil {
		return nil
	}

	return o.R.GetInvitation()
}

func (r *workspaceInvitationTeamR) GetInvitation() *WorkspaceInvitation {
	if r == nil {
		return nil
	}

	return r.Invitation
}

func (o *WorkspaceInvitationTeam) GetTeam() *WorkspaceTeam {
	if o == nil {
		return nil
	}

	return o.R.GetTeam()
}

func (r *workspaceInvitationTeamR) GetTeam() *WorkspaceTeam {
	if r == nil {
		return nil
	}

	return r.Team
}

// workspaceInvitationTeamL is where Load methods for each relationship are stored.
type workspaceInvitationTeamL struct{}

var (
	workspaceInvitationTeamAllColumns            = []string{"invitation_id", "team_id", "created_at"}
	workspaceInvitationTeamColumnsWithoutDefault = []string{"invitation_id", "team_id"}
	workspaceInvitationTeamColumnsWithDefault    = []string{"created_at"}
	workspaceInvitationTeamPrimaryKeyColumns     = []string{"invitation_id", "team_id"}
	workspaceInvitationTeamGeneratedColumns      = []string{}
)

type (
	// WorkspaceInvitationTeamSlice is an alias for a slice of pointers to WorkspaceInvitationTeam.
	// This should almost always be used instead of []WorkspaceInvitationTeam.
	WorkspaceInvitationTeamSlice []*WorkspaceInvitationTeam
	// WorkspaceInvitationTeamHook is the signature for custom WorkspaceInvitationTeam hook methods
	WorkspaceInvitationTeamHook func(context.Context, boil.ContextExecutor, *WorkspaceInvitationTeam) error

	workspaceInvitationTeamQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceInvitationTeamType                 = reflect.TypeOf(&WorkspaceInvitationTeam{})
	workspaceInvitationTeamMapping              = queries.MakeStructMapping(workspaceInvitationTeamType)
	workspaceInvitationTeamPrimaryKeyMapping, _ = queries.BindMapping(workspaceInvitationTeamType, workspaceInvitationTeamMapping, workspaceInvitationTeamPrimaryKeyColumns)
	workspaceInvitationTeamInsertCacheMut       sync.RWMutex
	workspaceInvitationTeamInsertCache          = make(map[string]insertCache)
	workspaceInvitationTeamUpdateCacheMut       sync.RWMutex
	workspaceInvitationTeamUpdateCache          = make(map[string]updateCache)
	workspaceInvitationTeamUpsertCacheMut       sync.RWMutex
	workspaceInvitationTeamUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceInvitationTeamAfterSelectMu sync.Mutex
var workspaceInvitationTeamAfterSelectHooks []WorkspaceInvitationTeamHook

var workspaceInvitationTeamBeforeInsertMu sync.Mutex
var workspaceInvitationTeamBeforeInsertHooks []WorkspaceInvitationTeamHook
var workspaceInvitationTeamAfterInsertMu sync.Mutex
var workspaceInvitationTeamAfterInsertHooks []WorkspaceInvitationTeamHook

var workspaceInvitationTeamBeforeUpdateMu sync.Mutex
var workspaceInvitationTeamBeforeUpdateHooks []WorkspaceInvitationTeamHook
var workspaceInvitationTeamAfterUpdateMu sync.Mutex
var workspaceInvitationTeamAfterUpdateHooks []WorkspaceInvitationTeamHook

var workspaceInvitationTeamBeforeDeleteMu sync.Mutex
var workspaceInvitationTeamBeforeDeleteHooks []WorkspaceInvitationTeamHook
var workspaceInvitationTeamAfterDeleteMu sync.Mutex
var workspaceInvitationTeamAfterDeleteHooks []WorkspaceInvitationTeamHook

var workspaceInvitationTeamBeforeUpsertMu sync.Mutex
var workspaceInvitationTeamBeforeUpsertHooks []WorkspaceInvitationTeamHook
var workspaceInvitationTeamAfterUpsertMu sync.Mutex
var workspaceInvitationTeamAfterUpsertHooks []WorkspaceInvitationTeamHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceInvitationTeam) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceInvitationTeam) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceInvitationTeam) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceInvitationTeam) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceInvitationTeam) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceInvitationTeam) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceInvitationTeam) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceInvitationTeam) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceInvitationTeam) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceInvitationTeamAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceInvitationTeamHook registers your hook function for all future operations.
func AddWorkspaceInvitationTeamHook(hookPoint boil.HookPoint, workspaceInvitationTeamHook WorkspaceInvitationTeamHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceInvitationTeamAfterSelectMu.Lock()
		workspaceInvitationTeamAfterSelectHooks = append(workspaceInvitationTeamAfterSelectHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceInvitationTeamBeforeInsertMu.Lock()
		workspaceInvitationTeamBeforeInsertHooks = append(workspaceInvitationTeamBeforeInsertHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceInvitationTeamAfterInsertMu.Lock()
		workspaceInvitationTeamAfterInsertHooks = append(workspaceInvitationTeamAfterInsertHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceInvitationTeamBeforeUpdateMu.Lock()
		workspaceInvitationTeamBeforeUpdateHooks = append(workspaceInvitationTeamBeforeUpdateHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceInvitationTeamAfterUpdateMu.Lock()
		workspaceInvitationTeamAfterUpdateHooks = append(workspaceInvitationTeamAfterUpdateHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceInvitationTeamBeforeDeleteMu.Lock()
		workspaceInvitationTeamBeforeDeleteHooks = append(workspaceInvitationTeamBeforeDeleteHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceInvitationTeamAfterDeleteMu.Lock()
		workspaceInvitationTeamAfterDeleteHooks = append(workspaceInvitationTeamAfterDeleteHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceInvitationTeamBeforeUpsertMu.Lock()
		workspaceInvitationTeamBeforeUpsertHooks = append(workspaceInvitationTeamBeforeUpsertHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceInvitationTeamAfterUpsertMu.Lock()
		workspaceInvitationTeamAfterUpsertHooks = append(workspaceInvitationTeamAfterUpsertHooks, workspaceInvitationTeamHook)
		workspaceInvitationTeamAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceInvitationTeam record from the query.
func (q workspaceInvitationTeamQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceInvitationTeam, error) {
	o := &WorkspaceInvitationTeam{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_invitation_teams")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceInvitationTeam records from the query.
func (q workspaceInvitationTeamQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceInvitationTeamSlice, error) {
	var o []*WorkspaceInvitationTeam

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceInvitationTeam slice")
	}

	if len(workspaceInvitationTeamAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceInvitationTeam records in the query.
func (q workspaceInvitationTeamQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_invitation_teams rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceInvitationTeamQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_invitation_teams exists")
	}

	return count > 0, nil
}

// Invitation pointed to by the foreign key.
func (o *WorkspaceInvitationTeam) Invitation(mods ...qm.QueryMod) workspaceInvitationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.InvitationID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceInvitations(queryMods...)
}

// Team pointed to by the foreign key.
func (o *WorkspaceInvitationTeam) Team(mods ...qm.QueryMod) workspaceTeamQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TeamID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceTeams(queryMods...)
}

// LoadInvitation allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceInvitationTeamL) LoadInvitation(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceInvitationTeam any, mods queries.Applicator) error {
	var slice []*WorkspaceInvitationTeam
	var object *WorkspaceInvitationTeam

	if singular {
		var ok bool
		object, ok = maybeWorkspaceInvitationTeam.(*WorkspaceInvitationTeam)
		if !ok {
			object = new(WorkspaceInvitationTeam)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceInvitationTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceInvitationTeam))
			}
		}
	} else {
		s, ok := maybeWorkspaceInvitationTeam.(*[]*WorkspaceInvitationTeam)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceInvitationTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceInvitationTeam))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceInvitationTeamR{}
		}
		args[object.InvitationID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceInvitationTeamR{}
			}

			args[obj.InvitationID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_invitations`),
		qm.WhereIn(`workspace_invitations.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceInvitation")
	}

	var resultSlice []*WorkspaceInvitation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceInvitation")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_invitations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_invitations")
	}

	if len(workspaceInvitationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Invitation = foreign
		if foreign.R == nil {
			foreign.R = &workspaceInvitationR{}
		}
		foreign.R.InvitationWorkspaceInvitationTeams = append(foreign.R.InvitationWorkspaceInvitationTeams, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.InvitationID == foreign.ID {
				local.R.Invitation = foreign
				if foreign.R == nil {
					foreign.R = &workspaceInvitationR{}
				}
				foreign.R.InvitationWorkspaceInvitationTeams = append(foreign.R.InvitationWorkspaceInvitationTeams, local)
				break
			}
		}
	}

	return nil
}

// LoadTeam allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceInvitationTeamL) LoadTeam(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceInvitationTeam any, mods queries.Applicator) error {
	var slice []*WorkspaceInvitationTeam
	var object *WorkspaceInvitationTeam

	if singular {
		var ok bool
		object, ok = maybeWorkspaceInvitationTeam.(*WorkspaceInvitationTeam)
		if !ok {
			object = new(WorkspaceInvitationTeam)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceInvitationTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceInvitationTeam))
			}
		}
	} else {
		s, ok := maybeWorkspaceInvitationTeam.(*[]*WorkspaceInvitationTeam)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceInvitationTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceInvitationTeam))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceInvitationTeamR{}
		}
		args[object.TeamID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceInvitationTeamR{}
			}

			args[obj.TeamID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_teams`),
		qm.WhereIn(`workspace_teams.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceTeam")
	}

	var resultSlice []*WorkspaceTeam
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceTeam")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_teams")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_teams")
	}

	if len(workspaceTeamAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Team = foreign
		if foreign.R == nil {
			foreign.R = &workspaceTeamR{}
		}
		foreign.R.TeamWorkspaceInvitationTeams = append(foreign.R.TeamWorkspaceInvitationTeams, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TeamID == foreign.ID {
				local.R.Team = foreign
				if foreign.R == nil {
					foreign.R = &workspaceTeamR{}
				}
				foreign.R.TeamWorkspaceInvitationTeams = append(foreign.R.TeamWorkspaceInvitationTeams, local)
				break
			}
		}
	}

	return nil
}

// SetInvitation of the workspaceInvitationTeam to the related item.
// Sets o.R.Invitation to related.
// Adds o to related.R.InvitationWorkspaceInvitationTeams.
func (o *WorkspaceInvitationTeam) SetInvitation(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceInvitation) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_invitation_teams\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"invitation_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceInvitationTeamPrimaryKeyColumns),
	)
	values := []any{related.ID, o.InvitationID, o.TeamID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.InvitationID = related.ID
	if o.R == nil {
		o.R = &workspaceInvitationTeamR{
			Invitation: related,
		}
	} else {
		o.R.Invitation = related
	}

	if related.R == nil {
		related.R = &workspaceInvitationR{
			InvitationWorkspaceInvitationTeams: WorkspaceInvitationTeamSlice{o},
		}
	} else {
		related.R.InvitationWorkspaceInvitationTeams = append(related.R.InvitationWorkspaceInvitationTeams, o)
	}

	return nil
}

// SetTeam of the workspaceInvitationTeam to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamWorkspaceInvitationTeams.
func (o *WorkspaceInvitationTeam) SetTeam(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceTeam) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_invitation_teams\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceInvitationTeamPrimaryKeyColumns),
	)
	values := []any{related.ID, o.InvitationID, o.TeamID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TeamID = related.ID
	if o.R == nil {
		o.R = &workspaceInvitationTeamR{
			Team: related,
		}
	} else {
		o.R.Team = related
	}

	if related.R == nil {
		related.R = &workspaceTeamR{
			TeamWorkspaceInvitationTeams: WorkspaceInvitationTeamSlice{o},
		}
	} else {
		related.R.TeamWorkspaceInvitationTeams = append(related.R.TeamWorkspaceInvitationTeams, o)
	}

	return nil
}

// WorkspaceInvitationTeams retrieves all the records using an executor.
func WorkspaceInvitationTeams(mods ...qm.QueryMod) workspaceInvitationTeamQuery {
	mods = append(mods, qm.From("\"workspace_invitation_teams\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_invitation_teams\".*"})
	}

	return workspaceInvitationTeamQuery{q}
}

// FindWorkspaceInvitationTeam retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceInvitationTeam(ctx context.Context, exec boil.ContextExecutor, invitationID string, teamID string, selectCols ...string) (*WorkspaceInvitationTeam, error) {
	workspaceInvitationTeamObj := &WorkspaceInvitationTeam{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_invitation_teams\" where \"invitation_id\"=$1 AND \"team_id\"=$2", sel,
	)

	q := queries.Raw(query, invitationID, teamID)

	err := q.Bind(ctx, exec, workspaceInvitationTeamObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_invitation_teams")
	}

	if err = workspaceInvitationTeamObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceInvitationTeamObj, err
	}

	return workspaceInvitationTeamObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceInvitationTeam) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_invitation_teams provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceInvitationTeamColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceInvitationTeamInsertCacheMut.RLock()
	cache, cached := workspaceInvitationTeamInsertCache[key]
	workspaceInvitationTeamInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceInvitationTeamAllColumns,
			workspaceInvitationTeamColumnsWithDefault,
			workspaceInvitationTeamColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceInvitationTeamType, workspaceInvitationTeamMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceInvitationTeamType, workspaceInvitationTeamMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_invitation_teams\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_invitation_teams\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_invitation_teams")
	}

	if !cached {
		workspaceInvitationTeamInsertCacheMut.Lock()
		workspaceInvitationTeamInsertCache[key] = cache
		workspaceInvitationTeamInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceInvitationTeam.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceInvitationTeam) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceInvitationTeamUpdateCacheMut.RLock()
	cache, cached := workspaceInvitationTeamUpdateCache[key]
	workspaceInvitationTeamUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceInvitationTeamAllColumns,
			workspaceInvitationTeamPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_invitation_teams, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_invitation_teams\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceInvitationTeamPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceInvitationTeamType, workspaceInvitationTeamMapping, append(wl, workspaceInvitationTeamPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_invitation_teams row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_invitation_teams")
	}

	if !cached {
		workspaceInvitationTeamUpdateCacheMut.Lock()
		workspaceInvitationTeamUpdateCache[key] = cache
		workspaceInvitationTeamUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceInvitationTeamQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_invitation_teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_invitation_teams")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceInvitationTeamSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceInvitationTeamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_invitation_teams\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceInvitationTeamPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceInvitationTeam slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceInvitationTeam")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceInvitationTeam) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_invitation_teams provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceInvitationTeamColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceInvitationTeamUpsertCacheMut.RLock()
	cache, cached := workspaceInvitationTeamUpsertCache[key]
	workspaceInvitationTeamUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceInvitationTeamAllColumns,
			workspaceInvitationTeamColumnsWithDefault,
			workspaceInvitationTeamColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceInvitationTeamAllColumns,
			workspaceInvitationTeamPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_invitation_teams, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceInvitationTeamAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceInvitationTeamPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_invitation_teams, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceInvitationTeamPrimaryKeyColumns))
			copy(conflict, workspaceInvitationTeamPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_invitation_teams\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceInvitationTeamType, workspaceInvitationTeamMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceInvitationTeamType, workspaceInvitationTeamMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_invitation_teams")
	}

	if !cached {
		workspaceInvitationTeamUpsertCacheMut.Lock()
		workspaceInvitationTeamUpsertCache[key] = cache
		workspaceInvitationTeamUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceInvitationTeam record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceInvitationTeam) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceInvitationTeam provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceInvitationTeamPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_invitation_teams\" WHERE \"invitation_id\"=$1 AND \"team_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_invitation_teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_invitation_teams")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceInvitationTeamQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceInvitationTeamQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_invitation_teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_invitation_teams")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceInvitationTeamSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceInvitationTeamBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceInvitationTeamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_invitation_teams\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceInvitationTeamPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceInvitationTeam slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_invitation_teams")
	}

	if len(workspaceInvitationTeamAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceInvitationTeam) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceInvitationTeam(ctx, exec, o.InvitationID, o.TeamID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceInvitationTeamSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceInvitationTeamSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceInvitationTeamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_invitation_teams\".* FROM \"workspace_invitation_teams\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceInvitationTeamPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceInvitationTeamSlice")
	}

	*o = slice

	return nil
}

// WorkspaceInvitationTeamExists checks if the WorkspaceInvitationTeam row exists.
func WorkspaceInvitationTeamExists(ctx context.Context, exec boil.ContextExecutor, invitationID string, teamID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_invitation_teams\" where \"invitation_id\"=$1 AND \"team_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, invitationID, teamID)
	}
	row := exec.QueryRowContext(ctx, sql, invitationID, teamID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_invitation_teams exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceInvitationTeam row exists.
func (o *WorkspaceInvitationTeam) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceInvitationTeamExists(ctx, exec, o.InvitationID, o.TeamID)
}
