// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceNotificationDelivery is an object representing the database table.
type WorkspaceNotificationDelivery struct {
	EventID     string    `boil:"event_id" json:"event_id" toml:"event_id" yaml:"event_id"`
	AccountID   string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	WorkspaceID string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	Reason      string    `boil:"reason" json:"reason" toml:"reason" yaml:"reason"`
	Inbox       bool      `boil:"inbox" json:"inbox" toml:"inbox" yaml:"inbox"`
	Email       bool      `boil:"email" json:"email" toml:"email" yaml:"email"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *workspaceNotificationDeliveryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceNotificationDeliveryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceNotificationDeliveryColumns = struct {
	EventID     string
	AccountID   string
	WorkspaceID string
	Reason      string
	Inbox       string
	Email       string
	CreatedAt   string
}{
	EventID:     "event_id",
	AccountID:   "account_id",
	WorkspaceID: "workspace_id",
	Reason:      "reason",
	Inbox:       "inbox",
	Email:       "email",
	CreatedAt:   "created_at",
}

var WorkspaceNotificationDeliveryTableColumns = struct {
	EventID     string
	AccountID   string
	WorkspaceID string
	Reason      string
	Inbox       string
	Email       string
	CreatedAt   string
}{
	EventID:     "workspace_notification_deliveries.event_id",
	AccountID:   "workspace_notification_deliveries.account_id",
	WorkspaceID: "workspace_notification_deliveries.workspace_id",
	Reason:      "workspace_notification_deliveries.reason",
	Inbox:       "workspace_notification_deliveries.inbox",
	Email:       "workspace_notification_deliveries.email",
	CreatedAt:   "workspace_notification_deliveries.created_at",
}

// Generated where

var WorkspaceNotificationDeliveryWhere = struct {
	EventID     whereHelperstring
	AccountID   whereHelperstring
	WorkspaceID whereHelperstring
	Reason      whereHelperstring
	Inbox       whereHelperbool
	Email       whereHelperbool
	CreatedAt   whereHelpertime_Time
}{
	EventID:     whereHelperstring{field: "\"workspace_notification_deliveries\".\"event_id\""},
	AccountID:   whereHelperstring{field: "\"workspace_notification_deliveries\".\"account_id\""},
	WorkspaceID: whereHelperstring{field: "\"workspace_notification_deliveries\".\"workspace_id\""},
	Reason:      whereHelperstring{field: "\"workspace_notification_deliveries\".\"reason\""},
	Inbox:       whereHelperbool{field: "\"workspace_notification_deliveries\".\"inbox\""},
	Email:       whereHelperbool{field: "\"workspace_notification_deliveries\".\"email\""},
	CreatedAt:   whereHelpertime_Time{field: "\"workspace_notification_deliveries\".\"created_at\""},
}

// WorkspaceNotificationDeliveryRels is where relationship names are stored.
var WorkspaceNotificationDeliveryRels = struct {
	Event string
}{
	Event: "Event",
}

// workspaceNotificationDeliveryR is where relationships are stored.
type workspaceNotificationDeliveryR struct {
	Event *WorkspaceNotificationEvent `boil:"Event" json:"Event" toml:"Event" yaml:"Event"`
}

// NewStruct creates a new relationship struct
func (*workspaceNotificationDeliveryR) NewStruct() *workspaceNotificationDeliveryR {
	return &workspaceNotificationDeliveryR{}
}

func (o *WorkspaceNotificationDelivery) GetEvent() *WorkspaceNotificationEvent {
	if o == nil {
		return nil
	}

	return o.R.GetEvent()
}

func (r *workspaceNotificationDeliveryR) GetEvent() *WorkspaceNotificationEvent {
	if r == nil {
		return nil
	}

	return r.Event
}

// workspaceNotificationDeliveryL is where Load methods for each relationship are stored.
type workspaceNotificationDeliveryL struct{}

var (
	workspaceNotificationDeliveryAllColumns            = []string{"event_id", "account_id", "workspace_id", "reason", "inbox", "email", "created_at"}
	workspaceNotificationDeliveryColumnsWithoutDefault = []string{"event_id", "account_id", "workspace_id", "reason", "inbox", "email"}
	workspaceNotificationDeliveryColumnsWithDefault    = []string{"created_at"}
	workspaceNotificationDeliveryPrimaryKeyColumns     = []string{"event_id", "account_id"}
	workspaceNotificationDeliveryGeneratedColumns      = []string{}
)

type (
	// WorkspaceNotificationDeliverySlice is an alias for a slice of pointers to WorkspaceNotificationDelivery.
	// This should almost always be used instead of []WorkspaceNotificationDelivery.
	WorkspaceNotificationDeliverySlice []*WorkspaceNotificationDelivery
	// WorkspaceNotificationDeliveryHook is the signature for custom WorkspaceNotificationDelivery hook methods
	WorkspaceNotificationDeliveryHook func(context.Context, boil.ContextExecutor, *WorkspaceNotificationDelivery) error

	workspaceNotificationDeliveryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceNotificationDeliveryType                 = reflect.TypeOf(&WorkspaceNotificationDelivery{})
	workspaceNotificationDeliveryMapping              = queries.MakeStructMapping(workspaceNotificationDeliveryType)
	workspaceNotificationDeliveryPrimaryKeyMapping, _ = queries.BindMapping(workspaceNotificationDeliveryType, workspaceNotificationDeliveryMapping, workspaceNotificationDeliveryPrimaryKeyColumns)
	workspaceNotificationDeliveryInsertCacheMut       sync.RWMutex
	workspaceNotificationDeliveryInsertCache          = make(map[string]insertCache)
	workspaceNotificationDeliveryUpdateCacheMut       sync.RWMutex
	workspaceNotificationDeliveryUpdateCache          = make(map[string]updateCache)
	workspaceNotificationDeliveryUpsertCacheMut       sync.RWMutex
	workspaceNotificationDeliveryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceNotificationDeliveryAfterSelectMu sync.Mutex
var workspaceNotificationDeliveryAfterSelectHooks []WorkspaceNotificationDeliveryHook

var workspaceNotificationDeliveryBeforeInsertMu sync.Mutex
var workspaceNotificationDeliveryBeforeInsertHooks []WorkspaceNotificationDeliveryHook
var workspaceNotificationDeliveryAfterInsertMu sync.Mutex
var workspaceNotificationDeliveryAfterInsertHooks []WorkspaceNotificationDeliveryHook

var workspaceNotificationDeliveryBeforeUpdateMu sync.Mutex
var workspaceNotificationDeliveryBeforeUpdateHooks []WorkspaceNotificationDeliveryHook
var workspaceNotificationDeliveryAfterUpdateMu sync.Mutex
var workspaceNotificationDeliveryAfterUpdateHooks []WorkspaceNotificationDeliveryHook

var workspaceNotificationDeliveryBeforeDeleteMu sync.Mutex
var workspaceNotificationDeliveryBeforeDeleteHooks []WorkspaceNotificationDeliveryHook
var workspaceNotificationDeliveryAfterDeleteMu sync.Mutex
var workspaceNotificationDeliveryAfterDeleteHooks []WorkspaceNotificationDeliveryHook

var workspaceNotificationDeliveryBeforeUpsertMu sync.Mutex
var workspaceNotificationDeliveryBeforeUpsertHooks []WorkspaceNotificationDeliveryHook
var workspaceNotificationDeliveryAfterUpsertMu sync.Mutex
var workspaceNotificationDeliveryAfterUpsertHooks []WorkspaceNotificationDeliveryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceNotificationDelivery) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceNotificationDelivery) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceNotificationDelivery) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceNotificationDelivery) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceNotificationDelivery) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceNotificationDelivery) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceNotificationDelivery) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceNotificationDelivery) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceNotificationDelivery) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceNotificationDeliveryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceNotificationDeliveryHook registers your hook function for all future operations.
func AddWorkspaceNotificationDeliveryHook(hookPoint boil.HookPoint, workspaceNotificationDeliveryHook WorkspaceNotificationDeliveryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceNotificationDeliveryAfterSelectMu.Lock()
		workspaceNotificationDeliveryAfterSelectHooks = append(workspaceNotificationDeliveryAfterSelectHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceNotificationDeliveryBeforeInsertMu.Lock()
		workspaceNotificationDeliveryBeforeInsertHooks = append(workspaceNotificationDeliveryBeforeInsertHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceNotificationDeliveryAfterInsertMu.Lock()
		workspaceNotificationDeliveryAfterInsertHooks = append(workspaceNotificationDeliveryAfterInsertHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceNotificationDeliveryBeforeUpdateMu.Lock()
		workspaceNotificationDeliveryBeforeUpdateHooks = append(workspaceNotificationDeliveryBeforeUpdateHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceNotificationDeliveryAfterUpdateMu.Lock()
		workspaceNotificationDeliveryAfterUpdateHooks = append(workspaceNotificationDeliveryAfterUpdateHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceNotificationDeliveryBeforeDeleteMu.Lock()
		workspaceNotificationDeliveryBeforeDeleteHooks = append(workspaceNotificationDeliveryBeforeDeleteHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceNotificationDeliveryAfterDeleteMu.Lock()
		workspaceNotificationDeliveryAfterDeleteHooks = append(workspaceNotificationDeliveryAfterDeleteHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceNotificationDeliveryBeforeUpsertMu.Lock()
		workspaceNotificationDeliveryBeforeUpsertHooks = append(workspaceNotificationDeliveryBeforeUpsertHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceNotificationDeliveryAfterUpsertMu.Lock()
		workspaceNotificationDeliveryAfterUpsertHooks = append(workspaceNotificationDeliveryAfterUpsertHooks, workspaceNotificationDeliveryHook)
		workspaceNotificationDeliveryAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceNotificationDelivery record from the query.
func (q workspaceNotificationDeliveryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceNotificationDelivery, error) {
	o := &WorkspaceNotificationDelivery{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_notification_deliveries")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceNotificationDelivery records from the query.
func (q workspaceNotificationDeliveryQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceNotificationDeliverySlice, error) {
	var o []*WorkspaceNotificationDelivery

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceNotificationDelivery slice")
	}

	if len(workspaceNotificationDeliveryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceNotificationDelivery records in the query.
func (q workspaceNotificationDeliveryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_notification_deliveries rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceNotificationDeliveryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_notification_deliveries exists")
	}

	return count > 0, nil
}

// Event pointed to by the foreign key.
func (o *WorkspaceNotificationDelivery) Event(mods ...qm.QueryMod) workspaceNotificationEventQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.EventID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceNotificationEvents(queryMods...)
}

// LoadEvent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceNotificationDeliveryL) LoadEvent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceNotificationDelivery any, mods queries.Applicator) error {
	var slice []*WorkspaceNotificationDelivery
	var object *WorkspaceNotificationDelivery

	if singular {
		var ok bool
		object, ok = maybeWorkspaceNotificationDelivery.(*WorkspaceNotificationDelivery)
		if !ok {
			object = new(WorkspaceNotificationDelivery)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceNotificationDelivery)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceNotificationDelivery))
			}
		}
	} else {
		s, ok := maybeWorkspaceNotificationDelivery.(*[]*WorkspaceNotificationDelivery)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceNotificationDelivery)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceNotificationDelivery))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceNotificationDeliveryR{}
		}
		args[object.EventID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceNotificationDeliveryR{}
			}

			args[obj.EventID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_notification_events`),
		qm.WhereIn(`workspace_notification_events.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceNotificationEvent")
	}

	var resultSlice []*WorkspaceNotificationEvent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceNotificationEvent")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_notification_events")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_notification_events")
	}

	if len(workspaceNotificationEventAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Event = foreign
		if foreign.R == nil {
			foreign.R = &workspaceNotificationEventR{}
		}
		foreign.R.EventWorkspaceNotificationDeliveries = append(foreign.R.EventWorkspaceNotificationDeliveries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EventID == foreign.ID {
				local.R.Event = foreign
				if foreign.R == nil {
					foreign.R = &workspaceNotificationEventR{}
				}
				foreign.R.EventWorkspaceNotificationDeliveries = append(foreign.R.EventWorkspaceNotificationDeliveries, local)
				break
			}
		}
	}

	return nil
}

// SetEvent of the workspaceNotificationDelivery to the related item.
// Sets o.R.Event to related.
// Adds o to related.R.EventWorkspaceNotificationDeliveries.
func (o *WorkspaceNotificationDelivery) SetEvent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceNotificationEvent) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_notification_deliveries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"event_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceNotificationDeliveryPrimaryKeyColumns),
	)
	values := []any{related.ID, o.EventID, o.AccountID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EventID = related.ID
	if o.R == nil {
		o.R = &workspaceNotificationDeliveryR{
			Event: related,
		}
	} else {
		o.R.Event = related
	}

	if related.R == nil {
		related.R = &workspaceNotificationEventR{
			EventWorkspaceNotificationDeliveries: WorkspaceNotificationDeliverySlice{o},
		}
	} else {
		related.R.EventWorkspaceNotificationDeliveries = append(related.R.EventWorkspaceNotificationDeliveries, o)
	}

	return nil
}

// WorkspaceNotificationDeliveries retrieves all the records using an executor.
func WorkspaceNotificationDeliveries(mods ...qm.QueryMod) workspaceNotificationDeliveryQuery {
	mods = append(mods, qm.From("\"workspace_notification_deliveries\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_notification_deliveries\".*"})
	}

	return workspaceNotificationDeliveryQuery{q}
}

// FindWorkspaceNotificationDelivery retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceNotificationDelivery(ctx context.Context, exec boil.ContextExecutor, eventID string, accountID string, selectCols ...string) (*WorkspaceNotificationDelivery, error) {
	workspaceNotificationDeliveryObj := &WorkspaceNotificationDelivery{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_notification_deliveries\" where \"event_id\"=$1 AND \"account_id\"=$2", sel,
	)

	q := queries.Raw(query, eventID, accountID)

	err := q.Bind(ctx, exec, workspaceNotificationDeliveryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_notification_deliveries")
	}

	if err = workspaceNotificationDeliveryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceNotificationDeliveryObj, err
	}

	return workspaceNotificationDeliveryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceNotificationDelivery) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_notification_deliveries provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceNotificationDeliveryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceNotificationDeliveryInsertCacheMut.RLock()
	cache, cached := workspaceNotificationDeliveryInsertCache[key]
	workspaceNotificationDeliveryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceNotificationDeliveryAllColumns,
			workspaceNotificationDeliveryColumnsWithDefault,
			workspaceNotificationDeliveryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceNotificationDeliveryType, workspaceNotificationDeliveryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceNotificationDeliveryType, workspaceNotificationDeliveryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_notification_deliveries\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_notification_deliveries\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_notification_deliveries")
	}

	if !cached {
		workspaceNotificationDeliveryInsertCacheMut.Lock()
		workspaceNotificationDeliveryInsertCache[key] = cache
		workspaceNotificationDeliveryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceNotificationDelivery.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceNotificationDelivery) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceNotificationDeliveryUpdateCacheMut.RLock()
	cache, cached := workspaceNotificationDeliveryUpdateCache[key]
	workspaceNotificationDeliveryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceNotificationDeliveryAllColumns,
			workspaceNotificationDeliveryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_notification_deliveries, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_notification_deliveries\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceNotificationDeliveryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceNotificationDeliveryType, workspaceNotificationDeliveryMapping, append(wl, workspaceNotificationDeliveryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_notification_deliveries row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_notification_deliveries")
	}

	if !cached {
		workspaceNotificationDeliveryUpdateCacheMut.Lock()
		workspaceNotificationDeliveryUpdateCache[key] = cache
		workspaceNotificationDeliveryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceNotificationDeliveryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_notification_deliveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_notification_deliveries")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceNotificationDeliverySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceNotificationDeliveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_notification_deliveries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceNotificationDeliveryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceNotificationDelivery slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceNotificationDelivery")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceNotificationDelivery) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_notification_deliveries provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceNotificationDeliveryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceNotificationDeliveryUpsertCacheMut.RLock()
	cache, cached := workspaceNotificationDeliveryUpsertCache[key]
	workspaceNotificationDeliveryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceNotificationDeliveryAllColumns,
			workspaceNotificationDeliveryColumnsWithDefault,
			workspaceNotificationDeliveryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceNotificationDeliveryAllColumns,
			workspaceNotificationDeliveryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_notification_deliveries, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceNotificationDeliveryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceNotificationDeliveryPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_notification_deliveries, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceNotificationDeliveryPrimaryKeyColumns))
			copy(conflict, workspaceNotificationDeliveryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_notification_deliveries\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceNotificationDeliveryType, workspaceNotificationDeliveryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceNotificationDeliveryType, workspaceNotificationDeliveryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_notification_deliveries")
	}

	if !cached {
		workspaceNotificationDeliveryUpsertCacheMut.Lock()
		workspaceNotificationDeliveryUpsertCache[key] = cache
		workspaceNotificationDeliveryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceNotificationDelivery record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceNotificationDelivery) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceNotificationDelivery provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceNotificationDeliveryPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_notification_deliveries\" WHERE \"event_id\"=$1 AND \"account_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_notification_deliveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_notification_deliveries")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceNotificationDeliveryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceNotificationDeliveryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_notification_deliveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_notification_deliveries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceNotificationDeliverySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceNotificationDeliveryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceNotificationDeliveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_notification_deliveries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceNotificationDeliveryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceNotificationDelivery slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_notification_deliveries")
	}

	if len(workspaceNotificationDeliveryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceNotificationDelivery) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceNotificationDelivery(ctx, exec, o.EventID, o.AccountID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceNotificationDeliverySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceNotificationDeliverySlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceNotificationDeliveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_notification_deliveries\".* FROM \"workspace_notification_deliveries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceNotificationDeliveryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceNotificationDeliverySlice")
	}

	*o = slice

	return nil
}

// WorkspaceNotificationDeliveryExists checks if the WorkspaceNotificationDelivery row exists.
func WorkspaceNotificationDeliveryExists(ctx context.Context, exec boil.ContextExecutor, eventID string, accountID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_notification_deliveries\" where \"event_id\"=$1 AND \"account_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, eventID, accountID)
	}
	row := exec.QueryRowContext(ctx, sql, eventID, accountID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_notification_deliveries exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceNotificationDelivery row exists.
func (o *WorkspaceNotificationDelivery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceNotificationDeliveryExists(ctx, exec, o.EventID, o.AccountID)
}
