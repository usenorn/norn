// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceProjectMember is an object representing the database table.
type WorkspaceProjectMember struct {
	ID          string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	ProjectID   string    `boil:"project_id" json:"project_id" toml:"project_id" yaml:"project_id"`
	AccountID   string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceProjectMemberR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceProjectMemberL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceProjectMemberColumns = struct {
	ID          string
	WorkspaceID string
	ProjectID   string
	AccountID   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	WorkspaceID: "workspace_id",
	ProjectID:   "project_id",
	AccountID:   "account_id",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var WorkspaceProjectMemberTableColumns = struct {
	ID          string
	WorkspaceID string
	ProjectID   string
	AccountID   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "workspace_project_members.id",
	WorkspaceID: "workspace_project_members.workspace_id",
	ProjectID:   "workspace_project_members.project_id",
	AccountID:   "workspace_project_members.account_id",
	CreatedAt:   "workspace_project_members.created_at",
	UpdatedAt:   "workspace_project_members.updated_at",
}

// Generated where

var WorkspaceProjectMemberWhere = struct {
	ID          whereHelperstring
	WorkspaceID whereHelperstring
	ProjectID   whereHelperstring
	AccountID   whereHelperstring
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"workspace_project_members\".\"id\""},
	WorkspaceID: whereHelperstring{field: "\"workspace_project_members\".\"workspace_id\""},
	ProjectID:   whereHelperstring{field: "\"workspace_project_members\".\"project_id\""},
	AccountID:   whereHelperstring{field: "\"workspace_project_members\".\"account_id\""},
	CreatedAt:   whereHelpertime_Time{field: "\"workspace_project_members\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"workspace_project_members\".\"updated_at\""},
}

// WorkspaceProjectMemberRels is where relationship names are stored.
var WorkspaceProjectMemberRels = struct {
}{}

// workspaceProjectMemberR is where relationships are stored.
type workspaceProjectMemberR struct {
}

// NewStruct creates a new relationship struct
func (*workspaceProjectMemberR) NewStruct() *workspaceProjectMemberR {
	return &workspaceProjectMemberR{}
}

// workspaceProjectMemberL is where Load methods for each relationship are stored.
type workspaceProjectMemberL struct{}

var (
	workspaceProjectMemberAllColumns            = []string{"id", "workspace_id", "project_id", "account_id", "created_at", "updated_at"}
	workspaceProjectMemberColumnsWithoutDefault = []string{"workspace_id", "project_id", "account_id"}
	workspaceProjectMemberColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	workspaceProjectMemberPrimaryKeyColumns     = []string{"id"}
	workspaceProjectMemberGeneratedColumns      = []string{}
)

type (
	// WorkspaceProjectMemberSlice is an alias for a slice of pointers to WorkspaceProjectMember.
	// This should almost always be used instead of []WorkspaceProjectMember.
	WorkspaceProjectMemberSlice []*WorkspaceProjectMember
	// WorkspaceProjectMemberHook is the signature for custom WorkspaceProjectMember hook methods
	WorkspaceProjectMemberHook func(context.Context, boil.ContextExecutor, *WorkspaceProjectMember) error

	workspaceProjectMemberQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceProjectMemberType                 = reflect.TypeOf(&WorkspaceProjectMember{})
	workspaceProjectMemberMapping              = queries.MakeStructMapping(workspaceProjectMemberType)
	workspaceProjectMemberPrimaryKeyMapping, _ = queries.BindMapping(workspaceProjectMemberType, workspaceProjectMemberMapping, workspaceProjectMemberPrimaryKeyColumns)
	workspaceProjectMemberInsertCacheMut       sync.RWMutex
	workspaceProjectMemberInsertCache          = make(map[string]insertCache)
	workspaceProjectMemberUpdateCacheMut       sync.RWMutex
	workspaceProjectMemberUpdateCache          = make(map[string]updateCache)
	workspaceProjectMemberUpsertCacheMut       sync.RWMutex
	workspaceProjectMemberUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceProjectMemberAfterSelectMu sync.Mutex
var workspaceProjectMemberAfterSelectHooks []WorkspaceProjectMemberHook

var workspaceProjectMemberBeforeInsertMu sync.Mutex
var workspaceProjectMemberBeforeInsertHooks []WorkspaceProjectMemberHook
var workspaceProjectMemberAfterInsertMu sync.Mutex
var workspaceProjectMemberAfterInsertHooks []WorkspaceProjectMemberHook

var workspaceProjectMemberBeforeUpdateMu sync.Mutex
var workspaceProjectMemberBeforeUpdateHooks []WorkspaceProjectMemberHook
var workspaceProjectMemberAfterUpdateMu sync.Mutex
var workspaceProjectMemberAfterUpdateHooks []WorkspaceProjectMemberHook

var workspaceProjectMemberBeforeDeleteMu sync.Mutex
var workspaceProjectMemberBeforeDeleteHooks []WorkspaceProjectMemberHook
var workspaceProjectMemberAfterDeleteMu sync.Mutex
var workspaceProjectMemberAfterDeleteHooks []WorkspaceProjectMemberHook

var workspaceProjectMemberBeforeUpsertMu sync.Mutex
var workspaceProjectMemberBeforeUpsertHooks []WorkspaceProjectMemberHook
var workspaceProjectMemberAfterUpsertMu sync.Mutex
var workspaceProjectMemberAfterUpsertHooks []WorkspaceProjectMemberHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceProjectMember) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceProjectMember) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceProjectMember) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceProjectMember) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceProjectMember) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceProjectMember) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceProjectMember) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceProjectMember) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceProjectMember) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceProjectMemberAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceProjectMemberHook registers your hook function for all future operations.
func AddWorkspaceProjectMemberHook(hookPoint boil.HookPoint, workspaceProjectMemberHook WorkspaceProjectMemberHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceProjectMemberAfterSelectMu.Lock()
		workspaceProjectMemberAfterSelectHooks = append(workspaceProjectMemberAfterSelectHooks, workspaceProjectMemberHook)
		workspaceProjectMemberAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceProjectMemberBeforeInsertMu.Lock()
		workspaceProjectMemberBeforeInsertHooks = append(workspaceProjectMemberBeforeInsertHooks, workspaceProjectMemberHook)
		workspaceProjectMemberBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceProjectMemberAfterInsertMu.Lock()
		workspaceProjectMemberAfterInsertHooks = append(workspaceProjectMemberAfterInsertHooks, workspaceProjectMemberHook)
		workspaceProjectMemberAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceProjectMemberBeforeUpdateMu.Lock()
		workspaceProjectMemberBeforeUpdateHooks = append(workspaceProjectMemberBeforeUpdateHooks, workspaceProjectMemberHook)
		workspaceProjectMemberBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceProjectMemberAfterUpdateMu.Lock()
		workspaceProjectMemberAfterUpdateHooks = append(workspaceProjectMemberAfterUpdateHooks, workspaceProjectMemberHook)
		workspaceProjectMemberAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceProjectMemberBeforeDeleteMu.Lock()
		workspaceProjectMemberBeforeDeleteHooks = append(workspaceProjectMemberBeforeDeleteHooks, workspaceProjectMemberHook)
		workspaceProjectMemberBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceProjectMemberAfterDeleteMu.Lock()
		workspaceProjectMemberAfterDeleteHooks = append(workspaceProjectMemberAfterDeleteHooks, workspaceProjectMemberHook)
		workspaceProjectMemberAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceProjectMemberBeforeUpsertMu.Lock()
		workspaceProjectMemberBeforeUpsertHooks = append(workspaceProjectMemberBeforeUpsertHooks, workspaceProjectMemberHook)
		workspaceProjectMemberBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceProjectMemberAfterUpsertMu.Lock()
		workspaceProjectMemberAfterUpsertHooks = append(workspaceProjectMemberAfterUpsertHooks, workspaceProjectMemberHook)
		workspaceProjectMemberAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceProjectMember record from the query.
func (q workspaceProjectMemberQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceProjectMember, error) {
	o := &WorkspaceProjectMember{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_project_members")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceProjectMember records from the query.
func (q workspaceProjectMemberQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceProjectMemberSlice, error) {
	var o []*WorkspaceProjectMember

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceProjectMember slice")
	}

	if len(workspaceProjectMemberAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceProjectMember records in the query.
func (q workspaceProjectMemberQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_project_members rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceProjectMemberQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_project_members exists")
	}

	return count > 0, nil
}

// WorkspaceProjectMembers retrieves all the records using an executor.
func WorkspaceProjectMembers(mods ...qm.QueryMod) workspaceProjectMemberQuery {
	mods = append(mods, qm.From("\"workspace_project_members\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_project_members\".*"})
	}

	return workspaceProjectMemberQuery{q}
}

// FindWorkspaceProjectMember retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceProjectMember(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceProjectMember, error) {
	workspaceProjectMemberObj := &WorkspaceProjectMember{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_project_members\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceProjectMemberObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_project_members")
	}

	if err = workspaceProjectMemberObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceProjectMemberObj, err
	}

	return workspaceProjectMemberObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceProjectMember) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_project_members provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceProjectMemberColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceProjectMemberInsertCacheMut.RLock()
	cache, cached := workspaceProjectMemberInsertCache[key]
	workspaceProjectMemberInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceProjectMemberAllColumns,
			workspaceProjectMemberColumnsWithDefault,
			workspaceProjectMemberColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceProjectMemberType, workspaceProjectMemberMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceProjectMemberType, workspaceProjectMemberMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_project_members\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_project_members\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_project_members")
	}

	if !cached {
		workspaceProjectMemberInsertCacheMut.Lock()
		workspaceProjectMemberInsertCache[key] = cache
		workspaceProjectMemberInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceProjectMember.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceProjectMember) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceProjectMemberUpdateCacheMut.RLock()
	cache, cached := workspaceProjectMemberUpdateCache[key]
	workspaceProjectMemberUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceProjectMemberAllColumns,
			workspaceProjectMemberPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_project_members, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_project_members\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceProjectMemberPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceProjectMemberType, workspaceProjectMemberMapping, append(wl, workspaceProjectMemberPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_project_members row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_project_members")
	}

	if !cached {
		workspaceProjectMemberUpdateCacheMut.Lock()
		workspaceProjectMemberUpdateCache[key] = cache
		workspaceProjectMemberUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceProjectMemberQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_project_members")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_project_members")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceProjectMemberSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceProjectMemberPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_project_members\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceProjectMemberPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceProjectMember slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceProjectMember")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceProjectMember) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_project_members provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceProjectMemberColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceProjectMemberUpsertCacheMut.RLock()
	cache, cached := workspaceProjectMemberUpsertCache[key]
	workspaceProjectMemberUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceProjectMemberAllColumns,
			workspaceProjectMemberColumnsWithDefault,
			workspaceProjectMemberColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceProjectMemberAllColumns,
			workspaceProjectMemberPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_project_members, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceProjectMemberAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceProjectMemberPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_project_members, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceProjectMemberPrimaryKeyColumns))
			copy(conflict, workspaceProjectMemberPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_project_members\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceProjectMemberType, workspaceProjectMemberMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceProjectMemberType, workspaceProjectMemberMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_project_members")
	}

	if !cached {
		workspaceProjectMemberUpsertCacheMut.Lock()
		workspaceProjectMemberUpsertCache[key] = cache
		workspaceProjectMemberUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceProjectMember record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceProjectMember) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceProjectMember provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceProjectMemberPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_project_members\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_project_members")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_project_members")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceProjectMemberQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceProjectMemberQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_project_members")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_project_members")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceProjectMemberSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceProjectMemberBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceProjectMemberPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_project_members\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceProjectMemberPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceProjectMember slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_project_members")
	}

	if len(workspaceProjectMemberAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceProjectMember) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceProjectMember(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceProjectMemberSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceProjectMemberSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceProjectMemberPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_project_members\".* FROM \"workspace_project_members\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceProjectMemberPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceProjectMemberSlice")
	}

	*o = slice

	return nil
}

// WorkspaceProjectMemberExists checks if the WorkspaceProjectMember row exists.
func WorkspaceProjectMemberExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_project_members\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_project_members exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceProjectMember row exists.
func (o *WorkspaceProjectMember) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceProjectMemberExists(ctx, exec, o.ID)
}
