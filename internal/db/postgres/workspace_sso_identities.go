// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceSsoIdentity is an object representing the database table.
type WorkspaceSsoIdentity struct {
	WorkspaceID string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	AccountID   string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	Subject     string    `boil:"subject" json:"subject" toml:"subject" yaml:"subject"`
	LinkedAt    time.Time `boil:"linked_at" json:"linked_at" toml:"linked_at" yaml:"linked_at"`
	Issuer      string    `boil:"issuer" json:"issuer" toml:"issuer" yaml:"issuer"`

	R *workspaceSsoIdentityR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceSsoIdentityL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceSsoIdentityColumns = struct {
	WorkspaceID string
	AccountID   string
	Subject     string
	LinkedAt    string
	Issuer      string
}{
	WorkspaceID: "workspace_id",
	AccountID:   "account_id",
	Subject:     "subject",
	LinkedAt:    "linked_at",
	Issuer:      "issuer",
}

var WorkspaceSsoIdentityTableColumns = struct {
	WorkspaceID string
	AccountID   string
	Subject     string
	LinkedAt    string
	Issuer      string
}{
	WorkspaceID: "workspace_sso_identities.workspace_id",
	AccountID:   "workspace_sso_identities.account_id",
	Subject:     "workspace_sso_identities.subject",
	LinkedAt:    "workspace_sso_identities.linked_at",
	Issuer:      "workspace_sso_identities.issuer",
}

// Generated where

var WorkspaceSsoIdentityWhere = struct {
	WorkspaceID whereHelperstring
	AccountID   whereHelperstring
	Subject     whereHelperstring
	LinkedAt    whereHelpertime_Time
	Issuer      whereHelperstring
}{
	WorkspaceID: whereHelperstring{field: "\"workspace_sso_identities\".\"workspace_id\""},
	AccountID:   whereHelperstring{field: "\"workspace_sso_identities\".\"account_id\""},
	Subject:     whereHelperstring{field: "\"workspace_sso_identities\".\"subject\""},
	LinkedAt:    whereHelpertime_Time{field: "\"workspace_sso_identities\".\"linked_at\""},
	Issuer:      whereHelperstring{field: "\"workspace_sso_identities\".\"issuer\""},
}

// WorkspaceSsoIdentityRels is where relationship names are stored.
var WorkspaceSsoIdentityRels = struct {
	Account   string
	Workspace string
}{
	Account:   "Account",
	Workspace: "Workspace",
}

// workspaceSsoIdentityR is where relationships are stored.
type workspaceSsoIdentityR struct {
	Account   *Account                `boil:"Account" json:"Account" toml:"Account" yaml:"Account"`
	Workspace *WorkspaceSsoConnection `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*workspaceSsoIdentityR) NewStruct() *workspaceSsoIdentityR {
	return &workspaceSsoIdentityR{}
}

func (o *WorkspaceSsoIdentity) GetAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetAccount()
}

func (r *workspaceSsoIdentityR) GetAccount() *Account {
	if r == nil {
		return nil
	}

	return r.Account
}

func (o *WorkspaceSsoIdentity) GetWorkspace() *WorkspaceSsoConnection {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceSsoIdentityR) GetWorkspace() *WorkspaceSsoConnection {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// workspaceSsoIdentityL is where Load methods for each relationship are stored.
type workspaceSsoIdentityL struct{}

var (
	workspaceSsoIdentityAllColumns            = []string{"workspace_id", "account_id", "subject", "linked_at", "issuer"}
	workspaceSsoIdentityColumnsWithoutDefault = []string{"workspace_id", "account_id", "subject", "issuer"}
	workspaceSsoIdentityColumnsWithDefault    = []string{"linked_at"}
	workspaceSsoIdentityPrimaryKeyColumns     = []string{"workspace_id", "account_id"}
	workspaceSsoIdentityGeneratedColumns      = []string{}
)

type (
	// WorkspaceSsoIdentitySlice is an alias for a slice of pointers to WorkspaceSsoIdentity.
	// This should almost always be used instead of []WorkspaceSsoIdentity.
	WorkspaceSsoIdentitySlice []*WorkspaceSsoIdentity
	// WorkspaceSsoIdentityHook is the signature for custom WorkspaceSsoIdentity hook methods
	WorkspaceSsoIdentityHook func(context.Context, boil.ContextExecutor, *WorkspaceSsoIdentity) error

	workspaceSsoIdentityQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceSsoIdentityType                 = reflect.TypeOf(&WorkspaceSsoIdentity{})
	workspaceSsoIdentityMapping              = queries.MakeStructMapping(workspaceSsoIdentityType)
	workspaceSsoIdentityPrimaryKeyMapping, _ = queries.BindMapping(workspaceSsoIdentityType, workspaceSsoIdentityMapping, workspaceSsoIdentityPrimaryKeyColumns)
	workspaceSsoIdentityInsertCacheMut       sync.RWMutex
	workspaceSsoIdentityInsertCache          = make(map[string]insertCache)
	workspaceSsoIdentityUpdateCacheMut       sync.RWMutex
	workspaceSsoIdentityUpdateCache          = make(map[string]updateCache)
	workspaceSsoIdentityUpsertCacheMut       sync.RWMutex
	workspaceSsoIdentityUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceSsoIdentityAfterSelectMu sync.Mutex
var workspaceSsoIdentityAfterSelectHooks []WorkspaceSsoIdentityHook

var workspaceSsoIdentityBeforeInsertMu sync.Mutex
var workspaceSsoIdentityBeforeInsertHooks []WorkspaceSsoIdentityHook
var workspaceSsoIdentityAfterInsertMu sync.Mutex
var workspaceSsoIdentityAfterInsertHooks []WorkspaceSsoIdentityHook

var workspaceSsoIdentityBeforeUpdateMu sync.Mutex
var workspaceSsoIdentityBeforeUpdateHooks []WorkspaceSsoIdentityHook
var workspaceSsoIdentityAfterUpdateMu sync.Mutex
var workspaceSsoIdentityAfterUpdateHooks []WorkspaceSsoIdentityHook

var workspaceSsoIdentityBeforeDeleteMu sync.Mutex
var workspaceSsoIdentityBeforeDeleteHooks []WorkspaceSsoIdentityHook
var workspaceSsoIdentityAfterDeleteMu sync.Mutex
var workspaceSsoIdentityAfterDeleteHooks []WorkspaceSsoIdentityHook

var workspaceSsoIdentityBeforeUpsertMu sync.Mutex
var workspaceSsoIdentityBeforeUpsertHooks []WorkspaceSsoIdentityHook
var workspaceSsoIdentityAfterUpsertMu sync.Mutex
var workspaceSsoIdentityAfterUpsertHooks []WorkspaceSsoIdentityHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceSsoIdentity) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceSsoIdentity) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceSsoIdentity) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceSsoIdentity) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceSsoIdentity) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceSsoIdentity) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceSsoIdentity) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceSsoIdentity) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceSsoIdentity) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSsoIdentityAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceSsoIdentityHook registers your hook function for all future operations.
func AddWorkspaceSsoIdentityHook(hookPoint boil.HookPoint, workspaceSsoIdentityHook WorkspaceSsoIdentityHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceSsoIdentityAfterSelectMu.Lock()
		workspaceSsoIdentityAfterSelectHooks = append(workspaceSsoIdentityAfterSelectHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceSsoIdentityBeforeInsertMu.Lock()
		workspaceSsoIdentityBeforeInsertHooks = append(workspaceSsoIdentityBeforeInsertHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceSsoIdentityAfterInsertMu.Lock()
		workspaceSsoIdentityAfterInsertHooks = append(workspaceSsoIdentityAfterInsertHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceSsoIdentityBeforeUpdateMu.Lock()
		workspaceSsoIdentityBeforeUpdateHooks = append(workspaceSsoIdentityBeforeUpdateHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceSsoIdentityAfterUpdateMu.Lock()
		workspaceSsoIdentityAfterUpdateHooks = append(workspaceSsoIdentityAfterUpdateHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceSsoIdentityBeforeDeleteMu.Lock()
		workspaceSsoIdentityBeforeDeleteHooks = append(workspaceSsoIdentityBeforeDeleteHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceSsoIdentityAfterDeleteMu.Lock()
		workspaceSsoIdentityAfterDeleteHooks = append(workspaceSsoIdentityAfterDeleteHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceSsoIdentityBeforeUpsertMu.Lock()
		workspaceSsoIdentityBeforeUpsertHooks = append(workspaceSsoIdentityBeforeUpsertHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceSsoIdentityAfterUpsertMu.Lock()
		workspaceSsoIdentityAfterUpsertHooks = append(workspaceSsoIdentityAfterUpsertHooks, workspaceSsoIdentityHook)
		workspaceSsoIdentityAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceSsoIdentity record from the query.
func (q workspaceSsoIdentityQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceSsoIdentity, error) {
	o := &WorkspaceSsoIdentity{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_sso_identities")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceSsoIdentity records from the query.
func (q workspaceSsoIdentityQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceSsoIdentitySlice, error) {
	var o []*WorkspaceSsoIdentity

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceSsoIdentity slice")
	}

	if len(workspaceSsoIdentityAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceSsoIdentity records in the query.
func (q workspaceSsoIdentityQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_sso_identities rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceSsoIdentityQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_sso_identities exists")
	}

	return count > 0, nil
}

// Account pointed to by the foreign key.
func (o *WorkspaceSsoIdentity) Account(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceSsoIdentity) Workspace(mods ...qm.QueryMod) workspaceSsoConnectionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"workspace_id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceSsoConnections(queryMods...)
}

// LoadAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceSsoIdentityL) LoadAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSsoIdentity any, mods queries.Applicator) error {
	var slice []*WorkspaceSsoIdentity
	var object *WorkspaceSsoIdentity

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSsoIdentity.(*WorkspaceSsoIdentity)
		if !ok {
			object = new(WorkspaceSsoIdentity)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSsoIdentity)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSsoIdentity))
			}
		}
	} else {
		s, ok := maybeWorkspaceSsoIdentity.(*[]*WorkspaceSsoIdentity)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSsoIdentity)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSsoIdentity))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSsoIdentityR{}
		}
		args[object.AccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSsoIdentityR{}
			}

			args[obj.AccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Account = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.WorkspaceSsoIdentities = append(foreign.R.WorkspaceSsoIdentities, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountID == foreign.ID {
				local.R.Account = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.WorkspaceSsoIdentities = append(foreign.R.WorkspaceSsoIdentities, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceSsoIdentityL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSsoIdentity any, mods queries.Applicator) error {
	var slice []*WorkspaceSsoIdentity
	var object *WorkspaceSsoIdentity

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSsoIdentity.(*WorkspaceSsoIdentity)
		if !ok {
			object = new(WorkspaceSsoIdentity)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSsoIdentity)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSsoIdentity))
			}
		}
	} else {
		s, ok := maybeWorkspaceSsoIdentity.(*[]*WorkspaceSsoIdentity)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSsoIdentity)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSsoIdentity))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSsoIdentityR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSsoIdentityR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_sso_connections`),
		qm.WhereIn(`workspace_sso_connections.workspace_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceSsoConnection")
	}

	var resultSlice []*WorkspaceSsoConnection
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceSsoConnection")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_sso_connections")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_sso_connections")
	}

	if len(workspaceSsoConnectionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceSsoConnectionR{}
		}
		foreign.R.WorkspaceWorkspaceSsoIdentities = append(foreign.R.WorkspaceWorkspaceSsoIdentities, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.WorkspaceID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceSsoConnectionR{}
				}
				foreign.R.WorkspaceWorkspaceSsoIdentities = append(foreign.R.WorkspaceWorkspaceSsoIdentities, local)
				break
			}
		}
	}

	return nil
}

// SetAccount of the workspaceSsoIdentity to the related item.
// Sets o.R.Account to related.
// Adds o to related.R.WorkspaceSsoIdentities.
func (o *WorkspaceSsoIdentity) SetAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_sso_identities\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceSsoIdentityPrimaryKeyColumns),
	)
	values := []any{related.ID, o.WorkspaceID, o.AccountID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountID = related.ID
	if o.R == nil {
		o.R = &workspaceSsoIdentityR{
			Account: related,
		}
	} else {
		o.R.Account = related
	}

	if related.R == nil {
		related.R = &accountR{
			WorkspaceSsoIdentities: WorkspaceSsoIdentitySlice{o},
		}
	} else {
		related.R.WorkspaceSsoIdentities = append(related.R.WorkspaceSsoIdentities, o)
	}

	return nil
}

// SetWorkspace of the workspaceSsoIdentity to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceWorkspaceSsoIdentities.
func (o *WorkspaceSsoIdentity) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceSsoConnection) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_sso_identities\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceSsoIdentityPrimaryKeyColumns),
	)
	values := []any{related.WorkspaceID, o.WorkspaceID, o.AccountID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.WorkspaceID
	if o.R == nil {
		o.R = &workspaceSsoIdentityR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceSsoConnectionR{
			WorkspaceWorkspaceSsoIdentities: WorkspaceSsoIdentitySlice{o},
		}
	} else {
		related.R.WorkspaceWorkspaceSsoIdentities = append(related.R.WorkspaceWorkspaceSsoIdentities, o)
	}

	return nil
}

// WorkspaceSsoIdentities retrieves all the records using an executor.
func WorkspaceSsoIdentities(mods ...qm.QueryMod) workspaceSsoIdentityQuery {
	mods = append(mods, qm.From("\"workspace_sso_identities\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_sso_identities\".*"})
	}

	return workspaceSsoIdentityQuery{q}
}

// FindWorkspaceSsoIdentity retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceSsoIdentity(ctx context.Context, exec boil.ContextExecutor, workspaceID string, accountID string, selectCols ...string) (*WorkspaceSsoIdentity, error) {
	workspaceSsoIdentityObj := &WorkspaceSsoIdentity{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_sso_identities\" where \"workspace_id\"=$1 AND \"account_id\"=$2", sel,
	)

	q := queries.Raw(query, workspaceID, accountID)

	err := q.Bind(ctx, exec, workspaceSsoIdentityObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_sso_identities")
	}

	if err = workspaceSsoIdentityObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceSsoIdentityObj, err
	}

	return workspaceSsoIdentityObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceSsoIdentity) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_sso_identities provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceSsoIdentityColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceSsoIdentityInsertCacheMut.RLock()
	cache, cached := workspaceSsoIdentityInsertCache[key]
	workspaceSsoIdentityInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceSsoIdentityAllColumns,
			workspaceSsoIdentityColumnsWithDefault,
			workspaceSsoIdentityColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceSsoIdentityType, workspaceSsoIdentityMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceSsoIdentityType, workspaceSsoIdentityMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_sso_identities\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_sso_identities\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_sso_identities")
	}

	if !cached {
		workspaceSsoIdentityInsertCacheMut.Lock()
		workspaceSsoIdentityInsertCache[key] = cache
		workspaceSsoIdentityInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceSsoIdentity.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceSsoIdentity) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceSsoIdentityUpdateCacheMut.RLock()
	cache, cached := workspaceSsoIdentityUpdateCache[key]
	workspaceSsoIdentityUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceSsoIdentityAllColumns,
			workspaceSsoIdentityPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_sso_identities, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_sso_identities\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceSsoIdentityPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceSsoIdentityType, workspaceSsoIdentityMapping, append(wl, workspaceSsoIdentityPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_sso_identities row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_sso_identities")
	}

	if !cached {
		workspaceSsoIdentityUpdateCacheMut.Lock()
		workspaceSsoIdentityUpdateCache[key] = cache
		workspaceSsoIdentityUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceSsoIdentityQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_sso_identities")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_sso_identities")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceSsoIdentitySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSsoIdentityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_sso_identities\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceSsoIdentityPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceSsoIdentity slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceSsoIdentity")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceSsoIdentity) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_sso_identities provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceSsoIdentityColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceSsoIdentityUpsertCacheMut.RLock()
	cache, cached := workspaceSsoIdentityUpsertCache[key]
	workspaceSsoIdentityUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceSsoIdentityAllColumns,
			workspaceSsoIdentityColumnsWithDefault,
			workspaceSsoIdentityColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceSsoIdentityAllColumns,
			workspaceSsoIdentityPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_sso_identities, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceSsoIdentityAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceSsoIdentityPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_sso_identities, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceSsoIdentityPrimaryKeyColumns))
			copy(conflict, workspaceSsoIdentityPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_sso_identities\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceSsoIdentityType, workspaceSsoIdentityMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceSsoIdentityType, workspaceSsoIdentityMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_sso_identities")
	}

	if !cached {
		workspaceSsoIdentityUpsertCacheMut.Lock()
		workspaceSsoIdentityUpsertCache[key] = cache
		workspaceSsoIdentityUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceSsoIdentity record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceSsoIdentity) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceSsoIdentity provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceSsoIdentityPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_sso_identities\" WHERE \"workspace_id\"=$1 AND \"account_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_sso_identities")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_sso_identities")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceSsoIdentityQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceSsoIdentityQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_sso_identities")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_sso_identities")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceSsoIdentitySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceSsoIdentityBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSsoIdentityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_sso_identities\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceSsoIdentityPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceSsoIdentity slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_sso_identities")
	}

	if len(workspaceSsoIdentityAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceSsoIdentity) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceSsoIdentity(ctx, exec, o.WorkspaceID, o.AccountID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceSsoIdentitySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceSsoIdentitySlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSsoIdentityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_sso_identities\".* FROM \"workspace_sso_identities\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceSsoIdentityPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceSsoIdentitySlice")
	}

	*o = slice

	return nil
}

// WorkspaceSsoIdentityExists checks if the WorkspaceSsoIdentity row exists.
func WorkspaceSsoIdentityExists(ctx context.Context, exec boil.ContextExecutor, workspaceID string, accountID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_sso_identities\" where \"workspace_id\"=$1 AND \"account_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, workspaceID, accountID)
	}
	row := exec.QueryRowContext(ctx, sql, workspaceID, accountID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_sso_identities exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceSsoIdentity row exists.
func (o *WorkspaceSsoIdentity) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceSsoIdentityExists(ctx, exec, o.WorkspaceID, o.AccountID)
}
