// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceSCMReleaseLink is an object representing the database table.
type WorkspaceSCMReleaseLink struct {
	ReleaseID string    `boil:"release_id" json:"release_id" toml:"release_id" yaml:"release_id"`
	LinkID    string    `boil:"link_id" json:"link_id" toml:"link_id" yaml:"link_id"`
	IssueID   string    `boil:"issue_id" json:"issue_id" toml:"issue_id" yaml:"issue_id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *workspaceSCMReleaseLinkR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceSCMReleaseLinkL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceSCMReleaseLinkColumns = struct {
	ReleaseID string
	LinkID    string
	IssueID   string
	CreatedAt string
}{
	ReleaseID: "release_id",
	LinkID:    "link_id",
	IssueID:   "issue_id",
	CreatedAt: "created_at",
}

var WorkspaceSCMReleaseLinkTableColumns = struct {
	ReleaseID string
	LinkID    string
	IssueID   string
	CreatedAt string
}{
	ReleaseID: "workspace_scm_release_links.release_id",
	LinkID:    "workspace_scm_release_links.link_id",
	IssueID:   "workspace_scm_release_links.issue_id",
	CreatedAt: "workspace_scm_release_links.created_at",
}

// Generated where

var WorkspaceSCMReleaseLinkWhere = struct {
	ReleaseID whereHelperstring
	LinkID    whereHelperstring
	IssueID   whereHelperstring
	CreatedAt whereHelpertime_Time
}{
	ReleaseID: whereHelperstring{field: "\"workspace_scm_release_links\".\"release_id\""},
	LinkID:    whereHelperstring{field: "\"workspace_scm_release_links\".\"link_id\""},
	IssueID:   whereHelperstring{field: "\"workspace_scm_release_links\".\"issue_id\""},
	CreatedAt: whereHelpertime_Time{field: "\"workspace_scm_release_links\".\"created_at\""},
}

// WorkspaceSCMReleaseLinkRels is where relationship names are stored.
var WorkspaceSCMReleaseLinkRels = struct {
	Issue   string
	Link    string
	Release string
}{
	Issue:   "Issue",
	Link:    "Link",
	Release: "Release",
}

// workspaceSCMReleaseLinkR is where relationships are stored.
type workspaceSCMReleaseLinkR struct {
	Issue   *WorkspaceIssue      `boil:"Issue" json:"Issue" toml:"Issue" yaml:"Issue"`
	Link    *WorkspaceCodeLink   `boil:"Link" json:"Link" toml:"Link" yaml:"Link"`
	Release *WorkspaceSCMRelease `boil:"Release" json:"Release" toml:"Release" yaml:"Release"`
}

// NewStruct creates a new relationship struct
func (*workspaceSCMReleaseLinkR) NewStruct() *workspaceSCMReleaseLinkR {
	return &workspaceSCMReleaseLinkR{}
}

func (o *WorkspaceSCMReleaseLink) GetIssue() *WorkspaceIssue {
	if o == nil {
		return nil
	}

	return o.R.GetIssue()
}

func (r *workspaceSCMReleaseLinkR) GetIssue() *WorkspaceIssue {
	if r == nil {
		return nil
	}

	return r.Issue
}

func (o *WorkspaceSCMReleaseLink) GetLink() *WorkspaceCodeLink {
	if o == nil {
		return nil
	}

	return o.R.GetLink()
}

func (r *workspaceSCMReleaseLinkR) GetLink() *WorkspaceCodeLink {
	if r == nil {
		return nil
	}

	return r.Link
}

func (o *WorkspaceSCMReleaseLink) GetRelease() *WorkspaceSCMRelease {
	if o == nil {
		return nil
	}

	return o.R.GetRelease()
}

func (r *workspaceSCMReleaseLinkR) GetRelease() *WorkspaceSCMRelease {
	if r == nil {
		return nil
	}

	return r.Release
}

// workspaceSCMReleaseLinkL is where Load methods for each relationship are stored.
type workspaceSCMReleaseLinkL struct{}

var (
	workspaceSCMReleaseLinkAllColumns            = []string{"release_id", "link_id", "issue_id", "created_at"}
	workspaceSCMReleaseLinkColumnsWithoutDefault = []string{"release_id", "link_id", "issue_id"}
	workspaceSCMReleaseLinkColumnsWithDefault    = []string{"created_at"}
	workspaceSCMReleaseLinkPrimaryKeyColumns     = []string{"release_id", "link_id"}
	workspaceSCMReleaseLinkGeneratedColumns      = []string{}
)

type (
	// WorkspaceSCMReleaseLinkSlice is an alias for a slice of pointers to WorkspaceSCMReleaseLink.
	// This should almost always be used instead of []WorkspaceSCMReleaseLink.
	WorkspaceSCMReleaseLinkSlice []*WorkspaceSCMReleaseLink
	// WorkspaceSCMReleaseLinkHook is the signature for custom WorkspaceSCMReleaseLink hook methods
	WorkspaceSCMReleaseLinkHook func(context.Context, boil.ContextExecutor, *WorkspaceSCMReleaseLink) error

	workspaceSCMReleaseLinkQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceSCMReleaseLinkType                 = reflect.TypeOf(&WorkspaceSCMReleaseLink{})
	workspaceSCMReleaseLinkMapping              = queries.MakeStructMapping(workspaceSCMReleaseLinkType)
	workspaceSCMReleaseLinkPrimaryKeyMapping, _ = queries.BindMapping(workspaceSCMReleaseLinkType, workspaceSCMReleaseLinkMapping, workspaceSCMReleaseLinkPrimaryKeyColumns)
	workspaceSCMReleaseLinkInsertCacheMut       sync.RWMutex
	workspaceSCMReleaseLinkInsertCache          = make(map[string]insertCache)
	workspaceSCMReleaseLinkUpdateCacheMut       sync.RWMutex
	workspaceSCMReleaseLinkUpdateCache          = make(map[string]updateCache)
	workspaceSCMReleaseLinkUpsertCacheMut       sync.RWMutex
	workspaceSCMReleaseLinkUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceSCMReleaseLinkAfterSelectMu sync.Mutex
var workspaceSCMReleaseLinkAfterSelectHooks []WorkspaceSCMReleaseLinkHook

var workspaceSCMReleaseLinkBeforeInsertMu sync.Mutex
var workspaceSCMReleaseLinkBeforeInsertHooks []WorkspaceSCMReleaseLinkHook
var workspaceSCMReleaseLinkAfterInsertMu sync.Mutex
var workspaceSCMReleaseLinkAfterInsertHooks []WorkspaceSCMReleaseLinkHook

var workspaceSCMReleaseLinkBeforeUpdateMu sync.Mutex
var workspaceSCMReleaseLinkBeforeUpdateHooks []WorkspaceSCMReleaseLinkHook
var workspaceSCMReleaseLinkAfterUpdateMu sync.Mutex
var workspaceSCMReleaseLinkAfterUpdateHooks []WorkspaceSCMReleaseLinkHook

var workspaceSCMReleaseLinkBeforeDeleteMu sync.Mutex
var workspaceSCMReleaseLinkBeforeDeleteHooks []WorkspaceSCMReleaseLinkHook
var workspaceSCMReleaseLinkAfterDeleteMu sync.Mutex
var workspaceSCMReleaseLinkAfterDeleteHooks []WorkspaceSCMReleaseLinkHook

var workspaceSCMReleaseLinkBeforeUpsertMu sync.Mutex
var workspaceSCMReleaseLinkBeforeUpsertHooks []WorkspaceSCMReleaseLinkHook
var workspaceSCMReleaseLinkAfterUpsertMu sync.Mutex
var workspaceSCMReleaseLinkAfterUpsertHooks []WorkspaceSCMReleaseLinkHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceSCMReleaseLink) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceSCMReleaseLink) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceSCMReleaseLink) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceSCMReleaseLink) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceSCMReleaseLink) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceSCMReleaseLink) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceSCMReleaseLink) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceSCMReleaseLink) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceSCMReleaseLink) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceSCMReleaseLinkAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceSCMReleaseLinkHook registers your hook function for all future operations.
func AddWorkspaceSCMReleaseLinkHook(hookPoint boil.HookPoint, workspaceSCMReleaseLinkHook WorkspaceSCMReleaseLinkHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceSCMReleaseLinkAfterSelectMu.Lock()
		workspaceSCMReleaseLinkAfterSelectHooks = append(workspaceSCMReleaseLinkAfterSelectHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceSCMReleaseLinkBeforeInsertMu.Lock()
		workspaceSCMReleaseLinkBeforeInsertHooks = append(workspaceSCMReleaseLinkBeforeInsertHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceSCMReleaseLinkAfterInsertMu.Lock()
		workspaceSCMReleaseLinkAfterInsertHooks = append(workspaceSCMReleaseLinkAfterInsertHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceSCMReleaseLinkBeforeUpdateMu.Lock()
		workspaceSCMReleaseLinkBeforeUpdateHooks = append(workspaceSCMReleaseLinkBeforeUpdateHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceSCMReleaseLinkAfterUpdateMu.Lock()
		workspaceSCMReleaseLinkAfterUpdateHooks = append(workspaceSCMReleaseLinkAfterUpdateHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceSCMReleaseLinkBeforeDeleteMu.Lock()
		workspaceSCMReleaseLinkBeforeDeleteHooks = append(workspaceSCMReleaseLinkBeforeDeleteHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceSCMReleaseLinkAfterDeleteMu.Lock()
		workspaceSCMReleaseLinkAfterDeleteHooks = append(workspaceSCMReleaseLinkAfterDeleteHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceSCMReleaseLinkBeforeUpsertMu.Lock()
		workspaceSCMReleaseLinkBeforeUpsertHooks = append(workspaceSCMReleaseLinkBeforeUpsertHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceSCMReleaseLinkAfterUpsertMu.Lock()
		workspaceSCMReleaseLinkAfterUpsertHooks = append(workspaceSCMReleaseLinkAfterUpsertHooks, workspaceSCMReleaseLinkHook)
		workspaceSCMReleaseLinkAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceSCMReleaseLink record from the query.
func (q workspaceSCMReleaseLinkQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceSCMReleaseLink, error) {
	o := &WorkspaceSCMReleaseLink{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_scm_release_links")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceSCMReleaseLink records from the query.
func (q workspaceSCMReleaseLinkQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceSCMReleaseLinkSlice, error) {
	var o []*WorkspaceSCMReleaseLink

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceSCMReleaseLink slice")
	}

	if len(workspaceSCMReleaseLinkAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceSCMReleaseLink records in the query.
func (q workspaceSCMReleaseLinkQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_scm_release_links rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceSCMReleaseLinkQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_scm_release_links exists")
	}

	return count > 0, nil
}

// Issue pointed to by the foreign key.
func (o *WorkspaceSCMReleaseLink) Issue(mods ...qm.QueryMod) workspaceIssueQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IssueID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceIssues(queryMods...)
}

// Link pointed to by the foreign key.
func (o *WorkspaceSCMReleaseLink) Link(mods ...qm.QueryMod) workspaceCodeLinkQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.LinkID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceCodeLinks(queryMods...)
}

// Release pointed to by the foreign key.
func (o *WorkspaceSCMReleaseLink) Release(mods ...qm.QueryMod) workspaceSCMReleaseQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ReleaseID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceSCMReleases(queryMods...)
}

// LoadIssue allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceSCMReleaseLinkL) LoadIssue(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSCMReleaseLink any, mods queries.Applicator) error {
	var slice []*WorkspaceSCMReleaseLink
	var object *WorkspaceSCMReleaseLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSCMReleaseLink.(*WorkspaceSCMReleaseLink)
		if !ok {
			object = new(WorkspaceSCMReleaseLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSCMReleaseLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSCMReleaseLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceSCMReleaseLink.(*[]*WorkspaceSCMReleaseLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSCMReleaseLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSCMReleaseLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSCMReleaseLinkR{}
		}
		args[object.IssueID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSCMReleaseLinkR{}
			}

			args[obj.IssueID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceIssue")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceIssue")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Issue = foreign
		if foreign.R == nil {
			foreign.R = &workspaceIssueR{}
		}
		foreign.R.IssueWorkspaceSCMReleaseLinks = append(foreign.R.IssueWorkspaceSCMReleaseLinks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.IssueID == foreign.ID {
				local.R.Issue = foreign
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.IssueWorkspaceSCMReleaseLinks = append(foreign.R.IssueWorkspaceSCMReleaseLinks, local)
				break
			}
		}
	}

	return nil
}

// LoadLink allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceSCMReleaseLinkL) LoadLink(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSCMReleaseLink any, mods queries.Applicator) error {
	var slice []*WorkspaceSCMReleaseLink
	var object *WorkspaceSCMReleaseLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSCMReleaseLink.(*WorkspaceSCMReleaseLink)
		if !ok {
			object = new(WorkspaceSCMReleaseLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSCMReleaseLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSCMReleaseLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceSCMReleaseLink.(*[]*WorkspaceSCMReleaseLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSCMReleaseLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSCMReleaseLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSCMReleaseLinkR{}
		}
		args[object.LinkID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSCMReleaseLinkR{}
			}

			args[obj.LinkID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_code_links`),
		qm.WhereIn(`workspace_code_links.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceCodeLink")
	}

	var resultSlice []*WorkspaceCodeLink
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceCodeLink")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_code_links")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_code_links")
	}

	if len(workspaceCodeLinkAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Link = foreign
		if foreign.R == nil {
			foreign.R = &workspaceCodeLinkR{}
		}
		foreign.R.LinkWorkspaceSCMReleaseLinks = append(foreign.R.LinkWorkspaceSCMReleaseLinks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.LinkID == foreign.ID {
				local.R.Link = foreign
				if foreign.R == nil {
					foreign.R = &workspaceCodeLinkR{}
				}
				foreign.R.LinkWorkspaceSCMReleaseLinks = append(foreign.R.LinkWorkspaceSCMReleaseLinks, local)
				break
			}
		}
	}

	return nil
}

// LoadRelease allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceSCMReleaseLinkL) LoadRelease(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceSCMReleaseLink any, mods queries.Applicator) error {
	var slice []*WorkspaceSCMReleaseLink
	var object *WorkspaceSCMReleaseLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceSCMReleaseLink.(*WorkspaceSCMReleaseLink)
		if !ok {
			object = new(WorkspaceSCMReleaseLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceSCMReleaseLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceSCMReleaseLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceSCMReleaseLink.(*[]*WorkspaceSCMReleaseLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceSCMReleaseLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceSCMReleaseLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceSCMReleaseLinkR{}
		}
		args[object.ReleaseID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceSCMReleaseLinkR{}
			}

			args[obj.ReleaseID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_scm_releases`),
		qm.WhereIn(`workspace_scm_releases.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceSCMRelease")
	}

	var resultSlice []*WorkspaceSCMRelease
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceSCMRelease")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_scm_releases")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_scm_releases")
	}

	if len(workspaceSCMReleaseAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Release = foreign
		if foreign.R == nil {
			foreign.R = &workspaceSCMReleaseR{}
		}
		foreign.R.ReleaseWorkspaceSCMReleaseLinks = append(foreign.R.ReleaseWorkspaceSCMReleaseLinks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ReleaseID == foreign.ID {
				local.R.Release = foreign
				if foreign.R == nil {
					foreign.R = &workspaceSCMReleaseR{}
				}
				foreign.R.ReleaseWorkspaceSCMReleaseLinks = append(foreign.R.ReleaseWorkspaceSCMReleaseLinks, local)
				break
			}
		}
	}

	return nil
}

// SetIssue of the workspaceSCMReleaseLink to the related item.
// Sets o.R.Issue to related.
// Adds o to related.R.IssueWorkspaceSCMReleaseLinks.
func (o *WorkspaceSCMReleaseLink) SetIssue(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceIssue) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_scm_release_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"issue_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceSCMReleaseLinkPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ReleaseID, o.LinkID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.IssueID = related.ID
	if o.R == nil {
		o.R = &workspaceSCMReleaseLinkR{
			Issue: related,
		}
	} else {
		o.R.Issue = related
	}

	if related.R == nil {
		related.R = &workspaceIssueR{
			IssueWorkspaceSCMReleaseLinks: WorkspaceSCMReleaseLinkSlice{o},
		}
	} else {
		related.R.IssueWorkspaceSCMReleaseLinks = append(related.R.IssueWorkspaceSCMReleaseLinks, o)
	}

	return nil
}

// SetLink of the workspaceSCMReleaseLink to the related item.
// Sets o.R.Link to related.
// Adds o to related.R.LinkWorkspaceSCMReleaseLinks.
func (o *WorkspaceSCMReleaseLink) SetLink(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceCodeLink) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_scm_release_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"link_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceSCMReleaseLinkPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ReleaseID, o.LinkID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.LinkID = related.ID
	if o.R == nil {
		o.R = &workspaceSCMReleaseLinkR{
			Link: related,
		}
	} else {
		o.R.Link = related
	}

	if related.R == nil {
		related.R = &workspaceCodeLinkR{
			LinkWorkspaceSCMReleaseLinks: WorkspaceSCMReleaseLinkSlice{o},
		}
	} else {
		related.R.LinkWorkspaceSCMReleaseLinks = append(related.R.LinkWorkspaceSCMReleaseLinks, o)
	}

	return nil
}

// SetRelease of the workspaceSCMReleaseLink to the related item.
// Sets o.R.Release to related.
// Adds o to related.R.ReleaseWorkspaceSCMReleaseLinks.
func (o *WorkspaceSCMReleaseLink) SetRelease(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceSCMRelease) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_scm_release_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"release_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceSCMReleaseLinkPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ReleaseID, o.LinkID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ReleaseID = related.ID
	if o.R == nil {
		o.R = &workspaceSCMReleaseLinkR{
			Release: related,
		}
	} else {
		o.R.Release = related
	}

	if related.R == nil {
		related.R = &workspaceSCMReleaseR{
			ReleaseWorkspaceSCMReleaseLinks: WorkspaceSCMReleaseLinkSlice{o},
		}
	} else {
		related.R.ReleaseWorkspaceSCMReleaseLinks = append(related.R.ReleaseWorkspaceSCMReleaseLinks, o)
	}

	return nil
}

// WorkspaceSCMReleaseLinks retrieves all the records using an executor.
func WorkspaceSCMReleaseLinks(mods ...qm.QueryMod) workspaceSCMReleaseLinkQuery {
	mods = append(mods, qm.From("\"workspace_scm_release_links\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_scm_release_links\".*"})
	}

	return workspaceSCMReleaseLinkQuery{q}
}

// FindWorkspaceSCMReleaseLink retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceSCMReleaseLink(ctx context.Context, exec boil.ContextExecutor, releaseID string, linkID string, selectCols ...string) (*WorkspaceSCMReleaseLink, error) {
	workspaceSCMReleaseLinkObj := &WorkspaceSCMReleaseLink{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_scm_release_links\" where \"release_id\"=$1 AND \"link_id\"=$2", sel,
	)

	q := queries.Raw(query, releaseID, linkID)

	err := q.Bind(ctx, exec, workspaceSCMReleaseLinkObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_scm_release_links")
	}

	if err = workspaceSCMReleaseLinkObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceSCMReleaseLinkObj, err
	}

	return workspaceSCMReleaseLinkObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceSCMReleaseLink) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_scm_release_links provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceSCMReleaseLinkColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceSCMReleaseLinkInsertCacheMut.RLock()
	cache, cached := workspaceSCMReleaseLinkInsertCache[key]
	workspaceSCMReleaseLinkInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceSCMReleaseLinkAllColumns,
			workspaceSCMReleaseLinkColumnsWithDefault,
			workspaceSCMReleaseLinkColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceSCMReleaseLinkType, workspaceSCMReleaseLinkMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceSCMReleaseLinkType, workspaceSCMReleaseLinkMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_scm_release_links\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_scm_release_links\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_scm_release_links")
	}

	if !cached {
		workspaceSCMReleaseLinkInsertCacheMut.Lock()
		workspaceSCMReleaseLinkInsertCache[key] = cache
		workspaceSCMReleaseLinkInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceSCMReleaseLink.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceSCMReleaseLink) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceSCMReleaseLinkUpdateCacheMut.RLock()
	cache, cached := workspaceSCMReleaseLinkUpdateCache[key]
	workspaceSCMReleaseLinkUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceSCMReleaseLinkAllColumns,
			workspaceSCMReleaseLinkPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_scm_release_links, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_scm_release_links\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceSCMReleaseLinkPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceSCMReleaseLinkType, workspaceSCMReleaseLinkMapping, append(wl, workspaceSCMReleaseLinkPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_scm_release_links row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_scm_release_links")
	}

	if !cached {
		workspaceSCMReleaseLinkUpdateCacheMut.Lock()
		workspaceSCMReleaseLinkUpdateCache[key] = cache
		workspaceSCMReleaseLinkUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceSCMReleaseLinkQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_scm_release_links")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_scm_release_links")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceSCMReleaseLinkSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSCMReleaseLinkPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_scm_release_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceSCMReleaseLinkPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceSCMReleaseLink slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceSCMReleaseLink")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceSCMReleaseLink) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_scm_release_links provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceSCMReleaseLinkColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceSCMReleaseLinkUpsertCacheMut.RLock()
	cache, cached := workspaceSCMReleaseLinkUpsertCache[key]
	workspaceSCMReleaseLinkUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceSCMReleaseLinkAllColumns,
			workspaceSCMReleaseLinkColumnsWithDefault,
			workspaceSCMReleaseLinkColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceSCMReleaseLinkAllColumns,
			workspaceSCMReleaseLinkPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_scm_release_links, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceSCMReleaseLinkAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceSCMReleaseLinkPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_scm_release_links, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceSCMReleaseLinkPrimaryKeyColumns))
			copy(conflict, workspaceSCMReleaseLinkPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_scm_release_links\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceSCMReleaseLinkType, workspaceSCMReleaseLinkMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceSCMReleaseLinkType, workspaceSCMReleaseLinkMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_scm_release_links")
	}

	if !cached {
		workspaceSCMReleaseLinkUpsertCacheMut.Lock()
		workspaceSCMReleaseLinkUpsertCache[key] = cache
		workspaceSCMReleaseLinkUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceSCMReleaseLink record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceSCMReleaseLink) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceSCMReleaseLink provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceSCMReleaseLinkPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_scm_release_links\" WHERE \"release_id\"=$1 AND \"link_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_scm_release_links")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_scm_release_links")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceSCMReleaseLinkQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceSCMReleaseLinkQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_scm_release_links")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_scm_release_links")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceSCMReleaseLinkSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceSCMReleaseLinkBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSCMReleaseLinkPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_scm_release_links\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceSCMReleaseLinkPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceSCMReleaseLink slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_scm_release_links")
	}

	if len(workspaceSCMReleaseLinkAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceSCMReleaseLink) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceSCMReleaseLink(ctx, exec, o.ReleaseID, o.LinkID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceSCMReleaseLinkSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceSCMReleaseLinkSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceSCMReleaseLinkPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_scm_release_links\".* FROM \"workspace_scm_release_links\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceSCMReleaseLinkPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceSCMReleaseLinkSlice")
	}

	*o = slice

	return nil
}

// WorkspaceSCMReleaseLinkExists checks if the WorkspaceSCMReleaseLink row exists.
func WorkspaceSCMReleaseLinkExists(ctx context.Context, exec boil.ContextExecutor, releaseID string, linkID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_scm_release_links\" where \"release_id\"=$1 AND \"link_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, releaseID, linkID)
	}
	row := exec.QueryRowContext(ctx, sql, releaseID, linkID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_scm_release_links exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceSCMReleaseLink row exists.
func (o *WorkspaceSCMReleaseLink) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceSCMReleaseLinkExists(ctx, exec, o.ReleaseID, o.LinkID)
}
