// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceCycleScopeChange is an object representing the database table.
type WorkspaceCycleScopeChange struct {
	ID             string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	CycleID        string      `boil:"cycle_id" json:"cycle_id" toml:"cycle_id" yaml:"cycle_id"`
	IssueID        string      `boil:"issue_id" json:"issue_id" toml:"issue_id" yaml:"issue_id"`
	Change         string      `boil:"change" json:"change" toml:"change" yaml:"change"`
	ActorAccountID null.String `boil:"actor_account_id" json:"actor_account_id,omitempty" toml:"actor_account_id" yaml:"actor_account_id,omitempty"`
	ChangedAt      time.Time   `boil:"changed_at" json:"changed_at" toml:"changed_at" yaml:"changed_at"`

	R *workspaceCycleScopeChangeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceCycleScopeChangeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceCycleScopeChangeColumns = struct {
	ID             string
	CycleID        string
	IssueID        string
	Change         string
	ActorAccountID string
	ChangedAt      string
}{
	ID:             "id",
	CycleID:        "cycle_id",
	IssueID:        "issue_id",
	Change:         "change",
	ActorAccountID: "actor_account_id",
	ChangedAt:      "changed_at",
}

var WorkspaceCycleScopeChangeTableColumns = struct {
	ID             string
	CycleID        string
	IssueID        string
	Change         string
	ActorAccountID string
	ChangedAt      string
}{
	ID:             "workspace_cycle_scope_changes.id",
	CycleID:        "workspace_cycle_scope_changes.cycle_id",
	IssueID:        "workspace_cycle_scope_changes.issue_id",
	Change:         "workspace_cycle_scope_changes.change",
	ActorAccountID: "workspace_cycle_scope_changes.actor_account_id",
	ChangedAt:      "workspace_cycle_scope_changes.changed_at",
}

// Generated where

var WorkspaceCycleScopeChangeWhere = struct {
	ID             whereHelperstring
	CycleID        whereHelperstring
	IssueID        whereHelperstring
	Change         whereHelperstring
	ActorAccountID whereHelpernull_String
	ChangedAt      whereHelpertime_Time
}{
	ID:             whereHelperstring{field: "\"workspace_cycle_scope_changes\".\"id\""},
	CycleID:        whereHelperstring{field: "\"workspace_cycle_scope_changes\".\"cycle_id\""},
	IssueID:        whereHelperstring{field: "\"workspace_cycle_scope_changes\".\"issue_id\""},
	Change:         whereHelperstring{field: "\"workspace_cycle_scope_changes\".\"change\""},
	ActorAccountID: whereHelpernull_String{field: "\"workspace_cycle_scope_changes\".\"actor_account_id\""},
	ChangedAt:      whereHelpertime_Time{field: "\"workspace_cycle_scope_changes\".\"changed_at\""},
}

// WorkspaceCycleScopeChangeRels is where relationship names are stored.
var WorkspaceCycleScopeChangeRels = struct {
	ActorAccount string
	Cycle        string
	Issue        string
}{
	ActorAccount: "ActorAccount",
	Cycle:        "Cycle",
	Issue:        "Issue",
}

// workspaceCycleScopeChangeR is where relationships are stored.
type workspaceCycleScopeChangeR struct {
	ActorAccount *Account        `boil:"ActorAccount" json:"ActorAccount" toml:"ActorAccount" yaml:"ActorAccount"`
	Cycle        *WorkspaceCycle `boil:"Cycle" json:"Cycle" toml:"Cycle" yaml:"Cycle"`
	Issue        *WorkspaceIssue `boil:"Issue" json:"Issue" toml:"Issue" yaml:"Issue"`
}

// NewStruct creates a new relationship struct
func (*workspaceCycleScopeChangeR) NewStruct() *workspaceCycleScopeChangeR {
	return &workspaceCycleScopeChangeR{}
}

func (o *WorkspaceCycleScopeChange) GetActorAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetActorAccount()
}

func (r *workspaceCycleScopeChangeR) GetActorAccount() *Account {
	if r == nil {
		return nil
	}

	return r.ActorAccount
}

func (o *WorkspaceCycleScopeChange) GetCycle() *WorkspaceCycle {
	if o == nil {
		return nil
	}

	return o.R.GetCycle()
}

func (r *workspaceCycleScopeChangeR) GetCycle() *WorkspaceCycle {
	if r == nil {
		return nil
	}

	return r.Cycle
}

func (o *WorkspaceCycleScopeChange) GetIssue() *WorkspaceIssue {
	if o == nil {
		return nil
	}

	return o.R.GetIssue()
}

func (r *workspaceCycleScopeChangeR) GetIssue() *WorkspaceIssue {
	if r == nil {
		return nil
	}

	return r.Issue
}

// workspaceCycleScopeChangeL is where Load methods for each relationship are stored.
type workspaceCycleScopeChangeL struct{}

var (
	workspaceCycleScopeChangeAllColumns            = []string{"id", "cycle_id", "issue_id", "change", "actor_account_id", "changed_at"}
	workspaceCycleScopeChangeColumnsWithoutDefault = []string{"cycle_id", "issue_id", "change"}
	workspaceCycleScopeChangeColumnsWithDefault    = []string{"id", "actor_account_id", "changed_at"}
	workspaceCycleScopeChangePrimaryKeyColumns     = []string{"id"}
	workspaceCycleScopeChangeGeneratedColumns      = []string{}
)

type (
	// WorkspaceCycleScopeChangeSlice is an alias for a slice of pointers to WorkspaceCycleScopeChange.
	// This should almost always be used instead of []WorkspaceCycleScopeChange.
	WorkspaceCycleScopeChangeSlice []*WorkspaceCycleScopeChange
	// WorkspaceCycleScopeChangeHook is the signature for custom WorkspaceCycleScopeChange hook methods
	WorkspaceCycleScopeChangeHook func(context.Context, boil.ContextExecutor, *WorkspaceCycleScopeChange) error

	workspaceCycleScopeChangeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceCycleScopeChangeType                 = reflect.TypeOf(&WorkspaceCycleScopeChange{})
	workspaceCycleScopeChangeMapping              = queries.MakeStructMapping(workspaceCycleScopeChangeType)
	workspaceCycleScopeChangePrimaryKeyMapping, _ = queries.BindMapping(workspaceCycleScopeChangeType, workspaceCycleScopeChangeMapping, workspaceCycleScopeChangePrimaryKeyColumns)
	workspaceCycleScopeChangeInsertCacheMut       sync.RWMutex
	workspaceCycleScopeChangeInsertCache          = make(map[string]insertCache)
	workspaceCycleScopeChangeUpdateCacheMut       sync.RWMutex
	workspaceCycleScopeChangeUpdateCache          = make(map[string]updateCache)
	workspaceCycleScopeChangeUpsertCacheMut       sync.RWMutex
	workspaceCycleScopeChangeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceCycleScopeChangeAfterSelectMu sync.Mutex
var workspaceCycleScopeChangeAfterSelectHooks []WorkspaceCycleScopeChangeHook

var workspaceCycleScopeChangeBeforeInsertMu sync.Mutex
var workspaceCycleScopeChangeBeforeInsertHooks []WorkspaceCycleScopeChangeHook
var workspaceCycleScopeChangeAfterInsertMu sync.Mutex
var workspaceCycleScopeChangeAfterInsertHooks []WorkspaceCycleScopeChangeHook

var workspaceCycleScopeChangeBeforeUpdateMu sync.Mutex
var workspaceCycleScopeChangeBeforeUpdateHooks []WorkspaceCycleScopeChangeHook
var workspaceCycleScopeChangeAfterUpdateMu sync.Mutex
var workspaceCycleScopeChangeAfterUpdateHooks []WorkspaceCycleScopeChangeHook

var workspaceCycleScopeChangeBeforeDeleteMu sync.Mutex
var workspaceCycleScopeChangeBeforeDeleteHooks []WorkspaceCycleScopeChangeHook
var workspaceCycleScopeChangeAfterDeleteMu sync.Mutex
var workspaceCycleScopeChangeAfterDeleteHooks []WorkspaceCycleScopeChangeHook

var workspaceCycleScopeChangeBeforeUpsertMu sync.Mutex
var workspaceCycleScopeChangeBeforeUpsertHooks []WorkspaceCycleScopeChangeHook
var workspaceCycleScopeChangeAfterUpsertMu sync.Mutex
var workspaceCycleScopeChangeAfterUpsertHooks []WorkspaceCycleScopeChangeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceCycleScopeChange) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceCycleScopeChange) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceCycleScopeChange) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceCycleScopeChange) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceCycleScopeChange) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceCycleScopeChange) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceCycleScopeChange) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceCycleScopeChange) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceCycleScopeChange) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCycleScopeChangeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceCycleScopeChangeHook registers your hook function for all future operations.
func AddWorkspaceCycleScopeChangeHook(hookPoint boil.HookPoint, workspaceCycleScopeChangeHook WorkspaceCycleScopeChangeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceCycleScopeChangeAfterSelectMu.Lock()
		workspaceCycleScopeChangeAfterSelectHooks = append(workspaceCycleScopeChangeAfterSelectHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceCycleScopeChangeBeforeInsertMu.Lock()
		workspaceCycleScopeChangeBeforeInsertHooks = append(workspaceCycleScopeChangeBeforeInsertHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceCycleScopeChangeAfterInsertMu.Lock()
		workspaceCycleScopeChangeAfterInsertHooks = append(workspaceCycleScopeChangeAfterInsertHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceCycleScopeChangeBeforeUpdateMu.Lock()
		workspaceCycleScopeChangeBeforeUpdateHooks = append(workspaceCycleScopeChangeBeforeUpdateHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceCycleScopeChangeAfterUpdateMu.Lock()
		workspaceCycleScopeChangeAfterUpdateHooks = append(workspaceCycleScopeChangeAfterUpdateHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceCycleScopeChangeBeforeDeleteMu.Lock()
		workspaceCycleScopeChangeBeforeDeleteHooks = append(workspaceCycleScopeChangeBeforeDeleteHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceCycleScopeChangeAfterDeleteMu.Lock()
		workspaceCycleScopeChangeAfterDeleteHooks = append(workspaceCycleScopeChangeAfterDeleteHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceCycleScopeChangeBeforeUpsertMu.Lock()
		workspaceCycleScopeChangeBeforeUpsertHooks = append(workspaceCycleScopeChangeBeforeUpsertHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceCycleScopeChangeAfterUpsertMu.Lock()
		workspaceCycleScopeChangeAfterUpsertHooks = append(workspaceCycleScopeChangeAfterUpsertHooks, workspaceCycleScopeChangeHook)
		workspaceCycleScopeChangeAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceCycleScopeChange record from the query.
func (q workspaceCycleScopeChangeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceCycleScopeChange, error) {
	o := &WorkspaceCycleScopeChange{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_cycle_scope_changes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceCycleScopeChange records from the query.
func (q workspaceCycleScopeChangeQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceCycleScopeChangeSlice, error) {
	var o []*WorkspaceCycleScopeChange

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceCycleScopeChange slice")
	}

	if len(workspaceCycleScopeChangeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceCycleScopeChange records in the query.
func (q workspaceCycleScopeChangeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_cycle_scope_changes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceCycleScopeChangeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_cycle_scope_changes exists")
	}

	return count > 0, nil
}

// ActorAccount pointed to by the foreign key.
func (o *WorkspaceCycleScopeChange) ActorAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ActorAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Cycle pointed to by the foreign key.
func (o *WorkspaceCycleScopeChange) Cycle(mods ...qm.QueryMod) workspaceCycleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CycleID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceCycles(queryMods...)
}

// Issue pointed to by the foreign key.
func (o *WorkspaceCycleScopeChange) Issue(mods ...qm.QueryMod) workspaceIssueQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IssueID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceIssues(queryMods...)
}

// LoadActorAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCycleScopeChangeL) LoadActorAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCycleScopeChange any, mods queries.Applicator) error {
	var slice []*WorkspaceCycleScopeChange
	var object *WorkspaceCycleScopeChange

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCycleScopeChange.(*WorkspaceCycleScopeChange)
		if !ok {
			object = new(WorkspaceCycleScopeChange)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCycleScopeChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCycleScopeChange))
			}
		}
	} else {
		s, ok := maybeWorkspaceCycleScopeChange.(*[]*WorkspaceCycleScopeChange)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCycleScopeChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCycleScopeChange))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCycleScopeChangeR{}
		}
		if !queries.IsNil(object.ActorAccountID) {
			args[object.ActorAccountID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCycleScopeChangeR{}
			}

			if !queries.IsNil(obj.ActorAccountID) {
				args[obj.ActorAccountID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ActorAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.ActorAccountWorkspaceCycleScopeChanges = append(foreign.R.ActorAccountWorkspaceCycleScopeChanges, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ActorAccountID, foreign.ID) {
				local.R.ActorAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.ActorAccountWorkspaceCycleScopeChanges = append(foreign.R.ActorAccountWorkspaceCycleScopeChanges, local)
				break
			}
		}
	}

	return nil
}

// LoadCycle allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCycleScopeChangeL) LoadCycle(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCycleScopeChange any, mods queries.Applicator) error {
	var slice []*WorkspaceCycleScopeChange
	var object *WorkspaceCycleScopeChange

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCycleScopeChange.(*WorkspaceCycleScopeChange)
		if !ok {
			object = new(WorkspaceCycleScopeChange)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCycleScopeChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCycleScopeChange))
			}
		}
	} else {
		s, ok := maybeWorkspaceCycleScopeChange.(*[]*WorkspaceCycleScopeChange)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCycleScopeChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCycleScopeChange))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCycleScopeChangeR{}
		}
		args[object.CycleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCycleScopeChangeR{}
			}

			args[obj.CycleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_cycles`),
		qm.WhereIn(`workspace_cycles.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceCycle")
	}

	var resultSlice []*WorkspaceCycle
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceCycle")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_cycles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_cycles")
	}

	if len(workspaceCycleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Cycle = foreign
		if foreign.R == nil {
			foreign.R = &workspaceCycleR{}
		}
		foreign.R.CycleWorkspaceCycleScopeChanges = append(foreign.R.CycleWorkspaceCycleScopeChanges, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CycleID == foreign.ID {
				local.R.Cycle = foreign
				if foreign.R == nil {
					foreign.R = &workspaceCycleR{}
				}
				foreign.R.CycleWorkspaceCycleScopeChanges = append(foreign.R.CycleWorkspaceCycleScopeChanges, local)
				break
			}
		}
	}

	return nil
}

// LoadIssue allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCycleScopeChangeL) LoadIssue(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCycleScopeChange any, mods queries.Applicator) error {
	var slice []*WorkspaceCycleScopeChange
	var object *WorkspaceCycleScopeChange

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCycleScopeChange.(*WorkspaceCycleScopeChange)
		if !ok {
			object = new(WorkspaceCycleScopeChange)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCycleScopeChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCycleScopeChange))
			}
		}
	} else {
		s, ok := maybeWorkspaceCycleScopeChange.(*[]*WorkspaceCycleScopeChange)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCycleScopeChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCycleScopeChange))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCycleScopeChangeR{}
		}
		args[object.IssueID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCycleScopeChangeR{}
			}

			args[obj.IssueID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceIssue")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceIssue")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Issue = foreign
		if foreign.R == nil {
			foreign.R = &workspaceIssueR{}
		}
		foreign.R.IssueWorkspaceCycleScopeChanges = append(foreign.R.IssueWorkspaceCycleScopeChanges, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.IssueID == foreign.ID {
				local.R.Issue = foreign
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.IssueWorkspaceCycleScopeChanges = append(foreign.R.IssueWorkspaceCycleScopeChanges, local)
				break
			}
		}
	}

	return nil
}

// SetActorAccount of the workspaceCycleScopeChange to the related item.
// Sets o.R.ActorAccount to related.
// Adds o to related.R.ActorAccountWorkspaceCycleScopeChanges.
func (o *WorkspaceCycleScopeChange) SetActorAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_cycle_scope_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"actor_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCycleScopeChangePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ActorAccountID, related.ID)
	if o.R == nil {
		o.R = &workspaceCycleScopeChangeR{
			ActorAccount: related,
		}
	} else {
		o.R.ActorAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			ActorAccountWorkspaceCycleScopeChanges: WorkspaceCycleScopeChangeSlice{o},
		}
	} else {
		related.R.ActorAccountWorkspaceCycleScopeChanges = append(related.R.ActorAccountWorkspaceCycleScopeChanges, o)
	}

	return nil
}

// RemoveActorAccount relationship.
// Sets o.R.ActorAccount to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceCycleScopeChange) RemoveActorAccount(ctx context.Context, exec boil.ContextExecutor, related *Account) error {
	var err error

	queries.SetScanner(&o.ActorAccountID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("actor_account_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ActorAccount = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ActorAccountWorkspaceCycleScopeChanges {
		if queries.Equal(o.ActorAccountID, ri.ActorAccountID) {
			continue
		}

		ln := len(related.R.ActorAccountWorkspaceCycleScopeChanges)
		if ln > 1 && i < ln-1 {
			related.R.ActorAccountWorkspaceCycleScopeChanges[i] = related.R.ActorAccountWorkspaceCycleScopeChanges[ln-1]
		}
		related.R.ActorAccountWorkspaceCycleScopeChanges = related.R.ActorAccountWorkspaceCycleScopeChanges[:ln-1]
		break
	}
	return nil
}

// SetCycle of the workspaceCycleScopeChange to the related item.
// Sets o.R.Cycle to related.
// Adds o to related.R.CycleWorkspaceCycleScopeChanges.
func (o *WorkspaceCycleScopeChange) SetCycle(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceCycle) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_cycle_scope_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"cycle_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCycleScopeChangePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CycleID = related.ID
	if o.R == nil {
		o.R = &workspaceCycleScopeChangeR{
			Cycle: related,
		}
	} else {
		o.R.Cycle = related
	}

	if related.R == nil {
		related.R = &workspaceCycleR{
			CycleWorkspaceCycleScopeChanges: WorkspaceCycleScopeChangeSlice{o},
		}
	} else {
		related.R.CycleWorkspaceCycleScopeChanges = append(related.R.CycleWorkspaceCycleScopeChanges, o)
	}

	return nil
}

// SetIssue of the workspaceCycleScopeChange to the related item.
// Sets o.R.Issue to related.
// Adds o to related.R.IssueWorkspaceCycleScopeChanges.
func (o *WorkspaceCycleScopeChange) SetIssue(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceIssue) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_cycle_scope_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"issue_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCycleScopeChangePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.IssueID = related.ID
	if o.R == nil {
		o.R = &workspaceCycleScopeChangeR{
			Issue: related,
		}
	} else {
		o.R.Issue = related
	}

	if related.R == nil {
		related.R = &workspaceIssueR{
			IssueWorkspaceCycleScopeChanges: WorkspaceCycleScopeChangeSlice{o},
		}
	} else {
		related.R.IssueWorkspaceCycleScopeChanges = append(related.R.IssueWorkspaceCycleScopeChanges, o)
	}

	return nil
}

// WorkspaceCycleScopeChanges retrieves all the records using an executor.
func WorkspaceCycleScopeChanges(mods ...qm.QueryMod) workspaceCycleScopeChangeQuery {
	mods = append(mods, qm.From("\"workspace_cycle_scope_changes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_cycle_scope_changes\".*"})
	}

	return workspaceCycleScopeChangeQuery{q}
}

// FindWorkspaceCycleScopeChange retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceCycleScopeChange(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceCycleScopeChange, error) {
	workspaceCycleScopeChangeObj := &WorkspaceCycleScopeChange{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_cycle_scope_changes\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceCycleScopeChangeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_cycle_scope_changes")
	}

	if err = workspaceCycleScopeChangeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceCycleScopeChangeObj, err
	}

	return workspaceCycleScopeChangeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceCycleScopeChange) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_cycle_scope_changes provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCycleScopeChangeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceCycleScopeChangeInsertCacheMut.RLock()
	cache, cached := workspaceCycleScopeChangeInsertCache[key]
	workspaceCycleScopeChangeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceCycleScopeChangeAllColumns,
			workspaceCycleScopeChangeColumnsWithDefault,
			workspaceCycleScopeChangeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceCycleScopeChangeType, workspaceCycleScopeChangeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceCycleScopeChangeType, workspaceCycleScopeChangeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_cycle_scope_changes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_cycle_scope_changes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_cycle_scope_changes")
	}

	if !cached {
		workspaceCycleScopeChangeInsertCacheMut.Lock()
		workspaceCycleScopeChangeInsertCache[key] = cache
		workspaceCycleScopeChangeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceCycleScopeChange.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceCycleScopeChange) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceCycleScopeChangeUpdateCacheMut.RLock()
	cache, cached := workspaceCycleScopeChangeUpdateCache[key]
	workspaceCycleScopeChangeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceCycleScopeChangeAllColumns,
			workspaceCycleScopeChangePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_cycle_scope_changes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_cycle_scope_changes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceCycleScopeChangePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceCycleScopeChangeType, workspaceCycleScopeChangeMapping, append(wl, workspaceCycleScopeChangePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_cycle_scope_changes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_cycle_scope_changes")
	}

	if !cached {
		workspaceCycleScopeChangeUpdateCacheMut.Lock()
		workspaceCycleScopeChangeUpdateCache[key] = cache
		workspaceCycleScopeChangeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceCycleScopeChangeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_cycle_scope_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_cycle_scope_changes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceCycleScopeChangeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCycleScopeChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_cycle_scope_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceCycleScopeChangePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceCycleScopeChange slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceCycleScopeChange")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceCycleScopeChange) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_cycle_scope_changes provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCycleScopeChangeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceCycleScopeChangeUpsertCacheMut.RLock()
	cache, cached := workspaceCycleScopeChangeUpsertCache[key]
	workspaceCycleScopeChangeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceCycleScopeChangeAllColumns,
			workspaceCycleScopeChangeColumnsWithDefault,
			workspaceCycleScopeChangeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceCycleScopeChangeAllColumns,
			workspaceCycleScopeChangePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_cycle_scope_changes, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceCycleScopeChangeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceCycleScopeChangePrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_cycle_scope_changes, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceCycleScopeChangePrimaryKeyColumns))
			copy(conflict, workspaceCycleScopeChangePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_cycle_scope_changes\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceCycleScopeChangeType, workspaceCycleScopeChangeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceCycleScopeChangeType, workspaceCycleScopeChangeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_cycle_scope_changes")
	}

	if !cached {
		workspaceCycleScopeChangeUpsertCacheMut.Lock()
		workspaceCycleScopeChangeUpsertCache[key] = cache
		workspaceCycleScopeChangeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceCycleScopeChange record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceCycleScopeChange) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceCycleScopeChange provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceCycleScopeChangePrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_cycle_scope_changes\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_cycle_scope_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_cycle_scope_changes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceCycleScopeChangeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceCycleScopeChangeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_cycle_scope_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_cycle_scope_changes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceCycleScopeChangeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceCycleScopeChangeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCycleScopeChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_cycle_scope_changes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCycleScopeChangePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceCycleScopeChange slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_cycle_scope_changes")
	}

	if len(workspaceCycleScopeChangeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceCycleScopeChange) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceCycleScopeChange(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceCycleScopeChangeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceCycleScopeChangeSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCycleScopeChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_cycle_scope_changes\".* FROM \"workspace_cycle_scope_changes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCycleScopeChangePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceCycleScopeChangeSlice")
	}

	*o = slice

	return nil
}

// WorkspaceCycleScopeChangeExists checks if the WorkspaceCycleScopeChange row exists.
func WorkspaceCycleScopeChangeExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_cycle_scope_changes\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_cycle_scope_changes exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceCycleScopeChange row exists.
func (o *WorkspaceCycleScopeChange) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceCycleScopeChangeExists(ctx, exec, o.ID)
}
