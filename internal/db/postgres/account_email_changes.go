// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// AccountEmailChange is an object representing the database table.
type AccountEmailChange struct {
	ID          string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	AccountID   string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	NewEmail    string    `boil:"new_email" json:"new_email" toml:"new_email" yaml:"new_email"`
	TokenHash   []byte    `boil:"token_hash" json:"token_hash" toml:"token_hash" yaml:"token_hash"`
	RequestedAt time.Time `boil:"requested_at" json:"requested_at" toml:"requested_at" yaml:"requested_at"`
	ExpiresAt   time.Time `boil:"expires_at" json:"expires_at" toml:"expires_at" yaml:"expires_at"`
	ConfirmedAt null.Time `boil:"confirmed_at" json:"confirmed_at,omitempty" toml:"confirmed_at" yaml:"confirmed_at,omitempty"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *accountEmailChangeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L accountEmailChangeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AccountEmailChangeColumns = struct {
	ID          string
	AccountID   string
	NewEmail    string
	TokenHash   string
	RequestedAt string
	ExpiresAt   string
	ConfirmedAt string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	AccountID:   "account_id",
	NewEmail:    "new_email",
	TokenHash:   "token_hash",
	RequestedAt: "requested_at",
	ExpiresAt:   "expires_at",
	ConfirmedAt: "confirmed_at",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var AccountEmailChangeTableColumns = struct {
	ID          string
	AccountID   string
	NewEmail    string
	TokenHash   string
	RequestedAt string
	ExpiresAt   string
	ConfirmedAt string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "account_email_changes.id",
	AccountID:   "account_email_changes.account_id",
	NewEmail:    "account_email_changes.new_email",
	TokenHash:   "account_email_changes.token_hash",
	RequestedAt: "account_email_changes.requested_at",
	ExpiresAt:   "account_email_changes.expires_at",
	ConfirmedAt: "account_email_changes.confirmed_at",
	CreatedAt:   "account_email_changes.created_at",
	UpdatedAt:   "account_email_changes.updated_at",
}

// Generated where

type whereHelperstring struct{ field string }

func (w whereHelperstring) EQ(x string) qm.QueryMod      { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperstring) NEQ(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperstring) LT(x string) qm.QueryMod      { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperstring) LTE(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperstring) GT(x string) qm.QueryMod      { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperstring) GTE(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperstring) LIKE(x string) qm.QueryMod    { return qm.Where(w.field+" LIKE ?", x) }
func (w whereHelperstring) NLIKE(x string) qm.QueryMod   { return qm.Where(w.field+" NOT LIKE ?", x) }
func (w whereHelperstring) ILIKE(x string) qm.QueryMod   { return qm.Where(w.field+" ILIKE ?", x) }
func (w whereHelperstring) NILIKE(x string) qm.QueryMod  { return qm.Where(w.field+" NOT ILIKE ?", x) }
func (w whereHelperstring) SIMILAR(x string) qm.QueryMod { return qm.Where(w.field+" SIMILAR TO ?", x) }
func (w whereHelperstring) NSIMILAR(x string) qm.QueryMod {
	return qm.Where(w.field+" NOT SIMILAR TO ?", x)
}
func (w whereHelperstring) IN(slice []string) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperstring) NIN(slice []string) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelper__byte struct{ field string }

func (w whereHelper__byte) EQ(x []byte) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelper__byte) NEQ(x []byte) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelper__byte) LT(x []byte) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelper__byte) LTE(x []byte) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelper__byte) GT(x []byte) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelper__byte) GTE(x []byte) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelpernull_Time struct{ field string }

func (w whereHelpernull_Time) EQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Time) NEQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Time) LT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Time) LTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Time) GT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Time) GTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpernull_Time) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Time) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var AccountEmailChangeWhere = struct {
	ID          whereHelperstring
	AccountID   whereHelperstring
	NewEmail    whereHelperstring
	TokenHash   whereHelper__byte
	RequestedAt whereHelpertime_Time
	ExpiresAt   whereHelpertime_Time
	ConfirmedAt whereHelpernull_Time
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"account_email_changes\".\"id\""},
	AccountID:   whereHelperstring{field: "\"account_email_changes\".\"account_id\""},
	NewEmail:    whereHelperstring{field: "\"account_email_changes\".\"new_email\""},
	TokenHash:   whereHelper__byte{field: "\"account_email_changes\".\"token_hash\""},
	RequestedAt: whereHelpertime_Time{field: "\"account_email_changes\".\"requested_at\""},
	ExpiresAt:   whereHelpertime_Time{field: "\"account_email_changes\".\"expires_at\""},
	ConfirmedAt: whereHelpernull_Time{field: "\"account_email_changes\".\"confirmed_at\""},
	CreatedAt:   whereHelpertime_Time{field: "\"account_email_changes\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"account_email_changes\".\"updated_at\""},
}

// AccountEmailChangeRels is where relationship names are stored.
var AccountEmailChangeRels = struct {
	Account string
}{
	Account: "Account",
}

// accountEmailChangeR is where relationships are stored.
type accountEmailChangeR struct {
	Account *Account `boil:"Account" json:"Account" toml:"Account" yaml:"Account"`
}

// NewStruct creates a new relationship struct
func (*accountEmailChangeR) NewStruct() *accountEmailChangeR {
	return &accountEmailChangeR{}
}

func (o *AccountEmailChange) GetAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetAccount()
}

func (r *accountEmailChangeR) GetAccount() *Account {
	if r == nil {
		return nil
	}

	return r.Account
}

// accountEmailChangeL is where Load methods for each relationship are stored.
type accountEmailChangeL struct{}

var (
	accountEmailChangeAllColumns            = []string{"id", "account_id", "new_email", "token_hash", "requested_at", "expires_at", "confirmed_at", "created_at", "updated_at"}
	accountEmailChangeColumnsWithoutDefault = []string{"account_id", "new_email", "token_hash", "expires_at"}
	accountEmailChangeColumnsWithDefault    = []string{"id", "requested_at", "confirmed_at", "created_at", "updated_at"}
	accountEmailChangePrimaryKeyColumns     = []string{"id"}
	accountEmailChangeGeneratedColumns      = []string{}
)

type (
	// AccountEmailChangeSlice is an alias for a slice of pointers to AccountEmailChange.
	// This should almost always be used instead of []AccountEmailChange.
	AccountEmailChangeSlice []*AccountEmailChange
	// AccountEmailChangeHook is the signature for custom AccountEmailChange hook methods
	AccountEmailChangeHook func(context.Context, boil.ContextExecutor, *AccountEmailChange) error

	accountEmailChangeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	accountEmailChangeType                 = reflect.TypeOf(&AccountEmailChange{})
	accountEmailChangeMapping              = queries.MakeStructMapping(accountEmailChangeType)
	accountEmailChangePrimaryKeyMapping, _ = queries.BindMapping(accountEmailChangeType, accountEmailChangeMapping, accountEmailChangePrimaryKeyColumns)
	accountEmailChangeInsertCacheMut       sync.RWMutex
	accountEmailChangeInsertCache          = make(map[string]insertCache)
	accountEmailChangeUpdateCacheMut       sync.RWMutex
	accountEmailChangeUpdateCache          = make(map[string]updateCache)
	accountEmailChangeUpsertCacheMut       sync.RWMutex
	accountEmailChangeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var accountEmailChangeAfterSelectMu sync.Mutex
var accountEmailChangeAfterSelectHooks []AccountEmailChangeHook

var accountEmailChangeBeforeInsertMu sync.Mutex
var accountEmailChangeBeforeInsertHooks []AccountEmailChangeHook
var accountEmailChangeAfterInsertMu sync.Mutex
var accountEmailChangeAfterInsertHooks []AccountEmailChangeHook

var accountEmailChangeBeforeUpdateMu sync.Mutex
var accountEmailChangeBeforeUpdateHooks []AccountEmailChangeHook
var accountEmailChangeAfterUpdateMu sync.Mutex
var accountEmailChangeAfterUpdateHooks []AccountEmailChangeHook

var accountEmailChangeBeforeDeleteMu sync.Mutex
var accountEmailChangeBeforeDeleteHooks []AccountEmailChangeHook
var accountEmailChangeAfterDeleteMu sync.Mutex
var accountEmailChangeAfterDeleteHooks []AccountEmailChangeHook

var accountEmailChangeBeforeUpsertMu sync.Mutex
var accountEmailChangeBeforeUpsertHooks []AccountEmailChangeHook
var accountEmailChangeAfterUpsertMu sync.Mutex
var accountEmailChangeAfterUpsertHooks []AccountEmailChangeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AccountEmailChange) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AccountEmailChange) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AccountEmailChange) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AccountEmailChange) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AccountEmailChange) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AccountEmailChange) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AccountEmailChange) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AccountEmailChange) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AccountEmailChange) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range accountEmailChangeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAccountEmailChangeHook registers your hook function for all future operations.
func AddAccountEmailChangeHook(hookPoint boil.HookPoint, accountEmailChangeHook AccountEmailChangeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		accountEmailChangeAfterSelectMu.Lock()
		accountEmailChangeAfterSelectHooks = append(accountEmailChangeAfterSelectHooks, accountEmailChangeHook)
		accountEmailChangeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		accountEmailChangeBeforeInsertMu.Lock()
		accountEmailChangeBeforeInsertHooks = append(accountEmailChangeBeforeInsertHooks, accountEmailChangeHook)
		accountEmailChangeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		accountEmailChangeAfterInsertMu.Lock()
		accountEmailChangeAfterInsertHooks = append(accountEmailChangeAfterInsertHooks, accountEmailChangeHook)
		accountEmailChangeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		accountEmailChangeBeforeUpdateMu.Lock()
		accountEmailChangeBeforeUpdateHooks = append(accountEmailChangeBeforeUpdateHooks, accountEmailChangeHook)
		accountEmailChangeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		accountEmailChangeAfterUpdateMu.Lock()
		accountEmailChangeAfterUpdateHooks = append(accountEmailChangeAfterUpdateHooks, accountEmailChangeHook)
		accountEmailChangeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		accountEmailChangeBeforeDeleteMu.Lock()
		accountEmailChangeBeforeDeleteHooks = append(accountEmailChangeBeforeDeleteHooks, accountEmailChangeHook)
		accountEmailChangeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		accountEmailChangeAfterDeleteMu.Lock()
		accountEmailChangeAfterDeleteHooks = append(accountEmailChangeAfterDeleteHooks, accountEmailChangeHook)
		accountEmailChangeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		accountEmailChangeBeforeUpsertMu.Lock()
		accountEmailChangeBeforeUpsertHooks = append(accountEmailChangeBeforeUpsertHooks, accountEmailChangeHook)
		accountEmailChangeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		accountEmailChangeAfterUpsertMu.Lock()
		accountEmailChangeAfterUpsertHooks = append(accountEmailChangeAfterUpsertHooks, accountEmailChangeHook)
		accountEmailChangeAfterUpsertMu.Unlock()
	}
}

// One returns a single accountEmailChange record from the query.
func (q accountEmailChangeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AccountEmailChange, error) {
	o := &AccountEmailChange{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for account_email_changes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AccountEmailChange records from the query.
func (q accountEmailChangeQuery) All(ctx context.Context, exec boil.ContextExecutor) (AccountEmailChangeSlice, error) {
	var o []*AccountEmailChange

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to AccountEmailChange slice")
	}

	if len(accountEmailChangeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AccountEmailChange records in the query.
func (q accountEmailChangeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count account_email_changes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q accountEmailChangeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if account_email_changes exists")
	}

	return count > 0, nil
}

// Account pointed to by the foreign key.
func (o *AccountEmailChange) Account(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// LoadAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (accountEmailChangeL) LoadAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAccountEmailChange any, mods queries.Applicator) error {
	var slice []*AccountEmailChange
	var object *AccountEmailChange

	if singular {
		var ok bool
		object, ok = maybeAccountEmailChange.(*AccountEmailChange)
		if !ok {
			object = new(AccountEmailChange)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAccountEmailChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAccountEmailChange))
			}
		}
	} else {
		s, ok := maybeAccountEmailChange.(*[]*AccountEmailChange)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAccountEmailChange)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAccountEmailChange))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &accountEmailChangeR{}
		}
		args[object.AccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &accountEmailChangeR{}
			}

			args[obj.AccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Account = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.AccountEmailChanges = append(foreign.R.AccountEmailChanges, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountID == foreign.ID {
				local.R.Account = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.AccountEmailChanges = append(foreign.R.AccountEmailChanges, local)
				break
			}
		}
	}

	return nil
}

// SetAccount of the accountEmailChange to the related item.
// Sets o.R.Account to related.
// Adds o to related.R.AccountEmailChanges.
func (o *AccountEmailChange) SetAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"account_email_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
		strmangle.WhereClause("\"", "\"", 2, accountEmailChangePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountID = related.ID
	if o.R == nil {
		o.R = &accountEmailChangeR{
			Account: related,
		}
	} else {
		o.R.Account = related
	}

	if related.R == nil {
		related.R = &accountR{
			AccountEmailChanges: AccountEmailChangeSlice{o},
		}
	} else {
		related.R.AccountEmailChanges = append(related.R.AccountEmailChanges, o)
	}

	return nil
}

// AccountEmailChanges retrieves all the records using an executor.
func AccountEmailChanges(mods ...qm.QueryMod) accountEmailChangeQuery {
	mods = append(mods, qm.From("\"account_email_changes\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"account_email_changes\".*"})
	}

	return accountEmailChangeQuery{q}
}

// FindAccountEmailChange retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAccountEmailChange(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*AccountEmailChange, error) {
	accountEmailChangeObj := &AccountEmailChange{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"account_email_changes\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, accountEmailChangeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from account_email_changes")
	}

	if err = accountEmailChangeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return accountEmailChangeObj, err
	}

	return accountEmailChangeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AccountEmailChange) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no account_email_changes provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountEmailChangeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	accountEmailChangeInsertCacheMut.RLock()
	cache, cached := accountEmailChangeInsertCache[key]
	accountEmailChangeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			accountEmailChangeAllColumns,
			accountEmailChangeColumnsWithDefault,
			accountEmailChangeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(accountEmailChangeType, accountEmailChangeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(accountEmailChangeType, accountEmailChangeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"account_email_changes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"account_email_changes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into account_email_changes")
	}

	if !cached {
		accountEmailChangeInsertCacheMut.Lock()
		accountEmailChangeInsertCache[key] = cache
		accountEmailChangeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AccountEmailChange.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AccountEmailChange) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	accountEmailChangeUpdateCacheMut.RLock()
	cache, cached := accountEmailChangeUpdateCache[key]
	accountEmailChangeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			accountEmailChangeAllColumns,
			accountEmailChangePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update account_email_changes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"account_email_changes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, accountEmailChangePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(accountEmailChangeType, accountEmailChangeMapping, append(wl, accountEmailChangePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update account_email_changes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for account_email_changes")
	}

	if !cached {
		accountEmailChangeUpdateCacheMut.Lock()
		accountEmailChangeUpdateCache[key] = cache
		accountEmailChangeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q accountEmailChangeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for account_email_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for account_email_changes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AccountEmailChangeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountEmailChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"account_email_changes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, accountEmailChangePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in accountEmailChange slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all accountEmailChange")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AccountEmailChange) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no account_email_changes provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(accountEmailChangeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	accountEmailChangeUpsertCacheMut.RLock()
	cache, cached := accountEmailChangeUpsertCache[key]
	accountEmailChangeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			accountEmailChangeAllColumns,
			accountEmailChangeColumnsWithDefault,
			accountEmailChangeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			accountEmailChangeAllColumns,
			accountEmailChangePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert account_email_changes, could not build update column list")
		}

		ret := strmangle.SetComplement(accountEmailChangeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(accountEmailChangePrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert account_email_changes, could not build conflict column list")
			}

			conflict = make([]string, len(accountEmailChangePrimaryKeyColumns))
			copy(conflict, accountEmailChangePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"account_email_changes\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(accountEmailChangeType, accountEmailChangeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(accountEmailChangeType, accountEmailChangeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert account_email_changes")
	}

	if !cached {
		accountEmailChangeUpsertCacheMut.Lock()
		accountEmailChangeUpsertCache[key] = cache
		accountEmailChangeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AccountEmailChange record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AccountEmailChange) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no AccountEmailChange provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), accountEmailChangePrimaryKeyMapping)
	sql := "DELETE FROM \"account_email_changes\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from account_email_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for account_email_changes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q accountEmailChangeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no accountEmailChangeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from account_email_changes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for account_email_changes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AccountEmailChangeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(accountEmailChangeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountEmailChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"account_email_changes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, accountEmailChangePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from accountEmailChange slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for account_email_changes")
	}

	if len(accountEmailChangeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AccountEmailChange) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAccountEmailChange(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AccountEmailChangeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AccountEmailChangeSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountEmailChangePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"account_email_changes\".* FROM \"account_email_changes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, accountEmailChangePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in AccountEmailChangeSlice")
	}

	*o = slice

	return nil
}

// AccountEmailChangeExists checks if the AccountEmailChange row exists.
func AccountEmailChangeExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"account_email_changes\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if account_email_changes exists")
	}

	return exists, nil
}

// Exists checks if the AccountEmailChange row exists.
func (o *AccountEmailChange) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AccountEmailChangeExists(ctx, exec, o.ID)
}
