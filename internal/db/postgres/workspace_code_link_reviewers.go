// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceCodeLinkReviewer is an object representing the database table.
type WorkspaceCodeLinkReviewer struct {
	LinkID     string    `boil:"link_id" json:"link_id" toml:"link_id" yaml:"link_id"`
	Login      string    `boil:"login" json:"login" toml:"login" yaml:"login"`
	Verdict    string    `boil:"verdict" json:"verdict" toml:"verdict" yaml:"verdict"`
	URL        string    `boil:"url" json:"url" toml:"url" yaml:"url"`
	ReviewedAt null.Time `boil:"reviewed_at" json:"reviewed_at,omitempty" toml:"reviewed_at" yaml:"reviewed_at,omitempty"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceCodeLinkReviewerR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceCodeLinkReviewerL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceCodeLinkReviewerColumns = struct {
	LinkID     string
	Login      string
	Verdict    string
	URL        string
	ReviewedAt string
	UpdatedAt  string
}{
	LinkID:     "link_id",
	Login:      "login",
	Verdict:    "verdict",
	URL:        "url",
	ReviewedAt: "reviewed_at",
	UpdatedAt:  "updated_at",
}

var WorkspaceCodeLinkReviewerTableColumns = struct {
	LinkID     string
	Login      string
	Verdict    string
	URL        string
	ReviewedAt string
	UpdatedAt  string
}{
	LinkID:     "workspace_code_link_reviewers.link_id",
	Login:      "workspace_code_link_reviewers.login",
	Verdict:    "workspace_code_link_reviewers.verdict",
	URL:        "workspace_code_link_reviewers.url",
	ReviewedAt: "workspace_code_link_reviewers.reviewed_at",
	UpdatedAt:  "workspace_code_link_reviewers.updated_at",
}

// Generated where

var WorkspaceCodeLinkReviewerWhere = struct {
	LinkID     whereHelperstring
	Login      whereHelperstring
	Verdict    whereHelperstring
	URL        whereHelperstring
	ReviewedAt whereHelpernull_Time
	UpdatedAt  whereHelpertime_Time
}{
	LinkID:     whereHelperstring{field: "\"workspace_code_link_reviewers\".\"link_id\""},
	Login:      whereHelperstring{field: "\"workspace_code_link_reviewers\".\"login\""},
	Verdict:    whereHelperstring{field: "\"workspace_code_link_reviewers\".\"verdict\""},
	URL:        whereHelperstring{field: "\"workspace_code_link_reviewers\".\"url\""},
	ReviewedAt: whereHelpernull_Time{field: "\"workspace_code_link_reviewers\".\"reviewed_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"workspace_code_link_reviewers\".\"updated_at\""},
}

// WorkspaceCodeLinkReviewerRels is where relationship names are stored.
var WorkspaceCodeLinkReviewerRels = struct {
	Link string
}{
	Link: "Link",
}

// workspaceCodeLinkReviewerR is where relationships are stored.
type workspaceCodeLinkReviewerR struct {
	Link *WorkspaceCodeLink `boil:"Link" json:"Link" toml:"Link" yaml:"Link"`
}

// NewStruct creates a new relationship struct
func (*workspaceCodeLinkReviewerR) NewStruct() *workspaceCodeLinkReviewerR {
	return &workspaceCodeLinkReviewerR{}
}

func (o *WorkspaceCodeLinkReviewer) GetLink() *WorkspaceCodeLink {
	if o == nil {
		return nil
	}

	return o.R.GetLink()
}

func (r *workspaceCodeLinkReviewerR) GetLink() *WorkspaceCodeLink {
	if r == nil {
		return nil
	}

	return r.Link
}

// workspaceCodeLinkReviewerL is where Load methods for each relationship are stored.
type workspaceCodeLinkReviewerL struct{}

var (
	workspaceCodeLinkReviewerAllColumns            = []string{"link_id", "login", "verdict", "url", "reviewed_at", "updated_at"}
	workspaceCodeLinkReviewerColumnsWithoutDefault = []string{"link_id", "login", "verdict"}
	workspaceCodeLinkReviewerColumnsWithDefault    = []string{"url", "reviewed_at", "updated_at"}
	workspaceCodeLinkReviewerPrimaryKeyColumns     = []string{"link_id", "login"}
	workspaceCodeLinkReviewerGeneratedColumns      = []string{}
)

type (
	// WorkspaceCodeLinkReviewerSlice is an alias for a slice of pointers to WorkspaceCodeLinkReviewer.
	// This should almost always be used instead of []WorkspaceCodeLinkReviewer.
	WorkspaceCodeLinkReviewerSlice []*WorkspaceCodeLinkReviewer
	// WorkspaceCodeLinkReviewerHook is the signature for custom WorkspaceCodeLinkReviewer hook methods
	WorkspaceCodeLinkReviewerHook func(context.Context, boil.ContextExecutor, *WorkspaceCodeLinkReviewer) error

	workspaceCodeLinkReviewerQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceCodeLinkReviewerType                 = reflect.TypeOf(&WorkspaceCodeLinkReviewer{})
	workspaceCodeLinkReviewerMapping              = queries.MakeStructMapping(workspaceCodeLinkReviewerType)
	workspaceCodeLinkReviewerPrimaryKeyMapping, _ = queries.BindMapping(workspaceCodeLinkReviewerType, workspaceCodeLinkReviewerMapping, workspaceCodeLinkReviewerPrimaryKeyColumns)
	workspaceCodeLinkReviewerInsertCacheMut       sync.RWMutex
	workspaceCodeLinkReviewerInsertCache          = make(map[string]insertCache)
	workspaceCodeLinkReviewerUpdateCacheMut       sync.RWMutex
	workspaceCodeLinkReviewerUpdateCache          = make(map[string]updateCache)
	workspaceCodeLinkReviewerUpsertCacheMut       sync.RWMutex
	workspaceCodeLinkReviewerUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceCodeLinkReviewerAfterSelectMu sync.Mutex
var workspaceCodeLinkReviewerAfterSelectHooks []WorkspaceCodeLinkReviewerHook

var workspaceCodeLinkReviewerBeforeInsertMu sync.Mutex
var workspaceCodeLinkReviewerBeforeInsertHooks []WorkspaceCodeLinkReviewerHook
var workspaceCodeLinkReviewerAfterInsertMu sync.Mutex
var workspaceCodeLinkReviewerAfterInsertHooks []WorkspaceCodeLinkReviewerHook

var workspaceCodeLinkReviewerBeforeUpdateMu sync.Mutex
var workspaceCodeLinkReviewerBeforeUpdateHooks []WorkspaceCodeLinkReviewerHook
var workspaceCodeLinkReviewerAfterUpdateMu sync.Mutex
var workspaceCodeLinkReviewerAfterUpdateHooks []WorkspaceCodeLinkReviewerHook

var workspaceCodeLinkReviewerBeforeDeleteMu sync.Mutex
var workspaceCodeLinkReviewerBeforeDeleteHooks []WorkspaceCodeLinkReviewerHook
var workspaceCodeLinkReviewerAfterDeleteMu sync.Mutex
var workspaceCodeLinkReviewerAfterDeleteHooks []WorkspaceCodeLinkReviewerHook

var workspaceCodeLinkReviewerBeforeUpsertMu sync.Mutex
var workspaceCodeLinkReviewerBeforeUpsertHooks []WorkspaceCodeLinkReviewerHook
var workspaceCodeLinkReviewerAfterUpsertMu sync.Mutex
var workspaceCodeLinkReviewerAfterUpsertHooks []WorkspaceCodeLinkReviewerHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceCodeLinkReviewer) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceCodeLinkReviewer) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceCodeLinkReviewer) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceCodeLinkReviewer) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceCodeLinkReviewer) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceCodeLinkReviewer) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceCodeLinkReviewer) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceCodeLinkReviewer) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceCodeLinkReviewer) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkReviewerAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceCodeLinkReviewerHook registers your hook function for all future operations.
func AddWorkspaceCodeLinkReviewerHook(hookPoint boil.HookPoint, workspaceCodeLinkReviewerHook WorkspaceCodeLinkReviewerHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceCodeLinkReviewerAfterSelectMu.Lock()
		workspaceCodeLinkReviewerAfterSelectHooks = append(workspaceCodeLinkReviewerAfterSelectHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceCodeLinkReviewerBeforeInsertMu.Lock()
		workspaceCodeLinkReviewerBeforeInsertHooks = append(workspaceCodeLinkReviewerBeforeInsertHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceCodeLinkReviewerAfterInsertMu.Lock()
		workspaceCodeLinkReviewerAfterInsertHooks = append(workspaceCodeLinkReviewerAfterInsertHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceCodeLinkReviewerBeforeUpdateMu.Lock()
		workspaceCodeLinkReviewerBeforeUpdateHooks = append(workspaceCodeLinkReviewerBeforeUpdateHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceCodeLinkReviewerAfterUpdateMu.Lock()
		workspaceCodeLinkReviewerAfterUpdateHooks = append(workspaceCodeLinkReviewerAfterUpdateHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceCodeLinkReviewerBeforeDeleteMu.Lock()
		workspaceCodeLinkReviewerBeforeDeleteHooks = append(workspaceCodeLinkReviewerBeforeDeleteHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceCodeLinkReviewerAfterDeleteMu.Lock()
		workspaceCodeLinkReviewerAfterDeleteHooks = append(workspaceCodeLinkReviewerAfterDeleteHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceCodeLinkReviewerBeforeUpsertMu.Lock()
		workspaceCodeLinkReviewerBeforeUpsertHooks = append(workspaceCodeLinkReviewerBeforeUpsertHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceCodeLinkReviewerAfterUpsertMu.Lock()
		workspaceCodeLinkReviewerAfterUpsertHooks = append(workspaceCodeLinkReviewerAfterUpsertHooks, workspaceCodeLinkReviewerHook)
		workspaceCodeLinkReviewerAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceCodeLinkReviewer record from the query.
func (q workspaceCodeLinkReviewerQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceCodeLinkReviewer, error) {
	o := &WorkspaceCodeLinkReviewer{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_code_link_reviewers")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceCodeLinkReviewer records from the query.
func (q workspaceCodeLinkReviewerQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceCodeLinkReviewerSlice, error) {
	var o []*WorkspaceCodeLinkReviewer

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceCodeLinkReviewer slice")
	}

	if len(workspaceCodeLinkReviewerAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceCodeLinkReviewer records in the query.
func (q workspaceCodeLinkReviewerQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_code_link_reviewers rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceCodeLinkReviewerQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_code_link_reviewers exists")
	}

	return count > 0, nil
}

// Link pointed to by the foreign key.
func (o *WorkspaceCodeLinkReviewer) Link(mods ...qm.QueryMod) workspaceCodeLinkQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.LinkID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceCodeLinks(queryMods...)
}

// LoadLink allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCodeLinkReviewerL) LoadLink(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodeLinkReviewer any, mods queries.Applicator) error {
	var slice []*WorkspaceCodeLinkReviewer
	var object *WorkspaceCodeLinkReviewer

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodeLinkReviewer.(*WorkspaceCodeLinkReviewer)
		if !ok {
			object = new(WorkspaceCodeLinkReviewer)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodeLinkReviewer)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodeLinkReviewer))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodeLinkReviewer.(*[]*WorkspaceCodeLinkReviewer)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodeLinkReviewer)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodeLinkReviewer))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodeLinkReviewerR{}
		}
		args[object.LinkID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodeLinkReviewerR{}
			}

			args[obj.LinkID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_code_links`),
		qm.WhereIn(`workspace_code_links.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceCodeLink")
	}

	var resultSlice []*WorkspaceCodeLink
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceCodeLink")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_code_links")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_code_links")
	}

	if len(workspaceCodeLinkAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Link = foreign
		if foreign.R == nil {
			foreign.R = &workspaceCodeLinkR{}
		}
		foreign.R.LinkWorkspaceCodeLinkReviewers = append(foreign.R.LinkWorkspaceCodeLinkReviewers, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.LinkID == foreign.ID {
				local.R.Link = foreign
				if foreign.R == nil {
					foreign.R = &workspaceCodeLinkR{}
				}
				foreign.R.LinkWorkspaceCodeLinkReviewers = append(foreign.R.LinkWorkspaceCodeLinkReviewers, local)
				break
			}
		}
	}

	return nil
}

// SetLink of the workspaceCodeLinkReviewer to the related item.
// Sets o.R.Link to related.
// Adds o to related.R.LinkWorkspaceCodeLinkReviewers.
func (o *WorkspaceCodeLinkReviewer) SetLink(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceCodeLink) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_code_link_reviewers\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"link_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCodeLinkReviewerPrimaryKeyColumns),
	)
	values := []any{related.ID, o.LinkID, o.Login}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.LinkID = related.ID
	if o.R == nil {
		o.R = &workspaceCodeLinkReviewerR{
			Link: related,
		}
	} else {
		o.R.Link = related
	}

	if related.R == nil {
		related.R = &workspaceCodeLinkR{
			LinkWorkspaceCodeLinkReviewers: WorkspaceCodeLinkReviewerSlice{o},
		}
	} else {
		related.R.LinkWorkspaceCodeLinkReviewers = append(related.R.LinkWorkspaceCodeLinkReviewers, o)
	}

	return nil
}

// WorkspaceCodeLinkReviewers retrieves all the records using an executor.
func WorkspaceCodeLinkReviewers(mods ...qm.QueryMod) workspaceCodeLinkReviewerQuery {
	mods = append(mods, qm.From("\"workspace_code_link_reviewers\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_code_link_reviewers\".*"})
	}

	return workspaceCodeLinkReviewerQuery{q}
}

// FindWorkspaceCodeLinkReviewer retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceCodeLinkReviewer(ctx context.Context, exec boil.ContextExecutor, linkID string, login string, selectCols ...string) (*WorkspaceCodeLinkReviewer, error) {
	workspaceCodeLinkReviewerObj := &WorkspaceCodeLinkReviewer{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_code_link_reviewers\" where \"link_id\"=$1 AND \"login\"=$2", sel,
	)

	q := queries.Raw(query, linkID, login)

	err := q.Bind(ctx, exec, workspaceCodeLinkReviewerObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_code_link_reviewers")
	}

	if err = workspaceCodeLinkReviewerObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceCodeLinkReviewerObj, err
	}

	return workspaceCodeLinkReviewerObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceCodeLinkReviewer) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_code_link_reviewers provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCodeLinkReviewerColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceCodeLinkReviewerInsertCacheMut.RLock()
	cache, cached := workspaceCodeLinkReviewerInsertCache[key]
	workspaceCodeLinkReviewerInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceCodeLinkReviewerAllColumns,
			workspaceCodeLinkReviewerColumnsWithDefault,
			workspaceCodeLinkReviewerColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceCodeLinkReviewerType, workspaceCodeLinkReviewerMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceCodeLinkReviewerType, workspaceCodeLinkReviewerMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_code_link_reviewers\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_code_link_reviewers\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_code_link_reviewers")
	}

	if !cached {
		workspaceCodeLinkReviewerInsertCacheMut.Lock()
		workspaceCodeLinkReviewerInsertCache[key] = cache
		workspaceCodeLinkReviewerInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceCodeLinkReviewer.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceCodeLinkReviewer) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceCodeLinkReviewerUpdateCacheMut.RLock()
	cache, cached := workspaceCodeLinkReviewerUpdateCache[key]
	workspaceCodeLinkReviewerUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceCodeLinkReviewerAllColumns,
			workspaceCodeLinkReviewerPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_code_link_reviewers, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_code_link_reviewers\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceCodeLinkReviewerPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceCodeLinkReviewerType, workspaceCodeLinkReviewerMapping, append(wl, workspaceCodeLinkReviewerPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_code_link_reviewers row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_code_link_reviewers")
	}

	if !cached {
		workspaceCodeLinkReviewerUpdateCacheMut.Lock()
		workspaceCodeLinkReviewerUpdateCache[key] = cache
		workspaceCodeLinkReviewerUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceCodeLinkReviewerQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_code_link_reviewers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_code_link_reviewers")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceCodeLinkReviewerSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodeLinkReviewerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_code_link_reviewers\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceCodeLinkReviewerPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceCodeLinkReviewer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceCodeLinkReviewer")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceCodeLinkReviewer) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_code_link_reviewers provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCodeLinkReviewerColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceCodeLinkReviewerUpsertCacheMut.RLock()
	cache, cached := workspaceCodeLinkReviewerUpsertCache[key]
	workspaceCodeLinkReviewerUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceCodeLinkReviewerAllColumns,
			workspaceCodeLinkReviewerColumnsWithDefault,
			workspaceCodeLinkReviewerColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceCodeLinkReviewerAllColumns,
			workspaceCodeLinkReviewerPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_code_link_reviewers, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceCodeLinkReviewerAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceCodeLinkReviewerPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_code_link_reviewers, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceCodeLinkReviewerPrimaryKeyColumns))
			copy(conflict, workspaceCodeLinkReviewerPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_code_link_reviewers\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceCodeLinkReviewerType, workspaceCodeLinkReviewerMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceCodeLinkReviewerType, workspaceCodeLinkReviewerMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_code_link_reviewers")
	}

	if !cached {
		workspaceCodeLinkReviewerUpsertCacheMut.Lock()
		workspaceCodeLinkReviewerUpsertCache[key] = cache
		workspaceCodeLinkReviewerUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceCodeLinkReviewer record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceCodeLinkReviewer) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceCodeLinkReviewer provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceCodeLinkReviewerPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_code_link_reviewers\" WHERE \"link_id\"=$1 AND \"login\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_code_link_reviewers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_code_link_reviewers")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceCodeLinkReviewerQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceCodeLinkReviewerQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_code_link_reviewers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_code_link_reviewers")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceCodeLinkReviewerSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceCodeLinkReviewerBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodeLinkReviewerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_code_link_reviewers\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCodeLinkReviewerPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceCodeLinkReviewer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_code_link_reviewers")
	}

	if len(workspaceCodeLinkReviewerAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceCodeLinkReviewer) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceCodeLinkReviewer(ctx, exec, o.LinkID, o.Login)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceCodeLinkReviewerSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceCodeLinkReviewerSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodeLinkReviewerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_code_link_reviewers\".* FROM \"workspace_code_link_reviewers\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCodeLinkReviewerPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceCodeLinkReviewerSlice")
	}

	*o = slice

	return nil
}

// WorkspaceCodeLinkReviewerExists checks if the WorkspaceCodeLinkReviewer row exists.
func WorkspaceCodeLinkReviewerExists(ctx context.Context, exec boil.ContextExecutor, linkID string, login string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_code_link_reviewers\" where \"link_id\"=$1 AND \"login\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, linkID, login)
	}
	row := exec.QueryRowContext(ctx, sql, linkID, login)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_code_link_reviewers exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceCodeLinkReviewer row exists.
func (o *WorkspaceCodeLinkReviewer) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceCodeLinkReviewerExists(ctx, exec, o.LinkID, o.Login)
}
