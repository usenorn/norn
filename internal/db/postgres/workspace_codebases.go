// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceCodebasis is an object representing the database table.
type WorkspaceCodebasis struct {
	ID             string            `boil:"id" json:"id" toml:"id" yaml:"id"`
	RunnerID       string            `boil:"runner_id" json:"runner_id" toml:"runner_id" yaml:"runner_id"`
	Name           string            `boil:"name" json:"name" toml:"name" yaml:"name"`
	RootPath       string            `boil:"root_path" json:"root_path" toml:"root_path" yaml:"root_path"`
	State          string            `boil:"state" json:"state" toml:"state" yaml:"state"`
	SharedFiles    types.StringArray `boil:"shared_files" json:"shared_files" toml:"shared_files" yaml:"shared_files"`
	Runtimes       types.StringArray `boil:"runtimes" json:"runtimes" toml:"runtimes" yaml:"runtimes"`
	Tools          types.JSON        `boil:"tools" json:"tools" toml:"tools" yaml:"tools"`
	ConnectedAt    time.Time         `boil:"connected_at" json:"connected_at" toml:"connected_at" yaml:"connected_at"`
	LastSeenAt     null.Time         `boil:"last_seen_at" json:"last_seen_at,omitempty" toml:"last_seen_at" yaml:"last_seen_at,omitempty"`
	DisconnectedAt null.Time         `boil:"disconnected_at" json:"disconnected_at,omitempty" toml:"disconnected_at" yaml:"disconnected_at,omitempty"`
	UpdatedAt      time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	PreviewGateway string            `boil:"preview_gateway" json:"preview_gateway" toml:"preview_gateway" yaml:"preview_gateway"`

	R *workspaceCodebasisR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceCodebasisL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceCodebasisColumns = struct {
	ID             string
	RunnerID       string
	Name           string
	RootPath       string
	State          string
	SharedFiles    string
	Runtimes       string
	Tools          string
	ConnectedAt    string
	LastSeenAt     string
	DisconnectedAt string
	UpdatedAt      string
	PreviewGateway string
}{
	ID:             "id",
	RunnerID:       "runner_id",
	Name:           "name",
	RootPath:       "root_path",
	State:          "state",
	SharedFiles:    "shared_files",
	Runtimes:       "runtimes",
	Tools:          "tools",
	ConnectedAt:    "connected_at",
	LastSeenAt:     "last_seen_at",
	DisconnectedAt: "disconnected_at",
	UpdatedAt:      "updated_at",
	PreviewGateway: "preview_gateway",
}

var WorkspaceCodebasisTableColumns = struct {
	ID             string
	RunnerID       string
	Name           string
	RootPath       string
	State          string
	SharedFiles    string
	Runtimes       string
	Tools          string
	ConnectedAt    string
	LastSeenAt     string
	DisconnectedAt string
	UpdatedAt      string
	PreviewGateway string
}{
	ID:             "workspace_codebases.id",
	RunnerID:       "workspace_codebases.runner_id",
	Name:           "workspace_codebases.name",
	RootPath:       "workspace_codebases.root_path",
	State:          "workspace_codebases.state",
	SharedFiles:    "workspace_codebases.shared_files",
	Runtimes:       "workspace_codebases.runtimes",
	Tools:          "workspace_codebases.tools",
	ConnectedAt:    "workspace_codebases.connected_at",
	LastSeenAt:     "workspace_codebases.last_seen_at",
	DisconnectedAt: "workspace_codebases.disconnected_at",
	UpdatedAt:      "workspace_codebases.updated_at",
	PreviewGateway: "workspace_codebases.preview_gateway",
}

// Generated where

var WorkspaceCodebasisWhere = struct {
	ID             whereHelperstring
	RunnerID       whereHelperstring
	Name           whereHelperstring
	RootPath       whereHelperstring
	State          whereHelperstring
	SharedFiles    whereHelpertypes_StringArray
	Runtimes       whereHelpertypes_StringArray
	Tools          whereHelpertypes_JSON
	ConnectedAt    whereHelpertime_Time
	LastSeenAt     whereHelpernull_Time
	DisconnectedAt whereHelpernull_Time
	UpdatedAt      whereHelpertime_Time
	PreviewGateway whereHelperstring
}{
	ID:             whereHelperstring{field: "\"workspace_codebases\".\"id\""},
	RunnerID:       whereHelperstring{field: "\"workspace_codebases\".\"runner_id\""},
	Name:           whereHelperstring{field: "\"workspace_codebases\".\"name\""},
	RootPath:       whereHelperstring{field: "\"workspace_codebases\".\"root_path\""},
	State:          whereHelperstring{field: "\"workspace_codebases\".\"state\""},
	SharedFiles:    whereHelpertypes_StringArray{field: "\"workspace_codebases\".\"shared_files\""},
	Runtimes:       whereHelpertypes_StringArray{field: "\"workspace_codebases\".\"runtimes\""},
	Tools:          whereHelpertypes_JSON{field: "\"workspace_codebases\".\"tools\""},
	ConnectedAt:    whereHelpertime_Time{field: "\"workspace_codebases\".\"connected_at\""},
	LastSeenAt:     whereHelpernull_Time{field: "\"workspace_codebases\".\"last_seen_at\""},
	DisconnectedAt: whereHelpernull_Time{field: "\"workspace_codebases\".\"disconnected_at\""},
	UpdatedAt:      whereHelpertime_Time{field: "\"workspace_codebases\".\"updated_at\""},
	PreviewGateway: whereHelperstring{field: "\"workspace_codebases\".\"preview_gateway\""},
}

// WorkspaceCodebasisRels is where relationship names are stored.
var WorkspaceCodebasisRels = struct {
	Runner                                string
	CodebaseWorkspaceCodebaseRepositories string
	CodebaseWorkspaceExecutions           string
}{
	Runner:                                "Runner",
	CodebaseWorkspaceCodebaseRepositories: "CodebaseWorkspaceCodebaseRepositories",
	CodebaseWorkspaceExecutions:           "CodebaseWorkspaceExecutions",
}

// workspaceCodebasisR is where relationships are stored.
type workspaceCodebasisR struct {
	Runner                                *WorkspaceRunner                 `boil:"Runner" json:"Runner" toml:"Runner" yaml:"Runner"`
	CodebaseWorkspaceCodebaseRepositories WorkspaceCodebaseRepositorySlice `boil:"CodebaseWorkspaceCodebaseRepositories" json:"CodebaseWorkspaceCodebaseRepositories" toml:"CodebaseWorkspaceCodebaseRepositories" yaml:"CodebaseWorkspaceCodebaseRepositories"`
	CodebaseWorkspaceExecutions           WorkspaceExecutionSlice          `boil:"CodebaseWorkspaceExecutions" json:"CodebaseWorkspaceExecutions" toml:"CodebaseWorkspaceExecutions" yaml:"CodebaseWorkspaceExecutions"`
}

// NewStruct creates a new relationship struct
func (*workspaceCodebasisR) NewStruct() *workspaceCodebasisR {
	return &workspaceCodebasisR{}
}

func (o *WorkspaceCodebasis) GetRunner() *WorkspaceRunner {
	if o == nil {
		return nil
	}

	return o.R.GetRunner()
}

func (r *workspaceCodebasisR) GetRunner() *WorkspaceRunner {
	if r == nil {
		return nil
	}

	return r.Runner
}

func (o *WorkspaceCodebasis) GetCodebaseWorkspaceCodebaseRepositories() WorkspaceCodebaseRepositorySlice {
	if o == nil {
		return nil
	}

	return o.R.GetCodebaseWorkspaceCodebaseRepositories()
}

func (r *workspaceCodebasisR) GetCodebaseWorkspaceCodebaseRepositories() WorkspaceCodebaseRepositorySlice {
	if r == nil {
		return nil
	}

	return r.CodebaseWorkspaceCodebaseRepositories
}

func (o *WorkspaceCodebasis) GetCodebaseWorkspaceExecutions() WorkspaceExecutionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCodebaseWorkspaceExecutions()
}

func (r *workspaceCodebasisR) GetCodebaseWorkspaceExecutions() WorkspaceExecutionSlice {
	if r == nil {
		return nil
	}

	return r.CodebaseWorkspaceExecutions
}

// workspaceCodebasisL is where Load methods for each relationship are stored.
type workspaceCodebasisL struct{}

var (
	workspaceCodebasisAllColumns            = []string{"id", "runner_id", "name", "root_path", "state", "shared_files", "runtimes", "tools", "connected_at", "last_seen_at", "disconnected_at", "updated_at", "preview_gateway"}
	workspaceCodebasisColumnsWithoutDefault = []string{"runner_id", "name", "root_path"}
	workspaceCodebasisColumnsWithDefault    = []string{"id", "state", "shared_files", "runtimes", "tools", "connected_at", "last_seen_at", "disconnected_at", "updated_at", "preview_gateway"}
	workspaceCodebasisPrimaryKeyColumns     = []string{"id"}
	workspaceCodebasisGeneratedColumns      = []string{}
)

type (
	// WorkspaceCodebasisSlice is an alias for a slice of pointers to WorkspaceCodebasis.
	// This should almost always be used instead of []WorkspaceCodebasis.
	WorkspaceCodebasisSlice []*WorkspaceCodebasis
	// WorkspaceCodebasisHook is the signature for custom WorkspaceCodebasis hook methods
	WorkspaceCodebasisHook func(context.Context, boil.ContextExecutor, *WorkspaceCodebasis) error

	workspaceCodebasisQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceCodebasisType                 = reflect.TypeOf(&WorkspaceCodebasis{})
	workspaceCodebasisMapping              = queries.MakeStructMapping(workspaceCodebasisType)
	workspaceCodebasisPrimaryKeyMapping, _ = queries.BindMapping(workspaceCodebasisType, workspaceCodebasisMapping, workspaceCodebasisPrimaryKeyColumns)
	workspaceCodebasisInsertCacheMut       sync.RWMutex
	workspaceCodebasisInsertCache          = make(map[string]insertCache)
	workspaceCodebasisUpdateCacheMut       sync.RWMutex
	workspaceCodebasisUpdateCache          = make(map[string]updateCache)
	workspaceCodebasisUpsertCacheMut       sync.RWMutex
	workspaceCodebasisUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceCodebasisAfterSelectMu sync.Mutex
var workspaceCodebasisAfterSelectHooks []WorkspaceCodebasisHook

var workspaceCodebasisBeforeInsertMu sync.Mutex
var workspaceCodebasisBeforeInsertHooks []WorkspaceCodebasisHook
var workspaceCodebasisAfterInsertMu sync.Mutex
var workspaceCodebasisAfterInsertHooks []WorkspaceCodebasisHook

var workspaceCodebasisBeforeUpdateMu sync.Mutex
var workspaceCodebasisBeforeUpdateHooks []WorkspaceCodebasisHook
var workspaceCodebasisAfterUpdateMu sync.Mutex
var workspaceCodebasisAfterUpdateHooks []WorkspaceCodebasisHook

var workspaceCodebasisBeforeDeleteMu sync.Mutex
var workspaceCodebasisBeforeDeleteHooks []WorkspaceCodebasisHook
var workspaceCodebasisAfterDeleteMu sync.Mutex
var workspaceCodebasisAfterDeleteHooks []WorkspaceCodebasisHook

var workspaceCodebasisBeforeUpsertMu sync.Mutex
var workspaceCodebasisBeforeUpsertHooks []WorkspaceCodebasisHook
var workspaceCodebasisAfterUpsertMu sync.Mutex
var workspaceCodebasisAfterUpsertHooks []WorkspaceCodebasisHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceCodebasis) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceCodebasis) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceCodebasis) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceCodebasis) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceCodebasis) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceCodebasis) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceCodebasis) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceCodebasis) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceCodebasis) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodebasisAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceCodebasisHook registers your hook function for all future operations.
func AddWorkspaceCodebasisHook(hookPoint boil.HookPoint, workspaceCodebasisHook WorkspaceCodebasisHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceCodebasisAfterSelectMu.Lock()
		workspaceCodebasisAfterSelectHooks = append(workspaceCodebasisAfterSelectHooks, workspaceCodebasisHook)
		workspaceCodebasisAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceCodebasisBeforeInsertMu.Lock()
		workspaceCodebasisBeforeInsertHooks = append(workspaceCodebasisBeforeInsertHooks, workspaceCodebasisHook)
		workspaceCodebasisBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceCodebasisAfterInsertMu.Lock()
		workspaceCodebasisAfterInsertHooks = append(workspaceCodebasisAfterInsertHooks, workspaceCodebasisHook)
		workspaceCodebasisAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceCodebasisBeforeUpdateMu.Lock()
		workspaceCodebasisBeforeUpdateHooks = append(workspaceCodebasisBeforeUpdateHooks, workspaceCodebasisHook)
		workspaceCodebasisBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceCodebasisAfterUpdateMu.Lock()
		workspaceCodebasisAfterUpdateHooks = append(workspaceCodebasisAfterUpdateHooks, workspaceCodebasisHook)
		workspaceCodebasisAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceCodebasisBeforeDeleteMu.Lock()
		workspaceCodebasisBeforeDeleteHooks = append(workspaceCodebasisBeforeDeleteHooks, workspaceCodebasisHook)
		workspaceCodebasisBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceCodebasisAfterDeleteMu.Lock()
		workspaceCodebasisAfterDeleteHooks = append(workspaceCodebasisAfterDeleteHooks, workspaceCodebasisHook)
		workspaceCodebasisAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceCodebasisBeforeUpsertMu.Lock()
		workspaceCodebasisBeforeUpsertHooks = append(workspaceCodebasisBeforeUpsertHooks, workspaceCodebasisHook)
		workspaceCodebasisBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceCodebasisAfterUpsertMu.Lock()
		workspaceCodebasisAfterUpsertHooks = append(workspaceCodebasisAfterUpsertHooks, workspaceCodebasisHook)
		workspaceCodebasisAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceCodebasis record from the query.
func (q workspaceCodebasisQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceCodebasis, error) {
	o := &WorkspaceCodebasis{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_codebases")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceCodebasis records from the query.
func (q workspaceCodebasisQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceCodebasisSlice, error) {
	var o []*WorkspaceCodebasis

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceCodebasis slice")
	}

	if len(workspaceCodebasisAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceCodebasis records in the query.
func (q workspaceCodebasisQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_codebases rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceCodebasisQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_codebases exists")
	}

	return count > 0, nil
}

// Runner pointed to by the foreign key.
func (o *WorkspaceCodebasis) Runner(mods ...qm.QueryMod) workspaceRunnerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.RunnerID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceRunners(queryMods...)
}

// CodebaseWorkspaceCodebaseRepositories retrieves all the workspace_codebase_repository's WorkspaceCodebaseRepositories with an executor via codebase_id column.
func (o *WorkspaceCodebasis) CodebaseWorkspaceCodebaseRepositories(mods ...qm.QueryMod) workspaceCodebaseRepositoryQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_codebase_repositories\".\"codebase_id\"=?", o.ID),
	)

	return WorkspaceCodebaseRepositories(queryMods...)
}

// CodebaseWorkspaceExecutions retrieves all the workspace_execution's WorkspaceExecutions with an executor via codebase_id column.
func (o *WorkspaceCodebasis) CodebaseWorkspaceExecutions(mods ...qm.QueryMod) workspaceExecutionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_executions\".\"codebase_id\"=?", o.ID),
	)

	return WorkspaceExecutions(queryMods...)
}

// LoadRunner allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCodebasisL) LoadRunner(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodebasis any, mods queries.Applicator) error {
	var slice []*WorkspaceCodebasis
	var object *WorkspaceCodebasis

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodebasis.(*WorkspaceCodebasis)
		if !ok {
			object = new(WorkspaceCodebasis)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodebasis)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodebasis))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodebasis.(*[]*WorkspaceCodebasis)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodebasis)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodebasis))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodebasisR{}
		}
		args[object.RunnerID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodebasisR{}
			}

			args[obj.RunnerID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_runners`),
		qm.WhereIn(`workspace_runners.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceRunner")
	}

	var resultSlice []*WorkspaceRunner
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceRunner")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_runners")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_runners")
	}

	if len(workspaceRunnerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Runner = foreign
		if foreign.R == nil {
			foreign.R = &workspaceRunnerR{}
		}
		foreign.R.RunnerWorkspaceCodebases = append(foreign.R.RunnerWorkspaceCodebases, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.RunnerID == foreign.ID {
				local.R.Runner = foreign
				if foreign.R == nil {
					foreign.R = &workspaceRunnerR{}
				}
				foreign.R.RunnerWorkspaceCodebases = append(foreign.R.RunnerWorkspaceCodebases, local)
				break
			}
		}
	}

	return nil
}

// LoadCodebaseWorkspaceCodebaseRepositories allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceCodebasisL) LoadCodebaseWorkspaceCodebaseRepositories(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodebasis any, mods queries.Applicator) error {
	var slice []*WorkspaceCodebasis
	var object *WorkspaceCodebasis

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodebasis.(*WorkspaceCodebasis)
		if !ok {
			object = new(WorkspaceCodebasis)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodebasis)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodebasis))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodebasis.(*[]*WorkspaceCodebasis)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodebasis)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodebasis))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodebasisR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodebasisR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_codebase_repositories`),
		qm.WhereIn(`workspace_codebase_repositories.codebase_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_codebase_repositories")
	}

	var resultSlice []*WorkspaceCodebaseRepository
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_codebase_repositories")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_codebase_repositories")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_codebase_repositories")
	}

	if len(workspaceCodebaseRepositoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CodebaseWorkspaceCodebaseRepositories = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceCodebaseRepositoryR{}
			}
			foreign.R.Codebase = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CodebaseID {
				local.R.CodebaseWorkspaceCodebaseRepositories = append(local.R.CodebaseWorkspaceCodebaseRepositories, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceCodebaseRepositoryR{}
				}
				foreign.R.Codebase = local
				break
			}
		}
	}

	return nil
}

// LoadCodebaseWorkspaceExecutions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceCodebasisL) LoadCodebaseWorkspaceExecutions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodebasis any, mods queries.Applicator) error {
	var slice []*WorkspaceCodebasis
	var object *WorkspaceCodebasis

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodebasis.(*WorkspaceCodebasis)
		if !ok {
			object = new(WorkspaceCodebasis)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodebasis)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodebasis))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodebasis.(*[]*WorkspaceCodebasis)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodebasis)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodebasis))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodebasisR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodebasisR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_executions`),
		qm.WhereIn(`workspace_executions.codebase_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_executions")
	}

	var resultSlice []*WorkspaceExecution
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_executions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_executions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_executions")
	}

	if len(workspaceExecutionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CodebaseWorkspaceExecutions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceExecutionR{}
			}
			foreign.R.Codebase = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.CodebaseID) {
				local.R.CodebaseWorkspaceExecutions = append(local.R.CodebaseWorkspaceExecutions, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceExecutionR{}
				}
				foreign.R.Codebase = local
				break
			}
		}
	}

	return nil
}

// SetRunner of the workspaceCodebasis to the related item.
// Sets o.R.Runner to related.
// Adds o to related.R.RunnerWorkspaceCodebases.
func (o *WorkspaceCodebasis) SetRunner(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceRunner) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_codebases\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"runner_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCodebasisPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.RunnerID = related.ID
	if o.R == nil {
		o.R = &workspaceCodebasisR{
			Runner: related,
		}
	} else {
		o.R.Runner = related
	}

	if related.R == nil {
		related.R = &workspaceRunnerR{
			RunnerWorkspaceCodebases: WorkspaceCodebasisSlice{o},
		}
	} else {
		related.R.RunnerWorkspaceCodebases = append(related.R.RunnerWorkspaceCodebases, o)
	}

	return nil
}

// AddCodebaseWorkspaceCodebaseRepositories adds the given related objects to the existing relationships
// of the workspace_codebasis, optionally inserting them as new records.
// Appends related to o.R.CodebaseWorkspaceCodebaseRepositories.
// Sets related.R.Codebase appropriately.
func (o *WorkspaceCodebasis) AddCodebaseWorkspaceCodebaseRepositories(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceCodebaseRepository) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CodebaseID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_codebase_repositories\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"codebase_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceCodebaseRepositoryPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CodebaseID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceCodebasisR{
			CodebaseWorkspaceCodebaseRepositories: related,
		}
	} else {
		o.R.CodebaseWorkspaceCodebaseRepositories = append(o.R.CodebaseWorkspaceCodebaseRepositories, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceCodebaseRepositoryR{
				Codebase: o,
			}
		} else {
			rel.R.Codebase = o
		}
	}
	return nil
}

// AddCodebaseWorkspaceExecutions adds the given related objects to the existing relationships
// of the workspace_codebasis, optionally inserting them as new records.
// Appends related to o.R.CodebaseWorkspaceExecutions.
// Sets related.R.Codebase appropriately.
func (o *WorkspaceCodebasis) AddCodebaseWorkspaceExecutions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceExecution) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.CodebaseID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_executions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"codebase_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceExecutionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.CodebaseID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &workspaceCodebasisR{
			CodebaseWorkspaceExecutions: related,
		}
	} else {
		o.R.CodebaseWorkspaceExecutions = append(o.R.CodebaseWorkspaceExecutions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceExecutionR{
				Codebase: o,
			}
		} else {
			rel.R.Codebase = o
		}
	}
	return nil
}

// SetCodebaseWorkspaceExecutions removes all previously related items of the
// workspace_codebasis replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Codebase's CodebaseWorkspaceExecutions accordingly.
// Replaces o.R.CodebaseWorkspaceExecutions with related.
// Sets related.R.Codebase's CodebaseWorkspaceExecutions accordingly.
func (o *WorkspaceCodebasis) SetCodebaseWorkspaceExecutions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceExecution) error {
	query := "update \"workspace_executions\" set \"codebase_id\" = null where \"codebase_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.CodebaseWorkspaceExecutions {
			queries.SetScanner(&rel.CodebaseID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Codebase = nil
		}
		o.R.CodebaseWorkspaceExecutions = nil
	}

	return o.AddCodebaseWorkspaceExecutions(ctx, exec, insert, related...)
}

// RemoveCodebaseWorkspaceExecutions relationships from objects passed in.
// Removes related items from R.CodebaseWorkspaceExecutions (uses pointer comparison, removal does not keep order)
// Sets related.R.Codebase.
func (o *WorkspaceCodebasis) RemoveCodebaseWorkspaceExecutions(ctx context.Context, exec boil.ContextExecutor, related ...*WorkspaceExecution) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.CodebaseID, nil)
		if rel.R != nil {
			rel.R.Codebase = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("codebase_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.CodebaseWorkspaceExecutions {
			if rel != ri {
				continue
			}

			ln := len(o.R.CodebaseWorkspaceExecutions)
			if ln > 1 && i < ln-1 {
				o.R.CodebaseWorkspaceExecutions[i] = o.R.CodebaseWorkspaceExecutions[ln-1]
			}
			o.R.CodebaseWorkspaceExecutions = o.R.CodebaseWorkspaceExecutions[:ln-1]
			break
		}
	}

	return nil
}

// WorkspaceCodebases retrieves all the records using an executor.
func WorkspaceCodebases(mods ...qm.QueryMod) workspaceCodebasisQuery {
	mods = append(mods, qm.From("\"workspace_codebases\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_codebases\".*"})
	}

	return workspaceCodebasisQuery{q}
}

// FindWorkspaceCodebasis retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceCodebasis(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceCodebasis, error) {
	workspaceCodebasisObj := &WorkspaceCodebasis{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_codebases\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceCodebasisObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_codebases")
	}

	if err = workspaceCodebasisObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceCodebasisObj, err
	}

	return workspaceCodebasisObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceCodebasis) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_codebases provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCodebasisColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceCodebasisInsertCacheMut.RLock()
	cache, cached := workspaceCodebasisInsertCache[key]
	workspaceCodebasisInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceCodebasisAllColumns,
			workspaceCodebasisColumnsWithDefault,
			workspaceCodebasisColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceCodebasisType, workspaceCodebasisMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceCodebasisType, workspaceCodebasisMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_codebases\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_codebases\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_codebases")
	}

	if !cached {
		workspaceCodebasisInsertCacheMut.Lock()
		workspaceCodebasisInsertCache[key] = cache
		workspaceCodebasisInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceCodebasis.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceCodebasis) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceCodebasisUpdateCacheMut.RLock()
	cache, cached := workspaceCodebasisUpdateCache[key]
	workspaceCodebasisUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceCodebasisAllColumns,
			workspaceCodebasisPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_codebases, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_codebases\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceCodebasisPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceCodebasisType, workspaceCodebasisMapping, append(wl, workspaceCodebasisPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_codebases row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_codebases")
	}

	if !cached {
		workspaceCodebasisUpdateCacheMut.Lock()
		workspaceCodebasisUpdateCache[key] = cache
		workspaceCodebasisUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceCodebasisQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_codebases")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_codebases")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceCodebasisSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodebasisPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_codebases\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceCodebasisPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceCodebasis slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceCodebasis")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceCodebasis) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_codebases provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCodebasisColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceCodebasisUpsertCacheMut.RLock()
	cache, cached := workspaceCodebasisUpsertCache[key]
	workspaceCodebasisUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceCodebasisAllColumns,
			workspaceCodebasisColumnsWithDefault,
			workspaceCodebasisColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceCodebasisAllColumns,
			workspaceCodebasisPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_codebases, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceCodebasisAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceCodebasisPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_codebases, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceCodebasisPrimaryKeyColumns))
			copy(conflict, workspaceCodebasisPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_codebases\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceCodebasisType, workspaceCodebasisMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceCodebasisType, workspaceCodebasisMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_codebases")
	}

	if !cached {
		workspaceCodebasisUpsertCacheMut.Lock()
		workspaceCodebasisUpsertCache[key] = cache
		workspaceCodebasisUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceCodebasis record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceCodebasis) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceCodebasis provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceCodebasisPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_codebases\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_codebases")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_codebases")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceCodebasisQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceCodebasisQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_codebases")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_codebases")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceCodebasisSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceCodebasisBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodebasisPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_codebases\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCodebasisPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceCodebasis slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_codebases")
	}

	if len(workspaceCodebasisAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceCodebasis) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceCodebasis(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceCodebasisSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceCodebasisSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodebasisPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_codebases\".* FROM \"workspace_codebases\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCodebasisPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceCodebasisSlice")
	}

	*o = slice

	return nil
}

// WorkspaceCodebasisExists checks if the WorkspaceCodebasis row exists.
func WorkspaceCodebasisExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_codebases\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_codebases exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceCodebasis row exists.
func (o *WorkspaceCodebasis) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceCodebasisExists(ctx, exec, o.ID)
}
