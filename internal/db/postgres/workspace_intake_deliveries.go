// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceIntakeDelivery is an object representing the database table.
type WorkspaceIntakeDelivery struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	TeamID      string      `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	ExternalID  string      `boil:"external_id" json:"external_id" toml:"external_id" yaml:"external_id"`
	Recipient   string      `boil:"recipient" json:"recipient" toml:"recipient" yaml:"recipient"`
	Sender      string      `boil:"sender" json:"sender" toml:"sender" yaml:"sender"`
	Subject     string      `boil:"subject" json:"subject" toml:"subject" yaml:"subject"`
	ReceivedAt  time.Time   `boil:"received_at" json:"received_at" toml:"received_at" yaml:"received_at"`
	ProcessedAt null.Time   `boil:"processed_at" json:"processed_at,omitempty" toml:"processed_at" yaml:"processed_at,omitempty"`
	Outcome     string      `boil:"outcome" json:"outcome" toml:"outcome" yaml:"outcome"`
	IssueID     null.String `boil:"issue_id" json:"issue_id,omitempty" toml:"issue_id" yaml:"issue_id,omitempty"`
	Failure     string      `boil:"failure" json:"failure" toml:"failure" yaml:"failure"`

	R *workspaceIntakeDeliveryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceIntakeDeliveryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceIntakeDeliveryColumns = struct {
	ID          string
	WorkspaceID string
	TeamID      string
	ExternalID  string
	Recipient   string
	Sender      string
	Subject     string
	ReceivedAt  string
	ProcessedAt string
	Outcome     string
	IssueID     string
	Failure     string
}{
	ID:          "id",
	WorkspaceID: "workspace_id",
	TeamID:      "team_id",
	ExternalID:  "external_id",
	Recipient:   "recipient",
	Sender:      "sender",
	Subject:     "subject",
	ReceivedAt:  "received_at",
	ProcessedAt: "processed_at",
	Outcome:     "outcome",
	IssueID:     "issue_id",
	Failure:     "failure",
}

var WorkspaceIntakeDeliveryTableColumns = struct {
	ID          string
	WorkspaceID string
	TeamID      string
	ExternalID  string
	Recipient   string
	Sender      string
	Subject     string
	ReceivedAt  string
	ProcessedAt string
	Outcome     string
	IssueID     string
	Failure     string
}{
	ID:          "workspace_intake_deliveries.id",
	WorkspaceID: "workspace_intake_deliveries.workspace_id",
	TeamID:      "workspace_intake_deliveries.team_id",
	ExternalID:  "workspace_intake_deliveries.external_id",
	Recipient:   "workspace_intake_deliveries.recipient",
	Sender:      "workspace_intake_deliveries.sender",
	Subject:     "workspace_intake_deliveries.subject",
	ReceivedAt:  "workspace_intake_deliveries.received_at",
	ProcessedAt: "workspace_intake_deliveries.processed_at",
	Outcome:     "workspace_intake_deliveries.outcome",
	IssueID:     "workspace_intake_deliveries.issue_id",
	Failure:     "workspace_intake_deliveries.failure",
}

// Generated where

var WorkspaceIntakeDeliveryWhere = struct {
	ID          whereHelperstring
	WorkspaceID whereHelperstring
	TeamID      whereHelperstring
	ExternalID  whereHelperstring
	Recipient   whereHelperstring
	Sender      whereHelperstring
	Subject     whereHelperstring
	ReceivedAt  whereHelpertime_Time
	ProcessedAt whereHelpernull_Time
	Outcome     whereHelperstring
	IssueID     whereHelpernull_String
	Failure     whereHelperstring
}{
	ID:          whereHelperstring{field: "\"workspace_intake_deliveries\".\"id\""},
	WorkspaceID: whereHelperstring{field: "\"workspace_intake_deliveries\".\"workspace_id\""},
	TeamID:      whereHelperstring{field: "\"workspace_intake_deliveries\".\"team_id\""},
	ExternalID:  whereHelperstring{field: "\"workspace_intake_deliveries\".\"external_id\""},
	Recipient:   whereHelperstring{field: "\"workspace_intake_deliveries\".\"recipient\""},
	Sender:      whereHelperstring{field: "\"workspace_intake_deliveries\".\"sender\""},
	Subject:     whereHelperstring{field: "\"workspace_intake_deliveries\".\"subject\""},
	ReceivedAt:  whereHelpertime_Time{field: "\"workspace_intake_deliveries\".\"received_at\""},
	ProcessedAt: whereHelpernull_Time{field: "\"workspace_intake_deliveries\".\"processed_at\""},
	Outcome:     whereHelperstring{field: "\"workspace_intake_deliveries\".\"outcome\""},
	IssueID:     whereHelpernull_String{field: "\"workspace_intake_deliveries\".\"issue_id\""},
	Failure:     whereHelperstring{field: "\"workspace_intake_deliveries\".\"failure\""},
}

// WorkspaceIntakeDeliveryRels is where relationship names are stored.
var WorkspaceIntakeDeliveryRels = struct {
	Issue     string
	Workspace string
}{
	Issue:     "Issue",
	Workspace: "Workspace",
}

// workspaceIntakeDeliveryR is where relationships are stored.
type workspaceIntakeDeliveryR struct {
	Issue     *WorkspaceIssue `boil:"Issue" json:"Issue" toml:"Issue" yaml:"Issue"`
	Workspace *Workspace      `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*workspaceIntakeDeliveryR) NewStruct() *workspaceIntakeDeliveryR {
	return &workspaceIntakeDeliveryR{}
}

func (o *WorkspaceIntakeDelivery) GetIssue() *WorkspaceIssue {
	if o == nil {
		return nil
	}

	return o.R.GetIssue()
}

func (r *workspaceIntakeDeliveryR) GetIssue() *WorkspaceIssue {
	if r == nil {
		return nil
	}

	return r.Issue
}

func (o *WorkspaceIntakeDelivery) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceIntakeDeliveryR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// workspaceIntakeDeliveryL is where Load methods for each relationship are stored.
type workspaceIntakeDeliveryL struct{}

var (
	workspaceIntakeDeliveryAllColumns            = []string{"id", "workspace_id", "team_id", "external_id", "recipient", "sender", "subject", "received_at", "processed_at", "outcome", "issue_id", "failure"}
	workspaceIntakeDeliveryColumnsWithoutDefault = []string{"id", "workspace_id", "team_id", "external_id", "recipient", "sender", "received_at"}
	workspaceIntakeDeliveryColumnsWithDefault    = []string{"subject", "processed_at", "outcome", "issue_id", "failure"}
	workspaceIntakeDeliveryPrimaryKeyColumns     = []string{"id"}
	workspaceIntakeDeliveryGeneratedColumns      = []string{}
)

type (
	// WorkspaceIntakeDeliverySlice is an alias for a slice of pointers to WorkspaceIntakeDelivery.
	// This should almost always be used instead of []WorkspaceIntakeDelivery.
	WorkspaceIntakeDeliverySlice []*WorkspaceIntakeDelivery
	// WorkspaceIntakeDeliveryHook is the signature for custom WorkspaceIntakeDelivery hook methods
	WorkspaceIntakeDeliveryHook func(context.Context, boil.ContextExecutor, *WorkspaceIntakeDelivery) error

	workspaceIntakeDeliveryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceIntakeDeliveryType                 = reflect.TypeOf(&WorkspaceIntakeDelivery{})
	workspaceIntakeDeliveryMapping              = queries.MakeStructMapping(workspaceIntakeDeliveryType)
	workspaceIntakeDeliveryPrimaryKeyMapping, _ = queries.BindMapping(workspaceIntakeDeliveryType, workspaceIntakeDeliveryMapping, workspaceIntakeDeliveryPrimaryKeyColumns)
	workspaceIntakeDeliveryInsertCacheMut       sync.RWMutex
	workspaceIntakeDeliveryInsertCache          = make(map[string]insertCache)
	workspaceIntakeDeliveryUpdateCacheMut       sync.RWMutex
	workspaceIntakeDeliveryUpdateCache          = make(map[string]updateCache)
	workspaceIntakeDeliveryUpsertCacheMut       sync.RWMutex
	workspaceIntakeDeliveryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceIntakeDeliveryAfterSelectMu sync.Mutex
var workspaceIntakeDeliveryAfterSelectHooks []WorkspaceIntakeDeliveryHook

var workspaceIntakeDeliveryBeforeInsertMu sync.Mutex
var workspaceIntakeDeliveryBeforeInsertHooks []WorkspaceIntakeDeliveryHook
var workspaceIntakeDeliveryAfterInsertMu sync.Mutex
var workspaceIntakeDeliveryAfterInsertHooks []WorkspaceIntakeDeliveryHook

var workspaceIntakeDeliveryBeforeUpdateMu sync.Mutex
var workspaceIntakeDeliveryBeforeUpdateHooks []WorkspaceIntakeDeliveryHook
var workspaceIntakeDeliveryAfterUpdateMu sync.Mutex
var workspaceIntakeDeliveryAfterUpdateHooks []WorkspaceIntakeDeliveryHook

var workspaceIntakeDeliveryBeforeDeleteMu sync.Mutex
var workspaceIntakeDeliveryBeforeDeleteHooks []WorkspaceIntakeDeliveryHook
var workspaceIntakeDeliveryAfterDeleteMu sync.Mutex
var workspaceIntakeDeliveryAfterDeleteHooks []WorkspaceIntakeDeliveryHook

var workspaceIntakeDeliveryBeforeUpsertMu sync.Mutex
var workspaceIntakeDeliveryBeforeUpsertHooks []WorkspaceIntakeDeliveryHook
var workspaceIntakeDeliveryAfterUpsertMu sync.Mutex
var workspaceIntakeDeliveryAfterUpsertHooks []WorkspaceIntakeDeliveryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceIntakeDelivery) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceIntakeDelivery) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceIntakeDelivery) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceIntakeDelivery) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceIntakeDelivery) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceIntakeDelivery) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceIntakeDelivery) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceIntakeDelivery) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceIntakeDelivery) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceIntakeDeliveryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceIntakeDeliveryHook registers your hook function for all future operations.
func AddWorkspaceIntakeDeliveryHook(hookPoint boil.HookPoint, workspaceIntakeDeliveryHook WorkspaceIntakeDeliveryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceIntakeDeliveryAfterSelectMu.Lock()
		workspaceIntakeDeliveryAfterSelectHooks = append(workspaceIntakeDeliveryAfterSelectHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceIntakeDeliveryBeforeInsertMu.Lock()
		workspaceIntakeDeliveryBeforeInsertHooks = append(workspaceIntakeDeliveryBeforeInsertHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceIntakeDeliveryAfterInsertMu.Lock()
		workspaceIntakeDeliveryAfterInsertHooks = append(workspaceIntakeDeliveryAfterInsertHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceIntakeDeliveryBeforeUpdateMu.Lock()
		workspaceIntakeDeliveryBeforeUpdateHooks = append(workspaceIntakeDeliveryBeforeUpdateHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceIntakeDeliveryAfterUpdateMu.Lock()
		workspaceIntakeDeliveryAfterUpdateHooks = append(workspaceIntakeDeliveryAfterUpdateHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceIntakeDeliveryBeforeDeleteMu.Lock()
		workspaceIntakeDeliveryBeforeDeleteHooks = append(workspaceIntakeDeliveryBeforeDeleteHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceIntakeDeliveryAfterDeleteMu.Lock()
		workspaceIntakeDeliveryAfterDeleteHooks = append(workspaceIntakeDeliveryAfterDeleteHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceIntakeDeliveryBeforeUpsertMu.Lock()
		workspaceIntakeDeliveryBeforeUpsertHooks = append(workspaceIntakeDeliveryBeforeUpsertHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceIntakeDeliveryAfterUpsertMu.Lock()
		workspaceIntakeDeliveryAfterUpsertHooks = append(workspaceIntakeDeliveryAfterUpsertHooks, workspaceIntakeDeliveryHook)
		workspaceIntakeDeliveryAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceIntakeDelivery record from the query.
func (q workspaceIntakeDeliveryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceIntakeDelivery, error) {
	o := &WorkspaceIntakeDelivery{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_intake_deliveries")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceIntakeDelivery records from the query.
func (q workspaceIntakeDeliveryQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceIntakeDeliverySlice, error) {
	var o []*WorkspaceIntakeDelivery

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceIntakeDelivery slice")
	}

	if len(workspaceIntakeDeliveryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceIntakeDelivery records in the query.
func (q workspaceIntakeDeliveryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_intake_deliveries rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceIntakeDeliveryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_intake_deliveries exists")
	}

	return count > 0, nil
}

// Issue pointed to by the foreign key.
func (o *WorkspaceIntakeDelivery) Issue(mods ...qm.QueryMod) workspaceIssueQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IssueID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceIssues(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceIntakeDelivery) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadIssue allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceIntakeDeliveryL) LoadIssue(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceIntakeDelivery any, mods queries.Applicator) error {
	var slice []*WorkspaceIntakeDelivery
	var object *WorkspaceIntakeDelivery

	if singular {
		var ok bool
		object, ok = maybeWorkspaceIntakeDelivery.(*WorkspaceIntakeDelivery)
		if !ok {
			object = new(WorkspaceIntakeDelivery)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceIntakeDelivery)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceIntakeDelivery))
			}
		}
	} else {
		s, ok := maybeWorkspaceIntakeDelivery.(*[]*WorkspaceIntakeDelivery)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceIntakeDelivery)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceIntakeDelivery))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceIntakeDeliveryR{}
		}
		if !queries.IsNil(object.IssueID) {
			args[object.IssueID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceIntakeDeliveryR{}
			}

			if !queries.IsNil(obj.IssueID) {
				args[obj.IssueID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceIssue")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceIssue")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Issue = foreign
		if foreign.R == nil {
			foreign.R = &workspaceIssueR{}
		}
		foreign.R.IssueWorkspaceIntakeDeliveries = append(foreign.R.IssueWorkspaceIntakeDeliveries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.IssueID, foreign.ID) {
				local.R.Issue = foreign
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.IssueWorkspaceIntakeDeliveries = append(foreign.R.IssueWorkspaceIntakeDeliveries, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceIntakeDeliveryL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceIntakeDelivery any, mods queries.Applicator) error {
	var slice []*WorkspaceIntakeDelivery
	var object *WorkspaceIntakeDelivery

	if singular {
		var ok bool
		object, ok = maybeWorkspaceIntakeDelivery.(*WorkspaceIntakeDelivery)
		if !ok {
			object = new(WorkspaceIntakeDelivery)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceIntakeDelivery)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceIntakeDelivery))
			}
		}
	} else {
		s, ok := maybeWorkspaceIntakeDelivery.(*[]*WorkspaceIntakeDelivery)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceIntakeDelivery)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceIntakeDelivery))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceIntakeDeliveryR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceIntakeDeliveryR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceIntakeDeliveries = append(foreign.R.WorkspaceIntakeDeliveries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceIntakeDeliveries = append(foreign.R.WorkspaceIntakeDeliveries, local)
				break
			}
		}
	}

	return nil
}

// SetIssue of the workspaceIntakeDelivery to the related item.
// Sets o.R.Issue to related.
// Adds o to related.R.IssueWorkspaceIntakeDeliveries.
func (o *WorkspaceIntakeDelivery) SetIssue(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceIssue) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_intake_deliveries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"issue_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceIntakeDeliveryPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.IssueID, related.ID)
	if o.R == nil {
		o.R = &workspaceIntakeDeliveryR{
			Issue: related,
		}
	} else {
		o.R.Issue = related
	}

	if related.R == nil {
		related.R = &workspaceIssueR{
			IssueWorkspaceIntakeDeliveries: WorkspaceIntakeDeliverySlice{o},
		}
	} else {
		related.R.IssueWorkspaceIntakeDeliveries = append(related.R.IssueWorkspaceIntakeDeliveries, o)
	}

	return nil
}

// RemoveIssue relationship.
// Sets o.R.Issue to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceIntakeDelivery) RemoveIssue(ctx context.Context, exec boil.ContextExecutor, related *WorkspaceIssue) error {
	var err error

	queries.SetScanner(&o.IssueID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("issue_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Issue = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.IssueWorkspaceIntakeDeliveries {
		if queries.Equal(o.IssueID, ri.IssueID) {
			continue
		}

		ln := len(related.R.IssueWorkspaceIntakeDeliveries)
		if ln > 1 && i < ln-1 {
			related.R.IssueWorkspaceIntakeDeliveries[i] = related.R.IssueWorkspaceIntakeDeliveries[ln-1]
		}
		related.R.IssueWorkspaceIntakeDeliveries = related.R.IssueWorkspaceIntakeDeliveries[:ln-1]
		break
	}
	return nil
}

// SetWorkspace of the workspaceIntakeDelivery to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceIntakeDeliveries.
func (o *WorkspaceIntakeDelivery) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_intake_deliveries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceIntakeDeliveryPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceIntakeDeliveryR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceIntakeDeliveries: WorkspaceIntakeDeliverySlice{o},
		}
	} else {
		related.R.WorkspaceIntakeDeliveries = append(related.R.WorkspaceIntakeDeliveries, o)
	}

	return nil
}

// WorkspaceIntakeDeliveries retrieves all the records using an executor.
func WorkspaceIntakeDeliveries(mods ...qm.QueryMod) workspaceIntakeDeliveryQuery {
	mods = append(mods, qm.From("\"workspace_intake_deliveries\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_intake_deliveries\".*"})
	}

	return workspaceIntakeDeliveryQuery{q}
}

// FindWorkspaceIntakeDelivery retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceIntakeDelivery(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceIntakeDelivery, error) {
	workspaceIntakeDeliveryObj := &WorkspaceIntakeDelivery{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_intake_deliveries\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceIntakeDeliveryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_intake_deliveries")
	}

	if err = workspaceIntakeDeliveryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceIntakeDeliveryObj, err
	}

	return workspaceIntakeDeliveryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceIntakeDelivery) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_intake_deliveries provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceIntakeDeliveryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceIntakeDeliveryInsertCacheMut.RLock()
	cache, cached := workspaceIntakeDeliveryInsertCache[key]
	workspaceIntakeDeliveryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceIntakeDeliveryAllColumns,
			workspaceIntakeDeliveryColumnsWithDefault,
			workspaceIntakeDeliveryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceIntakeDeliveryType, workspaceIntakeDeliveryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceIntakeDeliveryType, workspaceIntakeDeliveryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_intake_deliveries\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_intake_deliveries\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_intake_deliveries")
	}

	if !cached {
		workspaceIntakeDeliveryInsertCacheMut.Lock()
		workspaceIntakeDeliveryInsertCache[key] = cache
		workspaceIntakeDeliveryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceIntakeDelivery.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceIntakeDelivery) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceIntakeDeliveryUpdateCacheMut.RLock()
	cache, cached := workspaceIntakeDeliveryUpdateCache[key]
	workspaceIntakeDeliveryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceIntakeDeliveryAllColumns,
			workspaceIntakeDeliveryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_intake_deliveries, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_intake_deliveries\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceIntakeDeliveryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceIntakeDeliveryType, workspaceIntakeDeliveryMapping, append(wl, workspaceIntakeDeliveryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_intake_deliveries row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_intake_deliveries")
	}

	if !cached {
		workspaceIntakeDeliveryUpdateCacheMut.Lock()
		workspaceIntakeDeliveryUpdateCache[key] = cache
		workspaceIntakeDeliveryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceIntakeDeliveryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_intake_deliveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_intake_deliveries")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceIntakeDeliverySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceIntakeDeliveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_intake_deliveries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceIntakeDeliveryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceIntakeDelivery slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceIntakeDelivery")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceIntakeDelivery) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_intake_deliveries provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceIntakeDeliveryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceIntakeDeliveryUpsertCacheMut.RLock()
	cache, cached := workspaceIntakeDeliveryUpsertCache[key]
	workspaceIntakeDeliveryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceIntakeDeliveryAllColumns,
			workspaceIntakeDeliveryColumnsWithDefault,
			workspaceIntakeDeliveryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceIntakeDeliveryAllColumns,
			workspaceIntakeDeliveryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_intake_deliveries, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceIntakeDeliveryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceIntakeDeliveryPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_intake_deliveries, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceIntakeDeliveryPrimaryKeyColumns))
			copy(conflict, workspaceIntakeDeliveryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_intake_deliveries\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceIntakeDeliveryType, workspaceIntakeDeliveryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceIntakeDeliveryType, workspaceIntakeDeliveryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_intake_deliveries")
	}

	if !cached {
		workspaceIntakeDeliveryUpsertCacheMut.Lock()
		workspaceIntakeDeliveryUpsertCache[key] = cache
		workspaceIntakeDeliveryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceIntakeDelivery record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceIntakeDelivery) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceIntakeDelivery provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceIntakeDeliveryPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_intake_deliveries\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_intake_deliveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_intake_deliveries")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceIntakeDeliveryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceIntakeDeliveryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_intake_deliveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_intake_deliveries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceIntakeDeliverySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceIntakeDeliveryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceIntakeDeliveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_intake_deliveries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceIntakeDeliveryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceIntakeDelivery slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_intake_deliveries")
	}

	if len(workspaceIntakeDeliveryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceIntakeDelivery) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceIntakeDelivery(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceIntakeDeliverySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceIntakeDeliverySlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceIntakeDeliveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_intake_deliveries\".* FROM \"workspace_intake_deliveries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceIntakeDeliveryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceIntakeDeliverySlice")
	}

	*o = slice

	return nil
}

// WorkspaceIntakeDeliveryExists checks if the WorkspaceIntakeDelivery row exists.
func WorkspaceIntakeDeliveryExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_intake_deliveries\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_intake_deliveries exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceIntakeDelivery row exists.
func (o *WorkspaceIntakeDelivery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceIntakeDeliveryExists(ctx, exec, o.ID)
}
