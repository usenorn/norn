// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceTeamCycleCadence is an object representing the database table.
type WorkspaceTeamCycleCadence struct {
	TeamID      string    `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	WorkspaceID string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	LengthWeeks int       `boil:"length_weeks" json:"length_weeks" toml:"length_weeks" yaml:"length_weeks"`
	AnchorOn    time.Time `boil:"anchor_on" json:"anchor_on" toml:"anchor_on" yaml:"anchor_on"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceTeamCycleCadenceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceTeamCycleCadenceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceTeamCycleCadenceColumns = struct {
	TeamID      string
	WorkspaceID string
	LengthWeeks string
	AnchorOn    string
	CreatedAt   string
	UpdatedAt   string
}{
	TeamID:      "team_id",
	WorkspaceID: "workspace_id",
	LengthWeeks: "length_weeks",
	AnchorOn:    "anchor_on",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var WorkspaceTeamCycleCadenceTableColumns = struct {
	TeamID      string
	WorkspaceID string
	LengthWeeks string
	AnchorOn    string
	CreatedAt   string
	UpdatedAt   string
}{
	TeamID:      "workspace_team_cycle_cadences.team_id",
	WorkspaceID: "workspace_team_cycle_cadences.workspace_id",
	LengthWeeks: "workspace_team_cycle_cadences.length_weeks",
	AnchorOn:    "workspace_team_cycle_cadences.anchor_on",
	CreatedAt:   "workspace_team_cycle_cadences.created_at",
	UpdatedAt:   "workspace_team_cycle_cadences.updated_at",
}

// Generated where

var WorkspaceTeamCycleCadenceWhere = struct {
	TeamID      whereHelperstring
	WorkspaceID whereHelperstring
	LengthWeeks whereHelperint
	AnchorOn    whereHelpertime_Time
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	TeamID:      whereHelperstring{field: "\"workspace_team_cycle_cadences\".\"team_id\""},
	WorkspaceID: whereHelperstring{field: "\"workspace_team_cycle_cadences\".\"workspace_id\""},
	LengthWeeks: whereHelperint{field: "\"workspace_team_cycle_cadences\".\"length_weeks\""},
	AnchorOn:    whereHelpertime_Time{field: "\"workspace_team_cycle_cadences\".\"anchor_on\""},
	CreatedAt:   whereHelpertime_Time{field: "\"workspace_team_cycle_cadences\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"workspace_team_cycle_cadences\".\"updated_at\""},
}

// WorkspaceTeamCycleCadenceRels is where relationship names are stored.
var WorkspaceTeamCycleCadenceRels = struct {
}{}

// workspaceTeamCycleCadenceR is where relationships are stored.
type workspaceTeamCycleCadenceR struct {
}

// NewStruct creates a new relationship struct
func (*workspaceTeamCycleCadenceR) NewStruct() *workspaceTeamCycleCadenceR {
	return &workspaceTeamCycleCadenceR{}
}

// workspaceTeamCycleCadenceL is where Load methods for each relationship are stored.
type workspaceTeamCycleCadenceL struct{}

var (
	workspaceTeamCycleCadenceAllColumns            = []string{"team_id", "workspace_id", "length_weeks", "anchor_on", "created_at", "updated_at"}
	workspaceTeamCycleCadenceColumnsWithoutDefault = []string{"team_id", "workspace_id", "length_weeks", "anchor_on"}
	workspaceTeamCycleCadenceColumnsWithDefault    = []string{"created_at", "updated_at"}
	workspaceTeamCycleCadencePrimaryKeyColumns     = []string{"team_id"}
	workspaceTeamCycleCadenceGeneratedColumns      = []string{}
)

type (
	// WorkspaceTeamCycleCadenceSlice is an alias for a slice of pointers to WorkspaceTeamCycleCadence.
	// This should almost always be used instead of []WorkspaceTeamCycleCadence.
	WorkspaceTeamCycleCadenceSlice []*WorkspaceTeamCycleCadence
	// WorkspaceTeamCycleCadenceHook is the signature for custom WorkspaceTeamCycleCadence hook methods
	WorkspaceTeamCycleCadenceHook func(context.Context, boil.ContextExecutor, *WorkspaceTeamCycleCadence) error

	workspaceTeamCycleCadenceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceTeamCycleCadenceType                 = reflect.TypeOf(&WorkspaceTeamCycleCadence{})
	workspaceTeamCycleCadenceMapping              = queries.MakeStructMapping(workspaceTeamCycleCadenceType)
	workspaceTeamCycleCadencePrimaryKeyMapping, _ = queries.BindMapping(workspaceTeamCycleCadenceType, workspaceTeamCycleCadenceMapping, workspaceTeamCycleCadencePrimaryKeyColumns)
	workspaceTeamCycleCadenceInsertCacheMut       sync.RWMutex
	workspaceTeamCycleCadenceInsertCache          = make(map[string]insertCache)
	workspaceTeamCycleCadenceUpdateCacheMut       sync.RWMutex
	workspaceTeamCycleCadenceUpdateCache          = make(map[string]updateCache)
	workspaceTeamCycleCadenceUpsertCacheMut       sync.RWMutex
	workspaceTeamCycleCadenceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceTeamCycleCadenceAfterSelectMu sync.Mutex
var workspaceTeamCycleCadenceAfterSelectHooks []WorkspaceTeamCycleCadenceHook

var workspaceTeamCycleCadenceBeforeInsertMu sync.Mutex
var workspaceTeamCycleCadenceBeforeInsertHooks []WorkspaceTeamCycleCadenceHook
var workspaceTeamCycleCadenceAfterInsertMu sync.Mutex
var workspaceTeamCycleCadenceAfterInsertHooks []WorkspaceTeamCycleCadenceHook

var workspaceTeamCycleCadenceBeforeUpdateMu sync.Mutex
var workspaceTeamCycleCadenceBeforeUpdateHooks []WorkspaceTeamCycleCadenceHook
var workspaceTeamCycleCadenceAfterUpdateMu sync.Mutex
var workspaceTeamCycleCadenceAfterUpdateHooks []WorkspaceTeamCycleCadenceHook

var workspaceTeamCycleCadenceBeforeDeleteMu sync.Mutex
var workspaceTeamCycleCadenceBeforeDeleteHooks []WorkspaceTeamCycleCadenceHook
var workspaceTeamCycleCadenceAfterDeleteMu sync.Mutex
var workspaceTeamCycleCadenceAfterDeleteHooks []WorkspaceTeamCycleCadenceHook

var workspaceTeamCycleCadenceBeforeUpsertMu sync.Mutex
var workspaceTeamCycleCadenceBeforeUpsertHooks []WorkspaceTeamCycleCadenceHook
var workspaceTeamCycleCadenceAfterUpsertMu sync.Mutex
var workspaceTeamCycleCadenceAfterUpsertHooks []WorkspaceTeamCycleCadenceHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceTeamCycleCadence) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceTeamCycleCadence) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceTeamCycleCadence) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceTeamCycleCadence) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceTeamCycleCadence) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceTeamCycleCadence) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceTeamCycleCadence) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceTeamCycleCadence) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceTeamCycleCadence) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamCycleCadenceAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceTeamCycleCadenceHook registers your hook function for all future operations.
func AddWorkspaceTeamCycleCadenceHook(hookPoint boil.HookPoint, workspaceTeamCycleCadenceHook WorkspaceTeamCycleCadenceHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceTeamCycleCadenceAfterSelectMu.Lock()
		workspaceTeamCycleCadenceAfterSelectHooks = append(workspaceTeamCycleCadenceAfterSelectHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceTeamCycleCadenceBeforeInsertMu.Lock()
		workspaceTeamCycleCadenceBeforeInsertHooks = append(workspaceTeamCycleCadenceBeforeInsertHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceTeamCycleCadenceAfterInsertMu.Lock()
		workspaceTeamCycleCadenceAfterInsertHooks = append(workspaceTeamCycleCadenceAfterInsertHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceTeamCycleCadenceBeforeUpdateMu.Lock()
		workspaceTeamCycleCadenceBeforeUpdateHooks = append(workspaceTeamCycleCadenceBeforeUpdateHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceTeamCycleCadenceAfterUpdateMu.Lock()
		workspaceTeamCycleCadenceAfterUpdateHooks = append(workspaceTeamCycleCadenceAfterUpdateHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceTeamCycleCadenceBeforeDeleteMu.Lock()
		workspaceTeamCycleCadenceBeforeDeleteHooks = append(workspaceTeamCycleCadenceBeforeDeleteHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceTeamCycleCadenceAfterDeleteMu.Lock()
		workspaceTeamCycleCadenceAfterDeleteHooks = append(workspaceTeamCycleCadenceAfterDeleteHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceTeamCycleCadenceBeforeUpsertMu.Lock()
		workspaceTeamCycleCadenceBeforeUpsertHooks = append(workspaceTeamCycleCadenceBeforeUpsertHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceTeamCycleCadenceAfterUpsertMu.Lock()
		workspaceTeamCycleCadenceAfterUpsertHooks = append(workspaceTeamCycleCadenceAfterUpsertHooks, workspaceTeamCycleCadenceHook)
		workspaceTeamCycleCadenceAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceTeamCycleCadence record from the query.
func (q workspaceTeamCycleCadenceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceTeamCycleCadence, error) {
	o := &WorkspaceTeamCycleCadence{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_team_cycle_cadences")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceTeamCycleCadence records from the query.
func (q workspaceTeamCycleCadenceQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceTeamCycleCadenceSlice, error) {
	var o []*WorkspaceTeamCycleCadence

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceTeamCycleCadence slice")
	}

	if len(workspaceTeamCycleCadenceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceTeamCycleCadence records in the query.
func (q workspaceTeamCycleCadenceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_team_cycle_cadences rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceTeamCycleCadenceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_team_cycle_cadences exists")
	}

	return count > 0, nil
}

// WorkspaceTeamCycleCadences retrieves all the records using an executor.
func WorkspaceTeamCycleCadences(mods ...qm.QueryMod) workspaceTeamCycleCadenceQuery {
	mods = append(mods, qm.From("\"workspace_team_cycle_cadences\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_team_cycle_cadences\".*"})
	}

	return workspaceTeamCycleCadenceQuery{q}
}

// FindWorkspaceTeamCycleCadence retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceTeamCycleCadence(ctx context.Context, exec boil.ContextExecutor, teamID string, selectCols ...string) (*WorkspaceTeamCycleCadence, error) {
	workspaceTeamCycleCadenceObj := &WorkspaceTeamCycleCadence{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_team_cycle_cadences\" where \"team_id\"=$1", sel,
	)

	q := queries.Raw(query, teamID)

	err := q.Bind(ctx, exec, workspaceTeamCycleCadenceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_team_cycle_cadences")
	}

	if err = workspaceTeamCycleCadenceObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceTeamCycleCadenceObj, err
	}

	return workspaceTeamCycleCadenceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceTeamCycleCadence) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_cycle_cadences provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamCycleCadenceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceTeamCycleCadenceInsertCacheMut.RLock()
	cache, cached := workspaceTeamCycleCadenceInsertCache[key]
	workspaceTeamCycleCadenceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceTeamCycleCadenceAllColumns,
			workspaceTeamCycleCadenceColumnsWithDefault,
			workspaceTeamCycleCadenceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamCycleCadenceType, workspaceTeamCycleCadenceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceTeamCycleCadenceType, workspaceTeamCycleCadenceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_team_cycle_cadences\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_team_cycle_cadences\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_team_cycle_cadences")
	}

	if !cached {
		workspaceTeamCycleCadenceInsertCacheMut.Lock()
		workspaceTeamCycleCadenceInsertCache[key] = cache
		workspaceTeamCycleCadenceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceTeamCycleCadence.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceTeamCycleCadence) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceTeamCycleCadenceUpdateCacheMut.RLock()
	cache, cached := workspaceTeamCycleCadenceUpdateCache[key]
	workspaceTeamCycleCadenceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceTeamCycleCadenceAllColumns,
			workspaceTeamCycleCadencePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_team_cycle_cadences, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_team_cycle_cadences\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceTeamCycleCadencePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceTeamCycleCadenceType, workspaceTeamCycleCadenceMapping, append(wl, workspaceTeamCycleCadencePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_team_cycle_cadences row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_team_cycle_cadences")
	}

	if !cached {
		workspaceTeamCycleCadenceUpdateCacheMut.Lock()
		workspaceTeamCycleCadenceUpdateCache[key] = cache
		workspaceTeamCycleCadenceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceTeamCycleCadenceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_team_cycle_cadences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_team_cycle_cadences")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceTeamCycleCadenceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamCycleCadencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_team_cycle_cadences\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceTeamCycleCadencePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceTeamCycleCadence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceTeamCycleCadence")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceTeamCycleCadence) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_cycle_cadences provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamCycleCadenceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceTeamCycleCadenceUpsertCacheMut.RLock()
	cache, cached := workspaceTeamCycleCadenceUpsertCache[key]
	workspaceTeamCycleCadenceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceTeamCycleCadenceAllColumns,
			workspaceTeamCycleCadenceColumnsWithDefault,
			workspaceTeamCycleCadenceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceTeamCycleCadenceAllColumns,
			workspaceTeamCycleCadencePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_team_cycle_cadences, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceTeamCycleCadenceAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceTeamCycleCadencePrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_team_cycle_cadences, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceTeamCycleCadencePrimaryKeyColumns))
			copy(conflict, workspaceTeamCycleCadencePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_team_cycle_cadences\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamCycleCadenceType, workspaceTeamCycleCadenceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceTeamCycleCadenceType, workspaceTeamCycleCadenceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_team_cycle_cadences")
	}

	if !cached {
		workspaceTeamCycleCadenceUpsertCacheMut.Lock()
		workspaceTeamCycleCadenceUpsertCache[key] = cache
		workspaceTeamCycleCadenceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceTeamCycleCadence record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceTeamCycleCadence) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceTeamCycleCadence provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceTeamCycleCadencePrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_team_cycle_cadences\" WHERE \"team_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_team_cycle_cadences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_team_cycle_cadences")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceTeamCycleCadenceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceTeamCycleCadenceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_team_cycle_cadences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_cycle_cadences")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceTeamCycleCadenceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceTeamCycleCadenceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamCycleCadencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_team_cycle_cadences\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamCycleCadencePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceTeamCycleCadence slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_cycle_cadences")
	}

	if len(workspaceTeamCycleCadenceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceTeamCycleCadence) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceTeamCycleCadence(ctx, exec, o.TeamID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceTeamCycleCadenceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceTeamCycleCadenceSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamCycleCadencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_team_cycle_cadences\".* FROM \"workspace_team_cycle_cadences\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamCycleCadencePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceTeamCycleCadenceSlice")
	}

	*o = slice

	return nil
}

// WorkspaceTeamCycleCadenceExists checks if the WorkspaceTeamCycleCadence row exists.
func WorkspaceTeamCycleCadenceExists(ctx context.Context, exec boil.ContextExecutor, teamID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_team_cycle_cadences\" where \"team_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, teamID)
	}
	row := exec.QueryRowContext(ctx, sql, teamID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_team_cycle_cadences exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceTeamCycleCadence row exists.
func (o *WorkspaceTeamCycleCadence) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceTeamCycleCadenceExists(ctx, exec, o.TeamID)
}
