// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceAgentMCPConnection is an object representing the database table.
type WorkspaceAgentMCPConnection struct {
	ServerID             string            `boil:"server_id" json:"server_id" toml:"server_id" yaml:"server_id"`
	WorkspaceID          string            `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	Status               string            `boil:"status" json:"status" toml:"status" yaml:"status"`
	Issuer               string            `boil:"issuer" json:"issuer" toml:"issuer" yaml:"issuer"`
	TokenEndpoint        string            `boil:"token_endpoint" json:"token_endpoint" toml:"token_endpoint" yaml:"token_endpoint"`
	ClientID             string            `boil:"client_id" json:"client_id" toml:"client_id" yaml:"client_id"`
	ClientSecretSealed   null.Bytes        `boil:"client_secret_sealed" json:"client_secret_sealed,omitempty" toml:"client_secret_sealed" yaml:"client_secret_sealed,omitempty"`
	Scopes               types.StringArray `boil:"scopes" json:"scopes" toml:"scopes" yaml:"scopes"`
	AccessTokenSealed    []byte            `boil:"access_token_sealed" json:"access_token_sealed" toml:"access_token_sealed" yaml:"access_token_sealed"`
	RefreshTokenSealed   null.Bytes        `boil:"refresh_token_sealed" json:"refresh_token_sealed,omitempty" toml:"refresh_token_sealed" yaml:"refresh_token_sealed,omitempty"`
	ExpiresAt            null.Time         `boil:"expires_at" json:"expires_at,omitempty" toml:"expires_at" yaml:"expires_at,omitempty"`
	Failure              string            `boil:"failure" json:"failure" toml:"failure" yaml:"failure"`
	ConnectedByAccountID string            `boil:"connected_by_account_id" json:"connected_by_account_id" toml:"connected_by_account_id" yaml:"connected_by_account_id"`
	ConnectedAt          time.Time         `boil:"connected_at" json:"connected_at" toml:"connected_at" yaml:"connected_at"`
	UpdatedAt            time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceAgentMCPConnectionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceAgentMCPConnectionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceAgentMCPConnectionColumns = struct {
	ServerID             string
	WorkspaceID          string
	Status               string
	Issuer               string
	TokenEndpoint        string
	ClientID             string
	ClientSecretSealed   string
	Scopes               string
	AccessTokenSealed    string
	RefreshTokenSealed   string
	ExpiresAt            string
	Failure              string
	ConnectedByAccountID string
	ConnectedAt          string
	UpdatedAt            string
}{
	ServerID:             "server_id",
	WorkspaceID:          "workspace_id",
	Status:               "status",
	Issuer:               "issuer",
	TokenEndpoint:        "token_endpoint",
	ClientID:             "client_id",
	ClientSecretSealed:   "client_secret_sealed",
	Scopes:               "scopes",
	AccessTokenSealed:    "access_token_sealed",
	RefreshTokenSealed:   "refresh_token_sealed",
	ExpiresAt:            "expires_at",
	Failure:              "failure",
	ConnectedByAccountID: "connected_by_account_id",
	ConnectedAt:          "connected_at",
	UpdatedAt:            "updated_at",
}

var WorkspaceAgentMCPConnectionTableColumns = struct {
	ServerID             string
	WorkspaceID          string
	Status               string
	Issuer               string
	TokenEndpoint        string
	ClientID             string
	ClientSecretSealed   string
	Scopes               string
	AccessTokenSealed    string
	RefreshTokenSealed   string
	ExpiresAt            string
	Failure              string
	ConnectedByAccountID string
	ConnectedAt          string
	UpdatedAt            string
}{
	ServerID:             "workspace_agent_mcp_connections.server_id",
	WorkspaceID:          "workspace_agent_mcp_connections.workspace_id",
	Status:               "workspace_agent_mcp_connections.status",
	Issuer:               "workspace_agent_mcp_connections.issuer",
	TokenEndpoint:        "workspace_agent_mcp_connections.token_endpoint",
	ClientID:             "workspace_agent_mcp_connections.client_id",
	ClientSecretSealed:   "workspace_agent_mcp_connections.client_secret_sealed",
	Scopes:               "workspace_agent_mcp_connections.scopes",
	AccessTokenSealed:    "workspace_agent_mcp_connections.access_token_sealed",
	RefreshTokenSealed:   "workspace_agent_mcp_connections.refresh_token_sealed",
	ExpiresAt:            "workspace_agent_mcp_connections.expires_at",
	Failure:              "workspace_agent_mcp_connections.failure",
	ConnectedByAccountID: "workspace_agent_mcp_connections.connected_by_account_id",
	ConnectedAt:          "workspace_agent_mcp_connections.connected_at",
	UpdatedAt:            "workspace_agent_mcp_connections.updated_at",
}

// Generated where

var WorkspaceAgentMCPConnectionWhere = struct {
	ServerID             whereHelperstring
	WorkspaceID          whereHelperstring
	Status               whereHelperstring
	Issuer               whereHelperstring
	TokenEndpoint        whereHelperstring
	ClientID             whereHelperstring
	ClientSecretSealed   whereHelpernull_Bytes
	Scopes               whereHelpertypes_StringArray
	AccessTokenSealed    whereHelper__byte
	RefreshTokenSealed   whereHelpernull_Bytes
	ExpiresAt            whereHelpernull_Time
	Failure              whereHelperstring
	ConnectedByAccountID whereHelperstring
	ConnectedAt          whereHelpertime_Time
	UpdatedAt            whereHelpertime_Time
}{
	ServerID:             whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"server_id\""},
	WorkspaceID:          whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"workspace_id\""},
	Status:               whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"status\""},
	Issuer:               whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"issuer\""},
	TokenEndpoint:        whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"token_endpoint\""},
	ClientID:             whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"client_id\""},
	ClientSecretSealed:   whereHelpernull_Bytes{field: "\"workspace_agent_mcp_connections\".\"client_secret_sealed\""},
	Scopes:               whereHelpertypes_StringArray{field: "\"workspace_agent_mcp_connections\".\"scopes\""},
	AccessTokenSealed:    whereHelper__byte{field: "\"workspace_agent_mcp_connections\".\"access_token_sealed\""},
	RefreshTokenSealed:   whereHelpernull_Bytes{field: "\"workspace_agent_mcp_connections\".\"refresh_token_sealed\""},
	ExpiresAt:            whereHelpernull_Time{field: "\"workspace_agent_mcp_connections\".\"expires_at\""},
	Failure:              whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"failure\""},
	ConnectedByAccountID: whereHelperstring{field: "\"workspace_agent_mcp_connections\".\"connected_by_account_id\""},
	ConnectedAt:          whereHelpertime_Time{field: "\"workspace_agent_mcp_connections\".\"connected_at\""},
	UpdatedAt:            whereHelpertime_Time{field: "\"workspace_agent_mcp_connections\".\"updated_at\""},
}

// WorkspaceAgentMCPConnectionRels is where relationship names are stored.
var WorkspaceAgentMCPConnectionRels = struct {
	ConnectedByAccount string
	Server             string
	Workspace          string
}{
	ConnectedByAccount: "ConnectedByAccount",
	Server:             "Server",
	Workspace:          "Workspace",
}

// workspaceAgentMCPConnectionR is where relationships are stored.
type workspaceAgentMCPConnectionR struct {
	ConnectedByAccount *Account                 `boil:"ConnectedByAccount" json:"ConnectedByAccount" toml:"ConnectedByAccount" yaml:"ConnectedByAccount"`
	Server             *WorkspaceAgentMCPServer `boil:"Server" json:"Server" toml:"Server" yaml:"Server"`
	Workspace          *Workspace               `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*workspaceAgentMCPConnectionR) NewStruct() *workspaceAgentMCPConnectionR {
	return &workspaceAgentMCPConnectionR{}
}

func (o *WorkspaceAgentMCPConnection) GetConnectedByAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetConnectedByAccount()
}

func (r *workspaceAgentMCPConnectionR) GetConnectedByAccount() *Account {
	if r == nil {
		return nil
	}

	return r.ConnectedByAccount
}

func (o *WorkspaceAgentMCPConnection) GetServer() *WorkspaceAgentMCPServer {
	if o == nil {
		return nil
	}

	return o.R.GetServer()
}

func (r *workspaceAgentMCPConnectionR) GetServer() *WorkspaceAgentMCPServer {
	if r == nil {
		return nil
	}

	return r.Server
}

func (o *WorkspaceAgentMCPConnection) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceAgentMCPConnectionR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// workspaceAgentMCPConnectionL is where Load methods for each relationship are stored.
type workspaceAgentMCPConnectionL struct{}

var (
	workspaceAgentMCPConnectionAllColumns            = []string{"server_id", "workspace_id", "status", "issuer", "token_endpoint", "client_id", "client_secret_sealed", "scopes", "access_token_sealed", "refresh_token_sealed", "expires_at", "failure", "connected_by_account_id", "connected_at", "updated_at"}
	workspaceAgentMCPConnectionColumnsWithoutDefault = []string{"server_id", "workspace_id", "status", "issuer", "token_endpoint", "client_id", "access_token_sealed", "connected_by_account_id", "connected_at"}
	workspaceAgentMCPConnectionColumnsWithDefault    = []string{"client_secret_sealed", "scopes", "refresh_token_sealed", "expires_at", "failure", "updated_at"}
	workspaceAgentMCPConnectionPrimaryKeyColumns     = []string{"server_id"}
	workspaceAgentMCPConnectionGeneratedColumns      = []string{}
)

type (
	// WorkspaceAgentMCPConnectionSlice is an alias for a slice of pointers to WorkspaceAgentMCPConnection.
	// This should almost always be used instead of []WorkspaceAgentMCPConnection.
	WorkspaceAgentMCPConnectionSlice []*WorkspaceAgentMCPConnection
	// WorkspaceAgentMCPConnectionHook is the signature for custom WorkspaceAgentMCPConnection hook methods
	WorkspaceAgentMCPConnectionHook func(context.Context, boil.ContextExecutor, *WorkspaceAgentMCPConnection) error

	workspaceAgentMCPConnectionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceAgentMCPConnectionType                 = reflect.TypeOf(&WorkspaceAgentMCPConnection{})
	workspaceAgentMCPConnectionMapping              = queries.MakeStructMapping(workspaceAgentMCPConnectionType)
	workspaceAgentMCPConnectionPrimaryKeyMapping, _ = queries.BindMapping(workspaceAgentMCPConnectionType, workspaceAgentMCPConnectionMapping, workspaceAgentMCPConnectionPrimaryKeyColumns)
	workspaceAgentMCPConnectionInsertCacheMut       sync.RWMutex
	workspaceAgentMCPConnectionInsertCache          = make(map[string]insertCache)
	workspaceAgentMCPConnectionUpdateCacheMut       sync.RWMutex
	workspaceAgentMCPConnectionUpdateCache          = make(map[string]updateCache)
	workspaceAgentMCPConnectionUpsertCacheMut       sync.RWMutex
	workspaceAgentMCPConnectionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceAgentMCPConnectionAfterSelectMu sync.Mutex
var workspaceAgentMCPConnectionAfterSelectHooks []WorkspaceAgentMCPConnectionHook

var workspaceAgentMCPConnectionBeforeInsertMu sync.Mutex
var workspaceAgentMCPConnectionBeforeInsertHooks []WorkspaceAgentMCPConnectionHook
var workspaceAgentMCPConnectionAfterInsertMu sync.Mutex
var workspaceAgentMCPConnectionAfterInsertHooks []WorkspaceAgentMCPConnectionHook

var workspaceAgentMCPConnectionBeforeUpdateMu sync.Mutex
var workspaceAgentMCPConnectionBeforeUpdateHooks []WorkspaceAgentMCPConnectionHook
var workspaceAgentMCPConnectionAfterUpdateMu sync.Mutex
var workspaceAgentMCPConnectionAfterUpdateHooks []WorkspaceAgentMCPConnectionHook

var workspaceAgentMCPConnectionBeforeDeleteMu sync.Mutex
var workspaceAgentMCPConnectionBeforeDeleteHooks []WorkspaceAgentMCPConnectionHook
var workspaceAgentMCPConnectionAfterDeleteMu sync.Mutex
var workspaceAgentMCPConnectionAfterDeleteHooks []WorkspaceAgentMCPConnectionHook

var workspaceAgentMCPConnectionBeforeUpsertMu sync.Mutex
var workspaceAgentMCPConnectionBeforeUpsertHooks []WorkspaceAgentMCPConnectionHook
var workspaceAgentMCPConnectionAfterUpsertMu sync.Mutex
var workspaceAgentMCPConnectionAfterUpsertHooks []WorkspaceAgentMCPConnectionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceAgentMCPConnection) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceAgentMCPConnection) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceAgentMCPConnection) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceAgentMCPConnection) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceAgentMCPConnection) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceAgentMCPConnection) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceAgentMCPConnection) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceAgentMCPConnection) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceAgentMCPConnection) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentMCPConnectionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceAgentMCPConnectionHook registers your hook function for all future operations.
func AddWorkspaceAgentMCPConnectionHook(hookPoint boil.HookPoint, workspaceAgentMCPConnectionHook WorkspaceAgentMCPConnectionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceAgentMCPConnectionAfterSelectMu.Lock()
		workspaceAgentMCPConnectionAfterSelectHooks = append(workspaceAgentMCPConnectionAfterSelectHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceAgentMCPConnectionBeforeInsertMu.Lock()
		workspaceAgentMCPConnectionBeforeInsertHooks = append(workspaceAgentMCPConnectionBeforeInsertHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceAgentMCPConnectionAfterInsertMu.Lock()
		workspaceAgentMCPConnectionAfterInsertHooks = append(workspaceAgentMCPConnectionAfterInsertHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceAgentMCPConnectionBeforeUpdateMu.Lock()
		workspaceAgentMCPConnectionBeforeUpdateHooks = append(workspaceAgentMCPConnectionBeforeUpdateHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceAgentMCPConnectionAfterUpdateMu.Lock()
		workspaceAgentMCPConnectionAfterUpdateHooks = append(workspaceAgentMCPConnectionAfterUpdateHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceAgentMCPConnectionBeforeDeleteMu.Lock()
		workspaceAgentMCPConnectionBeforeDeleteHooks = append(workspaceAgentMCPConnectionBeforeDeleteHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceAgentMCPConnectionAfterDeleteMu.Lock()
		workspaceAgentMCPConnectionAfterDeleteHooks = append(workspaceAgentMCPConnectionAfterDeleteHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceAgentMCPConnectionBeforeUpsertMu.Lock()
		workspaceAgentMCPConnectionBeforeUpsertHooks = append(workspaceAgentMCPConnectionBeforeUpsertHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceAgentMCPConnectionAfterUpsertMu.Lock()
		workspaceAgentMCPConnectionAfterUpsertHooks = append(workspaceAgentMCPConnectionAfterUpsertHooks, workspaceAgentMCPConnectionHook)
		workspaceAgentMCPConnectionAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceAgentMCPConnection record from the query.
func (q workspaceAgentMCPConnectionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceAgentMCPConnection, error) {
	o := &WorkspaceAgentMCPConnection{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_agent_mcp_connections")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceAgentMCPConnection records from the query.
func (q workspaceAgentMCPConnectionQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceAgentMCPConnectionSlice, error) {
	var o []*WorkspaceAgentMCPConnection

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceAgentMCPConnection slice")
	}

	if len(workspaceAgentMCPConnectionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceAgentMCPConnection records in the query.
func (q workspaceAgentMCPConnectionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_agent_mcp_connections rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceAgentMCPConnectionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_agent_mcp_connections exists")
	}

	return count > 0, nil
}

// ConnectedByAccount pointed to by the foreign key.
func (o *WorkspaceAgentMCPConnection) ConnectedByAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ConnectedByAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Server pointed to by the foreign key.
func (o *WorkspaceAgentMCPConnection) Server(mods ...qm.QueryMod) workspaceAgentMCPServerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ServerID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceAgentMCPServers(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceAgentMCPConnection) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadConnectedByAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentMCPConnectionL) LoadConnectedByAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentMCPConnection any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentMCPConnection
	var object *WorkspaceAgentMCPConnection

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentMCPConnection.(*WorkspaceAgentMCPConnection)
		if !ok {
			object = new(WorkspaceAgentMCPConnection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentMCPConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentMCPConnection))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentMCPConnection.(*[]*WorkspaceAgentMCPConnection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentMCPConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentMCPConnection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentMCPConnectionR{}
		}
		args[object.ConnectedByAccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentMCPConnectionR{}
			}

			args[obj.ConnectedByAccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ConnectedByAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.ConnectedByAccountWorkspaceAgentMCPConnections = append(foreign.R.ConnectedByAccountWorkspaceAgentMCPConnections, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ConnectedByAccountID == foreign.ID {
				local.R.ConnectedByAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.ConnectedByAccountWorkspaceAgentMCPConnections = append(foreign.R.ConnectedByAccountWorkspaceAgentMCPConnections, local)
				break
			}
		}
	}

	return nil
}

// LoadServer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentMCPConnectionL) LoadServer(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentMCPConnection any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentMCPConnection
	var object *WorkspaceAgentMCPConnection

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentMCPConnection.(*WorkspaceAgentMCPConnection)
		if !ok {
			object = new(WorkspaceAgentMCPConnection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentMCPConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentMCPConnection))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentMCPConnection.(*[]*WorkspaceAgentMCPConnection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentMCPConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentMCPConnection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentMCPConnectionR{}
		}
		args[object.ServerID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentMCPConnectionR{}
			}

			args[obj.ServerID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agent_mcp_servers`),
		qm.WhereIn(`workspace_agent_mcp_servers.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceAgentMCPServer")
	}

	var resultSlice []*WorkspaceAgentMCPServer
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceAgentMCPServer")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_agent_mcp_servers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agent_mcp_servers")
	}

	if len(workspaceAgentMCPServerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Server = foreign
		if foreign.R == nil {
			foreign.R = &workspaceAgentMCPServerR{}
		}
		foreign.R.ServerWorkspaceAgentMCPConnection = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ServerID == foreign.ID {
				local.R.Server = foreign
				if foreign.R == nil {
					foreign.R = &workspaceAgentMCPServerR{}
				}
				foreign.R.ServerWorkspaceAgentMCPConnection = local
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentMCPConnectionL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentMCPConnection any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentMCPConnection
	var object *WorkspaceAgentMCPConnection

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentMCPConnection.(*WorkspaceAgentMCPConnection)
		if !ok {
			object = new(WorkspaceAgentMCPConnection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentMCPConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentMCPConnection))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentMCPConnection.(*[]*WorkspaceAgentMCPConnection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentMCPConnection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentMCPConnection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentMCPConnectionR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentMCPConnectionR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceAgentMCPConnections = append(foreign.R.WorkspaceAgentMCPConnections, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceAgentMCPConnections = append(foreign.R.WorkspaceAgentMCPConnections, local)
				break
			}
		}
	}

	return nil
}

// SetConnectedByAccount of the workspaceAgentMCPConnection to the related item.
// Sets o.R.ConnectedByAccount to related.
// Adds o to related.R.ConnectedByAccountWorkspaceAgentMCPConnections.
func (o *WorkspaceAgentMCPConnection) SetConnectedByAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_mcp_connections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"connected_by_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentMCPConnectionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ServerID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ConnectedByAccountID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentMCPConnectionR{
			ConnectedByAccount: related,
		}
	} else {
		o.R.ConnectedByAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			ConnectedByAccountWorkspaceAgentMCPConnections: WorkspaceAgentMCPConnectionSlice{o},
		}
	} else {
		related.R.ConnectedByAccountWorkspaceAgentMCPConnections = append(related.R.ConnectedByAccountWorkspaceAgentMCPConnections, o)
	}

	return nil
}

// SetServer of the workspaceAgentMCPConnection to the related item.
// Sets o.R.Server to related.
// Adds o to related.R.ServerWorkspaceAgentMCPConnection.
func (o *WorkspaceAgentMCPConnection) SetServer(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceAgentMCPServer) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_mcp_connections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"server_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentMCPConnectionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ServerID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ServerID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentMCPConnectionR{
			Server: related,
		}
	} else {
		o.R.Server = related
	}

	if related.R == nil {
		related.R = &workspaceAgentMCPServerR{
			ServerWorkspaceAgentMCPConnection: o,
		}
	} else {
		related.R.ServerWorkspaceAgentMCPConnection = o
	}

	return nil
}

// SetWorkspace of the workspaceAgentMCPConnection to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceAgentMCPConnections.
func (o *WorkspaceAgentMCPConnection) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_mcp_connections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentMCPConnectionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ServerID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentMCPConnectionR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceAgentMCPConnections: WorkspaceAgentMCPConnectionSlice{o},
		}
	} else {
		related.R.WorkspaceAgentMCPConnections = append(related.R.WorkspaceAgentMCPConnections, o)
	}

	return nil
}

// WorkspaceAgentMCPConnections retrieves all the records using an executor.
func WorkspaceAgentMCPConnections(mods ...qm.QueryMod) workspaceAgentMCPConnectionQuery {
	mods = append(mods, qm.From("\"workspace_agent_mcp_connections\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_agent_mcp_connections\".*"})
	}

	return workspaceAgentMCPConnectionQuery{q}
}

// FindWorkspaceAgentMCPConnection retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceAgentMCPConnection(ctx context.Context, exec boil.ContextExecutor, serverID string, selectCols ...string) (*WorkspaceAgentMCPConnection, error) {
	workspaceAgentMCPConnectionObj := &WorkspaceAgentMCPConnection{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_agent_mcp_connections\" where \"server_id\"=$1", sel,
	)

	q := queries.Raw(query, serverID)

	err := q.Bind(ctx, exec, workspaceAgentMCPConnectionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_agent_mcp_connections")
	}

	if err = workspaceAgentMCPConnectionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceAgentMCPConnectionObj, err
	}

	return workspaceAgentMCPConnectionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceAgentMCPConnection) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_mcp_connections provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentMCPConnectionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceAgentMCPConnectionInsertCacheMut.RLock()
	cache, cached := workspaceAgentMCPConnectionInsertCache[key]
	workspaceAgentMCPConnectionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceAgentMCPConnectionAllColumns,
			workspaceAgentMCPConnectionColumnsWithDefault,
			workspaceAgentMCPConnectionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentMCPConnectionType, workspaceAgentMCPConnectionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceAgentMCPConnectionType, workspaceAgentMCPConnectionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_agent_mcp_connections\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_agent_mcp_connections\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_agent_mcp_connections")
	}

	if !cached {
		workspaceAgentMCPConnectionInsertCacheMut.Lock()
		workspaceAgentMCPConnectionInsertCache[key] = cache
		workspaceAgentMCPConnectionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceAgentMCPConnection.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceAgentMCPConnection) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceAgentMCPConnectionUpdateCacheMut.RLock()
	cache, cached := workspaceAgentMCPConnectionUpdateCache[key]
	workspaceAgentMCPConnectionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceAgentMCPConnectionAllColumns,
			workspaceAgentMCPConnectionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_agent_mcp_connections, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_agent_mcp_connections\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceAgentMCPConnectionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceAgentMCPConnectionType, workspaceAgentMCPConnectionMapping, append(wl, workspaceAgentMCPConnectionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_agent_mcp_connections row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_agent_mcp_connections")
	}

	if !cached {
		workspaceAgentMCPConnectionUpdateCacheMut.Lock()
		workspaceAgentMCPConnectionUpdateCache[key] = cache
		workspaceAgentMCPConnectionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceAgentMCPConnectionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_agent_mcp_connections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_agent_mcp_connections")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceAgentMCPConnectionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentMCPConnectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_agent_mcp_connections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceAgentMCPConnectionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceAgentMCPConnection slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceAgentMCPConnection")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceAgentMCPConnection) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_mcp_connections provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentMCPConnectionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceAgentMCPConnectionUpsertCacheMut.RLock()
	cache, cached := workspaceAgentMCPConnectionUpsertCache[key]
	workspaceAgentMCPConnectionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceAgentMCPConnectionAllColumns,
			workspaceAgentMCPConnectionColumnsWithDefault,
			workspaceAgentMCPConnectionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceAgentMCPConnectionAllColumns,
			workspaceAgentMCPConnectionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_agent_mcp_connections, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceAgentMCPConnectionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceAgentMCPConnectionPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_agent_mcp_connections, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceAgentMCPConnectionPrimaryKeyColumns))
			copy(conflict, workspaceAgentMCPConnectionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_agent_mcp_connections\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentMCPConnectionType, workspaceAgentMCPConnectionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceAgentMCPConnectionType, workspaceAgentMCPConnectionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_agent_mcp_connections")
	}

	if !cached {
		workspaceAgentMCPConnectionUpsertCacheMut.Lock()
		workspaceAgentMCPConnectionUpsertCache[key] = cache
		workspaceAgentMCPConnectionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceAgentMCPConnection record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceAgentMCPConnection) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceAgentMCPConnection provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceAgentMCPConnectionPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_agent_mcp_connections\" WHERE \"server_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_agent_mcp_connections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_agent_mcp_connections")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceAgentMCPConnectionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceAgentMCPConnectionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_agent_mcp_connections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_mcp_connections")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceAgentMCPConnectionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceAgentMCPConnectionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentMCPConnectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_agent_mcp_connections\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentMCPConnectionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceAgentMCPConnection slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_mcp_connections")
	}

	if len(workspaceAgentMCPConnectionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceAgentMCPConnection) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceAgentMCPConnection(ctx, exec, o.ServerID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceAgentMCPConnectionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceAgentMCPConnectionSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentMCPConnectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_agent_mcp_connections\".* FROM \"workspace_agent_mcp_connections\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentMCPConnectionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceAgentMCPConnectionSlice")
	}

	*o = slice

	return nil
}

// WorkspaceAgentMCPConnectionExists checks if the WorkspaceAgentMCPConnection row exists.
func WorkspaceAgentMCPConnectionExists(ctx context.Context, exec boil.ContextExecutor, serverID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_agent_mcp_connections\" where \"server_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, serverID)
	}
	row := exec.QueryRowContext(ctx, sql, serverID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_agent_mcp_connections exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceAgentMCPConnection row exists.
func (o *WorkspaceAgentMCPConnection) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceAgentMCPConnectionExists(ctx, exec, o.ServerID)
}
