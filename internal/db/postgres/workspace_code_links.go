// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceCodeLink is an object representing the database table.
type WorkspaceCodeLink struct {
	ID              string            `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID     string            `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	IssueID         string            `boil:"issue_id" json:"issue_id" toml:"issue_id" yaml:"issue_id"`
	RepositoryID    null.String       `boil:"repository_id" json:"repository_id,omitempty" toml:"repository_id" yaml:"repository_id,omitempty"`
	Provider        string            `boil:"provider" json:"provider" toml:"provider" yaml:"provider"`
	RepositoryName  string            `boil:"repository_name" json:"repository_name" toml:"repository_name" yaml:"repository_name"`
	Kind            string            `boil:"kind" json:"kind" toml:"kind" yaml:"kind"`
	ExternalID      string            `boil:"external_id" json:"external_id" toml:"external_id" yaml:"external_id"`
	Number          int               `boil:"number" json:"number" toml:"number" yaml:"number"`
	Title           string            `boil:"title" json:"title" toml:"title" yaml:"title"`
	URL             string            `boil:"url" json:"url" toml:"url" yaml:"url"`
	State           string            `boil:"state" json:"state" toml:"state" yaml:"state"`
	Action          string            `boil:"action" json:"action" toml:"action" yaml:"action"`
	Author          string            `boil:"author" json:"author" toml:"author" yaml:"author"`
	HeadBranch      string            `boil:"head_branch" json:"head_branch" toml:"head_branch" yaml:"head_branch"`
	BaseBranch      string            `boil:"base_branch" json:"base_branch" toml:"base_branch" yaml:"base_branch"`
	Paths           types.StringArray `boil:"paths" json:"paths" toml:"paths" yaml:"paths"`
	DetectedIn      string            `boil:"detected_in" json:"detected_in" toml:"detected_in" yaml:"detected_in"`
	Resolving       bool              `boil:"resolving" json:"resolving" toml:"resolving" yaml:"resolving"`
	SourceUpdatedAt null.Time         `boil:"source_updated_at" json:"source_updated_at,omitempty" toml:"source_updated_at" yaml:"source_updated_at,omitempty"`
	MergedAt        null.Time         `boil:"merged_at" json:"merged_at,omitempty" toml:"merged_at" yaml:"merged_at,omitempty"`
	ClosedAt        null.Time         `boil:"closed_at" json:"closed_at,omitempty" toml:"closed_at" yaml:"closed_at,omitempty"`
	CreatedAt       time.Time         `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceCodeLinkR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceCodeLinkL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceCodeLinkColumns = struct {
	ID              string
	WorkspaceID     string
	IssueID         string
	RepositoryID    string
	Provider        string
	RepositoryName  string
	Kind            string
	ExternalID      string
	Number          string
	Title           string
	URL             string
	State           string
	Action          string
	Author          string
	HeadBranch      string
	BaseBranch      string
	Paths           string
	DetectedIn      string
	Resolving       string
	SourceUpdatedAt string
	MergedAt        string
	ClosedAt        string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	WorkspaceID:     "workspace_id",
	IssueID:         "issue_id",
	RepositoryID:    "repository_id",
	Provider:        "provider",
	RepositoryName:  "repository_name",
	Kind:            "kind",
	ExternalID:      "external_id",
	Number:          "number",
	Title:           "title",
	URL:             "url",
	State:           "state",
	Action:          "action",
	Author:          "author",
	HeadBranch:      "head_branch",
	BaseBranch:      "base_branch",
	Paths:           "paths",
	DetectedIn:      "detected_in",
	Resolving:       "resolving",
	SourceUpdatedAt: "source_updated_at",
	MergedAt:        "merged_at",
	ClosedAt:        "closed_at",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var WorkspaceCodeLinkTableColumns = struct {
	ID              string
	WorkspaceID     string
	IssueID         string
	RepositoryID    string
	Provider        string
	RepositoryName  string
	Kind            string
	ExternalID      string
	Number          string
	Title           string
	URL             string
	State           string
	Action          string
	Author          string
	HeadBranch      string
	BaseBranch      string
	Paths           string
	DetectedIn      string
	Resolving       string
	SourceUpdatedAt string
	MergedAt        string
	ClosedAt        string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "workspace_code_links.id",
	WorkspaceID:     "workspace_code_links.workspace_id",
	IssueID:         "workspace_code_links.issue_id",
	RepositoryID:    "workspace_code_links.repository_id",
	Provider:        "workspace_code_links.provider",
	RepositoryName:  "workspace_code_links.repository_name",
	Kind:            "workspace_code_links.kind",
	ExternalID:      "workspace_code_links.external_id",
	Number:          "workspace_code_links.number",
	Title:           "workspace_code_links.title",
	URL:             "workspace_code_links.url",
	State:           "workspace_code_links.state",
	Action:          "workspace_code_links.action",
	Author:          "workspace_code_links.author",
	HeadBranch:      "workspace_code_links.head_branch",
	BaseBranch:      "workspace_code_links.base_branch",
	Paths:           "workspace_code_links.paths",
	DetectedIn:      "workspace_code_links.detected_in",
	Resolving:       "workspace_code_links.resolving",
	SourceUpdatedAt: "workspace_code_links.source_updated_at",
	MergedAt:        "workspace_code_links.merged_at",
	ClosedAt:        "workspace_code_links.closed_at",
	CreatedAt:       "workspace_code_links.created_at",
	UpdatedAt:       "workspace_code_links.updated_at",
}

// Generated where

var WorkspaceCodeLinkWhere = struct {
	ID              whereHelperstring
	WorkspaceID     whereHelperstring
	IssueID         whereHelperstring
	RepositoryID    whereHelpernull_String
	Provider        whereHelperstring
	RepositoryName  whereHelperstring
	Kind            whereHelperstring
	ExternalID      whereHelperstring
	Number          whereHelperint
	Title           whereHelperstring
	URL             whereHelperstring
	State           whereHelperstring
	Action          whereHelperstring
	Author          whereHelperstring
	HeadBranch      whereHelperstring
	BaseBranch      whereHelperstring
	Paths           whereHelpertypes_StringArray
	DetectedIn      whereHelperstring
	Resolving       whereHelperbool
	SourceUpdatedAt whereHelpernull_Time
	MergedAt        whereHelpernull_Time
	ClosedAt        whereHelpernull_Time
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
}{
	ID:              whereHelperstring{field: "\"workspace_code_links\".\"id\""},
	WorkspaceID:     whereHelperstring{field: "\"workspace_code_links\".\"workspace_id\""},
	IssueID:         whereHelperstring{field: "\"workspace_code_links\".\"issue_id\""},
	RepositoryID:    whereHelpernull_String{field: "\"workspace_code_links\".\"repository_id\""},
	Provider:        whereHelperstring{field: "\"workspace_code_links\".\"provider\""},
	RepositoryName:  whereHelperstring{field: "\"workspace_code_links\".\"repository_name\""},
	Kind:            whereHelperstring{field: "\"workspace_code_links\".\"kind\""},
	ExternalID:      whereHelperstring{field: "\"workspace_code_links\".\"external_id\""},
	Number:          whereHelperint{field: "\"workspace_code_links\".\"number\""},
	Title:           whereHelperstring{field: "\"workspace_code_links\".\"title\""},
	URL:             whereHelperstring{field: "\"workspace_code_links\".\"url\""},
	State:           whereHelperstring{field: "\"workspace_code_links\".\"state\""},
	Action:          whereHelperstring{field: "\"workspace_code_links\".\"action\""},
	Author:          whereHelperstring{field: "\"workspace_code_links\".\"author\""},
	HeadBranch:      whereHelperstring{field: "\"workspace_code_links\".\"head_branch\""},
	BaseBranch:      whereHelperstring{field: "\"workspace_code_links\".\"base_branch\""},
	Paths:           whereHelpertypes_StringArray{field: "\"workspace_code_links\".\"paths\""},
	DetectedIn:      whereHelperstring{field: "\"workspace_code_links\".\"detected_in\""},
	Resolving:       whereHelperbool{field: "\"workspace_code_links\".\"resolving\""},
	SourceUpdatedAt: whereHelpernull_Time{field: "\"workspace_code_links\".\"source_updated_at\""},
	MergedAt:        whereHelpernull_Time{field: "\"workspace_code_links\".\"merged_at\""},
	ClosedAt:        whereHelpernull_Time{field: "\"workspace_code_links\".\"closed_at\""},
	CreatedAt:       whereHelpertime_Time{field: "\"workspace_code_links\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"workspace_code_links\".\"updated_at\""},
}

// WorkspaceCodeLinkRels is where relationship names are stored.
var WorkspaceCodeLinkRels = struct {
	Issue                            string
	Repository                       string
	Workspace                        string
	LinkWorkspaceCodeLinkTransitions string
}{
	Issue:                            "Issue",
	Repository:                       "Repository",
	Workspace:                        "Workspace",
	LinkWorkspaceCodeLinkTransitions: "LinkWorkspaceCodeLinkTransitions",
}

// workspaceCodeLinkR is where relationships are stored.
type workspaceCodeLinkR struct {
	Issue                            *WorkspaceIssue                  `boil:"Issue" json:"Issue" toml:"Issue" yaml:"Issue"`
	Repository                       *WorkspaceSCMRepository          `boil:"Repository" json:"Repository" toml:"Repository" yaml:"Repository"`
	Workspace                        *Workspace                       `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
	LinkWorkspaceCodeLinkTransitions WorkspaceCodeLinkTransitionSlice `boil:"LinkWorkspaceCodeLinkTransitions" json:"LinkWorkspaceCodeLinkTransitions" toml:"LinkWorkspaceCodeLinkTransitions" yaml:"LinkWorkspaceCodeLinkTransitions"`
}

// NewStruct creates a new relationship struct
func (*workspaceCodeLinkR) NewStruct() *workspaceCodeLinkR {
	return &workspaceCodeLinkR{}
}

func (o *WorkspaceCodeLink) GetIssue() *WorkspaceIssue {
	if o == nil {
		return nil
	}

	return o.R.GetIssue()
}

func (r *workspaceCodeLinkR) GetIssue() *WorkspaceIssue {
	if r == nil {
		return nil
	}

	return r.Issue
}

func (o *WorkspaceCodeLink) GetRepository() *WorkspaceSCMRepository {
	if o == nil {
		return nil
	}

	return o.R.GetRepository()
}

func (r *workspaceCodeLinkR) GetRepository() *WorkspaceSCMRepository {
	if r == nil {
		return nil
	}

	return r.Repository
}

func (o *WorkspaceCodeLink) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceCodeLinkR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

func (o *WorkspaceCodeLink) GetLinkWorkspaceCodeLinkTransitions() WorkspaceCodeLinkTransitionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetLinkWorkspaceCodeLinkTransitions()
}

func (r *workspaceCodeLinkR) GetLinkWorkspaceCodeLinkTransitions() WorkspaceCodeLinkTransitionSlice {
	if r == nil {
		return nil
	}

	return r.LinkWorkspaceCodeLinkTransitions
}

// workspaceCodeLinkL is where Load methods for each relationship are stored.
type workspaceCodeLinkL struct{}

var (
	workspaceCodeLinkAllColumns            = []string{"id", "workspace_id", "issue_id", "repository_id", "provider", "repository_name", "kind", "external_id", "number", "title", "url", "state", "action", "author", "head_branch", "base_branch", "paths", "detected_in", "resolving", "source_updated_at", "merged_at", "closed_at", "created_at", "updated_at"}
	workspaceCodeLinkColumnsWithoutDefault = []string{"workspace_id", "issue_id", "provider", "repository_name", "kind", "external_id"}
	workspaceCodeLinkColumnsWithDefault    = []string{"id", "repository_id", "number", "title", "url", "state", "action", "author", "head_branch", "base_branch", "paths", "detected_in", "resolving", "source_updated_at", "merged_at", "closed_at", "created_at", "updated_at"}
	workspaceCodeLinkPrimaryKeyColumns     = []string{"id"}
	workspaceCodeLinkGeneratedColumns      = []string{}
)

type (
	// WorkspaceCodeLinkSlice is an alias for a slice of pointers to WorkspaceCodeLink.
	// This should almost always be used instead of []WorkspaceCodeLink.
	WorkspaceCodeLinkSlice []*WorkspaceCodeLink
	// WorkspaceCodeLinkHook is the signature for custom WorkspaceCodeLink hook methods
	WorkspaceCodeLinkHook func(context.Context, boil.ContextExecutor, *WorkspaceCodeLink) error

	workspaceCodeLinkQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceCodeLinkType                 = reflect.TypeOf(&WorkspaceCodeLink{})
	workspaceCodeLinkMapping              = queries.MakeStructMapping(workspaceCodeLinkType)
	workspaceCodeLinkPrimaryKeyMapping, _ = queries.BindMapping(workspaceCodeLinkType, workspaceCodeLinkMapping, workspaceCodeLinkPrimaryKeyColumns)
	workspaceCodeLinkInsertCacheMut       sync.RWMutex
	workspaceCodeLinkInsertCache          = make(map[string]insertCache)
	workspaceCodeLinkUpdateCacheMut       sync.RWMutex
	workspaceCodeLinkUpdateCache          = make(map[string]updateCache)
	workspaceCodeLinkUpsertCacheMut       sync.RWMutex
	workspaceCodeLinkUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceCodeLinkAfterSelectMu sync.Mutex
var workspaceCodeLinkAfterSelectHooks []WorkspaceCodeLinkHook

var workspaceCodeLinkBeforeInsertMu sync.Mutex
var workspaceCodeLinkBeforeInsertHooks []WorkspaceCodeLinkHook
var workspaceCodeLinkAfterInsertMu sync.Mutex
var workspaceCodeLinkAfterInsertHooks []WorkspaceCodeLinkHook

var workspaceCodeLinkBeforeUpdateMu sync.Mutex
var workspaceCodeLinkBeforeUpdateHooks []WorkspaceCodeLinkHook
var workspaceCodeLinkAfterUpdateMu sync.Mutex
var workspaceCodeLinkAfterUpdateHooks []WorkspaceCodeLinkHook

var workspaceCodeLinkBeforeDeleteMu sync.Mutex
var workspaceCodeLinkBeforeDeleteHooks []WorkspaceCodeLinkHook
var workspaceCodeLinkAfterDeleteMu sync.Mutex
var workspaceCodeLinkAfterDeleteHooks []WorkspaceCodeLinkHook

var workspaceCodeLinkBeforeUpsertMu sync.Mutex
var workspaceCodeLinkBeforeUpsertHooks []WorkspaceCodeLinkHook
var workspaceCodeLinkAfterUpsertMu sync.Mutex
var workspaceCodeLinkAfterUpsertHooks []WorkspaceCodeLinkHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceCodeLink) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceCodeLink) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceCodeLink) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceCodeLink) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceCodeLink) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceCodeLink) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceCodeLink) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceCodeLink) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceCodeLink) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceCodeLinkAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceCodeLinkHook registers your hook function for all future operations.
func AddWorkspaceCodeLinkHook(hookPoint boil.HookPoint, workspaceCodeLinkHook WorkspaceCodeLinkHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceCodeLinkAfterSelectMu.Lock()
		workspaceCodeLinkAfterSelectHooks = append(workspaceCodeLinkAfterSelectHooks, workspaceCodeLinkHook)
		workspaceCodeLinkAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceCodeLinkBeforeInsertMu.Lock()
		workspaceCodeLinkBeforeInsertHooks = append(workspaceCodeLinkBeforeInsertHooks, workspaceCodeLinkHook)
		workspaceCodeLinkBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceCodeLinkAfterInsertMu.Lock()
		workspaceCodeLinkAfterInsertHooks = append(workspaceCodeLinkAfterInsertHooks, workspaceCodeLinkHook)
		workspaceCodeLinkAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceCodeLinkBeforeUpdateMu.Lock()
		workspaceCodeLinkBeforeUpdateHooks = append(workspaceCodeLinkBeforeUpdateHooks, workspaceCodeLinkHook)
		workspaceCodeLinkBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceCodeLinkAfterUpdateMu.Lock()
		workspaceCodeLinkAfterUpdateHooks = append(workspaceCodeLinkAfterUpdateHooks, workspaceCodeLinkHook)
		workspaceCodeLinkAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceCodeLinkBeforeDeleteMu.Lock()
		workspaceCodeLinkBeforeDeleteHooks = append(workspaceCodeLinkBeforeDeleteHooks, workspaceCodeLinkHook)
		workspaceCodeLinkBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceCodeLinkAfterDeleteMu.Lock()
		workspaceCodeLinkAfterDeleteHooks = append(workspaceCodeLinkAfterDeleteHooks, workspaceCodeLinkHook)
		workspaceCodeLinkAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceCodeLinkBeforeUpsertMu.Lock()
		workspaceCodeLinkBeforeUpsertHooks = append(workspaceCodeLinkBeforeUpsertHooks, workspaceCodeLinkHook)
		workspaceCodeLinkBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceCodeLinkAfterUpsertMu.Lock()
		workspaceCodeLinkAfterUpsertHooks = append(workspaceCodeLinkAfterUpsertHooks, workspaceCodeLinkHook)
		workspaceCodeLinkAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceCodeLink record from the query.
func (q workspaceCodeLinkQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceCodeLink, error) {
	o := &WorkspaceCodeLink{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_code_links")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceCodeLink records from the query.
func (q workspaceCodeLinkQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceCodeLinkSlice, error) {
	var o []*WorkspaceCodeLink

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceCodeLink slice")
	}

	if len(workspaceCodeLinkAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceCodeLink records in the query.
func (q workspaceCodeLinkQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_code_links rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceCodeLinkQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_code_links exists")
	}

	return count > 0, nil
}

// Issue pointed to by the foreign key.
func (o *WorkspaceCodeLink) Issue(mods ...qm.QueryMod) workspaceIssueQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IssueID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceIssues(queryMods...)
}

// Repository pointed to by the foreign key.
func (o *WorkspaceCodeLink) Repository(mods ...qm.QueryMod) workspaceSCMRepositoryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.RepositoryID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceSCMRepositories(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceCodeLink) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LinkWorkspaceCodeLinkTransitions retrieves all the workspace_code_link_transition's WorkspaceCodeLinkTransitions with an executor via link_id column.
func (o *WorkspaceCodeLink) LinkWorkspaceCodeLinkTransitions(mods ...qm.QueryMod) workspaceCodeLinkTransitionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_code_link_transitions\".\"link_id\"=?", o.ID),
	)

	return WorkspaceCodeLinkTransitions(queryMods...)
}

// LoadIssue allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCodeLinkL) LoadIssue(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodeLink any, mods queries.Applicator) error {
	var slice []*WorkspaceCodeLink
	var object *WorkspaceCodeLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodeLink.(*WorkspaceCodeLink)
		if !ok {
			object = new(WorkspaceCodeLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodeLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodeLink.(*[]*WorkspaceCodeLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodeLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodeLinkR{}
		}
		args[object.IssueID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodeLinkR{}
			}

			args[obj.IssueID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issues`),
		qm.WhereIn(`workspace_issues.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceIssue")
	}

	var resultSlice []*WorkspaceIssue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceIssue")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_issues")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issues")
	}

	if len(workspaceIssueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Issue = foreign
		if foreign.R == nil {
			foreign.R = &workspaceIssueR{}
		}
		foreign.R.IssueWorkspaceCodeLinks = append(foreign.R.IssueWorkspaceCodeLinks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.IssueID == foreign.ID {
				local.R.Issue = foreign
				if foreign.R == nil {
					foreign.R = &workspaceIssueR{}
				}
				foreign.R.IssueWorkspaceCodeLinks = append(foreign.R.IssueWorkspaceCodeLinks, local)
				break
			}
		}
	}

	return nil
}

// LoadRepository allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCodeLinkL) LoadRepository(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodeLink any, mods queries.Applicator) error {
	var slice []*WorkspaceCodeLink
	var object *WorkspaceCodeLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodeLink.(*WorkspaceCodeLink)
		if !ok {
			object = new(WorkspaceCodeLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodeLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodeLink.(*[]*WorkspaceCodeLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodeLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodeLinkR{}
		}
		if !queries.IsNil(object.RepositoryID) {
			args[object.RepositoryID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodeLinkR{}
			}

			if !queries.IsNil(obj.RepositoryID) {
				args[obj.RepositoryID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_scm_repositories`),
		qm.WhereIn(`workspace_scm_repositories.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceSCMRepository")
	}

	var resultSlice []*WorkspaceSCMRepository
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceSCMRepository")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_scm_repositories")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_scm_repositories")
	}

	if len(workspaceSCMRepositoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Repository = foreign
		if foreign.R == nil {
			foreign.R = &workspaceSCMRepositoryR{}
		}
		foreign.R.RepositoryWorkspaceCodeLinks = append(foreign.R.RepositoryWorkspaceCodeLinks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RepositoryID, foreign.ID) {
				local.R.Repository = foreign
				if foreign.R == nil {
					foreign.R = &workspaceSCMRepositoryR{}
				}
				foreign.R.RepositoryWorkspaceCodeLinks = append(foreign.R.RepositoryWorkspaceCodeLinks, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceCodeLinkL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodeLink any, mods queries.Applicator) error {
	var slice []*WorkspaceCodeLink
	var object *WorkspaceCodeLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodeLink.(*WorkspaceCodeLink)
		if !ok {
			object = new(WorkspaceCodeLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodeLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodeLink.(*[]*WorkspaceCodeLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodeLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodeLinkR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodeLinkR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceCodeLinks = append(foreign.R.WorkspaceCodeLinks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceCodeLinks = append(foreign.R.WorkspaceCodeLinks, local)
				break
			}
		}
	}

	return nil
}

// LoadLinkWorkspaceCodeLinkTransitions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceCodeLinkL) LoadLinkWorkspaceCodeLinkTransitions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceCodeLink any, mods queries.Applicator) error {
	var slice []*WorkspaceCodeLink
	var object *WorkspaceCodeLink

	if singular {
		var ok bool
		object, ok = maybeWorkspaceCodeLink.(*WorkspaceCodeLink)
		if !ok {
			object = new(WorkspaceCodeLink)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceCodeLink))
			}
		}
	} else {
		s, ok := maybeWorkspaceCodeLink.(*[]*WorkspaceCodeLink)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceCodeLink)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceCodeLink))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceCodeLinkR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceCodeLinkR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_code_link_transitions`),
		qm.WhereIn(`workspace_code_link_transitions.link_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_code_link_transitions")
	}

	var resultSlice []*WorkspaceCodeLinkTransition
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_code_link_transitions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_code_link_transitions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_code_link_transitions")
	}

	if len(workspaceCodeLinkTransitionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.LinkWorkspaceCodeLinkTransitions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceCodeLinkTransitionR{}
			}
			foreign.R.Link = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.LinkID {
				local.R.LinkWorkspaceCodeLinkTransitions = append(local.R.LinkWorkspaceCodeLinkTransitions, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceCodeLinkTransitionR{}
				}
				foreign.R.Link = local
				break
			}
		}
	}

	return nil
}

// SetIssue of the workspaceCodeLink to the related item.
// Sets o.R.Issue to related.
// Adds o to related.R.IssueWorkspaceCodeLinks.
func (o *WorkspaceCodeLink) SetIssue(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceIssue) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_code_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"issue_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCodeLinkPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.IssueID = related.ID
	if o.R == nil {
		o.R = &workspaceCodeLinkR{
			Issue: related,
		}
	} else {
		o.R.Issue = related
	}

	if related.R == nil {
		related.R = &workspaceIssueR{
			IssueWorkspaceCodeLinks: WorkspaceCodeLinkSlice{o},
		}
	} else {
		related.R.IssueWorkspaceCodeLinks = append(related.R.IssueWorkspaceCodeLinks, o)
	}

	return nil
}

// SetRepository of the workspaceCodeLink to the related item.
// Sets o.R.Repository to related.
// Adds o to related.R.RepositoryWorkspaceCodeLinks.
func (o *WorkspaceCodeLink) SetRepository(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceSCMRepository) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_code_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"repository_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCodeLinkPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RepositoryID, related.ID)
	if o.R == nil {
		o.R = &workspaceCodeLinkR{
			Repository: related,
		}
	} else {
		o.R.Repository = related
	}

	if related.R == nil {
		related.R = &workspaceSCMRepositoryR{
			RepositoryWorkspaceCodeLinks: WorkspaceCodeLinkSlice{o},
		}
	} else {
		related.R.RepositoryWorkspaceCodeLinks = append(related.R.RepositoryWorkspaceCodeLinks, o)
	}

	return nil
}

// RemoveRepository relationship.
// Sets o.R.Repository to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceCodeLink) RemoveRepository(ctx context.Context, exec boil.ContextExecutor, related *WorkspaceSCMRepository) error {
	var err error

	queries.SetScanner(&o.RepositoryID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("repository_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Repository = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RepositoryWorkspaceCodeLinks {
		if queries.Equal(o.RepositoryID, ri.RepositoryID) {
			continue
		}

		ln := len(related.R.RepositoryWorkspaceCodeLinks)
		if ln > 1 && i < ln-1 {
			related.R.RepositoryWorkspaceCodeLinks[i] = related.R.RepositoryWorkspaceCodeLinks[ln-1]
		}
		related.R.RepositoryWorkspaceCodeLinks = related.R.RepositoryWorkspaceCodeLinks[:ln-1]
		break
	}
	return nil
}

// SetWorkspace of the workspaceCodeLink to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceCodeLinks.
func (o *WorkspaceCodeLink) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_code_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceCodeLinkPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceCodeLinkR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceCodeLinks: WorkspaceCodeLinkSlice{o},
		}
	} else {
		related.R.WorkspaceCodeLinks = append(related.R.WorkspaceCodeLinks, o)
	}

	return nil
}

// AddLinkWorkspaceCodeLinkTransitions adds the given related objects to the existing relationships
// of the workspace_code_link, optionally inserting them as new records.
// Appends related to o.R.LinkWorkspaceCodeLinkTransitions.
// Sets related.R.Link appropriately.
func (o *WorkspaceCodeLink) AddLinkWorkspaceCodeLinkTransitions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceCodeLinkTransition) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.LinkID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_code_link_transitions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"link_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceCodeLinkTransitionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.LinkID, rel.Transition}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.LinkID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceCodeLinkR{
			LinkWorkspaceCodeLinkTransitions: related,
		}
	} else {
		o.R.LinkWorkspaceCodeLinkTransitions = append(o.R.LinkWorkspaceCodeLinkTransitions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceCodeLinkTransitionR{
				Link: o,
			}
		} else {
			rel.R.Link = o
		}
	}
	return nil
}

// WorkspaceCodeLinks retrieves all the records using an executor.
func WorkspaceCodeLinks(mods ...qm.QueryMod) workspaceCodeLinkQuery {
	mods = append(mods, qm.From("\"workspace_code_links\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_code_links\".*"})
	}

	return workspaceCodeLinkQuery{q}
}

// FindWorkspaceCodeLink retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceCodeLink(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceCodeLink, error) {
	workspaceCodeLinkObj := &WorkspaceCodeLink{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_code_links\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceCodeLinkObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_code_links")
	}

	if err = workspaceCodeLinkObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceCodeLinkObj, err
	}

	return workspaceCodeLinkObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceCodeLink) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_code_links provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCodeLinkColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceCodeLinkInsertCacheMut.RLock()
	cache, cached := workspaceCodeLinkInsertCache[key]
	workspaceCodeLinkInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceCodeLinkAllColumns,
			workspaceCodeLinkColumnsWithDefault,
			workspaceCodeLinkColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceCodeLinkType, workspaceCodeLinkMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceCodeLinkType, workspaceCodeLinkMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_code_links\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_code_links\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_code_links")
	}

	if !cached {
		workspaceCodeLinkInsertCacheMut.Lock()
		workspaceCodeLinkInsertCache[key] = cache
		workspaceCodeLinkInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceCodeLink.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceCodeLink) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceCodeLinkUpdateCacheMut.RLock()
	cache, cached := workspaceCodeLinkUpdateCache[key]
	workspaceCodeLinkUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceCodeLinkAllColumns,
			workspaceCodeLinkPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_code_links, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_code_links\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceCodeLinkPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceCodeLinkType, workspaceCodeLinkMapping, append(wl, workspaceCodeLinkPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_code_links row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_code_links")
	}

	if !cached {
		workspaceCodeLinkUpdateCacheMut.Lock()
		workspaceCodeLinkUpdateCache[key] = cache
		workspaceCodeLinkUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceCodeLinkQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_code_links")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_code_links")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceCodeLinkSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodeLinkPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_code_links\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceCodeLinkPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceCodeLink slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceCodeLink")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceCodeLink) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_code_links provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceCodeLinkColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceCodeLinkUpsertCacheMut.RLock()
	cache, cached := workspaceCodeLinkUpsertCache[key]
	workspaceCodeLinkUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceCodeLinkAllColumns,
			workspaceCodeLinkColumnsWithDefault,
			workspaceCodeLinkColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceCodeLinkAllColumns,
			workspaceCodeLinkPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_code_links, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceCodeLinkAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceCodeLinkPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_code_links, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceCodeLinkPrimaryKeyColumns))
			copy(conflict, workspaceCodeLinkPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_code_links\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceCodeLinkType, workspaceCodeLinkMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceCodeLinkType, workspaceCodeLinkMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_code_links")
	}

	if !cached {
		workspaceCodeLinkUpsertCacheMut.Lock()
		workspaceCodeLinkUpsertCache[key] = cache
		workspaceCodeLinkUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceCodeLink record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceCodeLink) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceCodeLink provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceCodeLinkPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_code_links\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_code_links")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_code_links")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceCodeLinkQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceCodeLinkQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_code_links")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_code_links")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceCodeLinkSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceCodeLinkBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodeLinkPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_code_links\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCodeLinkPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceCodeLink slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_code_links")
	}

	if len(workspaceCodeLinkAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceCodeLink) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceCodeLink(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceCodeLinkSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceCodeLinkSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceCodeLinkPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_code_links\".* FROM \"workspace_code_links\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceCodeLinkPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceCodeLinkSlice")
	}

	*o = slice

	return nil
}

// WorkspaceCodeLinkExists checks if the WorkspaceCodeLink row exists.
func WorkspaceCodeLinkExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_code_links\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_code_links exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceCodeLink row exists.
func (o *WorkspaceCodeLink) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceCodeLinkExists(ctx, exec, o.ID)
}
