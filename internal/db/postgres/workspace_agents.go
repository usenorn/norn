// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceAgent is an object representing the database table.
type WorkspaceAgent struct {
	ID             string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID    string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	AccountID      string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	OwnerAccountID string    `boil:"owner_account_id" json:"owner_account_id" toml:"owner_account_id" yaml:"owner_account_id"`
	Name           string    `boil:"name" json:"name" toml:"name" yaml:"name"`
	Status         string    `boil:"status" json:"status" toml:"status" yaml:"status"`
	ActionLimit    null.Int  `boil:"action_limit" json:"action_limit,omitempty" toml:"action_limit" yaml:"action_limit,omitempty"`
	DisabledAt     null.Time `boil:"disabled_at" json:"disabled_at,omitempty" toml:"disabled_at" yaml:"disabled_at,omitempty"`
	CreatedAt      time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt      time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	Icon           string    `boil:"icon" json:"icon" toml:"icon" yaml:"icon"`

	R *workspaceAgentR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceAgentL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceAgentColumns = struct {
	ID             string
	WorkspaceID    string
	AccountID      string
	OwnerAccountID string
	Name           string
	Status         string
	ActionLimit    string
	DisabledAt     string
	CreatedAt      string
	UpdatedAt      string
	Icon           string
}{
	ID:             "id",
	WorkspaceID:    "workspace_id",
	AccountID:      "account_id",
	OwnerAccountID: "owner_account_id",
	Name:           "name",
	Status:         "status",
	ActionLimit:    "action_limit",
	DisabledAt:     "disabled_at",
	CreatedAt:      "created_at",
	UpdatedAt:      "updated_at",
	Icon:           "icon",
}

var WorkspaceAgentTableColumns = struct {
	ID             string
	WorkspaceID    string
	AccountID      string
	OwnerAccountID string
	Name           string
	Status         string
	ActionLimit    string
	DisabledAt     string
	CreatedAt      string
	UpdatedAt      string
	Icon           string
}{
	ID:             "workspace_agents.id",
	WorkspaceID:    "workspace_agents.workspace_id",
	AccountID:      "workspace_agents.account_id",
	OwnerAccountID: "workspace_agents.owner_account_id",
	Name:           "workspace_agents.name",
	Status:         "workspace_agents.status",
	ActionLimit:    "workspace_agents.action_limit",
	DisabledAt:     "workspace_agents.disabled_at",
	CreatedAt:      "workspace_agents.created_at",
	UpdatedAt:      "workspace_agents.updated_at",
	Icon:           "workspace_agents.icon",
}

// Generated where

var WorkspaceAgentWhere = struct {
	ID             whereHelperstring
	WorkspaceID    whereHelperstring
	AccountID      whereHelperstring
	OwnerAccountID whereHelperstring
	Name           whereHelperstring
	Status         whereHelperstring
	ActionLimit    whereHelpernull_Int
	DisabledAt     whereHelpernull_Time
	CreatedAt      whereHelpertime_Time
	UpdatedAt      whereHelpertime_Time
	Icon           whereHelperstring
}{
	ID:             whereHelperstring{field: "\"workspace_agents\".\"id\""},
	WorkspaceID:    whereHelperstring{field: "\"workspace_agents\".\"workspace_id\""},
	AccountID:      whereHelperstring{field: "\"workspace_agents\".\"account_id\""},
	OwnerAccountID: whereHelperstring{field: "\"workspace_agents\".\"owner_account_id\""},
	Name:           whereHelperstring{field: "\"workspace_agents\".\"name\""},
	Status:         whereHelperstring{field: "\"workspace_agents\".\"status\""},
	ActionLimit:    whereHelpernull_Int{field: "\"workspace_agents\".\"action_limit\""},
	DisabledAt:     whereHelpernull_Time{field: "\"workspace_agents\".\"disabled_at\""},
	CreatedAt:      whereHelpertime_Time{field: "\"workspace_agents\".\"created_at\""},
	UpdatedAt:      whereHelpertime_Time{field: "\"workspace_agents\".\"updated_at\""},
	Icon:           whereHelperstring{field: "\"workspace_agents\".\"icon\""},
}

// WorkspaceAgentRels is where relationship names are stored.
var WorkspaceAgentRels = struct {
	Account                        string
	OwnerAccount                   string
	Workspace                      string
	AgentWorkspaceAgentProposals   string
	AgentWorkspaceExecutions       string
	AgentWorkspaceIssueDelegations string
	AgentWorkspaceRunners          string
}{
	Account:                        "Account",
	OwnerAccount:                   "OwnerAccount",
	Workspace:                      "Workspace",
	AgentWorkspaceAgentProposals:   "AgentWorkspaceAgentProposals",
	AgentWorkspaceExecutions:       "AgentWorkspaceExecutions",
	AgentWorkspaceIssueDelegations: "AgentWorkspaceIssueDelegations",
	AgentWorkspaceRunners:          "AgentWorkspaceRunners",
}

// workspaceAgentR is where relationships are stored.
type workspaceAgentR struct {
	Account                        *Account                      `boil:"Account" json:"Account" toml:"Account" yaml:"Account"`
	OwnerAccount                   *Account                      `boil:"OwnerAccount" json:"OwnerAccount" toml:"OwnerAccount" yaml:"OwnerAccount"`
	Workspace                      *Workspace                    `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
	AgentWorkspaceAgentProposals   WorkspaceAgentProposalSlice   `boil:"AgentWorkspaceAgentProposals" json:"AgentWorkspaceAgentProposals" toml:"AgentWorkspaceAgentProposals" yaml:"AgentWorkspaceAgentProposals"`
	AgentWorkspaceExecutions       WorkspaceExecutionSlice       `boil:"AgentWorkspaceExecutions" json:"AgentWorkspaceExecutions" toml:"AgentWorkspaceExecutions" yaml:"AgentWorkspaceExecutions"`
	AgentWorkspaceIssueDelegations WorkspaceIssueDelegationSlice `boil:"AgentWorkspaceIssueDelegations" json:"AgentWorkspaceIssueDelegations" toml:"AgentWorkspaceIssueDelegations" yaml:"AgentWorkspaceIssueDelegations"`
	AgentWorkspaceRunners          WorkspaceRunnerSlice          `boil:"AgentWorkspaceRunners" json:"AgentWorkspaceRunners" toml:"AgentWorkspaceRunners" yaml:"AgentWorkspaceRunners"`
}

// NewStruct creates a new relationship struct
func (*workspaceAgentR) NewStruct() *workspaceAgentR {
	return &workspaceAgentR{}
}

func (o *WorkspaceAgent) GetAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetAccount()
}

func (r *workspaceAgentR) GetAccount() *Account {
	if r == nil {
		return nil
	}

	return r.Account
}

func (o *WorkspaceAgent) GetOwnerAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetOwnerAccount()
}

func (r *workspaceAgentR) GetOwnerAccount() *Account {
	if r == nil {
		return nil
	}

	return r.OwnerAccount
}

func (o *WorkspaceAgent) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceAgentR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

func (o *WorkspaceAgent) GetAgentWorkspaceAgentProposals() WorkspaceAgentProposalSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAgentWorkspaceAgentProposals()
}

func (r *workspaceAgentR) GetAgentWorkspaceAgentProposals() WorkspaceAgentProposalSlice {
	if r == nil {
		return nil
	}

	return r.AgentWorkspaceAgentProposals
}

func (o *WorkspaceAgent) GetAgentWorkspaceExecutions() WorkspaceExecutionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAgentWorkspaceExecutions()
}

func (r *workspaceAgentR) GetAgentWorkspaceExecutions() WorkspaceExecutionSlice {
	if r == nil {
		return nil
	}

	return r.AgentWorkspaceExecutions
}

func (o *WorkspaceAgent) GetAgentWorkspaceIssueDelegations() WorkspaceIssueDelegationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAgentWorkspaceIssueDelegations()
}

func (r *workspaceAgentR) GetAgentWorkspaceIssueDelegations() WorkspaceIssueDelegationSlice {
	if r == nil {
		return nil
	}

	return r.AgentWorkspaceIssueDelegations
}

func (o *WorkspaceAgent) GetAgentWorkspaceRunners() WorkspaceRunnerSlice {
	if o == nil {
		return nil
	}

	return o.R.GetAgentWorkspaceRunners()
}

func (r *workspaceAgentR) GetAgentWorkspaceRunners() WorkspaceRunnerSlice {
	if r == nil {
		return nil
	}

	return r.AgentWorkspaceRunners
}

// workspaceAgentL is where Load methods for each relationship are stored.
type workspaceAgentL struct{}

var (
	workspaceAgentAllColumns            = []string{"id", "workspace_id", "account_id", "owner_account_id", "name", "status", "action_limit", "disabled_at", "created_at", "updated_at", "icon"}
	workspaceAgentColumnsWithoutDefault = []string{"workspace_id", "account_id", "owner_account_id", "name"}
	workspaceAgentColumnsWithDefault    = []string{"id", "status", "action_limit", "disabled_at", "created_at", "updated_at", "icon"}
	workspaceAgentPrimaryKeyColumns     = []string{"id"}
	workspaceAgentGeneratedColumns      = []string{}
)

type (
	// WorkspaceAgentSlice is an alias for a slice of pointers to WorkspaceAgent.
	// This should almost always be used instead of []WorkspaceAgent.
	WorkspaceAgentSlice []*WorkspaceAgent
	// WorkspaceAgentHook is the signature for custom WorkspaceAgent hook methods
	WorkspaceAgentHook func(context.Context, boil.ContextExecutor, *WorkspaceAgent) error

	workspaceAgentQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceAgentType                 = reflect.TypeOf(&WorkspaceAgent{})
	workspaceAgentMapping              = queries.MakeStructMapping(workspaceAgentType)
	workspaceAgentPrimaryKeyMapping, _ = queries.BindMapping(workspaceAgentType, workspaceAgentMapping, workspaceAgentPrimaryKeyColumns)
	workspaceAgentInsertCacheMut       sync.RWMutex
	workspaceAgentInsertCache          = make(map[string]insertCache)
	workspaceAgentUpdateCacheMut       sync.RWMutex
	workspaceAgentUpdateCache          = make(map[string]updateCache)
	workspaceAgentUpsertCacheMut       sync.RWMutex
	workspaceAgentUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceAgentAfterSelectMu sync.Mutex
var workspaceAgentAfterSelectHooks []WorkspaceAgentHook

var workspaceAgentBeforeInsertMu sync.Mutex
var workspaceAgentBeforeInsertHooks []WorkspaceAgentHook
var workspaceAgentAfterInsertMu sync.Mutex
var workspaceAgentAfterInsertHooks []WorkspaceAgentHook

var workspaceAgentBeforeUpdateMu sync.Mutex
var workspaceAgentBeforeUpdateHooks []WorkspaceAgentHook
var workspaceAgentAfterUpdateMu sync.Mutex
var workspaceAgentAfterUpdateHooks []WorkspaceAgentHook

var workspaceAgentBeforeDeleteMu sync.Mutex
var workspaceAgentBeforeDeleteHooks []WorkspaceAgentHook
var workspaceAgentAfterDeleteMu sync.Mutex
var workspaceAgentAfterDeleteHooks []WorkspaceAgentHook

var workspaceAgentBeforeUpsertMu sync.Mutex
var workspaceAgentBeforeUpsertHooks []WorkspaceAgentHook
var workspaceAgentAfterUpsertMu sync.Mutex
var workspaceAgentAfterUpsertHooks []WorkspaceAgentHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceAgent) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceAgent) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceAgent) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceAgent) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceAgent) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceAgent) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceAgent) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceAgent) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceAgent) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceAgentHook registers your hook function for all future operations.
func AddWorkspaceAgentHook(hookPoint boil.HookPoint, workspaceAgentHook WorkspaceAgentHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceAgentAfterSelectMu.Lock()
		workspaceAgentAfterSelectHooks = append(workspaceAgentAfterSelectHooks, workspaceAgentHook)
		workspaceAgentAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceAgentBeforeInsertMu.Lock()
		workspaceAgentBeforeInsertHooks = append(workspaceAgentBeforeInsertHooks, workspaceAgentHook)
		workspaceAgentBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceAgentAfterInsertMu.Lock()
		workspaceAgentAfterInsertHooks = append(workspaceAgentAfterInsertHooks, workspaceAgentHook)
		workspaceAgentAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceAgentBeforeUpdateMu.Lock()
		workspaceAgentBeforeUpdateHooks = append(workspaceAgentBeforeUpdateHooks, workspaceAgentHook)
		workspaceAgentBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceAgentAfterUpdateMu.Lock()
		workspaceAgentAfterUpdateHooks = append(workspaceAgentAfterUpdateHooks, workspaceAgentHook)
		workspaceAgentAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceAgentBeforeDeleteMu.Lock()
		workspaceAgentBeforeDeleteHooks = append(workspaceAgentBeforeDeleteHooks, workspaceAgentHook)
		workspaceAgentBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceAgentAfterDeleteMu.Lock()
		workspaceAgentAfterDeleteHooks = append(workspaceAgentAfterDeleteHooks, workspaceAgentHook)
		workspaceAgentAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceAgentBeforeUpsertMu.Lock()
		workspaceAgentBeforeUpsertHooks = append(workspaceAgentBeforeUpsertHooks, workspaceAgentHook)
		workspaceAgentBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceAgentAfterUpsertMu.Lock()
		workspaceAgentAfterUpsertHooks = append(workspaceAgentAfterUpsertHooks, workspaceAgentHook)
		workspaceAgentAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceAgent record from the query.
func (q workspaceAgentQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceAgent, error) {
	o := &WorkspaceAgent{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_agents")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceAgent records from the query.
func (q workspaceAgentQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceAgentSlice, error) {
	var o []*WorkspaceAgent

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceAgent slice")
	}

	if len(workspaceAgentAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceAgent records in the query.
func (q workspaceAgentQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_agents rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceAgentQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_agents exists")
	}

	return count > 0, nil
}

// Account pointed to by the foreign key.
func (o *WorkspaceAgent) Account(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// OwnerAccount pointed to by the foreign key.
func (o *WorkspaceAgent) OwnerAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.OwnerAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceAgent) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// AgentWorkspaceAgentProposals retrieves all the workspace_agent_proposal's WorkspaceAgentProposals with an executor via agent_id column.
func (o *WorkspaceAgent) AgentWorkspaceAgentProposals(mods ...qm.QueryMod) workspaceAgentProposalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_agent_proposals\".\"agent_id\"=?", o.ID),
	)

	return WorkspaceAgentProposals(queryMods...)
}

// AgentWorkspaceExecutions retrieves all the workspace_execution's WorkspaceExecutions with an executor via agent_id column.
func (o *WorkspaceAgent) AgentWorkspaceExecutions(mods ...qm.QueryMod) workspaceExecutionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_executions\".\"agent_id\"=?", o.ID),
	)

	return WorkspaceExecutions(queryMods...)
}

// AgentWorkspaceIssueDelegations retrieves all the workspace_issue_delegation's WorkspaceIssueDelegations with an executor via agent_id column.
func (o *WorkspaceAgent) AgentWorkspaceIssueDelegations(mods ...qm.QueryMod) workspaceIssueDelegationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_issue_delegations\".\"agent_id\"=?", o.ID),
	)

	return WorkspaceIssueDelegations(queryMods...)
}

// AgentWorkspaceRunners retrieves all the workspace_runner's WorkspaceRunners with an executor via agent_id column.
func (o *WorkspaceAgent) AgentWorkspaceRunners(mods ...qm.QueryMod) workspaceRunnerQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_runners\".\"agent_id\"=?", o.ID),
	)

	return WorkspaceRunners(queryMods...)
}

// LoadAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentL) LoadAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.AccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}

			args[obj.AccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Account = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.WorkspaceAgent = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountID == foreign.ID {
				local.R.Account = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.WorkspaceAgent = local
				break
			}
		}
	}

	return nil
}

// LoadOwnerAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentL) LoadOwnerAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.OwnerAccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}

			args[obj.OwnerAccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.OwnerAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.OwnerAccountWorkspaceAgents = append(foreign.R.OwnerAccountWorkspaceAgents, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OwnerAccountID == foreign.ID {
				local.R.OwnerAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.OwnerAccountWorkspaceAgents = append(foreign.R.OwnerAccountWorkspaceAgents, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceAgents = append(foreign.R.WorkspaceAgents, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceAgents = append(foreign.R.WorkspaceAgents, local)
				break
			}
		}
	}

	return nil
}

// LoadAgentWorkspaceAgentProposals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceAgentL) LoadAgentWorkspaceAgentProposals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agent_proposals`),
		qm.WhereIn(`workspace_agent_proposals.agent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_agent_proposals")
	}

	var resultSlice []*WorkspaceAgentProposal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_agent_proposals")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_agent_proposals")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agent_proposals")
	}

	if len(workspaceAgentProposalAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AgentWorkspaceAgentProposals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceAgentProposalR{}
			}
			foreign.R.Agent = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AgentID {
				local.R.AgentWorkspaceAgentProposals = append(local.R.AgentWorkspaceAgentProposals, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceAgentProposalR{}
				}
				foreign.R.Agent = local
				break
			}
		}
	}

	return nil
}

// LoadAgentWorkspaceExecutions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceAgentL) LoadAgentWorkspaceExecutions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_executions`),
		qm.WhereIn(`workspace_executions.agent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_executions")
	}

	var resultSlice []*WorkspaceExecution
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_executions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_executions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_executions")
	}

	if len(workspaceExecutionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AgentWorkspaceExecutions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceExecutionR{}
			}
			foreign.R.Agent = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AgentID {
				local.R.AgentWorkspaceExecutions = append(local.R.AgentWorkspaceExecutions, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceExecutionR{}
				}
				foreign.R.Agent = local
				break
			}
		}
	}

	return nil
}

// LoadAgentWorkspaceIssueDelegations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceAgentL) LoadAgentWorkspaceIssueDelegations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_issue_delegations`),
		qm.WhereIn(`workspace_issue_delegations.agent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_issue_delegations")
	}

	var resultSlice []*WorkspaceIssueDelegation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_issue_delegations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_issue_delegations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_issue_delegations")
	}

	if len(workspaceIssueDelegationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AgentWorkspaceIssueDelegations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceIssueDelegationR{}
			}
			foreign.R.Agent = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AgentID {
				local.R.AgentWorkspaceIssueDelegations = append(local.R.AgentWorkspaceIssueDelegations, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceIssueDelegationR{}
				}
				foreign.R.Agent = local
				break
			}
		}
	}

	return nil
}

// LoadAgentWorkspaceRunners allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceAgentL) LoadAgentWorkspaceRunners(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgent any, mods queries.Applicator) error {
	var slice []*WorkspaceAgent
	var object *WorkspaceAgent

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgent.(*WorkspaceAgent)
		if !ok {
			object = new(WorkspaceAgent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgent))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgent.(*[]*WorkspaceAgent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgent))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_runners`),
		qm.WhereIn(`workspace_runners.agent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_runners")
	}

	var resultSlice []*WorkspaceRunner
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_runners")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_runners")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_runners")
	}

	if len(workspaceRunnerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.AgentWorkspaceRunners = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceRunnerR{}
			}
			foreign.R.Agent = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AgentID {
				local.R.AgentWorkspaceRunners = append(local.R.AgentWorkspaceRunners, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceRunnerR{}
				}
				foreign.R.Agent = local
				break
			}
		}
	}

	return nil
}

// SetAccount of the workspaceAgent to the related item.
// Sets o.R.Account to related.
// Adds o to related.R.WorkspaceAgent.
func (o *WorkspaceAgent) SetAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agents\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentR{
			Account: related,
		}
	} else {
		o.R.Account = related
	}

	if related.R == nil {
		related.R = &accountR{
			WorkspaceAgent: o,
		}
	} else {
		related.R.WorkspaceAgent = o
	}

	return nil
}

// SetOwnerAccount of the workspaceAgent to the related item.
// Sets o.R.OwnerAccount to related.
// Adds o to related.R.OwnerAccountWorkspaceAgents.
func (o *WorkspaceAgent) SetOwnerAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agents\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"owner_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OwnerAccountID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentR{
			OwnerAccount: related,
		}
	} else {
		o.R.OwnerAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			OwnerAccountWorkspaceAgents: WorkspaceAgentSlice{o},
		}
	} else {
		related.R.OwnerAccountWorkspaceAgents = append(related.R.OwnerAccountWorkspaceAgents, o)
	}

	return nil
}

// SetWorkspace of the workspaceAgent to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceAgents.
func (o *WorkspaceAgent) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agents\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceAgents: WorkspaceAgentSlice{o},
		}
	} else {
		related.R.WorkspaceAgents = append(related.R.WorkspaceAgents, o)
	}

	return nil
}

// AddAgentWorkspaceAgentProposals adds the given related objects to the existing relationships
// of the workspace_agent, optionally inserting them as new records.
// Appends related to o.R.AgentWorkspaceAgentProposals.
// Sets related.R.Agent appropriately.
func (o *WorkspaceAgent) AddAgentWorkspaceAgentProposals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceAgentProposal) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AgentID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_agent_proposals\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceAgentProposalPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AgentID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceAgentR{
			AgentWorkspaceAgentProposals: related,
		}
	} else {
		o.R.AgentWorkspaceAgentProposals = append(o.R.AgentWorkspaceAgentProposals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceAgentProposalR{
				Agent: o,
			}
		} else {
			rel.R.Agent = o
		}
	}
	return nil
}

// AddAgentWorkspaceExecutions adds the given related objects to the existing relationships
// of the workspace_agent, optionally inserting them as new records.
// Appends related to o.R.AgentWorkspaceExecutions.
// Sets related.R.Agent appropriately.
func (o *WorkspaceAgent) AddAgentWorkspaceExecutions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceExecution) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AgentID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_executions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceExecutionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AgentID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceAgentR{
			AgentWorkspaceExecutions: related,
		}
	} else {
		o.R.AgentWorkspaceExecutions = append(o.R.AgentWorkspaceExecutions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceExecutionR{
				Agent: o,
			}
		} else {
			rel.R.Agent = o
		}
	}
	return nil
}

// AddAgentWorkspaceIssueDelegations adds the given related objects to the existing relationships
// of the workspace_agent, optionally inserting them as new records.
// Appends related to o.R.AgentWorkspaceIssueDelegations.
// Sets related.R.Agent appropriately.
func (o *WorkspaceAgent) AddAgentWorkspaceIssueDelegations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceIssueDelegation) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AgentID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_issue_delegations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceIssueDelegationPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AgentID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceAgentR{
			AgentWorkspaceIssueDelegations: related,
		}
	} else {
		o.R.AgentWorkspaceIssueDelegations = append(o.R.AgentWorkspaceIssueDelegations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceIssueDelegationR{
				Agent: o,
			}
		} else {
			rel.R.Agent = o
		}
	}
	return nil
}

// AddAgentWorkspaceRunners adds the given related objects to the existing relationships
// of the workspace_agent, optionally inserting them as new records.
// Appends related to o.R.AgentWorkspaceRunners.
// Sets related.R.Agent appropriately.
func (o *WorkspaceAgent) AddAgentWorkspaceRunners(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceRunner) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AgentID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_runners\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceRunnerPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AgentID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceAgentR{
			AgentWorkspaceRunners: related,
		}
	} else {
		o.R.AgentWorkspaceRunners = append(o.R.AgentWorkspaceRunners, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceRunnerR{
				Agent: o,
			}
		} else {
			rel.R.Agent = o
		}
	}
	return nil
}

// WorkspaceAgents retrieves all the records using an executor.
func WorkspaceAgents(mods ...qm.QueryMod) workspaceAgentQuery {
	mods = append(mods, qm.From("\"workspace_agents\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_agents\".*"})
	}

	return workspaceAgentQuery{q}
}

// FindWorkspaceAgent retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceAgent(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceAgent, error) {
	workspaceAgentObj := &WorkspaceAgent{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_agents\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceAgentObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_agents")
	}

	if err = workspaceAgentObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceAgentObj, err
	}

	return workspaceAgentObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceAgent) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agents provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceAgentInsertCacheMut.RLock()
	cache, cached := workspaceAgentInsertCache[key]
	workspaceAgentInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceAgentAllColumns,
			workspaceAgentColumnsWithDefault,
			workspaceAgentColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentType, workspaceAgentMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceAgentType, workspaceAgentMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_agents\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_agents\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_agents")
	}

	if !cached {
		workspaceAgentInsertCacheMut.Lock()
		workspaceAgentInsertCache[key] = cache
		workspaceAgentInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceAgent.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceAgent) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceAgentUpdateCacheMut.RLock()
	cache, cached := workspaceAgentUpdateCache[key]
	workspaceAgentUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceAgentAllColumns,
			workspaceAgentPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_agents, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_agents\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceAgentPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceAgentType, workspaceAgentMapping, append(wl, workspaceAgentPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_agents row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_agents")
	}

	if !cached {
		workspaceAgentUpdateCacheMut.Lock()
		workspaceAgentUpdateCache[key] = cache
		workspaceAgentUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceAgentQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_agents")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_agents")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceAgentSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_agents\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceAgentPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceAgent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceAgent")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceAgent) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agents provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceAgentUpsertCacheMut.RLock()
	cache, cached := workspaceAgentUpsertCache[key]
	workspaceAgentUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceAgentAllColumns,
			workspaceAgentColumnsWithDefault,
			workspaceAgentColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceAgentAllColumns,
			workspaceAgentPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_agents, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceAgentAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceAgentPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_agents, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceAgentPrimaryKeyColumns))
			copy(conflict, workspaceAgentPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_agents\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentType, workspaceAgentMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceAgentType, workspaceAgentMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_agents")
	}

	if !cached {
		workspaceAgentUpsertCacheMut.Lock()
		workspaceAgentUpsertCache[key] = cache
		workspaceAgentUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceAgent record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceAgent) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceAgent provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceAgentPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_agents\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_agents")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_agents")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceAgentQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceAgentQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_agents")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agents")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceAgentSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceAgentBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_agents\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceAgent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agents")
	}

	if len(workspaceAgentAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceAgent) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceAgent(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceAgentSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceAgentSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_agents\".* FROM \"workspace_agents\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceAgentSlice")
	}

	*o = slice

	return nil
}

// WorkspaceAgentExists checks if the WorkspaceAgent row exists.
func WorkspaceAgentExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_agents\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_agents exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceAgent row exists.
func (o *WorkspaceAgent) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceAgentExists(ctx, exec, o.ID)
}
