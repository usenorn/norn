// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceTeamTriageSetting is an object representing the database table.
type WorkspaceTeamTriageSetting struct {
	TeamID            string    `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	WorkspaceID       string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	RouteAgents       bool      `boil:"route_agents" json:"route_agents" toml:"route_agents" yaml:"route_agents"`
	RouteIntegrations bool      `boil:"route_integrations" json:"route_integrations" toml:"route_integrations" yaml:"route_integrations"`
	RouteNonMembers   bool      `boil:"route_non_members" json:"route_non_members" toml:"route_non_members" yaml:"route_non_members"`
	CreatedAt         time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceTeamTriageSettingR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceTeamTriageSettingL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceTeamTriageSettingColumns = struct {
	TeamID            string
	WorkspaceID       string
	RouteAgents       string
	RouteIntegrations string
	RouteNonMembers   string
	CreatedAt         string
	UpdatedAt         string
}{
	TeamID:            "team_id",
	WorkspaceID:       "workspace_id",
	RouteAgents:       "route_agents",
	RouteIntegrations: "route_integrations",
	RouteNonMembers:   "route_non_members",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
}

var WorkspaceTeamTriageSettingTableColumns = struct {
	TeamID            string
	WorkspaceID       string
	RouteAgents       string
	RouteIntegrations string
	RouteNonMembers   string
	CreatedAt         string
	UpdatedAt         string
}{
	TeamID:            "workspace_team_triage_settings.team_id",
	WorkspaceID:       "workspace_team_triage_settings.workspace_id",
	RouteAgents:       "workspace_team_triage_settings.route_agents",
	RouteIntegrations: "workspace_team_triage_settings.route_integrations",
	RouteNonMembers:   "workspace_team_triage_settings.route_non_members",
	CreatedAt:         "workspace_team_triage_settings.created_at",
	UpdatedAt:         "workspace_team_triage_settings.updated_at",
}

// Generated where

var WorkspaceTeamTriageSettingWhere = struct {
	TeamID            whereHelperstring
	WorkspaceID       whereHelperstring
	RouteAgents       whereHelperbool
	RouteIntegrations whereHelperbool
	RouteNonMembers   whereHelperbool
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
}{
	TeamID:            whereHelperstring{field: "\"workspace_team_triage_settings\".\"team_id\""},
	WorkspaceID:       whereHelperstring{field: "\"workspace_team_triage_settings\".\"workspace_id\""},
	RouteAgents:       whereHelperbool{field: "\"workspace_team_triage_settings\".\"route_agents\""},
	RouteIntegrations: whereHelperbool{field: "\"workspace_team_triage_settings\".\"route_integrations\""},
	RouteNonMembers:   whereHelperbool{field: "\"workspace_team_triage_settings\".\"route_non_members\""},
	CreatedAt:         whereHelpertime_Time{field: "\"workspace_team_triage_settings\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"workspace_team_triage_settings\".\"updated_at\""},
}

// WorkspaceTeamTriageSettingRels is where relationship names are stored.
var WorkspaceTeamTriageSettingRels = struct {
}{}

// workspaceTeamTriageSettingR is where relationships are stored.
type workspaceTeamTriageSettingR struct {
}

// NewStruct creates a new relationship struct
func (*workspaceTeamTriageSettingR) NewStruct() *workspaceTeamTriageSettingR {
	return &workspaceTeamTriageSettingR{}
}

// workspaceTeamTriageSettingL is where Load methods for each relationship are stored.
type workspaceTeamTriageSettingL struct{}

var (
	workspaceTeamTriageSettingAllColumns            = []string{"team_id", "workspace_id", "route_agents", "route_integrations", "route_non_members", "created_at", "updated_at"}
	workspaceTeamTriageSettingColumnsWithoutDefault = []string{"team_id", "workspace_id"}
	workspaceTeamTriageSettingColumnsWithDefault    = []string{"route_agents", "route_integrations", "route_non_members", "created_at", "updated_at"}
	workspaceTeamTriageSettingPrimaryKeyColumns     = []string{"team_id"}
	workspaceTeamTriageSettingGeneratedColumns      = []string{}
)

type (
	// WorkspaceTeamTriageSettingSlice is an alias for a slice of pointers to WorkspaceTeamTriageSetting.
	// This should almost always be used instead of []WorkspaceTeamTriageSetting.
	WorkspaceTeamTriageSettingSlice []*WorkspaceTeamTriageSetting
	// WorkspaceTeamTriageSettingHook is the signature for custom WorkspaceTeamTriageSetting hook methods
	WorkspaceTeamTriageSettingHook func(context.Context, boil.ContextExecutor, *WorkspaceTeamTriageSetting) error

	workspaceTeamTriageSettingQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceTeamTriageSettingType                 = reflect.TypeOf(&WorkspaceTeamTriageSetting{})
	workspaceTeamTriageSettingMapping              = queries.MakeStructMapping(workspaceTeamTriageSettingType)
	workspaceTeamTriageSettingPrimaryKeyMapping, _ = queries.BindMapping(workspaceTeamTriageSettingType, workspaceTeamTriageSettingMapping, workspaceTeamTriageSettingPrimaryKeyColumns)
	workspaceTeamTriageSettingInsertCacheMut       sync.RWMutex
	workspaceTeamTriageSettingInsertCache          = make(map[string]insertCache)
	workspaceTeamTriageSettingUpdateCacheMut       sync.RWMutex
	workspaceTeamTriageSettingUpdateCache          = make(map[string]updateCache)
	workspaceTeamTriageSettingUpsertCacheMut       sync.RWMutex
	workspaceTeamTriageSettingUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceTeamTriageSettingAfterSelectMu sync.Mutex
var workspaceTeamTriageSettingAfterSelectHooks []WorkspaceTeamTriageSettingHook

var workspaceTeamTriageSettingBeforeInsertMu sync.Mutex
var workspaceTeamTriageSettingBeforeInsertHooks []WorkspaceTeamTriageSettingHook
var workspaceTeamTriageSettingAfterInsertMu sync.Mutex
var workspaceTeamTriageSettingAfterInsertHooks []WorkspaceTeamTriageSettingHook

var workspaceTeamTriageSettingBeforeUpdateMu sync.Mutex
var workspaceTeamTriageSettingBeforeUpdateHooks []WorkspaceTeamTriageSettingHook
var workspaceTeamTriageSettingAfterUpdateMu sync.Mutex
var workspaceTeamTriageSettingAfterUpdateHooks []WorkspaceTeamTriageSettingHook

var workspaceTeamTriageSettingBeforeDeleteMu sync.Mutex
var workspaceTeamTriageSettingBeforeDeleteHooks []WorkspaceTeamTriageSettingHook
var workspaceTeamTriageSettingAfterDeleteMu sync.Mutex
var workspaceTeamTriageSettingAfterDeleteHooks []WorkspaceTeamTriageSettingHook

var workspaceTeamTriageSettingBeforeUpsertMu sync.Mutex
var workspaceTeamTriageSettingBeforeUpsertHooks []WorkspaceTeamTriageSettingHook
var workspaceTeamTriageSettingAfterUpsertMu sync.Mutex
var workspaceTeamTriageSettingAfterUpsertHooks []WorkspaceTeamTriageSettingHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceTeamTriageSetting) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceTeamTriageSetting) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceTeamTriageSetting) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceTeamTriageSetting) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceTeamTriageSetting) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceTeamTriageSetting) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceTeamTriageSetting) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceTeamTriageSetting) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceTeamTriageSetting) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamTriageSettingAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceTeamTriageSettingHook registers your hook function for all future operations.
func AddWorkspaceTeamTriageSettingHook(hookPoint boil.HookPoint, workspaceTeamTriageSettingHook WorkspaceTeamTriageSettingHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceTeamTriageSettingAfterSelectMu.Lock()
		workspaceTeamTriageSettingAfterSelectHooks = append(workspaceTeamTriageSettingAfterSelectHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceTeamTriageSettingBeforeInsertMu.Lock()
		workspaceTeamTriageSettingBeforeInsertHooks = append(workspaceTeamTriageSettingBeforeInsertHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceTeamTriageSettingAfterInsertMu.Lock()
		workspaceTeamTriageSettingAfterInsertHooks = append(workspaceTeamTriageSettingAfterInsertHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceTeamTriageSettingBeforeUpdateMu.Lock()
		workspaceTeamTriageSettingBeforeUpdateHooks = append(workspaceTeamTriageSettingBeforeUpdateHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceTeamTriageSettingAfterUpdateMu.Lock()
		workspaceTeamTriageSettingAfterUpdateHooks = append(workspaceTeamTriageSettingAfterUpdateHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceTeamTriageSettingBeforeDeleteMu.Lock()
		workspaceTeamTriageSettingBeforeDeleteHooks = append(workspaceTeamTriageSettingBeforeDeleteHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceTeamTriageSettingAfterDeleteMu.Lock()
		workspaceTeamTriageSettingAfterDeleteHooks = append(workspaceTeamTriageSettingAfterDeleteHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceTeamTriageSettingBeforeUpsertMu.Lock()
		workspaceTeamTriageSettingBeforeUpsertHooks = append(workspaceTeamTriageSettingBeforeUpsertHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceTeamTriageSettingAfterUpsertMu.Lock()
		workspaceTeamTriageSettingAfterUpsertHooks = append(workspaceTeamTriageSettingAfterUpsertHooks, workspaceTeamTriageSettingHook)
		workspaceTeamTriageSettingAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceTeamTriageSetting record from the query.
func (q workspaceTeamTriageSettingQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceTeamTriageSetting, error) {
	o := &WorkspaceTeamTriageSetting{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_team_triage_settings")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceTeamTriageSetting records from the query.
func (q workspaceTeamTriageSettingQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceTeamTriageSettingSlice, error) {
	var o []*WorkspaceTeamTriageSetting

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceTeamTriageSetting slice")
	}

	if len(workspaceTeamTriageSettingAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceTeamTriageSetting records in the query.
func (q workspaceTeamTriageSettingQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_team_triage_settings rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceTeamTriageSettingQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_team_triage_settings exists")
	}

	return count > 0, nil
}

// WorkspaceTeamTriageSettings retrieves all the records using an executor.
func WorkspaceTeamTriageSettings(mods ...qm.QueryMod) workspaceTeamTriageSettingQuery {
	mods = append(mods, qm.From("\"workspace_team_triage_settings\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_team_triage_settings\".*"})
	}

	return workspaceTeamTriageSettingQuery{q}
}

// FindWorkspaceTeamTriageSetting retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceTeamTriageSetting(ctx context.Context, exec boil.ContextExecutor, teamID string, selectCols ...string) (*WorkspaceTeamTriageSetting, error) {
	workspaceTeamTriageSettingObj := &WorkspaceTeamTriageSetting{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_team_triage_settings\" where \"team_id\"=$1", sel,
	)

	q := queries.Raw(query, teamID)

	err := q.Bind(ctx, exec, workspaceTeamTriageSettingObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_team_triage_settings")
	}

	if err = workspaceTeamTriageSettingObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceTeamTriageSettingObj, err
	}

	return workspaceTeamTriageSettingObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceTeamTriageSetting) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_triage_settings provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamTriageSettingColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceTeamTriageSettingInsertCacheMut.RLock()
	cache, cached := workspaceTeamTriageSettingInsertCache[key]
	workspaceTeamTriageSettingInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceTeamTriageSettingAllColumns,
			workspaceTeamTriageSettingColumnsWithDefault,
			workspaceTeamTriageSettingColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamTriageSettingType, workspaceTeamTriageSettingMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceTeamTriageSettingType, workspaceTeamTriageSettingMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_team_triage_settings\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_team_triage_settings\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_team_triage_settings")
	}

	if !cached {
		workspaceTeamTriageSettingInsertCacheMut.Lock()
		workspaceTeamTriageSettingInsertCache[key] = cache
		workspaceTeamTriageSettingInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceTeamTriageSetting.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceTeamTriageSetting) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceTeamTriageSettingUpdateCacheMut.RLock()
	cache, cached := workspaceTeamTriageSettingUpdateCache[key]
	workspaceTeamTriageSettingUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceTeamTriageSettingAllColumns,
			workspaceTeamTriageSettingPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_team_triage_settings, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_team_triage_settings\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceTeamTriageSettingPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceTeamTriageSettingType, workspaceTeamTriageSettingMapping, append(wl, workspaceTeamTriageSettingPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_team_triage_settings row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_team_triage_settings")
	}

	if !cached {
		workspaceTeamTriageSettingUpdateCacheMut.Lock()
		workspaceTeamTriageSettingUpdateCache[key] = cache
		workspaceTeamTriageSettingUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceTeamTriageSettingQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_team_triage_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_team_triage_settings")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceTeamTriageSettingSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamTriageSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_team_triage_settings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceTeamTriageSettingPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceTeamTriageSetting slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceTeamTriageSetting")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceTeamTriageSetting) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_triage_settings provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamTriageSettingColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceTeamTriageSettingUpsertCacheMut.RLock()
	cache, cached := workspaceTeamTriageSettingUpsertCache[key]
	workspaceTeamTriageSettingUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceTeamTriageSettingAllColumns,
			workspaceTeamTriageSettingColumnsWithDefault,
			workspaceTeamTriageSettingColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceTeamTriageSettingAllColumns,
			workspaceTeamTriageSettingPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_team_triage_settings, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceTeamTriageSettingAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceTeamTriageSettingPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_team_triage_settings, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceTeamTriageSettingPrimaryKeyColumns))
			copy(conflict, workspaceTeamTriageSettingPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_team_triage_settings\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamTriageSettingType, workspaceTeamTriageSettingMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceTeamTriageSettingType, workspaceTeamTriageSettingMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_team_triage_settings")
	}

	if !cached {
		workspaceTeamTriageSettingUpsertCacheMut.Lock()
		workspaceTeamTriageSettingUpsertCache[key] = cache
		workspaceTeamTriageSettingUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceTeamTriageSetting record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceTeamTriageSetting) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceTeamTriageSetting provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceTeamTriageSettingPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_team_triage_settings\" WHERE \"team_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_team_triage_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_team_triage_settings")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceTeamTriageSettingQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceTeamTriageSettingQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_team_triage_settings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_triage_settings")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceTeamTriageSettingSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceTeamTriageSettingBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamTriageSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_team_triage_settings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamTriageSettingPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceTeamTriageSetting slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_triage_settings")
	}

	if len(workspaceTeamTriageSettingAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceTeamTriageSetting) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceTeamTriageSetting(ctx, exec, o.TeamID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceTeamTriageSettingSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceTeamTriageSettingSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamTriageSettingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_team_triage_settings\".* FROM \"workspace_team_triage_settings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamTriageSettingPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceTeamTriageSettingSlice")
	}

	*o = slice

	return nil
}

// WorkspaceTeamTriageSettingExists checks if the WorkspaceTeamTriageSetting row exists.
func WorkspaceTeamTriageSettingExists(ctx context.Context, exec boil.ContextExecutor, teamID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_team_triage_settings\" where \"team_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, teamID)
	}
	row := exec.QueryRowContext(ctx, sql, teamID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_team_triage_settings exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceTeamTriageSetting row exists.
func (o *WorkspaceTeamTriageSetting) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceTeamTriageSettingExists(ctx, exec, o.TeamID)
}
