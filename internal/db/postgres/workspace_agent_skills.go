// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceAgentSkill is an object representing the database table.
type WorkspaceAgentSkill struct {
	ID                 string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID        string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	AgentID            null.String `boil:"agent_id" json:"agent_id,omitempty" toml:"agent_id" yaml:"agent_id,omitempty"`
	Name               string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	Description        string      `boil:"description" json:"description" toml:"description" yaml:"description"`
	Source             string      `boil:"source" json:"source" toml:"source" yaml:"source"`
	Origin             string      `boil:"origin" json:"origin" toml:"origin" yaml:"origin"`
	Path               string      `boil:"path" json:"path" toml:"path" yaml:"path"`
	Ref                string      `boil:"ref" json:"ref" toml:"ref" yaml:"ref"`
	Revision           string      `boil:"revision" json:"revision" toml:"revision" yaml:"revision"`
	Instructions       string      `boil:"instructions" json:"instructions" toml:"instructions" yaml:"instructions"`
	ContentHash        string      `boil:"content_hash" json:"content_hash" toml:"content_hash" yaml:"content_hash"`
	ObjectKey          string      `boil:"object_key" json:"object_key" toml:"object_key" yaml:"object_key"`
	SizeBytes          int64       `boil:"size_bytes" json:"size_bytes" toml:"size_bytes" yaml:"size_bytes"`
	FileCount          int         `boil:"file_count" json:"file_count" toml:"file_count" yaml:"file_count"`
	CreatedByAccountID string      `boil:"created_by_account_id" json:"created_by_account_id" toml:"created_by_account_id" yaml:"created_by_account_id"`
	CreatedAt          time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt          time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceAgentSkillR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceAgentSkillL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceAgentSkillColumns = struct {
	ID                 string
	WorkspaceID        string
	AgentID            string
	Name               string
	Description        string
	Source             string
	Origin             string
	Path               string
	Ref                string
	Revision           string
	Instructions       string
	ContentHash        string
	ObjectKey          string
	SizeBytes          string
	FileCount          string
	CreatedByAccountID string
	CreatedAt          string
	UpdatedAt          string
}{
	ID:                 "id",
	WorkspaceID:        "workspace_id",
	AgentID:            "agent_id",
	Name:               "name",
	Description:        "description",
	Source:             "source",
	Origin:             "origin",
	Path:               "path",
	Ref:                "ref",
	Revision:           "revision",
	Instructions:       "instructions",
	ContentHash:        "content_hash",
	ObjectKey:          "object_key",
	SizeBytes:          "size_bytes",
	FileCount:          "file_count",
	CreatedByAccountID: "created_by_account_id",
	CreatedAt:          "created_at",
	UpdatedAt:          "updated_at",
}

var WorkspaceAgentSkillTableColumns = struct {
	ID                 string
	WorkspaceID        string
	AgentID            string
	Name               string
	Description        string
	Source             string
	Origin             string
	Path               string
	Ref                string
	Revision           string
	Instructions       string
	ContentHash        string
	ObjectKey          string
	SizeBytes          string
	FileCount          string
	CreatedByAccountID string
	CreatedAt          string
	UpdatedAt          string
}{
	ID:                 "workspace_agent_skills.id",
	WorkspaceID:        "workspace_agent_skills.workspace_id",
	AgentID:            "workspace_agent_skills.agent_id",
	Name:               "workspace_agent_skills.name",
	Description:        "workspace_agent_skills.description",
	Source:             "workspace_agent_skills.source",
	Origin:             "workspace_agent_skills.origin",
	Path:               "workspace_agent_skills.path",
	Ref:                "workspace_agent_skills.ref",
	Revision:           "workspace_agent_skills.revision",
	Instructions:       "workspace_agent_skills.instructions",
	ContentHash:        "workspace_agent_skills.content_hash",
	ObjectKey:          "workspace_agent_skills.object_key",
	SizeBytes:          "workspace_agent_skills.size_bytes",
	FileCount:          "workspace_agent_skills.file_count",
	CreatedByAccountID: "workspace_agent_skills.created_by_account_id",
	CreatedAt:          "workspace_agent_skills.created_at",
	UpdatedAt:          "workspace_agent_skills.updated_at",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var WorkspaceAgentSkillWhere = struct {
	ID                 whereHelperstring
	WorkspaceID        whereHelperstring
	AgentID            whereHelpernull_String
	Name               whereHelperstring
	Description        whereHelperstring
	Source             whereHelperstring
	Origin             whereHelperstring
	Path               whereHelperstring
	Ref                whereHelperstring
	Revision           whereHelperstring
	Instructions       whereHelperstring
	ContentHash        whereHelperstring
	ObjectKey          whereHelperstring
	SizeBytes          whereHelperint64
	FileCount          whereHelperint
	CreatedByAccountID whereHelperstring
	CreatedAt          whereHelpertime_Time
	UpdatedAt          whereHelpertime_Time
}{
	ID:                 whereHelperstring{field: "\"workspace_agent_skills\".\"id\""},
	WorkspaceID:        whereHelperstring{field: "\"workspace_agent_skills\".\"workspace_id\""},
	AgentID:            whereHelpernull_String{field: "\"workspace_agent_skills\".\"agent_id\""},
	Name:               whereHelperstring{field: "\"workspace_agent_skills\".\"name\""},
	Description:        whereHelperstring{field: "\"workspace_agent_skills\".\"description\""},
	Source:             whereHelperstring{field: "\"workspace_agent_skills\".\"source\""},
	Origin:             whereHelperstring{field: "\"workspace_agent_skills\".\"origin\""},
	Path:               whereHelperstring{field: "\"workspace_agent_skills\".\"path\""},
	Ref:                whereHelperstring{field: "\"workspace_agent_skills\".\"ref\""},
	Revision:           whereHelperstring{field: "\"workspace_agent_skills\".\"revision\""},
	Instructions:       whereHelperstring{field: "\"workspace_agent_skills\".\"instructions\""},
	ContentHash:        whereHelperstring{field: "\"workspace_agent_skills\".\"content_hash\""},
	ObjectKey:          whereHelperstring{field: "\"workspace_agent_skills\".\"object_key\""},
	SizeBytes:          whereHelperint64{field: "\"workspace_agent_skills\".\"size_bytes\""},
	FileCount:          whereHelperint{field: "\"workspace_agent_skills\".\"file_count\""},
	CreatedByAccountID: whereHelperstring{field: "\"workspace_agent_skills\".\"created_by_account_id\""},
	CreatedAt:          whereHelpertime_Time{field: "\"workspace_agent_skills\".\"created_at\""},
	UpdatedAt:          whereHelpertime_Time{field: "\"workspace_agent_skills\".\"updated_at\""},
}

// WorkspaceAgentSkillRels is where relationship names are stored.
var WorkspaceAgentSkillRels = struct {
	Agent                               string
	CreatedByAccount                    string
	Workspace                           string
	SkillWorkspaceAgentSkillAttachments string
}{
	Agent:                               "Agent",
	CreatedByAccount:                    "CreatedByAccount",
	Workspace:                           "Workspace",
	SkillWorkspaceAgentSkillAttachments: "SkillWorkspaceAgentSkillAttachments",
}

// workspaceAgentSkillR is where relationships are stored.
type workspaceAgentSkillR struct {
	Agent                               *WorkspaceAgent                    `boil:"Agent" json:"Agent" toml:"Agent" yaml:"Agent"`
	CreatedByAccount                    *Account                           `boil:"CreatedByAccount" json:"CreatedByAccount" toml:"CreatedByAccount" yaml:"CreatedByAccount"`
	Workspace                           *Workspace                         `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
	SkillWorkspaceAgentSkillAttachments WorkspaceAgentSkillAttachmentSlice `boil:"SkillWorkspaceAgentSkillAttachments" json:"SkillWorkspaceAgentSkillAttachments" toml:"SkillWorkspaceAgentSkillAttachments" yaml:"SkillWorkspaceAgentSkillAttachments"`
}

// NewStruct creates a new relationship struct
func (*workspaceAgentSkillR) NewStruct() *workspaceAgentSkillR {
	return &workspaceAgentSkillR{}
}

func (o *WorkspaceAgentSkill) GetAgent() *WorkspaceAgent {
	if o == nil {
		return nil
	}

	return o.R.GetAgent()
}

func (r *workspaceAgentSkillR) GetAgent() *WorkspaceAgent {
	if r == nil {
		return nil
	}

	return r.Agent
}

func (o *WorkspaceAgentSkill) GetCreatedByAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByAccount()
}

func (r *workspaceAgentSkillR) GetCreatedByAccount() *Account {
	if r == nil {
		return nil
	}

	return r.CreatedByAccount
}

func (o *WorkspaceAgentSkill) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceAgentSkillR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

func (o *WorkspaceAgentSkill) GetSkillWorkspaceAgentSkillAttachments() WorkspaceAgentSkillAttachmentSlice {
	if o == nil {
		return nil
	}

	return o.R.GetSkillWorkspaceAgentSkillAttachments()
}

func (r *workspaceAgentSkillR) GetSkillWorkspaceAgentSkillAttachments() WorkspaceAgentSkillAttachmentSlice {
	if r == nil {
		return nil
	}

	return r.SkillWorkspaceAgentSkillAttachments
}

// workspaceAgentSkillL is where Load methods for each relationship are stored.
type workspaceAgentSkillL struct{}

var (
	workspaceAgentSkillAllColumns            = []string{"id", "workspace_id", "agent_id", "name", "description", "source", "origin", "path", "ref", "revision", "instructions", "content_hash", "object_key", "size_bytes", "file_count", "created_by_account_id", "created_at", "updated_at"}
	workspaceAgentSkillColumnsWithoutDefault = []string{"workspace_id", "name", "description", "source", "instructions", "content_hash", "object_key", "size_bytes", "file_count", "created_by_account_id"}
	workspaceAgentSkillColumnsWithDefault    = []string{"id", "agent_id", "origin", "path", "ref", "revision", "created_at", "updated_at"}
	workspaceAgentSkillPrimaryKeyColumns     = []string{"id"}
	workspaceAgentSkillGeneratedColumns      = []string{}
)

type (
	// WorkspaceAgentSkillSlice is an alias for a slice of pointers to WorkspaceAgentSkill.
	// This should almost always be used instead of []WorkspaceAgentSkill.
	WorkspaceAgentSkillSlice []*WorkspaceAgentSkill
	// WorkspaceAgentSkillHook is the signature for custom WorkspaceAgentSkill hook methods
	WorkspaceAgentSkillHook func(context.Context, boil.ContextExecutor, *WorkspaceAgentSkill) error

	workspaceAgentSkillQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceAgentSkillType                 = reflect.TypeOf(&WorkspaceAgentSkill{})
	workspaceAgentSkillMapping              = queries.MakeStructMapping(workspaceAgentSkillType)
	workspaceAgentSkillPrimaryKeyMapping, _ = queries.BindMapping(workspaceAgentSkillType, workspaceAgentSkillMapping, workspaceAgentSkillPrimaryKeyColumns)
	workspaceAgentSkillInsertCacheMut       sync.RWMutex
	workspaceAgentSkillInsertCache          = make(map[string]insertCache)
	workspaceAgentSkillUpdateCacheMut       sync.RWMutex
	workspaceAgentSkillUpdateCache          = make(map[string]updateCache)
	workspaceAgentSkillUpsertCacheMut       sync.RWMutex
	workspaceAgentSkillUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceAgentSkillAfterSelectMu sync.Mutex
var workspaceAgentSkillAfterSelectHooks []WorkspaceAgentSkillHook

var workspaceAgentSkillBeforeInsertMu sync.Mutex
var workspaceAgentSkillBeforeInsertHooks []WorkspaceAgentSkillHook
var workspaceAgentSkillAfterInsertMu sync.Mutex
var workspaceAgentSkillAfterInsertHooks []WorkspaceAgentSkillHook

var workspaceAgentSkillBeforeUpdateMu sync.Mutex
var workspaceAgentSkillBeforeUpdateHooks []WorkspaceAgentSkillHook
var workspaceAgentSkillAfterUpdateMu sync.Mutex
var workspaceAgentSkillAfterUpdateHooks []WorkspaceAgentSkillHook

var workspaceAgentSkillBeforeDeleteMu sync.Mutex
var workspaceAgentSkillBeforeDeleteHooks []WorkspaceAgentSkillHook
var workspaceAgentSkillAfterDeleteMu sync.Mutex
var workspaceAgentSkillAfterDeleteHooks []WorkspaceAgentSkillHook

var workspaceAgentSkillBeforeUpsertMu sync.Mutex
var workspaceAgentSkillBeforeUpsertHooks []WorkspaceAgentSkillHook
var workspaceAgentSkillAfterUpsertMu sync.Mutex
var workspaceAgentSkillAfterUpsertHooks []WorkspaceAgentSkillHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceAgentSkill) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceAgentSkill) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceAgentSkill) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceAgentSkill) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceAgentSkill) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceAgentSkill) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceAgentSkill) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceAgentSkill) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceAgentSkill) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceAgentSkillHook registers your hook function for all future operations.
func AddWorkspaceAgentSkillHook(hookPoint boil.HookPoint, workspaceAgentSkillHook WorkspaceAgentSkillHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceAgentSkillAfterSelectMu.Lock()
		workspaceAgentSkillAfterSelectHooks = append(workspaceAgentSkillAfterSelectHooks, workspaceAgentSkillHook)
		workspaceAgentSkillAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceAgentSkillBeforeInsertMu.Lock()
		workspaceAgentSkillBeforeInsertHooks = append(workspaceAgentSkillBeforeInsertHooks, workspaceAgentSkillHook)
		workspaceAgentSkillBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceAgentSkillAfterInsertMu.Lock()
		workspaceAgentSkillAfterInsertHooks = append(workspaceAgentSkillAfterInsertHooks, workspaceAgentSkillHook)
		workspaceAgentSkillAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceAgentSkillBeforeUpdateMu.Lock()
		workspaceAgentSkillBeforeUpdateHooks = append(workspaceAgentSkillBeforeUpdateHooks, workspaceAgentSkillHook)
		workspaceAgentSkillBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceAgentSkillAfterUpdateMu.Lock()
		workspaceAgentSkillAfterUpdateHooks = append(workspaceAgentSkillAfterUpdateHooks, workspaceAgentSkillHook)
		workspaceAgentSkillAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceAgentSkillBeforeDeleteMu.Lock()
		workspaceAgentSkillBeforeDeleteHooks = append(workspaceAgentSkillBeforeDeleteHooks, workspaceAgentSkillHook)
		workspaceAgentSkillBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceAgentSkillAfterDeleteMu.Lock()
		workspaceAgentSkillAfterDeleteHooks = append(workspaceAgentSkillAfterDeleteHooks, workspaceAgentSkillHook)
		workspaceAgentSkillAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceAgentSkillBeforeUpsertMu.Lock()
		workspaceAgentSkillBeforeUpsertHooks = append(workspaceAgentSkillBeforeUpsertHooks, workspaceAgentSkillHook)
		workspaceAgentSkillBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceAgentSkillAfterUpsertMu.Lock()
		workspaceAgentSkillAfterUpsertHooks = append(workspaceAgentSkillAfterUpsertHooks, workspaceAgentSkillHook)
		workspaceAgentSkillAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceAgentSkill record from the query.
func (q workspaceAgentSkillQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceAgentSkill, error) {
	o := &WorkspaceAgentSkill{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_agent_skills")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceAgentSkill records from the query.
func (q workspaceAgentSkillQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceAgentSkillSlice, error) {
	var o []*WorkspaceAgentSkill

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceAgentSkill slice")
	}

	if len(workspaceAgentSkillAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceAgentSkill records in the query.
func (q workspaceAgentSkillQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_agent_skills rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceAgentSkillQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_agent_skills exists")
	}

	return count > 0, nil
}

// Agent pointed to by the foreign key.
func (o *WorkspaceAgentSkill) Agent(mods ...qm.QueryMod) workspaceAgentQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AgentID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceAgents(queryMods...)
}

// CreatedByAccount pointed to by the foreign key.
func (o *WorkspaceAgentSkill) CreatedByAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CreatedByAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceAgentSkill) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// SkillWorkspaceAgentSkillAttachments retrieves all the workspace_agent_skill_attachment's WorkspaceAgentSkillAttachments with an executor via skill_id column.
func (o *WorkspaceAgentSkill) SkillWorkspaceAgentSkillAttachments(mods ...qm.QueryMod) workspaceAgentSkillAttachmentQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"workspace_agent_skill_attachments\".\"skill_id\"=?", o.ID),
	)

	return WorkspaceAgentSkillAttachments(queryMods...)
}

// LoadAgent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillL) LoadAgent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkill any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkill
	var object *WorkspaceAgentSkill

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkill.(*WorkspaceAgentSkill)
		if !ok {
			object = new(WorkspaceAgentSkill)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkill))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkill.(*[]*WorkspaceAgentSkill)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkill))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillR{}
		}
		if !queries.IsNil(object.AgentID) {
			args[object.AgentID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillR{}
			}

			if !queries.IsNil(obj.AgentID) {
				args[obj.AgentID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agents`),
		qm.WhereIn(`workspace_agents.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceAgent")
	}

	var resultSlice []*WorkspaceAgent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceAgent")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_agents")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agents")
	}

	if len(workspaceAgentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Agent = foreign
		if foreign.R == nil {
			foreign.R = &workspaceAgentR{}
		}
		foreign.R.AgentWorkspaceAgentSkills = append(foreign.R.AgentWorkspaceAgentSkills, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.AgentID, foreign.ID) {
				local.R.Agent = foreign
				if foreign.R == nil {
					foreign.R = &workspaceAgentR{}
				}
				foreign.R.AgentWorkspaceAgentSkills = append(foreign.R.AgentWorkspaceAgentSkills, local)
				break
			}
		}
	}

	return nil
}

// LoadCreatedByAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillL) LoadCreatedByAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkill any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkill
	var object *WorkspaceAgentSkill

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkill.(*WorkspaceAgentSkill)
		if !ok {
			object = new(WorkspaceAgentSkill)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkill))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkill.(*[]*WorkspaceAgentSkill)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkill))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillR{}
		}
		args[object.CreatedByAccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillR{}
			}

			args[obj.CreatedByAccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CreatedByAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.CreatedByAccountWorkspaceAgentSkills = append(foreign.R.CreatedByAccountWorkspaceAgentSkills, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CreatedByAccountID == foreign.ID {
				local.R.CreatedByAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.CreatedByAccountWorkspaceAgentSkills = append(foreign.R.CreatedByAccountWorkspaceAgentSkills, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkill any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkill
	var object *WorkspaceAgentSkill

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkill.(*WorkspaceAgentSkill)
		if !ok {
			object = new(WorkspaceAgentSkill)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkill))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkill.(*[]*WorkspaceAgentSkill)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkill))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceAgentSkills = append(foreign.R.WorkspaceAgentSkills, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceAgentSkills = append(foreign.R.WorkspaceAgentSkills, local)
				break
			}
		}
	}

	return nil
}

// LoadSkillWorkspaceAgentSkillAttachments allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (workspaceAgentSkillL) LoadSkillWorkspaceAgentSkillAttachments(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkill any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkill
	var object *WorkspaceAgentSkill

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkill.(*WorkspaceAgentSkill)
		if !ok {
			object = new(WorkspaceAgentSkill)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkill))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkill.(*[]*WorkspaceAgentSkill)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkill))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agent_skill_attachments`),
		qm.WhereIn(`workspace_agent_skill_attachments.skill_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load workspace_agent_skill_attachments")
	}

	var resultSlice []*WorkspaceAgentSkillAttachment
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice workspace_agent_skill_attachments")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on workspace_agent_skill_attachments")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agent_skill_attachments")
	}

	if len(workspaceAgentSkillAttachmentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.SkillWorkspaceAgentSkillAttachments = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &workspaceAgentSkillAttachmentR{}
			}
			foreign.R.Skill = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.SkillID {
				local.R.SkillWorkspaceAgentSkillAttachments = append(local.R.SkillWorkspaceAgentSkillAttachments, foreign)
				if foreign.R == nil {
					foreign.R = &workspaceAgentSkillAttachmentR{}
				}
				foreign.R.Skill = local
				break
			}
		}
	}

	return nil
}

// SetAgent of the workspaceAgentSkill to the related item.
// Sets o.R.Agent to related.
// Adds o to related.R.AgentWorkspaceAgentSkills.
func (o *WorkspaceAgentSkill) SetAgent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceAgent) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.AgentID, related.ID)
	if o.R == nil {
		o.R = &workspaceAgentSkillR{
			Agent: related,
		}
	} else {
		o.R.Agent = related
	}

	if related.R == nil {
		related.R = &workspaceAgentR{
			AgentWorkspaceAgentSkills: WorkspaceAgentSkillSlice{o},
		}
	} else {
		related.R.AgentWorkspaceAgentSkills = append(related.R.AgentWorkspaceAgentSkills, o)
	}

	return nil
}

// RemoveAgent relationship.
// Sets o.R.Agent to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceAgentSkill) RemoveAgent(ctx context.Context, exec boil.ContextExecutor, related *WorkspaceAgent) error {
	var err error

	queries.SetScanner(&o.AgentID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("agent_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Agent = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.AgentWorkspaceAgentSkills {
		if queries.Equal(o.AgentID, ri.AgentID) {
			continue
		}

		ln := len(related.R.AgentWorkspaceAgentSkills)
		if ln > 1 && i < ln-1 {
			related.R.AgentWorkspaceAgentSkills[i] = related.R.AgentWorkspaceAgentSkills[ln-1]
		}
		related.R.AgentWorkspaceAgentSkills = related.R.AgentWorkspaceAgentSkills[:ln-1]
		break
	}
	return nil
}

// SetCreatedByAccount of the workspaceAgentSkill to the related item.
// Sets o.R.CreatedByAccount to related.
// Adds o to related.R.CreatedByAccountWorkspaceAgentSkills.
func (o *WorkspaceAgentSkill) SetCreatedByAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"created_by_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CreatedByAccountID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentSkillR{
			CreatedByAccount: related,
		}
	} else {
		o.R.CreatedByAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			CreatedByAccountWorkspaceAgentSkills: WorkspaceAgentSkillSlice{o},
		}
	} else {
		related.R.CreatedByAccountWorkspaceAgentSkills = append(related.R.CreatedByAccountWorkspaceAgentSkills, o)
	}

	return nil
}

// SetWorkspace of the workspaceAgentSkill to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceAgentSkills.
func (o *WorkspaceAgentSkill) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentSkillR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceAgentSkills: WorkspaceAgentSkillSlice{o},
		}
	} else {
		related.R.WorkspaceAgentSkills = append(related.R.WorkspaceAgentSkills, o)
	}

	return nil
}

// AddSkillWorkspaceAgentSkillAttachments adds the given related objects to the existing relationships
// of the workspace_agent_skill, optionally inserting them as new records.
// Appends related to o.R.SkillWorkspaceAgentSkillAttachments.
// Sets related.R.Skill appropriately.
func (o *WorkspaceAgentSkill) AddSkillWorkspaceAgentSkillAttachments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorkspaceAgentSkillAttachment) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.SkillID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"skill_id"}),
				strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillAttachmentPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.AgentID, rel.SkillID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.SkillID = o.ID
		}
	}

	if o.R == nil {
		o.R = &workspaceAgentSkillR{
			SkillWorkspaceAgentSkillAttachments: related,
		}
	} else {
		o.R.SkillWorkspaceAgentSkillAttachments = append(o.R.SkillWorkspaceAgentSkillAttachments, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &workspaceAgentSkillAttachmentR{
				Skill: o,
			}
		} else {
			rel.R.Skill = o
		}
	}
	return nil
}

// WorkspaceAgentSkills retrieves all the records using an executor.
func WorkspaceAgentSkills(mods ...qm.QueryMod) workspaceAgentSkillQuery {
	mods = append(mods, qm.From("\"workspace_agent_skills\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_agent_skills\".*"})
	}

	return workspaceAgentSkillQuery{q}
}

// FindWorkspaceAgentSkill retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceAgentSkill(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceAgentSkill, error) {
	workspaceAgentSkillObj := &WorkspaceAgentSkill{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_agent_skills\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceAgentSkillObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_agent_skills")
	}

	if err = workspaceAgentSkillObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceAgentSkillObj, err
	}

	return workspaceAgentSkillObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceAgentSkill) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_skills provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentSkillColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceAgentSkillInsertCacheMut.RLock()
	cache, cached := workspaceAgentSkillInsertCache[key]
	workspaceAgentSkillInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceAgentSkillAllColumns,
			workspaceAgentSkillColumnsWithDefault,
			workspaceAgentSkillColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentSkillType, workspaceAgentSkillMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceAgentSkillType, workspaceAgentSkillMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_agent_skills\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_agent_skills\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_agent_skills")
	}

	if !cached {
		workspaceAgentSkillInsertCacheMut.Lock()
		workspaceAgentSkillInsertCache[key] = cache
		workspaceAgentSkillInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceAgentSkill.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceAgentSkill) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceAgentSkillUpdateCacheMut.RLock()
	cache, cached := workspaceAgentSkillUpdateCache[key]
	workspaceAgentSkillUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceAgentSkillAllColumns,
			workspaceAgentSkillPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_agent_skills, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_agent_skills\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceAgentSkillPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceAgentSkillType, workspaceAgentSkillMapping, append(wl, workspaceAgentSkillPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_agent_skills row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_agent_skills")
	}

	if !cached {
		workspaceAgentSkillUpdateCacheMut.Lock()
		workspaceAgentSkillUpdateCache[key] = cache
		workspaceAgentSkillUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceAgentSkillQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_agent_skills")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_agent_skills")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceAgentSkillSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentSkillPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_agent_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceAgentSkillPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceAgentSkill slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceAgentSkill")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceAgentSkill) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_skills provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentSkillColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceAgentSkillUpsertCacheMut.RLock()
	cache, cached := workspaceAgentSkillUpsertCache[key]
	workspaceAgentSkillUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceAgentSkillAllColumns,
			workspaceAgentSkillColumnsWithDefault,
			workspaceAgentSkillColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceAgentSkillAllColumns,
			workspaceAgentSkillPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_agent_skills, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceAgentSkillAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceAgentSkillPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_agent_skills, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceAgentSkillPrimaryKeyColumns))
			copy(conflict, workspaceAgentSkillPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_agent_skills\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentSkillType, workspaceAgentSkillMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceAgentSkillType, workspaceAgentSkillMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_agent_skills")
	}

	if !cached {
		workspaceAgentSkillUpsertCacheMut.Lock()
		workspaceAgentSkillUpsertCache[key] = cache
		workspaceAgentSkillUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceAgentSkill record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceAgentSkill) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceAgentSkill provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceAgentSkillPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_agent_skills\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_agent_skills")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_agent_skills")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceAgentSkillQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceAgentSkillQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_agent_skills")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_skills")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceAgentSkillSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceAgentSkillBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentSkillPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_agent_skills\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentSkillPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceAgentSkill slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_skills")
	}

	if len(workspaceAgentSkillAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceAgentSkill) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceAgentSkill(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceAgentSkillSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceAgentSkillSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentSkillPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_agent_skills\".* FROM \"workspace_agent_skills\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentSkillPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceAgentSkillSlice")
	}

	*o = slice

	return nil
}

// WorkspaceAgentSkillExists checks if the WorkspaceAgentSkill row exists.
func WorkspaceAgentSkillExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_agent_skills\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_agent_skills exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceAgentSkill row exists.
func (o *WorkspaceAgentSkill) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceAgentSkillExists(ctx, exec, o.ID)
}
