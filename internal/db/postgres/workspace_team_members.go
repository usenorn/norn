// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceTeamMember is an object representing the database table.
type WorkspaceTeamMember struct {
	ID          string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorkspaceID string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	TeamID      string    `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	AccountID   string    `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *workspaceTeamMemberR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceTeamMemberL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceTeamMemberColumns = struct {
	ID          string
	WorkspaceID string
	TeamID      string
	AccountID   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	WorkspaceID: "workspace_id",
	TeamID:      "team_id",
	AccountID:   "account_id",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var WorkspaceTeamMemberTableColumns = struct {
	ID          string
	WorkspaceID string
	TeamID      string
	AccountID   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "workspace_team_members.id",
	WorkspaceID: "workspace_team_members.workspace_id",
	TeamID:      "workspace_team_members.team_id",
	AccountID:   "workspace_team_members.account_id",
	CreatedAt:   "workspace_team_members.created_at",
	UpdatedAt:   "workspace_team_members.updated_at",
}

// Generated where

var WorkspaceTeamMemberWhere = struct {
	ID          whereHelperstring
	WorkspaceID whereHelperstring
	TeamID      whereHelperstring
	AccountID   whereHelperstring
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"workspace_team_members\".\"id\""},
	WorkspaceID: whereHelperstring{field: "\"workspace_team_members\".\"workspace_id\""},
	TeamID:      whereHelperstring{field: "\"workspace_team_members\".\"team_id\""},
	AccountID:   whereHelperstring{field: "\"workspace_team_members\".\"account_id\""},
	CreatedAt:   whereHelpertime_Time{field: "\"workspace_team_members\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"workspace_team_members\".\"updated_at\""},
}

// WorkspaceTeamMemberRels is where relationship names are stored.
var WorkspaceTeamMemberRels = struct {
}{}

// workspaceTeamMemberR is where relationships are stored.
type workspaceTeamMemberR struct {
}

// NewStruct creates a new relationship struct
func (*workspaceTeamMemberR) NewStruct() *workspaceTeamMemberR {
	return &workspaceTeamMemberR{}
}

// workspaceTeamMemberL is where Load methods for each relationship are stored.
type workspaceTeamMemberL struct{}

var (
	workspaceTeamMemberAllColumns            = []string{"id", "workspace_id", "team_id", "account_id", "created_at", "updated_at"}
	workspaceTeamMemberColumnsWithoutDefault = []string{"workspace_id", "team_id", "account_id"}
	workspaceTeamMemberColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	workspaceTeamMemberPrimaryKeyColumns     = []string{"id"}
	workspaceTeamMemberGeneratedColumns      = []string{}
)

type (
	// WorkspaceTeamMemberSlice is an alias for a slice of pointers to WorkspaceTeamMember.
	// This should almost always be used instead of []WorkspaceTeamMember.
	WorkspaceTeamMemberSlice []*WorkspaceTeamMember
	// WorkspaceTeamMemberHook is the signature for custom WorkspaceTeamMember hook methods
	WorkspaceTeamMemberHook func(context.Context, boil.ContextExecutor, *WorkspaceTeamMember) error

	workspaceTeamMemberQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceTeamMemberType                 = reflect.TypeOf(&WorkspaceTeamMember{})
	workspaceTeamMemberMapping              = queries.MakeStructMapping(workspaceTeamMemberType)
	workspaceTeamMemberPrimaryKeyMapping, _ = queries.BindMapping(workspaceTeamMemberType, workspaceTeamMemberMapping, workspaceTeamMemberPrimaryKeyColumns)
	workspaceTeamMemberInsertCacheMut       sync.RWMutex
	workspaceTeamMemberInsertCache          = make(map[string]insertCache)
	workspaceTeamMemberUpdateCacheMut       sync.RWMutex
	workspaceTeamMemberUpdateCache          = make(map[string]updateCache)
	workspaceTeamMemberUpsertCacheMut       sync.RWMutex
	workspaceTeamMemberUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceTeamMemberAfterSelectMu sync.Mutex
var workspaceTeamMemberAfterSelectHooks []WorkspaceTeamMemberHook

var workspaceTeamMemberBeforeInsertMu sync.Mutex
var workspaceTeamMemberBeforeInsertHooks []WorkspaceTeamMemberHook
var workspaceTeamMemberAfterInsertMu sync.Mutex
var workspaceTeamMemberAfterInsertHooks []WorkspaceTeamMemberHook

var workspaceTeamMemberBeforeUpdateMu sync.Mutex
var workspaceTeamMemberBeforeUpdateHooks []WorkspaceTeamMemberHook
var workspaceTeamMemberAfterUpdateMu sync.Mutex
var workspaceTeamMemberAfterUpdateHooks []WorkspaceTeamMemberHook

var workspaceTeamMemberBeforeDeleteMu sync.Mutex
var workspaceTeamMemberBeforeDeleteHooks []WorkspaceTeamMemberHook
var workspaceTeamMemberAfterDeleteMu sync.Mutex
var workspaceTeamMemberAfterDeleteHooks []WorkspaceTeamMemberHook

var workspaceTeamMemberBeforeUpsertMu sync.Mutex
var workspaceTeamMemberBeforeUpsertHooks []WorkspaceTeamMemberHook
var workspaceTeamMemberAfterUpsertMu sync.Mutex
var workspaceTeamMemberAfterUpsertHooks []WorkspaceTeamMemberHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceTeamMember) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceTeamMember) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceTeamMember) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceTeamMember) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceTeamMember) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceTeamMember) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceTeamMember) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceTeamMember) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceTeamMember) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamMemberAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceTeamMemberHook registers your hook function for all future operations.
func AddWorkspaceTeamMemberHook(hookPoint boil.HookPoint, workspaceTeamMemberHook WorkspaceTeamMemberHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceTeamMemberAfterSelectMu.Lock()
		workspaceTeamMemberAfterSelectHooks = append(workspaceTeamMemberAfterSelectHooks, workspaceTeamMemberHook)
		workspaceTeamMemberAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceTeamMemberBeforeInsertMu.Lock()
		workspaceTeamMemberBeforeInsertHooks = append(workspaceTeamMemberBeforeInsertHooks, workspaceTeamMemberHook)
		workspaceTeamMemberBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceTeamMemberAfterInsertMu.Lock()
		workspaceTeamMemberAfterInsertHooks = append(workspaceTeamMemberAfterInsertHooks, workspaceTeamMemberHook)
		workspaceTeamMemberAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceTeamMemberBeforeUpdateMu.Lock()
		workspaceTeamMemberBeforeUpdateHooks = append(workspaceTeamMemberBeforeUpdateHooks, workspaceTeamMemberHook)
		workspaceTeamMemberBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceTeamMemberAfterUpdateMu.Lock()
		workspaceTeamMemberAfterUpdateHooks = append(workspaceTeamMemberAfterUpdateHooks, workspaceTeamMemberHook)
		workspaceTeamMemberAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceTeamMemberBeforeDeleteMu.Lock()
		workspaceTeamMemberBeforeDeleteHooks = append(workspaceTeamMemberBeforeDeleteHooks, workspaceTeamMemberHook)
		workspaceTeamMemberBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceTeamMemberAfterDeleteMu.Lock()
		workspaceTeamMemberAfterDeleteHooks = append(workspaceTeamMemberAfterDeleteHooks, workspaceTeamMemberHook)
		workspaceTeamMemberAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceTeamMemberBeforeUpsertMu.Lock()
		workspaceTeamMemberBeforeUpsertHooks = append(workspaceTeamMemberBeforeUpsertHooks, workspaceTeamMemberHook)
		workspaceTeamMemberBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceTeamMemberAfterUpsertMu.Lock()
		workspaceTeamMemberAfterUpsertHooks = append(workspaceTeamMemberAfterUpsertHooks, workspaceTeamMemberHook)
		workspaceTeamMemberAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceTeamMember record from the query.
func (q workspaceTeamMemberQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceTeamMember, error) {
	o := &WorkspaceTeamMember{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_team_members")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceTeamMember records from the query.
func (q workspaceTeamMemberQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceTeamMemberSlice, error) {
	var o []*WorkspaceTeamMember

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceTeamMember slice")
	}

	if len(workspaceTeamMemberAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceTeamMember records in the query.
func (q workspaceTeamMemberQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_team_members rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceTeamMemberQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_team_members exists")
	}

	return count > 0, nil
}

// WorkspaceTeamMembers retrieves all the records using an executor.
func WorkspaceTeamMembers(mods ...qm.QueryMod) workspaceTeamMemberQuery {
	mods = append(mods, qm.From("\"workspace_team_members\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_team_members\".*"})
	}

	return workspaceTeamMemberQuery{q}
}

// FindWorkspaceTeamMember retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceTeamMember(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorkspaceTeamMember, error) {
	workspaceTeamMemberObj := &WorkspaceTeamMember{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_team_members\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, workspaceTeamMemberObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_team_members")
	}

	if err = workspaceTeamMemberObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceTeamMemberObj, err
	}

	return workspaceTeamMemberObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceTeamMember) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_members provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamMemberColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceTeamMemberInsertCacheMut.RLock()
	cache, cached := workspaceTeamMemberInsertCache[key]
	workspaceTeamMemberInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceTeamMemberAllColumns,
			workspaceTeamMemberColumnsWithDefault,
			workspaceTeamMemberColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamMemberType, workspaceTeamMemberMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceTeamMemberType, workspaceTeamMemberMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_team_members\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_team_members\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_team_members")
	}

	if !cached {
		workspaceTeamMemberInsertCacheMut.Lock()
		workspaceTeamMemberInsertCache[key] = cache
		workspaceTeamMemberInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceTeamMember.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceTeamMember) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceTeamMemberUpdateCacheMut.RLock()
	cache, cached := workspaceTeamMemberUpdateCache[key]
	workspaceTeamMemberUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceTeamMemberAllColumns,
			workspaceTeamMemberPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_team_members, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_team_members\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceTeamMemberPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceTeamMemberType, workspaceTeamMemberMapping, append(wl, workspaceTeamMemberPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_team_members row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_team_members")
	}

	if !cached {
		workspaceTeamMemberUpdateCacheMut.Lock()
		workspaceTeamMemberUpdateCache[key] = cache
		workspaceTeamMemberUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceTeamMemberQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_team_members")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_team_members")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceTeamMemberSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamMemberPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_team_members\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceTeamMemberPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceTeamMember slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceTeamMember")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceTeamMember) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_members provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamMemberColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceTeamMemberUpsertCacheMut.RLock()
	cache, cached := workspaceTeamMemberUpsertCache[key]
	workspaceTeamMemberUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceTeamMemberAllColumns,
			workspaceTeamMemberColumnsWithDefault,
			workspaceTeamMemberColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceTeamMemberAllColumns,
			workspaceTeamMemberPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_team_members, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceTeamMemberAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceTeamMemberPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_team_members, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceTeamMemberPrimaryKeyColumns))
			copy(conflict, workspaceTeamMemberPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_team_members\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamMemberType, workspaceTeamMemberMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceTeamMemberType, workspaceTeamMemberMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_team_members")
	}

	if !cached {
		workspaceTeamMemberUpsertCacheMut.Lock()
		workspaceTeamMemberUpsertCache[key] = cache
		workspaceTeamMemberUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceTeamMember record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceTeamMember) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceTeamMember provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceTeamMemberPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_team_members\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_team_members")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_team_members")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceTeamMemberQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceTeamMemberQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_team_members")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_members")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceTeamMemberSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceTeamMemberBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamMemberPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_team_members\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamMemberPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceTeamMember slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_members")
	}

	if len(workspaceTeamMemberAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceTeamMember) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceTeamMember(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceTeamMemberSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceTeamMemberSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamMemberPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_team_members\".* FROM \"workspace_team_members\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamMemberPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceTeamMemberSlice")
	}

	*o = slice

	return nil
}

// WorkspaceTeamMemberExists checks if the WorkspaceTeamMember row exists.
func WorkspaceTeamMemberExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_team_members\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_team_members exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceTeamMember row exists.
func (o *WorkspaceTeamMember) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceTeamMemberExists(ctx, exec, o.ID)
}
