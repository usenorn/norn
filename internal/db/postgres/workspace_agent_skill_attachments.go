// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceAgentSkillAttachment is an object representing the database table.
type WorkspaceAgentSkillAttachment struct {
	AgentID             string    `boil:"agent_id" json:"agent_id" toml:"agent_id" yaml:"agent_id"`
	SkillID             string    `boil:"skill_id" json:"skill_id" toml:"skill_id" yaml:"skill_id"`
	WorkspaceID         string    `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	AttachedByAccountID string    `boil:"attached_by_account_id" json:"attached_by_account_id" toml:"attached_by_account_id" yaml:"attached_by_account_id"`
	AttachedAt          time.Time `boil:"attached_at" json:"attached_at" toml:"attached_at" yaml:"attached_at"`

	R *workspaceAgentSkillAttachmentR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceAgentSkillAttachmentL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceAgentSkillAttachmentColumns = struct {
	AgentID             string
	SkillID             string
	WorkspaceID         string
	AttachedByAccountID string
	AttachedAt          string
}{
	AgentID:             "agent_id",
	SkillID:             "skill_id",
	WorkspaceID:         "workspace_id",
	AttachedByAccountID: "attached_by_account_id",
	AttachedAt:          "attached_at",
}

var WorkspaceAgentSkillAttachmentTableColumns = struct {
	AgentID             string
	SkillID             string
	WorkspaceID         string
	AttachedByAccountID string
	AttachedAt          string
}{
	AgentID:             "workspace_agent_skill_attachments.agent_id",
	SkillID:             "workspace_agent_skill_attachments.skill_id",
	WorkspaceID:         "workspace_agent_skill_attachments.workspace_id",
	AttachedByAccountID: "workspace_agent_skill_attachments.attached_by_account_id",
	AttachedAt:          "workspace_agent_skill_attachments.attached_at",
}

// Generated where

var WorkspaceAgentSkillAttachmentWhere = struct {
	AgentID             whereHelperstring
	SkillID             whereHelperstring
	WorkspaceID         whereHelperstring
	AttachedByAccountID whereHelperstring
	AttachedAt          whereHelpertime_Time
}{
	AgentID:             whereHelperstring{field: "\"workspace_agent_skill_attachments\".\"agent_id\""},
	SkillID:             whereHelperstring{field: "\"workspace_agent_skill_attachments\".\"skill_id\""},
	WorkspaceID:         whereHelperstring{field: "\"workspace_agent_skill_attachments\".\"workspace_id\""},
	AttachedByAccountID: whereHelperstring{field: "\"workspace_agent_skill_attachments\".\"attached_by_account_id\""},
	AttachedAt:          whereHelpertime_Time{field: "\"workspace_agent_skill_attachments\".\"attached_at\""},
}

// WorkspaceAgentSkillAttachmentRels is where relationship names are stored.
var WorkspaceAgentSkillAttachmentRels = struct {
	Agent             string
	AttachedByAccount string
	Skill             string
	Workspace         string
}{
	Agent:             "Agent",
	AttachedByAccount: "AttachedByAccount",
	Skill:             "Skill",
	Workspace:         "Workspace",
}

// workspaceAgentSkillAttachmentR is where relationships are stored.
type workspaceAgentSkillAttachmentR struct {
	Agent             *WorkspaceAgent      `boil:"Agent" json:"Agent" toml:"Agent" yaml:"Agent"`
	AttachedByAccount *Account             `boil:"AttachedByAccount" json:"AttachedByAccount" toml:"AttachedByAccount" yaml:"AttachedByAccount"`
	Skill             *WorkspaceAgentSkill `boil:"Skill" json:"Skill" toml:"Skill" yaml:"Skill"`
	Workspace         *Workspace           `boil:"Workspace" json:"Workspace" toml:"Workspace" yaml:"Workspace"`
}

// NewStruct creates a new relationship struct
func (*workspaceAgentSkillAttachmentR) NewStruct() *workspaceAgentSkillAttachmentR {
	return &workspaceAgentSkillAttachmentR{}
}

func (o *WorkspaceAgentSkillAttachment) GetAgent() *WorkspaceAgent {
	if o == nil {
		return nil
	}

	return o.R.GetAgent()
}

func (r *workspaceAgentSkillAttachmentR) GetAgent() *WorkspaceAgent {
	if r == nil {
		return nil
	}

	return r.Agent
}

func (o *WorkspaceAgentSkillAttachment) GetAttachedByAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetAttachedByAccount()
}

func (r *workspaceAgentSkillAttachmentR) GetAttachedByAccount() *Account {
	if r == nil {
		return nil
	}

	return r.AttachedByAccount
}

func (o *WorkspaceAgentSkillAttachment) GetSkill() *WorkspaceAgentSkill {
	if o == nil {
		return nil
	}

	return o.R.GetSkill()
}

func (r *workspaceAgentSkillAttachmentR) GetSkill() *WorkspaceAgentSkill {
	if r == nil {
		return nil
	}

	return r.Skill
}

func (o *WorkspaceAgentSkillAttachment) GetWorkspace() *Workspace {
	if o == nil {
		return nil
	}

	return o.R.GetWorkspace()
}

func (r *workspaceAgentSkillAttachmentR) GetWorkspace() *Workspace {
	if r == nil {
		return nil
	}

	return r.Workspace
}

// workspaceAgentSkillAttachmentL is where Load methods for each relationship are stored.
type workspaceAgentSkillAttachmentL struct{}

var (
	workspaceAgentSkillAttachmentAllColumns            = []string{"agent_id", "skill_id", "workspace_id", "attached_by_account_id", "attached_at"}
	workspaceAgentSkillAttachmentColumnsWithoutDefault = []string{"agent_id", "skill_id", "workspace_id", "attached_by_account_id"}
	workspaceAgentSkillAttachmentColumnsWithDefault    = []string{"attached_at"}
	workspaceAgentSkillAttachmentPrimaryKeyColumns     = []string{"agent_id", "skill_id"}
	workspaceAgentSkillAttachmentGeneratedColumns      = []string{}
)

type (
	// WorkspaceAgentSkillAttachmentSlice is an alias for a slice of pointers to WorkspaceAgentSkillAttachment.
	// This should almost always be used instead of []WorkspaceAgentSkillAttachment.
	WorkspaceAgentSkillAttachmentSlice []*WorkspaceAgentSkillAttachment
	// WorkspaceAgentSkillAttachmentHook is the signature for custom WorkspaceAgentSkillAttachment hook methods
	WorkspaceAgentSkillAttachmentHook func(context.Context, boil.ContextExecutor, *WorkspaceAgentSkillAttachment) error

	workspaceAgentSkillAttachmentQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceAgentSkillAttachmentType                 = reflect.TypeOf(&WorkspaceAgentSkillAttachment{})
	workspaceAgentSkillAttachmentMapping              = queries.MakeStructMapping(workspaceAgentSkillAttachmentType)
	workspaceAgentSkillAttachmentPrimaryKeyMapping, _ = queries.BindMapping(workspaceAgentSkillAttachmentType, workspaceAgentSkillAttachmentMapping, workspaceAgentSkillAttachmentPrimaryKeyColumns)
	workspaceAgentSkillAttachmentInsertCacheMut       sync.RWMutex
	workspaceAgentSkillAttachmentInsertCache          = make(map[string]insertCache)
	workspaceAgentSkillAttachmentUpdateCacheMut       sync.RWMutex
	workspaceAgentSkillAttachmentUpdateCache          = make(map[string]updateCache)
	workspaceAgentSkillAttachmentUpsertCacheMut       sync.RWMutex
	workspaceAgentSkillAttachmentUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceAgentSkillAttachmentAfterSelectMu sync.Mutex
var workspaceAgentSkillAttachmentAfterSelectHooks []WorkspaceAgentSkillAttachmentHook

var workspaceAgentSkillAttachmentBeforeInsertMu sync.Mutex
var workspaceAgentSkillAttachmentBeforeInsertHooks []WorkspaceAgentSkillAttachmentHook
var workspaceAgentSkillAttachmentAfterInsertMu sync.Mutex
var workspaceAgentSkillAttachmentAfterInsertHooks []WorkspaceAgentSkillAttachmentHook

var workspaceAgentSkillAttachmentBeforeUpdateMu sync.Mutex
var workspaceAgentSkillAttachmentBeforeUpdateHooks []WorkspaceAgentSkillAttachmentHook
var workspaceAgentSkillAttachmentAfterUpdateMu sync.Mutex
var workspaceAgentSkillAttachmentAfterUpdateHooks []WorkspaceAgentSkillAttachmentHook

var workspaceAgentSkillAttachmentBeforeDeleteMu sync.Mutex
var workspaceAgentSkillAttachmentBeforeDeleteHooks []WorkspaceAgentSkillAttachmentHook
var workspaceAgentSkillAttachmentAfterDeleteMu sync.Mutex
var workspaceAgentSkillAttachmentAfterDeleteHooks []WorkspaceAgentSkillAttachmentHook

var workspaceAgentSkillAttachmentBeforeUpsertMu sync.Mutex
var workspaceAgentSkillAttachmentBeforeUpsertHooks []WorkspaceAgentSkillAttachmentHook
var workspaceAgentSkillAttachmentAfterUpsertMu sync.Mutex
var workspaceAgentSkillAttachmentAfterUpsertHooks []WorkspaceAgentSkillAttachmentHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceAgentSkillAttachment) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceAgentSkillAttachment) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceAgentSkillAttachment) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceAgentSkillAttachment) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceAgentSkillAttachment) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceAgentSkillAttachment) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceAgentSkillAttachment) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceAgentSkillAttachment) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceAgentSkillAttachment) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceAgentSkillAttachmentAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceAgentSkillAttachmentHook registers your hook function for all future operations.
func AddWorkspaceAgentSkillAttachmentHook(hookPoint boil.HookPoint, workspaceAgentSkillAttachmentHook WorkspaceAgentSkillAttachmentHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceAgentSkillAttachmentAfterSelectMu.Lock()
		workspaceAgentSkillAttachmentAfterSelectHooks = append(workspaceAgentSkillAttachmentAfterSelectHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceAgentSkillAttachmentBeforeInsertMu.Lock()
		workspaceAgentSkillAttachmentBeforeInsertHooks = append(workspaceAgentSkillAttachmentBeforeInsertHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceAgentSkillAttachmentAfterInsertMu.Lock()
		workspaceAgentSkillAttachmentAfterInsertHooks = append(workspaceAgentSkillAttachmentAfterInsertHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceAgentSkillAttachmentBeforeUpdateMu.Lock()
		workspaceAgentSkillAttachmentBeforeUpdateHooks = append(workspaceAgentSkillAttachmentBeforeUpdateHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceAgentSkillAttachmentAfterUpdateMu.Lock()
		workspaceAgentSkillAttachmentAfterUpdateHooks = append(workspaceAgentSkillAttachmentAfterUpdateHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceAgentSkillAttachmentBeforeDeleteMu.Lock()
		workspaceAgentSkillAttachmentBeforeDeleteHooks = append(workspaceAgentSkillAttachmentBeforeDeleteHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceAgentSkillAttachmentAfterDeleteMu.Lock()
		workspaceAgentSkillAttachmentAfterDeleteHooks = append(workspaceAgentSkillAttachmentAfterDeleteHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceAgentSkillAttachmentBeforeUpsertMu.Lock()
		workspaceAgentSkillAttachmentBeforeUpsertHooks = append(workspaceAgentSkillAttachmentBeforeUpsertHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceAgentSkillAttachmentAfterUpsertMu.Lock()
		workspaceAgentSkillAttachmentAfterUpsertHooks = append(workspaceAgentSkillAttachmentAfterUpsertHooks, workspaceAgentSkillAttachmentHook)
		workspaceAgentSkillAttachmentAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceAgentSkillAttachment record from the query.
func (q workspaceAgentSkillAttachmentQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceAgentSkillAttachment, error) {
	o := &WorkspaceAgentSkillAttachment{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_agent_skill_attachments")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceAgentSkillAttachment records from the query.
func (q workspaceAgentSkillAttachmentQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceAgentSkillAttachmentSlice, error) {
	var o []*WorkspaceAgentSkillAttachment

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceAgentSkillAttachment slice")
	}

	if len(workspaceAgentSkillAttachmentAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceAgentSkillAttachment records in the query.
func (q workspaceAgentSkillAttachmentQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_agent_skill_attachments rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceAgentSkillAttachmentQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_agent_skill_attachments exists")
	}

	return count > 0, nil
}

// Agent pointed to by the foreign key.
func (o *WorkspaceAgentSkillAttachment) Agent(mods ...qm.QueryMod) workspaceAgentQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AgentID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceAgents(queryMods...)
}

// AttachedByAccount pointed to by the foreign key.
func (o *WorkspaceAgentSkillAttachment) AttachedByAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AttachedByAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// Skill pointed to by the foreign key.
func (o *WorkspaceAgentSkillAttachment) Skill(mods ...qm.QueryMod) workspaceAgentSkillQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SkillID),
	}

	queryMods = append(queryMods, mods...)

	return WorkspaceAgentSkills(queryMods...)
}

// Workspace pointed to by the foreign key.
func (o *WorkspaceAgentSkillAttachment) Workspace(mods ...qm.QueryMod) workspaceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WorkspaceID),
	}

	queryMods = append(queryMods, mods...)

	return Workspaces(queryMods...)
}

// LoadAgent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillAttachmentL) LoadAgent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkillAttachment any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkillAttachment
	var object *WorkspaceAgentSkillAttachment

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkillAttachment.(*WorkspaceAgentSkillAttachment)
		if !ok {
			object = new(WorkspaceAgentSkillAttachment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkillAttachment))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkillAttachment.(*[]*WorkspaceAgentSkillAttachment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkillAttachment))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillAttachmentR{}
		}
		args[object.AgentID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillAttachmentR{}
			}

			args[obj.AgentID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agents`),
		qm.WhereIn(`workspace_agents.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceAgent")
	}

	var resultSlice []*WorkspaceAgent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceAgent")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_agents")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agents")
	}

	if len(workspaceAgentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Agent = foreign
		if foreign.R == nil {
			foreign.R = &workspaceAgentR{}
		}
		foreign.R.AgentWorkspaceAgentSkillAttachments = append(foreign.R.AgentWorkspaceAgentSkillAttachments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AgentID == foreign.ID {
				local.R.Agent = foreign
				if foreign.R == nil {
					foreign.R = &workspaceAgentR{}
				}
				foreign.R.AgentWorkspaceAgentSkillAttachments = append(foreign.R.AgentWorkspaceAgentSkillAttachments, local)
				break
			}
		}
	}

	return nil
}

// LoadAttachedByAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillAttachmentL) LoadAttachedByAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkillAttachment any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkillAttachment
	var object *WorkspaceAgentSkillAttachment

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkillAttachment.(*WorkspaceAgentSkillAttachment)
		if !ok {
			object = new(WorkspaceAgentSkillAttachment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkillAttachment))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkillAttachment.(*[]*WorkspaceAgentSkillAttachment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkillAttachment))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillAttachmentR{}
		}
		args[object.AttachedByAccountID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillAttachmentR{}
			}

			args[obj.AttachedByAccountID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AttachedByAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.AttachedByAccountWorkspaceAgentSkillAttachments = append(foreign.R.AttachedByAccountWorkspaceAgentSkillAttachments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AttachedByAccountID == foreign.ID {
				local.R.AttachedByAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.AttachedByAccountWorkspaceAgentSkillAttachments = append(foreign.R.AttachedByAccountWorkspaceAgentSkillAttachments, local)
				break
			}
		}
	}

	return nil
}

// LoadSkill allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillAttachmentL) LoadSkill(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkillAttachment any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkillAttachment
	var object *WorkspaceAgentSkillAttachment

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkillAttachment.(*WorkspaceAgentSkillAttachment)
		if !ok {
			object = new(WorkspaceAgentSkillAttachment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkillAttachment))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkillAttachment.(*[]*WorkspaceAgentSkillAttachment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkillAttachment))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillAttachmentR{}
		}
		args[object.SkillID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillAttachmentR{}
			}

			args[obj.SkillID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspace_agent_skills`),
		qm.WhereIn(`workspace_agent_skills.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WorkspaceAgentSkill")
	}

	var resultSlice []*WorkspaceAgentSkill
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WorkspaceAgentSkill")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspace_agent_skills")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspace_agent_skills")
	}

	if len(workspaceAgentSkillAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Skill = foreign
		if foreign.R == nil {
			foreign.R = &workspaceAgentSkillR{}
		}
		foreign.R.SkillWorkspaceAgentSkillAttachments = append(foreign.R.SkillWorkspaceAgentSkillAttachments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SkillID == foreign.ID {
				local.R.Skill = foreign
				if foreign.R == nil {
					foreign.R = &workspaceAgentSkillR{}
				}
				foreign.R.SkillWorkspaceAgentSkillAttachments = append(foreign.R.SkillWorkspaceAgentSkillAttachments, local)
				break
			}
		}
	}

	return nil
}

// LoadWorkspace allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceAgentSkillAttachmentL) LoadWorkspace(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceAgentSkillAttachment any, mods queries.Applicator) error {
	var slice []*WorkspaceAgentSkillAttachment
	var object *WorkspaceAgentSkillAttachment

	if singular {
		var ok bool
		object, ok = maybeWorkspaceAgentSkillAttachment.(*WorkspaceAgentSkillAttachment)
		if !ok {
			object = new(WorkspaceAgentSkillAttachment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceAgentSkillAttachment))
			}
		}
	} else {
		s, ok := maybeWorkspaceAgentSkillAttachment.(*[]*WorkspaceAgentSkillAttachment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceAgentSkillAttachment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceAgentSkillAttachment))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceAgentSkillAttachmentR{}
		}
		args[object.WorkspaceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceAgentSkillAttachmentR{}
			}

			args[obj.WorkspaceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`workspaces`),
		qm.WhereIn(`workspaces.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Workspace")
	}

	var resultSlice []*Workspace
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Workspace")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for workspaces")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for workspaces")
	}

	if len(workspaceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Workspace = foreign
		if foreign.R == nil {
			foreign.R = &workspaceR{}
		}
		foreign.R.WorkspaceAgentSkillAttachments = append(foreign.R.WorkspaceAgentSkillAttachments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WorkspaceID == foreign.ID {
				local.R.Workspace = foreign
				if foreign.R == nil {
					foreign.R = &workspaceR{}
				}
				foreign.R.WorkspaceAgentSkillAttachments = append(foreign.R.WorkspaceAgentSkillAttachments, local)
				break
			}
		}
	}

	return nil
}

// SetAgent of the workspaceAgentSkillAttachment to the related item.
// Sets o.R.Agent to related.
// Adds o to related.R.AgentWorkspaceAgentSkillAttachments.
func (o *WorkspaceAgentSkillAttachment) SetAgent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceAgent) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"agent_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillAttachmentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.AgentID, o.SkillID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AgentID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentSkillAttachmentR{
			Agent: related,
		}
	} else {
		o.R.Agent = related
	}

	if related.R == nil {
		related.R = &workspaceAgentR{
			AgentWorkspaceAgentSkillAttachments: WorkspaceAgentSkillAttachmentSlice{o},
		}
	} else {
		related.R.AgentWorkspaceAgentSkillAttachments = append(related.R.AgentWorkspaceAgentSkillAttachments, o)
	}

	return nil
}

// SetAttachedByAccount of the workspaceAgentSkillAttachment to the related item.
// Sets o.R.AttachedByAccount to related.
// Adds o to related.R.AttachedByAccountWorkspaceAgentSkillAttachments.
func (o *WorkspaceAgentSkillAttachment) SetAttachedByAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"attached_by_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillAttachmentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.AgentID, o.SkillID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AttachedByAccountID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentSkillAttachmentR{
			AttachedByAccount: related,
		}
	} else {
		o.R.AttachedByAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			AttachedByAccountWorkspaceAgentSkillAttachments: WorkspaceAgentSkillAttachmentSlice{o},
		}
	} else {
		related.R.AttachedByAccountWorkspaceAgentSkillAttachments = append(related.R.AttachedByAccountWorkspaceAgentSkillAttachments, o)
	}

	return nil
}

// SetSkill of the workspaceAgentSkillAttachment to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.SkillWorkspaceAgentSkillAttachments.
func (o *WorkspaceAgentSkillAttachment) SetSkill(ctx context.Context, exec boil.ContextExecutor, insert bool, related *WorkspaceAgentSkill) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"skill_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillAttachmentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.AgentID, o.SkillID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SkillID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentSkillAttachmentR{
			Skill: related,
		}
	} else {
		o.R.Skill = related
	}

	if related.R == nil {
		related.R = &workspaceAgentSkillR{
			SkillWorkspaceAgentSkillAttachments: WorkspaceAgentSkillAttachmentSlice{o},
		}
	} else {
		related.R.SkillWorkspaceAgentSkillAttachments = append(related.R.SkillWorkspaceAgentSkillAttachments, o)
	}

	return nil
}

// SetWorkspace of the workspaceAgentSkillAttachment to the related item.
// Sets o.R.Workspace to related.
// Adds o to related.R.WorkspaceAgentSkillAttachments.
func (o *WorkspaceAgentSkillAttachment) SetWorkspace(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Workspace) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"workspace_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceAgentSkillAttachmentPrimaryKeyColumns),
	)
	values := []any{related.ID, o.AgentID, o.SkillID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WorkspaceID = related.ID
	if o.R == nil {
		o.R = &workspaceAgentSkillAttachmentR{
			Workspace: related,
		}
	} else {
		o.R.Workspace = related
	}

	if related.R == nil {
		related.R = &workspaceR{
			WorkspaceAgentSkillAttachments: WorkspaceAgentSkillAttachmentSlice{o},
		}
	} else {
		related.R.WorkspaceAgentSkillAttachments = append(related.R.WorkspaceAgentSkillAttachments, o)
	}

	return nil
}

// WorkspaceAgentSkillAttachments retrieves all the records using an executor.
func WorkspaceAgentSkillAttachments(mods ...qm.QueryMod) workspaceAgentSkillAttachmentQuery {
	mods = append(mods, qm.From("\"workspace_agent_skill_attachments\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_agent_skill_attachments\".*"})
	}

	return workspaceAgentSkillAttachmentQuery{q}
}

// FindWorkspaceAgentSkillAttachment retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceAgentSkillAttachment(ctx context.Context, exec boil.ContextExecutor, agentID string, skillID string, selectCols ...string) (*WorkspaceAgentSkillAttachment, error) {
	workspaceAgentSkillAttachmentObj := &WorkspaceAgentSkillAttachment{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_agent_skill_attachments\" where \"agent_id\"=$1 AND \"skill_id\"=$2", sel,
	)

	q := queries.Raw(query, agentID, skillID)

	err := q.Bind(ctx, exec, workspaceAgentSkillAttachmentObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_agent_skill_attachments")
	}

	if err = workspaceAgentSkillAttachmentObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceAgentSkillAttachmentObj, err
	}

	return workspaceAgentSkillAttachmentObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceAgentSkillAttachment) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_skill_attachments provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentSkillAttachmentColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceAgentSkillAttachmentInsertCacheMut.RLock()
	cache, cached := workspaceAgentSkillAttachmentInsertCache[key]
	workspaceAgentSkillAttachmentInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceAgentSkillAttachmentAllColumns,
			workspaceAgentSkillAttachmentColumnsWithDefault,
			workspaceAgentSkillAttachmentColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentSkillAttachmentType, workspaceAgentSkillAttachmentMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceAgentSkillAttachmentType, workspaceAgentSkillAttachmentMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_agent_skill_attachments\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_agent_skill_attachments\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_agent_skill_attachments")
	}

	if !cached {
		workspaceAgentSkillAttachmentInsertCacheMut.Lock()
		workspaceAgentSkillAttachmentInsertCache[key] = cache
		workspaceAgentSkillAttachmentInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceAgentSkillAttachment.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceAgentSkillAttachment) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceAgentSkillAttachmentUpdateCacheMut.RLock()
	cache, cached := workspaceAgentSkillAttachmentUpdateCache[key]
	workspaceAgentSkillAttachmentUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceAgentSkillAttachmentAllColumns,
			workspaceAgentSkillAttachmentPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_agent_skill_attachments, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceAgentSkillAttachmentPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceAgentSkillAttachmentType, workspaceAgentSkillAttachmentMapping, append(wl, workspaceAgentSkillAttachmentPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_agent_skill_attachments row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_agent_skill_attachments")
	}

	if !cached {
		workspaceAgentSkillAttachmentUpdateCacheMut.Lock()
		workspaceAgentSkillAttachmentUpdateCache[key] = cache
		workspaceAgentSkillAttachmentUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceAgentSkillAttachmentQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_agent_skill_attachments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_agent_skill_attachments")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceAgentSkillAttachmentSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentSkillAttachmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_agent_skill_attachments\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceAgentSkillAttachmentPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceAgentSkillAttachment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceAgentSkillAttachment")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceAgentSkillAttachment) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_agent_skill_attachments provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceAgentSkillAttachmentColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceAgentSkillAttachmentUpsertCacheMut.RLock()
	cache, cached := workspaceAgentSkillAttachmentUpsertCache[key]
	workspaceAgentSkillAttachmentUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceAgentSkillAttachmentAllColumns,
			workspaceAgentSkillAttachmentColumnsWithDefault,
			workspaceAgentSkillAttachmentColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceAgentSkillAttachmentAllColumns,
			workspaceAgentSkillAttachmentPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_agent_skill_attachments, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceAgentSkillAttachmentAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceAgentSkillAttachmentPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_agent_skill_attachments, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceAgentSkillAttachmentPrimaryKeyColumns))
			copy(conflict, workspaceAgentSkillAttachmentPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_agent_skill_attachments\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceAgentSkillAttachmentType, workspaceAgentSkillAttachmentMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceAgentSkillAttachmentType, workspaceAgentSkillAttachmentMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_agent_skill_attachments")
	}

	if !cached {
		workspaceAgentSkillAttachmentUpsertCacheMut.Lock()
		workspaceAgentSkillAttachmentUpsertCache[key] = cache
		workspaceAgentSkillAttachmentUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceAgentSkillAttachment record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceAgentSkillAttachment) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceAgentSkillAttachment provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceAgentSkillAttachmentPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_agent_skill_attachments\" WHERE \"agent_id\"=$1 AND \"skill_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_agent_skill_attachments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_agent_skill_attachments")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceAgentSkillAttachmentQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceAgentSkillAttachmentQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_agent_skill_attachments")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_skill_attachments")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceAgentSkillAttachmentSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceAgentSkillAttachmentBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentSkillAttachmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_agent_skill_attachments\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentSkillAttachmentPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceAgentSkillAttachment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_agent_skill_attachments")
	}

	if len(workspaceAgentSkillAttachmentAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceAgentSkillAttachment) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceAgentSkillAttachment(ctx, exec, o.AgentID, o.SkillID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceAgentSkillAttachmentSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceAgentSkillAttachmentSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceAgentSkillAttachmentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_agent_skill_attachments\".* FROM \"workspace_agent_skill_attachments\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceAgentSkillAttachmentPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceAgentSkillAttachmentSlice")
	}

	*o = slice

	return nil
}

// WorkspaceAgentSkillAttachmentExists checks if the WorkspaceAgentSkillAttachment row exists.
func WorkspaceAgentSkillAttachmentExists(ctx context.Context, exec boil.ContextExecutor, agentID string, skillID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_agent_skill_attachments\" where \"agent_id\"=$1 AND \"skill_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, agentID, skillID)
	}
	row := exec.QueryRowContext(ctx, sql, agentID, skillID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_agent_skill_attachments exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceAgentSkillAttachment row exists.
func (o *WorkspaceAgentSkillAttachment) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceAgentSkillAttachmentExists(ctx, exec, o.AgentID, o.SkillID)
}
