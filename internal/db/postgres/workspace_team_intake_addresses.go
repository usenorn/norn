// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbpostgres

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorkspaceTeamIntakeAddress is an object representing the database table.
type WorkspaceTeamIntakeAddress struct {
	TeamID             string      `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	WorkspaceID        string      `boil:"workspace_id" json:"workspace_id" toml:"workspace_id" yaml:"workspace_id"`
	LocalPart          string      `boil:"local_part" json:"local_part" toml:"local_part" yaml:"local_part"`
	Domain             string      `boil:"domain" json:"domain" toml:"domain" yaml:"domain"`
	EnabledByAccountID null.String `boil:"enabled_by_account_id" json:"enabled_by_account_id,omitempty" toml:"enabled_by_account_id" yaml:"enabled_by_account_id,omitempty"`
	CreatedAt          time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	RotatedAt          null.Time   `boil:"rotated_at" json:"rotated_at,omitempty" toml:"rotated_at" yaml:"rotated_at,omitempty"`

	R *workspaceTeamIntakeAddressR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L workspaceTeamIntakeAddressL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorkspaceTeamIntakeAddressColumns = struct {
	TeamID             string
	WorkspaceID        string
	LocalPart          string
	Domain             string
	EnabledByAccountID string
	CreatedAt          string
	RotatedAt          string
}{
	TeamID:             "team_id",
	WorkspaceID:        "workspace_id",
	LocalPart:          "local_part",
	Domain:             "domain",
	EnabledByAccountID: "enabled_by_account_id",
	CreatedAt:          "created_at",
	RotatedAt:          "rotated_at",
}

var WorkspaceTeamIntakeAddressTableColumns = struct {
	TeamID             string
	WorkspaceID        string
	LocalPart          string
	Domain             string
	EnabledByAccountID string
	CreatedAt          string
	RotatedAt          string
}{
	TeamID:             "workspace_team_intake_addresses.team_id",
	WorkspaceID:        "workspace_team_intake_addresses.workspace_id",
	LocalPart:          "workspace_team_intake_addresses.local_part",
	Domain:             "workspace_team_intake_addresses.domain",
	EnabledByAccountID: "workspace_team_intake_addresses.enabled_by_account_id",
	CreatedAt:          "workspace_team_intake_addresses.created_at",
	RotatedAt:          "workspace_team_intake_addresses.rotated_at",
}

// Generated where

var WorkspaceTeamIntakeAddressWhere = struct {
	TeamID             whereHelperstring
	WorkspaceID        whereHelperstring
	LocalPart          whereHelperstring
	Domain             whereHelperstring
	EnabledByAccountID whereHelpernull_String
	CreatedAt          whereHelpertime_Time
	RotatedAt          whereHelpernull_Time
}{
	TeamID:             whereHelperstring{field: "\"workspace_team_intake_addresses\".\"team_id\""},
	WorkspaceID:        whereHelperstring{field: "\"workspace_team_intake_addresses\".\"workspace_id\""},
	LocalPart:          whereHelperstring{field: "\"workspace_team_intake_addresses\".\"local_part\""},
	Domain:             whereHelperstring{field: "\"workspace_team_intake_addresses\".\"domain\""},
	EnabledByAccountID: whereHelpernull_String{field: "\"workspace_team_intake_addresses\".\"enabled_by_account_id\""},
	CreatedAt:          whereHelpertime_Time{field: "\"workspace_team_intake_addresses\".\"created_at\""},
	RotatedAt:          whereHelpernull_Time{field: "\"workspace_team_intake_addresses\".\"rotated_at\""},
}

// WorkspaceTeamIntakeAddressRels is where relationship names are stored.
var WorkspaceTeamIntakeAddressRels = struct {
	EnabledByAccount string
}{
	EnabledByAccount: "EnabledByAccount",
}

// workspaceTeamIntakeAddressR is where relationships are stored.
type workspaceTeamIntakeAddressR struct {
	EnabledByAccount *Account `boil:"EnabledByAccount" json:"EnabledByAccount" toml:"EnabledByAccount" yaml:"EnabledByAccount"`
}

// NewStruct creates a new relationship struct
func (*workspaceTeamIntakeAddressR) NewStruct() *workspaceTeamIntakeAddressR {
	return &workspaceTeamIntakeAddressR{}
}

func (o *WorkspaceTeamIntakeAddress) GetEnabledByAccount() *Account {
	if o == nil {
		return nil
	}

	return o.R.GetEnabledByAccount()
}

func (r *workspaceTeamIntakeAddressR) GetEnabledByAccount() *Account {
	if r == nil {
		return nil
	}

	return r.EnabledByAccount
}

// workspaceTeamIntakeAddressL is where Load methods for each relationship are stored.
type workspaceTeamIntakeAddressL struct{}

var (
	workspaceTeamIntakeAddressAllColumns            = []string{"team_id", "workspace_id", "local_part", "domain", "enabled_by_account_id", "created_at", "rotated_at"}
	workspaceTeamIntakeAddressColumnsWithoutDefault = []string{"team_id", "workspace_id", "local_part", "domain"}
	workspaceTeamIntakeAddressColumnsWithDefault    = []string{"enabled_by_account_id", "created_at", "rotated_at"}
	workspaceTeamIntakeAddressPrimaryKeyColumns     = []string{"team_id"}
	workspaceTeamIntakeAddressGeneratedColumns      = []string{}
)

type (
	// WorkspaceTeamIntakeAddressSlice is an alias for a slice of pointers to WorkspaceTeamIntakeAddress.
	// This should almost always be used instead of []WorkspaceTeamIntakeAddress.
	WorkspaceTeamIntakeAddressSlice []*WorkspaceTeamIntakeAddress
	// WorkspaceTeamIntakeAddressHook is the signature for custom WorkspaceTeamIntakeAddress hook methods
	WorkspaceTeamIntakeAddressHook func(context.Context, boil.ContextExecutor, *WorkspaceTeamIntakeAddress) error

	workspaceTeamIntakeAddressQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	workspaceTeamIntakeAddressType                 = reflect.TypeOf(&WorkspaceTeamIntakeAddress{})
	workspaceTeamIntakeAddressMapping              = queries.MakeStructMapping(workspaceTeamIntakeAddressType)
	workspaceTeamIntakeAddressPrimaryKeyMapping, _ = queries.BindMapping(workspaceTeamIntakeAddressType, workspaceTeamIntakeAddressMapping, workspaceTeamIntakeAddressPrimaryKeyColumns)
	workspaceTeamIntakeAddressInsertCacheMut       sync.RWMutex
	workspaceTeamIntakeAddressInsertCache          = make(map[string]insertCache)
	workspaceTeamIntakeAddressUpdateCacheMut       sync.RWMutex
	workspaceTeamIntakeAddressUpdateCache          = make(map[string]updateCache)
	workspaceTeamIntakeAddressUpsertCacheMut       sync.RWMutex
	workspaceTeamIntakeAddressUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var workspaceTeamIntakeAddressAfterSelectMu sync.Mutex
var workspaceTeamIntakeAddressAfterSelectHooks []WorkspaceTeamIntakeAddressHook

var workspaceTeamIntakeAddressBeforeInsertMu sync.Mutex
var workspaceTeamIntakeAddressBeforeInsertHooks []WorkspaceTeamIntakeAddressHook
var workspaceTeamIntakeAddressAfterInsertMu sync.Mutex
var workspaceTeamIntakeAddressAfterInsertHooks []WorkspaceTeamIntakeAddressHook

var workspaceTeamIntakeAddressBeforeUpdateMu sync.Mutex
var workspaceTeamIntakeAddressBeforeUpdateHooks []WorkspaceTeamIntakeAddressHook
var workspaceTeamIntakeAddressAfterUpdateMu sync.Mutex
var workspaceTeamIntakeAddressAfterUpdateHooks []WorkspaceTeamIntakeAddressHook

var workspaceTeamIntakeAddressBeforeDeleteMu sync.Mutex
var workspaceTeamIntakeAddressBeforeDeleteHooks []WorkspaceTeamIntakeAddressHook
var workspaceTeamIntakeAddressAfterDeleteMu sync.Mutex
var workspaceTeamIntakeAddressAfterDeleteHooks []WorkspaceTeamIntakeAddressHook

var workspaceTeamIntakeAddressBeforeUpsertMu sync.Mutex
var workspaceTeamIntakeAddressBeforeUpsertHooks []WorkspaceTeamIntakeAddressHook
var workspaceTeamIntakeAddressAfterUpsertMu sync.Mutex
var workspaceTeamIntakeAddressAfterUpsertHooks []WorkspaceTeamIntakeAddressHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorkspaceTeamIntakeAddress) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorkspaceTeamIntakeAddress) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorkspaceTeamIntakeAddress) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorkspaceTeamIntakeAddress) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorkspaceTeamIntakeAddress) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorkspaceTeamIntakeAddress) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorkspaceTeamIntakeAddress) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorkspaceTeamIntakeAddress) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorkspaceTeamIntakeAddress) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range workspaceTeamIntakeAddressAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorkspaceTeamIntakeAddressHook registers your hook function for all future operations.
func AddWorkspaceTeamIntakeAddressHook(hookPoint boil.HookPoint, workspaceTeamIntakeAddressHook WorkspaceTeamIntakeAddressHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		workspaceTeamIntakeAddressAfterSelectMu.Lock()
		workspaceTeamIntakeAddressAfterSelectHooks = append(workspaceTeamIntakeAddressAfterSelectHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		workspaceTeamIntakeAddressBeforeInsertMu.Lock()
		workspaceTeamIntakeAddressBeforeInsertHooks = append(workspaceTeamIntakeAddressBeforeInsertHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		workspaceTeamIntakeAddressAfterInsertMu.Lock()
		workspaceTeamIntakeAddressAfterInsertHooks = append(workspaceTeamIntakeAddressAfterInsertHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		workspaceTeamIntakeAddressBeforeUpdateMu.Lock()
		workspaceTeamIntakeAddressBeforeUpdateHooks = append(workspaceTeamIntakeAddressBeforeUpdateHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		workspaceTeamIntakeAddressAfterUpdateMu.Lock()
		workspaceTeamIntakeAddressAfterUpdateHooks = append(workspaceTeamIntakeAddressAfterUpdateHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		workspaceTeamIntakeAddressBeforeDeleteMu.Lock()
		workspaceTeamIntakeAddressBeforeDeleteHooks = append(workspaceTeamIntakeAddressBeforeDeleteHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		workspaceTeamIntakeAddressAfterDeleteMu.Lock()
		workspaceTeamIntakeAddressAfterDeleteHooks = append(workspaceTeamIntakeAddressAfterDeleteHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		workspaceTeamIntakeAddressBeforeUpsertMu.Lock()
		workspaceTeamIntakeAddressBeforeUpsertHooks = append(workspaceTeamIntakeAddressBeforeUpsertHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		workspaceTeamIntakeAddressAfterUpsertMu.Lock()
		workspaceTeamIntakeAddressAfterUpsertHooks = append(workspaceTeamIntakeAddressAfterUpsertHooks, workspaceTeamIntakeAddressHook)
		workspaceTeamIntakeAddressAfterUpsertMu.Unlock()
	}
}

// One returns a single workspaceTeamIntakeAddress record from the query.
func (q workspaceTeamIntakeAddressQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorkspaceTeamIntakeAddress, error) {
	o := &WorkspaceTeamIntakeAddress{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: failed to execute a one query for workspace_team_intake_addresses")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all WorkspaceTeamIntakeAddress records from the query.
func (q workspaceTeamIntakeAddressQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorkspaceTeamIntakeAddressSlice, error) {
	var o []*WorkspaceTeamIntakeAddress

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbpostgres: failed to assign all query results to WorkspaceTeamIntakeAddress slice")
	}

	if len(workspaceTeamIntakeAddressAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all WorkspaceTeamIntakeAddress records in the query.
func (q workspaceTeamIntakeAddressQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to count workspace_team_intake_addresses rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q workspaceTeamIntakeAddressQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: failed to check if workspace_team_intake_addresses exists")
	}

	return count > 0, nil
}

// EnabledByAccount pointed to by the foreign key.
func (o *WorkspaceTeamIntakeAddress) EnabledByAccount(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.EnabledByAccountID),
	}

	queryMods = append(queryMods, mods...)

	return Accounts(queryMods...)
}

// LoadEnabledByAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (workspaceTeamIntakeAddressL) LoadEnabledByAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorkspaceTeamIntakeAddress any, mods queries.Applicator) error {
	var slice []*WorkspaceTeamIntakeAddress
	var object *WorkspaceTeamIntakeAddress

	if singular {
		var ok bool
		object, ok = maybeWorkspaceTeamIntakeAddress.(*WorkspaceTeamIntakeAddress)
		if !ok {
			object = new(WorkspaceTeamIntakeAddress)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorkspaceTeamIntakeAddress)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorkspaceTeamIntakeAddress))
			}
		}
	} else {
		s, ok := maybeWorkspaceTeamIntakeAddress.(*[]*WorkspaceTeamIntakeAddress)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorkspaceTeamIntakeAddress)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorkspaceTeamIntakeAddress))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &workspaceTeamIntakeAddressR{}
		}
		if !queries.IsNil(object.EnabledByAccountID) {
			args[object.EnabledByAccountID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &workspaceTeamIntakeAddressR{}
			}

			if !queries.IsNil(obj.EnabledByAccountID) {
				args[obj.EnabledByAccountID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`accounts`),
		qm.WhereIn(`accounts.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for accounts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for accounts")
	}

	if len(accountAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.EnabledByAccount = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.EnabledByAccountWorkspaceTeamIntakeAddresses = append(foreign.R.EnabledByAccountWorkspaceTeamIntakeAddresses, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.EnabledByAccountID, foreign.ID) {
				local.R.EnabledByAccount = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.EnabledByAccountWorkspaceTeamIntakeAddresses = append(foreign.R.EnabledByAccountWorkspaceTeamIntakeAddresses, local)
				break
			}
		}
	}

	return nil
}

// SetEnabledByAccount of the workspaceTeamIntakeAddress to the related item.
// Sets o.R.EnabledByAccount to related.
// Adds o to related.R.EnabledByAccountWorkspaceTeamIntakeAddresses.
func (o *WorkspaceTeamIntakeAddress) SetEnabledByAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"workspace_team_intake_addresses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"enabled_by_account_id"}),
		strmangle.WhereClause("\"", "\"", 2, workspaceTeamIntakeAddressPrimaryKeyColumns),
	)
	values := []any{related.ID, o.TeamID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.EnabledByAccountID, related.ID)
	if o.R == nil {
		o.R = &workspaceTeamIntakeAddressR{
			EnabledByAccount: related,
		}
	} else {
		o.R.EnabledByAccount = related
	}

	if related.R == nil {
		related.R = &accountR{
			EnabledByAccountWorkspaceTeamIntakeAddresses: WorkspaceTeamIntakeAddressSlice{o},
		}
	} else {
		related.R.EnabledByAccountWorkspaceTeamIntakeAddresses = append(related.R.EnabledByAccountWorkspaceTeamIntakeAddresses, o)
	}

	return nil
}

// RemoveEnabledByAccount relationship.
// Sets o.R.EnabledByAccount to nil.
// Removes o from all passed in related items' relationships struct.
func (o *WorkspaceTeamIntakeAddress) RemoveEnabledByAccount(ctx context.Context, exec boil.ContextExecutor, related *Account) error {
	var err error

	queries.SetScanner(&o.EnabledByAccountID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("enabled_by_account_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.EnabledByAccount = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.EnabledByAccountWorkspaceTeamIntakeAddresses {
		if queries.Equal(o.EnabledByAccountID, ri.EnabledByAccountID) {
			continue
		}

		ln := len(related.R.EnabledByAccountWorkspaceTeamIntakeAddresses)
		if ln > 1 && i < ln-1 {
			related.R.EnabledByAccountWorkspaceTeamIntakeAddresses[i] = related.R.EnabledByAccountWorkspaceTeamIntakeAddresses[ln-1]
		}
		related.R.EnabledByAccountWorkspaceTeamIntakeAddresses = related.R.EnabledByAccountWorkspaceTeamIntakeAddresses[:ln-1]
		break
	}
	return nil
}

// WorkspaceTeamIntakeAddresses retrieves all the records using an executor.
func WorkspaceTeamIntakeAddresses(mods ...qm.QueryMod) workspaceTeamIntakeAddressQuery {
	mods = append(mods, qm.From("\"workspace_team_intake_addresses\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"workspace_team_intake_addresses\".*"})
	}

	return workspaceTeamIntakeAddressQuery{q}
}

// FindWorkspaceTeamIntakeAddress retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorkspaceTeamIntakeAddress(ctx context.Context, exec boil.ContextExecutor, teamID string, selectCols ...string) (*WorkspaceTeamIntakeAddress, error) {
	workspaceTeamIntakeAddressObj := &WorkspaceTeamIntakeAddress{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"workspace_team_intake_addresses\" where \"team_id\"=$1", sel,
	)

	q := queries.Raw(query, teamID)

	err := q.Bind(ctx, exec, workspaceTeamIntakeAddressObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbpostgres: unable to select from workspace_team_intake_addresses")
	}

	if err = workspaceTeamIntakeAddressObj.doAfterSelectHooks(ctx, exec); err != nil {
		return workspaceTeamIntakeAddressObj, err
	}

	return workspaceTeamIntakeAddressObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorkspaceTeamIntakeAddress) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_intake_addresses provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamIntakeAddressColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	workspaceTeamIntakeAddressInsertCacheMut.RLock()
	cache, cached := workspaceTeamIntakeAddressInsertCache[key]
	workspaceTeamIntakeAddressInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			workspaceTeamIntakeAddressAllColumns,
			workspaceTeamIntakeAddressColumnsWithDefault,
			workspaceTeamIntakeAddressColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamIntakeAddressType, workspaceTeamIntakeAddressMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(workspaceTeamIntakeAddressType, workspaceTeamIntakeAddressMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"workspace_team_intake_addresses\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"workspace_team_intake_addresses\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to insert into workspace_team_intake_addresses")
	}

	if !cached {
		workspaceTeamIntakeAddressInsertCacheMut.Lock()
		workspaceTeamIntakeAddressInsertCache[key] = cache
		workspaceTeamIntakeAddressInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the WorkspaceTeamIntakeAddress.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorkspaceTeamIntakeAddress) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	workspaceTeamIntakeAddressUpdateCacheMut.RLock()
	cache, cached := workspaceTeamIntakeAddressUpdateCache[key]
	workspaceTeamIntakeAddressUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			workspaceTeamIntakeAddressAllColumns,
			workspaceTeamIntakeAddressPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbpostgres: unable to update workspace_team_intake_addresses, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"workspace_team_intake_addresses\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, workspaceTeamIntakeAddressPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(workspaceTeamIntakeAddressType, workspaceTeamIntakeAddressMapping, append(wl, workspaceTeamIntakeAddressPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update workspace_team_intake_addresses row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by update for workspace_team_intake_addresses")
	}

	if !cached {
		workspaceTeamIntakeAddressUpdateCacheMut.Lock()
		workspaceTeamIntakeAddressUpdateCache[key] = cache
		workspaceTeamIntakeAddressUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q workspaceTeamIntakeAddressQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all for workspace_team_intake_addresses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected for workspace_team_intake_addresses")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorkspaceTeamIntakeAddressSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbpostgres: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamIntakeAddressPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"workspace_team_intake_addresses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, workspaceTeamIntakeAddressPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to update all in workspaceTeamIntakeAddress slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to retrieve rows affected all in update all workspaceTeamIntakeAddress")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorkspaceTeamIntakeAddress) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("dbpostgres: no workspace_team_intake_addresses provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(workspaceTeamIntakeAddressColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	workspaceTeamIntakeAddressUpsertCacheMut.RLock()
	cache, cached := workspaceTeamIntakeAddressUpsertCache[key]
	workspaceTeamIntakeAddressUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			workspaceTeamIntakeAddressAllColumns,
			workspaceTeamIntakeAddressColumnsWithDefault,
			workspaceTeamIntakeAddressColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			workspaceTeamIntakeAddressAllColumns,
			workspaceTeamIntakeAddressPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("dbpostgres: unable to upsert workspace_team_intake_addresses, could not build update column list")
		}

		ret := strmangle.SetComplement(workspaceTeamIntakeAddressAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(workspaceTeamIntakeAddressPrimaryKeyColumns) == 0 {
				return errors.New("dbpostgres: unable to upsert workspace_team_intake_addresses, could not build conflict column list")
			}

			conflict = make([]string, len(workspaceTeamIntakeAddressPrimaryKeyColumns))
			copy(conflict, workspaceTeamIntakeAddressPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"workspace_team_intake_addresses\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(workspaceTeamIntakeAddressType, workspaceTeamIntakeAddressMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(workspaceTeamIntakeAddressType, workspaceTeamIntakeAddressMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to upsert workspace_team_intake_addresses")
	}

	if !cached {
		workspaceTeamIntakeAddressUpsertCacheMut.Lock()
		workspaceTeamIntakeAddressUpsertCache[key] = cache
		workspaceTeamIntakeAddressUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single WorkspaceTeamIntakeAddress record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorkspaceTeamIntakeAddress) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbpostgres: no WorkspaceTeamIntakeAddress provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), workspaceTeamIntakeAddressPrimaryKeyMapping)
	sql := "DELETE FROM \"workspace_team_intake_addresses\" WHERE \"team_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete from workspace_team_intake_addresses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by delete for workspace_team_intake_addresses")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q workspaceTeamIntakeAddressQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbpostgres: no workspaceTeamIntakeAddressQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspace_team_intake_addresses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_intake_addresses")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorkspaceTeamIntakeAddressSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(workspaceTeamIntakeAddressBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamIntakeAddressPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"workspace_team_intake_addresses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamIntakeAddressPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: unable to delete all from workspaceTeamIntakeAddress slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbpostgres: failed to get rows affected by deleteall for workspace_team_intake_addresses")
	}

	if len(workspaceTeamIntakeAddressAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorkspaceTeamIntakeAddress) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorkspaceTeamIntakeAddress(ctx, exec, o.TeamID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorkspaceTeamIntakeAddressSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorkspaceTeamIntakeAddressSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), workspaceTeamIntakeAddressPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"workspace_team_intake_addresses\".* FROM \"workspace_team_intake_addresses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, workspaceTeamIntakeAddressPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbpostgres: unable to reload all in WorkspaceTeamIntakeAddressSlice")
	}

	*o = slice

	return nil
}

// WorkspaceTeamIntakeAddressExists checks if the WorkspaceTeamIntakeAddress row exists.
func WorkspaceTeamIntakeAddressExists(ctx context.Context, exec boil.ContextExecutor, teamID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"workspace_team_intake_addresses\" where \"team_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, teamID)
	}
	row := exec.QueryRowContext(ctx, sql, teamID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbpostgres: unable to check if workspace_team_intake_addresses exists")
	}

	return exists, nil
}

// Exists checks if the WorkspaceTeamIntakeAddress row exists.
func (o *WorkspaceTeamIntakeAddress) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorkspaceTeamIntakeAddressExists(ctx, exec, o.TeamID)
}
